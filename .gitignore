# dependencies / build output
node_modules/
.next/
out/
coverage/
*.tsbuildinfo

# test / eval tooling artifacts
test-results/
playwright-report/
.lighthouseci/
.vitest/
.eval/

# environment (secret-bearing; .env.tooling is committed & secret-free)
.env
.env.*.local

# PII / private assets (PB5, EVAL-016) — never committed until sanitised
public/resume.pdf
tests/forbidden.local.json

# OS / raw media
.DS_Store
content/media/**/*.mov
content/media/**/raw/
