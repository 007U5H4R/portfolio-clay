# Execution Ledger — Clay Portfolio · Stage 7

Committed, survives context resets. The orchestrator updates this after every task/step. Source-of-truth order: implementation code > PWA (Campfire) > this ledger / Markdown artifacts. Native Backlog IDs (id-map.json): TKT-01→TASK-1 … TKT-53→TASK-49.

## Environment (verified 2026-09-15, session start)
- Root: `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay/` — everything (repo, node_modules, pnpm store, Playwright browsers, Next cache, scratch) stays on E Drive (global scar).
- Internal disk: 22 GiB free / 35% used at start — recheck before installs.
- Node **v26.7.0** (matches A9 pin "Node 26.7"), pnpm **11.25.0** (matches). pnpm store: `/Volumes/E Drive/Dev/.pnpm-store/v11`. E-Drive `.cache` / `.scratch` / `.pnpm-store` present.
- PWA: Campfire, project `portfolio-clay`, onboarded Stage 6.2. Move statuses with `"/Volumes/E Drive/Dev/Code/Claude/PM Tools/backlog-md-fork/scripts/orchestrator/move-ticket.sh" "<root>" <TASK-id> "<status>"` (To Do / In Progress / In Review / Blocked / Done). Never hand-edit `backlog/`.

## SESSION AUTHORIZATION (2026-09-15) — autonomous mandate
Tushar is AFK and authorized: "complete all the stages of workflow and take decisions on my behalf." Therefore:
- **Every human-in-the-loop gate becomes a recorded decision, not a stop.** The TKT-02 visual gate is decided by the orchestrator (screenshots still captured as evidence; rubric scored; call logged as EXE-1). All open content/product questions take the plan's §E defaults, each logged EXE-n.
- **Hard stops NOT crossed autonomously (build up to them, leave turnkey):** (1) production deployment (external/irreversible); (2) creating a GitHub repo or Vercel project under Tushar's account, or buying a domain (account-scoped/cost; domain unknown); (3) publishing the resume/any PII — `resumeAvailable` stays false (PB5), which itself hard-blocks prod; (4) publishing unverified claims — build-time schema gate stays enforced, no threshold weakening.
- Net: drive code (M-001…M-007) + Stages 8–10 + `QA-report.md` to a deploy-ready branch; stop before external resource creation / prod promotion. Leave a turnkey checklist for Tushar.
- GateGuard hook left ON (safety); orchestrator self-answers its fact-check per file.
- **Reporting cadence (Tushar's request):** post a self-contained summary to Tushar at each MILESTONE boundary (M-001…M-007) and each major stage gate (8 Design Critique, 9 Code/Test, 10 Security, 11 Deploy), plus any hard blocker or decision awaiting him. Not per-task.

## Orchestration decisions (execution mechanics; not formal EXE entries)
- **Isolation = in-place branch `m-001-tracer`** (main stays at the planning commit), NOT the sibling worktree §A10 suggests. Reason: every concrete S01.xx gate and §0.2 path target `Portfolio-clay/` directly (S01.03 scaffolds *into the existing folder*; S01.01 gate is `git -C .../Portfolio-clay`); a sibling worktree would break those paths. HANDOFF rule 2 and the Stage-7 prompt both authorize "branch `m-001-tracer`". `EXE-1` is reserved for the TKT-02 visual-gate decision per S02g.03.
- **Subagent model tiers:** most-capable → `opus`, standard → `sonnet`, cheap → `haiku` (this harness's Agent tool takes a per-call `model`, so mixing tiers per dispatch is supported here). One fresh implementer per TSK; brief = `docs/briefs/<TSK>.md`, report = `docs/reports/<TSK>.md`.

## Phase 1 = M-001 · Tracer bullet + visual gate
DAG: **TSK-01 → {TSK-02, TSK-03} → {TSK-04, TSK-05, TSK-06} → TSK-07 → [TKT-02 human visual gate]**. Per PB1, after TKT-01 the M-002 non-visual tickets TKT-03 (schema) & TKT-07 (harness) may proceed in parallel with the TKT-02 wait; NO further visual work before TKT-02 approval.

| Task | Native | Tier | Status | Report | Notes |
|---|---|---|---|---|---|
| Repo setup / S01.01 | — | orchestrator | ✅ done | this ledger | git init, .gitignore, README, planning commit, branch m-001-tracer |
| TSK-01 scaffold + tokens | TASK-1 | opus | ✅ done | TSK-01.md | commit db5f7be; all gates green (build "all routes static (1)", tokens 13/13, fonts self-hosted). Reviewed+accepted. |
| TSK-02 avatar pipeline | TASK-1 | sonnet | ✅ done | TSK-02.md | commit 68bbb51; avatar.webp 94.7kB, alpha ok, 1450×1800. S02.02 spec authored, edge-shot deferred to TSK-07. Accepted. |
| TSK-03 clay primitives | TASK-1 | sonnet | ✅ done | TSK-03.md | commit 61ab50d; 35 tests, D1 proof ×2, /dev/primitives 200-dev/404-prod. Accepted. Surfaced ease-token gap → orchestrator fix EXE-3. ClayTileSize widened +40 (additive). |
| TSK-04 Header/Nav/Mobile | TASK-1 | sonnet | ✅ done | TSK-04.md | commit 43927c0; 39 tests; easings synced to EXE-3; nav=Home·Work·Thinking·About. Accepted. Devs: useSyncExternalStore hooks, matchMedia-direct reduced-motion, TP mark=ClayTile40. |
| TSK-05 Hero/Avatar/Tiles | TASK-1 | opus | ✅ done | TSK-05.md | commit 486f677; build static(4), 41 tests. Accepted. Fixed breakpoint (EXE-4). DRAFT copy: eyebrow/headline/support (ship DRAFT-labelled, list for Tushar). |
| TSK-06 ProjectCard/VT | TASK-1 | opus | ✅ done | TSK-06.md | commit 3bb9540; A14 fallback (EXE-5); 48 tests, static(5), /work/teachspark 200 /nope 404, tokens 13/13. VT hooks dormant (plain nav — browser won't fire morph without React VT). Accepted. |
| TSK-07 shots + eval + baseline | TASK-1 | opus | ✅ done | TSK-07.md | commits 212cae4+8ac938b; Chromium on E-Drive (df unchanged), baseline-v1.json (schema 1, 17 cases). EVAL-006/010/015 PASS, 004 PASS(info). **EVAL-008 FAIL** (F1 overflow@1024, F2 logo 40px), F3/F4 design deviations, F5 perf info. Avatar edge: no halo. |
| TKT-01 fix wave (F1/F2/F4) | TASK-1 | sonnet | ✅ done | TKT-01-fix | commit a477b03; EVAL-008 FAIL→PASS (0 overflow, 0 sub-44), header 96/68 exact, avatar responsive caps. Screenshots regenerated. But surfaced F6. |
| TKT-01 fix wave 2 (F6) | TASK-1 | opus | ✅ done | F6-debug | commit c865690; root cause = sticky in-flow header shrinks scroll range → clamps its own scrollY trigger → oscillation @w768. Fixed with hysteresis (enter40/exit8, band32≥delta28). Regression test @EVAL-015 all widths (fails pre-fix). EVAL-015 PASS ×4. |
| TKT-01 test-fix (F3 responsive) | TASK-1 | sonnet | ✅ done | — | commit d34189f; avatar-width tests → responsive column-cap contract. Suite GREEN: test 48/48, e2e 39-0, build static(5). Avatar is 252px @1024 (min-content headline squeezes 35fr track) — grid balance to M-003. |
| TKT-02 visual gate | TASK-2 | orchestrator | ✅ done | gate-tracer.md | **EXE-6 APPROVED** (on Tushar's behalf). EVAL-001 11.5/12, EVAL-009 11/12, avatar likeness PASS. Follow-ups: F3 hero balance→M-003, DRAFT copy→Tushar, F5 perf→TKT-14/49. |
| TKT-02 visual gate | TASK-2 | Tushar | ⬜ pending | gate-tracer.md | STOP: screenshots 390/768/1024/1440 + 6-item checklist |

## Phase 2 = M-002 · Foundations & quality harness (branch `m-002-foundations` from main)
Order (sequential): TKT-03 → TKT-04 → TKT-05 → TKT-06 → TKT-07 (TSK-08..12) → TKT-08. Phase-2 QA gate (§C): EVAL-013 proof, EVAL-016 forbidden scan, EVAL-006/007/008/010/017, first full `eval-run` vs baseline-v1 (no Critical regression).

| Ticket | Native | Tier | Status | Notes |
|---|---|---|---|---|
| TKT-03 schema + zod gate | TASK-3 | opus | ✅ done | commit c9e5e28; 71 tests, schema 36 exports, EVAL-013 proof (fixture build fails 3 issues+exit1, clean build green), no regression. tracer.ts deleted→projects.ts. Devs: conditional composition-invariants (only 1 project yet), ?filter= per E-2. Accepted. |
| TKT-04 primitive system complete | TASK-4 | opus | ✅ done | commit 0188109; 88 tests, D1 proofs genuine, tokens 13/13, glass/clay separated, primitives e2e 7/5, dev-route gate both halves, no regression. Accepted. Devs: after: tone overlay, a[data-inline-link] allowlist (WCAG 2.5.8), dev route build-time gated. |
| TKT-05 layout + Reveal + Footer | TASK-5 | sonnet | ✅ done | commit a4a8c9e; 89 tests, tokens 13/13, layout e2e green, no regression. Footer="Built with curiosity." ✓. Caught+fixed 2 real contrast bugs (Footer bg, eyebrow ink-3→ink-2 7.53:1). Plan §B S05.04 prose fixed by orchestrator. Accepted. |
| TKT-06 SEO/OG | TASK-6 | sonnet | ✅ done | commit 5d11201; 98 tests, all routes static incl 4 OG routes, eval-017 7/7, no regression. Fixed 2 Satori bugs (WebP→PNG, Buffer→ArrayBuffer). Accepted. Carry: TKT-45 must NOT re-add contact OG (added here); TKT-40/43/44 extend sitemap. |
| TKT-07a harness pt1 (TSK-08/09) | TASK-7 | opus | ✅ done | commit 8accb42; 17 cases OK (14 auto/3 manual), 112 unit tests, e2e 95-0, per-EVAL specs live/fixme documented. Accepted. |
| TKT-07b harness pt2 (TSK-10/11/12) | TASK-7 | opus | ✅ done | HEAD 633fc2c; crawler EVAL-011=0 dead, full A16 orchestrator (+--reuse), CI (pending remote), docs/eval.md, dev-route skip. **First full run eval-run-0.2.0-7a658d6: 11 PASS/1 FAIL(EVAL-005 bundle 219.8kB>180, informational→TKT-14/49)/2 SKIP/3 MANUAL, regressions [], all MUST-pass evals green.** Perf informational under swiftshader (host OOMs LHCI). TASK-7 Done. Accepted. |
| M-002 fix-wave (EXE-7 + TKT-08) | TASK-8+ | sonnet | ✅ done | commits f6268df (EXE-7: tile copy 14px, subtitle+TP `data-micro-label`, /contact ClayButton ≥44, eval-008 44-pass) + 0f9c301 (TKT-08 resume-pii test infra, skips, verified both ways, no PDF/PII). tokens 13/13. Accepted. TASK-8 → Blocked (needs Tushar's PDF). |
| M-002 QA-tester gate | — | sonnet | ✅ PASS-w/-accepted-risks | commit cac9f54; suite green (unit 117, e2e 105+20); TC 21 PASS/3 BLOCKED (CI-remote, 2 resume-PDF)/0 FAIL; gate-critical evals 006/007/008/010/013/016/017 PASS; EVAL-005 informational. No real defects. |

**✅ M-002 COMPLETE — merged to main (0fc1041) 2026-09-16.**

## Phase 3 = M-003 · Home complete (branch `m-003-home` from main)
**Runs SEQUENTIALLY** — every M-003 ticket runs Playwright e2e (dev server :3000), so parallel agents collide on the port; fan-out is deferred to M-005 (ffmpeg media, no server). Order: TKT-09 → TKT-10 → TKT-11 · TKT-12 · TKT-13 → TKT-14. **M-003 = the EXE-6 hero-balance revisit** (TKT-12 real FeaturedWork fills the sparse hero; judge grid ratio at TKT-14's 5-second test).
| Ticket | Native | Tier | Status | Notes |
|---|---|---|---|---|
| TKT-09 Ask knowledge+adapter | TASK-9 | opus | ✅ done | commit 669918c; 11/11 prompts, 0 fabricated, EVAL-012/013 pass. Added ALL_PROJECT_SLUGS to lib/anchors (cross-link gate for railcite/velora pre-TKT-12). Carry: anchors.test.ts dedup ALL_PROJECT_SLUGS later. |
| TKT-10 Ask inline UI | TASK-10 | opus | ✅ done | commit b1251d4; 163 tests, 4 states, EVAL-007 keyboard PASS, EVAL-008 FAIL→PASS, no regression. Split zod out of client bundle; fixed ClayPill focus-ring a11y bug. Accepted. **Carry: TKT-11 hoist AskProvider to layout.tsx.** Bundle / = 239.7kB (perf → TKT-14/49). |
| TKT-11 AskPanel | TASK-11 | opus | ✅ done | commit 0f6013a; 163 tests, e2e 27/0, focus-trap EVAL-007 verified, EVAL-011 live button passes, AskPanel lazy (not in / first-load, / = 236.7kB). AskProvider hoisted to layout. Ask feature complete. Accepted. |
| TKT-12 FeaturedWork real data | TASK-12 | opus | ✅ done | commit a3972e9; projects:3 (TeachSpark-large/RailCite/Velora), no fabricated metrics (metrics:[] pending M-005), content-gate passes, e2e featured 10/0, no regression. FeaturedWork = 2:1:1 row. Accepted. Carry: TKT-30 swap Velora exact PRD line. |
| TKT-13 HowIThink | TASK-13 | sonnet | ✅ done | commit 2a5b13b; 6 stages (thinking:6), verbatim examples, e2e 7/0, no regression. principle one-liners DRAFT-badged (editorial). Accepted. **FLAG→TKT-14: eval-017 sitemap-count assertion stale (hard-coded 4, now 6 after TKT-12) — make dynamic.** |
| TKT-14 home assembly + 5-sec test | TASK-14 | opus | ✅ done | commit 8d8d6c7; 167 tests, e2e 29, home assembled, eval-017 now DYNAMIC, CopyButton never-silent. Accepted. EVAL-001 = ~5/6 @1440, 3/6 @390 → EXE-9 hero rebalance next. Bundle / 239.9kB (→TKT-49). |
| M-003 hero rebalance (EXE-9) | TASK-14 | opus | ✅ done | commit 363609e; **EVAL-001 now 6/6 at ALL widths** (390/768/1024/1440; was 3/6@390). Avatar 200@390 / ~349@1024 / ~474@1440; grid 42/58@lg; lead hidden <md (headline+tiles carry it); no overflow, LCP preserved. Orchestrator verified 390 first-viewport (CTA above fold). Fixed pre-existing tracer resume-link ambiguity. Accepted. |

**M-003 build COMPLETE** (TKT-09..14 + EXE-9 all done).
- **M-003 QA gate (062c362): FAIL on 1 real P1 (TC-051)** — else all green: EVAL-012 11/11 0-fabricated, EVAL-007 focus-trap 6/6, EVAL-001 6/6 (independently re-scored), EVAL-006/008/010/011/013 pass; 18 TC pass. The 23 e2e "failures" were host-contention (w1440 parallel trace races; 0 at --workers=1). EVAL-005 bundle ~240kB informational (TKT-49).
- **TC-051 (P1): FIXED** — commit b173e4c; `max-md:max-w-none` → full-bleed @390 (drawer 400/400/480 @768/1024/1440), `@TC-051` regression test added. Playwright `workers:1` (this host reliably green only serial — workers:2 still flaked). **M-003 QA → PASS-WITH-ACCEPTED-RISKS**: sole real defect fixed; residual 2–3 e2e failures (tracer geometry + 44px-floor) are host-contention (load 6–23/8 cores), each 3/3 in isolation — documented, not logic bugs. bundle informational→TKT-49; TKT-08 blocked on PDF.
- **Host-load carry-forward:** this machine is resource-contended (leftover/other processes) → e2e is serial(workers:1) + occasionally flaky under load. Before M-004 e2e-heavy work, check `ps`/load; recommend CI retries. Not a code defect.

## Phase 5 = M-005 · Content & media (branch `m-005-content` from main) — IN PROGRESS
CONTENT lane (sequential — all touch data/projects.ts): TKT-28→29→30→{31,32,33}→TKT-54→TKT-39. MEDIA lane PARKED (Tushar records w/ Recordly[SAFE] or drops files → orchestrator encodes; featured-3 block PROD only). Per-ticket detail: Campfire + git + docs/reports + docs/trace/*.md.
- TKT-28 TeachSpark full ✅ commit 56de2fe; 8 chapters + 6 metrics (asOf 08-24) + 8-node thinking, 12 sources, gate PASS, forbidden clean, 0 fabrication, hedges preserved. Fixed 2 latent template a11y bugs (dl→div, ChapterNav overflow); TC-075/076/077 now run. Accepted.
- TKT-29 RailCite full ✅ commit 22b4958; 8 chapters, 6 metrics, live-with-date corpus policy, 0 fabrication (missing items labelled), TC-090 rules verified. Accepted.
- TKT-30 Velora full ✅ commit 635b358; 8 chapters (Nuptis-kill/pivot spine), honest build metrics, PRD line swapped (carry-forward closed), team baseline = secondary research, Trust Scores authored-not-verified. Accepted. **FEATURED-3 CASE STUDIES DONE.**
- TKT-31 Nuptis ✅ commit d89e2b7; full deep-dive (§8.4 distinct content justifies it), 0 header metrics by design (3 PRD metrics honestly "defined but unmeasured"), 13 sources, placeholder frames w/ real dims. Accepted. (Note: first attempt crashed on transient auth error → discarded partial + re-ran clean; auth now fine.)
- TKT-32 Cubicle ✅ commit 0d17b03; deep-dive, built-not-launched throughout (no live link, no fake usage, role "Team build" gap stated), 2 measured metrics, featured trio untouched, 10 sources. Accepted.
- TKT-33 Bhakti-Vilas ✅ commit 301c2db; shorter deep-dive (§8.6 sourced thinking chain), 0 header metrics (missing→honest), 14 sources, commit-split disclosed. Accepted. JS-off thin-slug moved to token-toli.
- TKT-54 (5 lighter builds) ✅ commit fa73dfd; all 5 card-level (facts in overview/metrics, chapters stay empty per committed DEEP_DIVE e2e contract), honesty catches (token-toli 4→3 hypotheses, pratyasa patent line, tegaki "No AI", BYO-ROM sanctioned), cinematic-portfolio linked-not-rebuilt. Accepted.
- TKT-39 EVAL-003 ✅ commit fcacef8; 8/8 questions mapped to sourced artifacts, fixed 6/8 wrong-chapter refs, honest. QA-001(Low) logged. Accepted. **M-005 BUILD COMPLETE (28,29,30,31,32,33,54,39).**
- **M-005 QA-gate flags:** (1) TC-091 refs missing `tests/unit/content-rules.test.ts` + `tests/fixtures/metric-allowlist.json` — real gap vs stale TC ref? (2) QA-001(Low): TeachSpark whitespace PrototypeFrame claimed but prose-only (non-blocking). (3) crawler.ts classifies fetch TIMEOUTS as `dead` unconditionally (only completed 403/429 get WARN) → EVAL-011 flakiness on slow external links — robustness gap. (4) TC-093: featured-3 truth pages need Tushar sign-off — orchestrator confirms on his behalf (autonomous mandate) but note as awaiting his eventual review.
- M-005 QA gate ✅ **PASS-WITH-ACCEPTED-RISKS** (commit a4b023d, docs/reports/M005-qa.md). **Truth audit: ZERO fabricated/untraceable claims** across 11 case studies + 3 professional entries. Baseline green, EVAL-003 8/8, EVAL-011/013/014 pass. TASK-27 closed (Cubicle N/A not deployed).
  - **M-005 accepted-risks / follow-ups (→ M-007 or backlog):** (a) TC-091 automation gap — `tests/unit/content-rules.test.ts` + `tests/fixtures/metric-allowlist.json` never built (a mechanical guard vs reintroducing "625"/"solo"/etc — content is manually-correct now; build in M-007 sweep). (b) crawler.ts timeout→`dead` classification (EVAL-011 external-link flakiness) — add retry/distinct verdict in M-007. (c) QA-001(Low) TeachSpark whitespace prototype prose-only — accepted. (d) TC-093 featured-3 truth sign-off — orchestrator-confirmed; awaits Tushar's eventual review.
  - **Process lesson (→ lesson-learnt):** a QA sub-fork over-scoped + made an unauthorized (but benign, single-file, correct) commit a4b023d. Tighten read-only fork mandates. No integrity impact.
- Next: merge m-005→main → PING.

## Phase 4 = M-004 · Work page & case-study system (branch `m-004-work` from main)
Sequential (e2e serial). Order: TKT-15 → TKT-16 → TKT-17 → TKT-18 → TKT-20 → TKT-21 → TKT-19 (last; needs 15+18). Per-ticket detail in Campfire + git log + docs/reports. Phase-4 QA gate (§C): EVAL-002 hops 1–3, EVAL-014 four states, EVAL-015 VT fallback across slugs, EVAL-004/007/010/011, full eval no regression.
- TKT-15 dataset ✅ commit 45ce7ba (projects:14, 0 fabrication, gate PASS). AC2: Token Toli/Pratyasa/Bhakti-Vilas → `experiments` filter (plan default). Metrics deferred to M-005.
- TKT-16 work page + FilterTabs ✅ commit 283a9c1; /work static(6), 178 tests, ?filter= URL-sync+back/fwd, EmptyState, keyboard tabs, EVAL-008 FAIL→PASS. Filters data-derived (diverges from SITEMAP mapping — data-truth wins). role=tab links. Accepted.
- TKT-17 ExperienceStrip ✅ commit 2ffad00; 50 e2e, 0 dead, reads as employment (flat rows, labeled non-product). Accepted.
- TKT-18 DemoVideo ✅ commit 7e03edc; 4 states verified (17 e2e), intent-gated mount, zero committed binaries, dev-route 404/200. Media type exported. Accepted.
- TKT-20 artifacts + MetricCard ✅ commit 7d3f02d; 8 variants (exhaustive never-switch), MetricCard sourcing guard (compile+runtime), SourceCaption hides /Volumes ref, 186 tests, /dev/artifacts 200/404, axe-clean. Accepted.
- TKT-21 ShowTheThinking ✅ commit 149798a; 8-node reveal, JS-off visible, AT summary, reduced-motion verified, 188 tests, dev-board 404/200. Accepted.
- TKT-19 case-study page ✅ commit 848dd3d; all 11 /work/[slug] static, 47 e2e, EVAL-002 hop-3 PASS, thin-content graceful (no dead anchors), fixed a contrast a11y bug. Accepted. **M-004 BUILD COMPLETE (15–21).**
- Flaky-test HARDENING ✅ commit 9ee9a7b; settle-waits + toPass() retry + whole-px 44px rounding (no thresholds weakened, no code touched). 0 assertion failures across ~5 runs (2 clean full = 252 pass/0 fail; targeted 4-spec 109/0). **Only failure mode = host OS OOM-kill mid-run (~60MB free, other processes) — environmental, not a defect.**
- **HOST RISK (carry-forward, worsening):** machine is memory/CPU-starved (load 15+, OOM-killing e2e; was fine an hour ago — shared box, other users). e2e is workers:1 + can OOM. M-005 (ffmpeg) is CPU/mem-heavy → may struggle. Not fixable by me (other users' load); definitive validation = CI/idle host. Watch `uptime`/`vm_stat` before heavy runs.
- Next: M-004 QA-tester gate → merge m-004→main → PING.
- **M-004 GATE MUST-RESOLVE (now diagnosed):** the recurring ~5 e2e failures (tracer hero-frame/tile-offset + ask-panel/ask-inline 44px sub-pixel) are FLAKY not stale — confirmed by ~5 agents: fail in full run, pass 3/3 isolated, unrelated to their diffs. → HARDEN these timing/measurement specs at the gate (await stable layout / measurement retry / tolerance) so they're reliably green; that's the scar. Not a code defect.
- Note: subagent Co-Authored-By trailer should match each subagent's ACTUAL model (some ran sonnet, briefs hardcoded Opus 4.8) — future briefs say "your session's trailer"; historical commits not amended (low value).

## Phase 6 = M-006 · About · Thinking · Playground · Contact · 404 (branch `m-006-pages` from main) — IN PROGRESS
DAG: **TKT-40 (TSK-22 → TSK-23 → TSK-24) → TKT-41 → TKT-42** (About, sequential — shared `app/about/page.tsx`), then **{TKT-43 Thinking, TKT-44 Playground, TKT-45 Contact, TKT-46 404} in parallel** (disjoint routes). Defaults applied (log as EXE-n if load-bearing): "American Express (via IntraEdge)", "7+ years", 2019–2022 gap note OMITTED until Tushar frames it, Soft Matter DOI = "DOI pending". Contact = email + LinkedIn + city (EXE-8: NO phone/DOB/address). Resume control = PB5 placeholder (`resumeAvailable:false`). Per-ticket detail: Campfire + git + docs/reports/*.md.
Phase-6 QA gate (§C / plan line 723): full `pnpm eval`; EVAL-002 recruiter path `/ → /work → /work/[slug] → /about → resume → /contact` ≤6 clicks; EVAL-007 timeline + AskPanel keyboard; EVAL-006/008/010 all new routes; EVAL-013/016 PII grep (repo + bundle); EVAL-017 OG tags for about/thinking/playground/contact; screenshots for all new routes at 390/768/1024/1440.
- TKT-40 / TSK-22 experience.ts + skills.ts data ✅ commit 75f60f5 (sonnet); 4 roles (godrej/quantiphi/shellkode/amex) + 4 clusters (product/ai/technology/execution), wired into `collections`. Gates: typecheck 0, vitest 194 pass (new 6/6, confirmed red first), prebuild `content OK (experience:4 skills:4)`. Truth audit CLEAN — every field traces to CONTENT_INVENTORY §4.3/§4.5, `scale:'not recorded'` for the 3 MISSING roles, all outcomes `self-reported`, no PMP/SAFe-cert/PII. Accepted.
  - Carry-forward (→ TSK-23/TKT-41 render): AmEx outcome strings carry an inline "(self-reported)" AND `kind:'self-reported'` — render the kind as a badge, don't double-print. Flagged items (non-blocking): gap-year framing omitted (default), role-artifacts MISSING, Soft Matter DOI→TKT-42, "AI"→"AI & GenAI" (schema name min(3)).
- TKT-40 / TSK-23 AboutHero + ProductJourney + app/about skeleton ✅ **DONE & ACCEPTED 2026-09-22** — commit `d2e557c` (sonnet). 9 files, clean (no eval-run/unrelated churn). Gates: typecheck 0, lint 0, vitest 194 pass (impl caught+fixed a forbidden-string in its OWN docstring — the ban check works on comments too), prebuild `content OK` unchanged, build `all routes static (10)` incl `/about`, e2e about.spec 13 pass/0 fail (reduced-motion ProductJourney opacity-only Δy<1px). Independent 2-stage review = **ACCEPT**, no blockers; confirmed by code inspection: "AI Product Manager" absent, 4 stages reveal-only (no click/hash), gap-note absent, no PII/cert. TASK-36.2 → Done. `id="experience"` left as clean placeholder div for TKT-41; clusters/Impact insertion point before it for TSK-24.
  - **DRAFT copy → Tushar (add to the M-001 DRAFT-copy carry-forward list):** (1) AboutHero bio (composed faithfully from CONTENT_INVENTORY §4.1 fragments, "AI Product Manager" omitted): *"Driven by curiosity, systems thinking, and a bias toward building products that solve real customer problems, I enjoy questioning assumptions and uncovering insights hidden in everyday experiences. I bring 7+ years building cloud-native, AI, and data-driven products across GCP and AWS."* (2) ProductJourney section eyebrow/title/lead ("Career arc" / "The product journey" / four-stages lead) — implementer's own framing, §4.2 only specified the 4 stage rows. Both shipped DRAFT-transparent, need Tushar's eventual sign-off; not blocking.
- TKT-40 / TSK-24 CapabilityClusters + Impact ✅ **DONE & ACCEPTED 2026-09-22** — commit `ad7bb9a` (sonnet). New `data/impact.ts` (19 Metric rows + 5 SourceRef, fail-loud resolution mirroring CaseStudyHeader), CapabilityClusters (4 clusters), Impact (MetricCard grid). Gates: typecheck 0, lint 0, vitest 200 pass (new impact tests red→green), prebuild `content OK`, build `all routes static (10)`, e2e about 15 pass/0 fail. Independent review = **ACCEPT**; confirmed byte-identical reuse of projects.ts TeachSpark/RailCite figures, kind/asOf mapping exact, type-only schema import, heading order clean. TASK-36.3 → Done.
- **✅ TKT-40 (TASK-36) COMPLETE** — all 3 subtasks (TSK-22/23/24) done & accepted. TASK-36 → Done. `id="experience"` placeholder intact for TKT-41. AC6 Lighthouse/eval is a page-level check deferred to the M-006 phase QA gate (after TKT-42), per the ticket.
- **M-006 carry-forwards (→ Stage 8 design critique / final QA, NOT blocking build):**
  - **(density) Impact = 19 near-identical MetricCards** — reviewer should-fix: reads as a wall of repeated "Self-reported / Résumé" cards, buries the strong RailCite/TeachSpark numbers (conflicts DESIGN_DIRECTION §1 rule 1). Fix at Stage 8: group by employer/project into ~5–6 compound cards (needs a small compound-metric card shape decision — out of scope for sp:1 TSK-24) OR curate headline metrics + demote the rest. Route to design critique / follow-up ticket.
  - **(consistency) "0 invented citations" context wording differs** between `/about` Impact ("by construction (lib/validate.ts)") and `/work/railcite` (validator sentence) — the TSK-24 brief itself directed the shorter wording; numbers match (0), only the context sentence diverges. Harmonise at Stage 8/9 truth-consistency pass. Minor.
  - **(DRAFT/placeholder → Tushar)** résumé `asOf` 2026-09-15 placeholder (one constant); AboutHero bio + ProductJourney heading + Impact section framing copy = implementer framing, need sign-off.
- **All M-006 briefs now written & turnkey:** TKT-41 (opus timeline), TKT-43 (Thinking), TKT-44 (Playground), TKT-45 (Contact — guardrails: don't re-add contact OG, extend existing stub), TKT-46 (404). TKT-42 brief to be written before its dispatch (depends on TKT-41 assembly).
- TKT-41 ExperienceTimeline ✅ **DONE & ACCEPTED 2026-09-22** — commit `f317925` (opus). components/timeline/{ExperienceTimeline,StoryCard,TimelineNode,timeline-logic}.tsx + e2e/unit. Gates: typecheck 0, lint 0, vitest 213 pass (13 new timeline-logic), prebuild OK, build `all routes static (10)` (/about still ○ Static), eval EVAL-006/007/008/010/011 5 pass/0 fail (008 FAIL→PASS), e2e timeline 14 pass. Independent **opus** review = **ACCEPT**: re-ran e2e itself (14 pass/0 fail), verified in CODE — Enter/Space open, Esc-returns-focus, arrow roving+wrap, one-open, deep-link amex on load (w1440+w390), SSR-safe useSyncExternalStore hash store (no hydration mismatch), reduced-motion CSS snap, aria-expanded/controls wired, "not recorded" literal, no double-printed kind badges. TASK-37 → Done.
  - **Minor carry-forwards (nits, non-blocking):** (a) post-interaction same-page hash re-click (`/about#experience-godrej` clicked while already on /about after a manual open) won't reopen — cross-page nav from /work (the actual AC) works (full load resets). Low-impact awareness only. (b) `formatRange` renders lowercase "present" (matches site convention). (c) segment-brighten realised as hovered-node region highlight; deep-link moves focus to opened card — both product judgment flags for Tushar (trivially reversible).
- TKT-42 (final About assembly) — **BUILT + COMMITTED `d8b472f`, orchestrator-finalized 2026-09-22; TASK-38 → In Review (lightweight code review dispatched).** Awards(3, no PMP/SAFe cert)/Research(patent IN 429867, Langmuir DOI, Soft Matter "DOI pending", NIT-Calicut rights)/Education(2), /about assembled SITEMAP order + CTAs + TP10 colophon, About OG, data/credentials.ts (§4.6–4.8). All gates re-verified by orchestrator on a calm host: typecheck/lint 0, vitest 219 pass, build all-static(10) (/about + /about/opengraph-image static), eval EVAL-002/004/013/017 4 pass/0 fail regressions:[], about.spec 19 pass, **Lighthouse /about desktop 100/100/96/100** (≥90/95/95/95 ✓). Honesty verified by grep+e2e (429867 only, DOI-pending, no fabricated DOI, PMP/SAFe absent). **→ ✅ TKT-40+41+42 = the whole /about page complete.**
  - **⚠️ PROCESS SCAR (→ lesson-learnt): the TKT-42 implementer's final `pnpm eval` monitor STALLED/OOM'd** under a host load spike (~5.0) — the eval process deadlocked at 0% CPU, its Monitor never fired, and the agent re-notified twice as "interim/waiting" without progressing. Orchestrator stop-rule triggered: confirmed the stall (process later died, :3000 down, no commit), `TaskStop`ped the agent, reaped stalled procs, re-ran+verified every gate on a calm host, wrote the report, committed. **Lessons:** (1) an implementer waiting on a background eval Monitor can deadlock if the eval OOMs — the Monitor should widen its filter to catch crash/exit, OR the orchestrator polls for the eval-run json/commit with a timeout stop-rule (used here). (2) On this host, run the heavy `pnpm eval` (LHCI + Playwright) ONLY when load is low; check `uptime` first. (3) Prefer having the implementer run `--skip-build` after its own build and gate on `evals/results/<label>.json` existence, not a live Monitor.
  - **Carry-forward → M-006 phase QA gate:** mobile Lighthouse perf assertion failed (status 1) = documented-env swiftshader/no-GPU LHCI (informational, as in M-002/M-003) — verify mobile perf on CI/idle host at the gate, not a code regression.
- **ORCHESTRATION DECISION — the 4 parallel pages (TKT-43/44/45/46) run SEQUENTIALLY, not concurrently.** Rationale (evidence-based, overrides the "≤2 concurrent" default): `playwright.config.ts` hardcodes `webServer: pnpm start` on **:3000** with `reuseExistingServer:true` + `workers:1` (hard, host scar). Two concurrent implementers would collide on :3000 OR the 2nd silently tests the 1st worktree's build (reuseExistingServer) — a correctness hazard, plus OOM risk from concurrent builds+browsers on this memory-contended host, plus the global-rule warning on silent worktree contamination. The resume mandate explicitly permitted "or run sequentially". For 4 small (sp:1–3) disjoint-route tickets, reliability > marginal wall-clock. Order: TKT-43 → TKT-44 → TKT-45 → TKT-46, each on m-006-pages directly (no worktree), each two-stage review + accept before the next. (If a future run has an idle host AND per-worktree distinct ports, concurrency could be revisited — not worth it here.)
- TKT-43 /thinking list + essay route ✅ **DONE & ACCEPTED 2026-09-22** — commits `313d087` (sonnet build) + `cd22388` (a11y fix). data/writing.ts (5 DRAFT essays, verbatim §5 quotes, draft:true, no dates — schema-enforced), components/thinking/{ThinkingHero,ThinkingList,EssayBody}, app/thinking/{page,[slug]/page,opengraph-image}, tests. Gates: typecheck/lint 0, vitest 226, build all-static(12) (/thinking + 5 slugs), e2e thinking-page 19 pass, eval EVAL-006/011/013/017 4 pass/0 fail regressions:[]. Review = ACCEPT w/ 1 should-fix → **fix loop round 1: essay titles were styled `<span>` not semantic headings (SR heading-nav couldn't find them; axe misses MISSING headings) → fixed to real `<h3>` + added a visually-hidden `<h2>` "Essays" to keep h1→h2→h3 outline (no skip) + regression test (getByRole heading level=3 ×5, scoped to <main>).** TASK-39 → Done.
  - **Flags → Tushar (non-blocking):** 5 essay TITLES are DRAFT pending sign-off; dek/framing paragraphs are authored connective text (restating only what's quoted); readingMinutes are estimates; essay-1 relatedProject=teachspark (ambiguous vs cubicle). **Filename note:** used `tests/e2e/thinking-page.spec.ts` (the brief's `thinking.spec.ts` already exists = TKT-21's /dev board; left untouched).
  - **Scar (a11y):** styled-span-as-heading is invisible to axe but breaks SR heading-nav + Design.md "title (h3)". Watch for it in TKT-44/45 tile/card titles — use real heading elements at the right level.
- TKT-44 /playground ✅ **DONE & ACCEPTED 2026-09-22** — commit `e07c335` (sonnet). PlaygroundHero + PlaygroundGrid (4 tiles: Pratyasa/butter, Tegaki/peach, dino-arcade/blush, cinematic/mint — live URLs from data/projects.ts, real `<h2>` titles per the a11y scar), app/playground/{page,opengraph-image}. Gates: typecheck/lint 0, vitest 226, build all-static(13), e2e playground 25 pass + full suite 340 pass, eval EVAL-008/009/011 2 pass/0 fail regressions:[] (0 dead; 10 warn = pre-existing LinkedIn bot-block). All 4 live URLs HTTP 200. Review = **ACCEPT** (no blockers): safety confirmed (excluded Slag City/Mock Interview/Game + ROM/BIOS have NO export to leak, named imports not slug loop), real h2 outline h1→h2 no-skip, URLs from data. TASK-40 → Done.
  - **⚠️ PHASE-GATE MUST-FIX (pre-existing, confirmed by 2 reviewers): `/about` is MISSING from the sitemap** — `app/sitemap.ts` STATIC_ROUTES = `["/","/work","/thinking","/contact","/playground"]`, never got `/about` (TKT-42 omission). Built sitemap.xml has /thinking + /playground + essay slugs but NOT /about. Also `tests/e2e/routes.json` static list is missing `/about` + `/thinking`. **At the M-006 phase QA gate: add `/about` (and confirm `/thinking`,`/playground`,`/contact`) to `STATIC_ROUTES` + `routes.json`, rebuild, verify sitemap.xml completeness + EVAL-017 OG for all new routes.**
  - Nits (non-blocking): ClayTile size=180 inert under !w-full (cosmetic); OG tone butter (playful, Design-permitted, disclosed).
- TKT-45 /contact ✅ **DONE & ACCEPTED 2026-09-22** — commit `c15b822` (sonnet). ContactCard (hero-tier lavender ClayCard, h1 "Still curious?", CopyButton email + mailto + LinkedIn + resume-from-resumeAction() placeholder + "Bengaluru, India"); extended the existing app/contact/page.tsx stub (contact OG left untouched). Gates: typecheck/lint 0, vitest 226, build all-static(13), e2e contact 11 pass (CopyButton denied fallback tested end-to-end) + eval-006/008/011, eval EVAL-002/007/011/013 4 pass/0 fail regressions:[]. Review = **ACCEPT** (no findings): no phone/DOB/address (email+LinkedIn+city only §7), no form (S10), resume not hard-coded, OG untouched, real h1. Resume-flip 200-download is test.fixme'd (TKT-08 no PDF) with the resumeAction() contract unit-mocked — precedented by eval-002's same fixme. TASK-41 → Done.
- TKT-46 404 not-found page ✅ **DONE & ACCEPTED 2026-09-22** — commit `d70106b` (sonnet). app/not-found.tsx (flat, one ClayCard, h1 "This page wandered off.", links /,/work,/contact; real 404 via Next App Router convention; chrome inherited). Gates: typecheck/lint 0, vitest 226, build all-static(13) (/_not-found emitted), e2e not-found 12 pass (response.status()===404 asserted), eval EVAL-011/015 0 fail 0 regressions. Review = **ACCEPT** (real 404 asserted, 3 links resolve, chrome inherited, real h1). Copy is implementer's own on-brand wording → flag for Tushar sign-off. TASK-42 → Done.
- **✅ ALL 6 M-006 TICKETS BUILT & ACCEPTED (TKT-40/41/42/43/44/45/46).**
- **✅ /about sitemap gap FIXED (phase-gate sweep):** added `/about` to `app/sitemap.ts` STATIC_ROUTES (now `/,/work,/about,/thinking,/playground,/contact`) + `/about`,`/thinking` to `tests/e2e/routes.json`. Verified: typecheck 0, build all-static(13), sitemap.xml now contains /about + /thinking + /playground + /contact. (lib/anchors.ts's separate link-validation STATIC_ROUTES already had /about.)
- **M-006 phase QA gate RUN 1 (commit 401d19f): FAIL on 1 critical — but it is a TEST-INFRA false positive, not a product defect.** Report `docs/reports/M006-qa.md`, eval `evals/results/eval-run-0.2.0-401d19f.json` (12 pass/2 fail/3 manual; e2e 382 pass/1 fail). **Everything product-side CLEAN:** EVAL-006/007/008/010 pass all new routes ×4 widths; EVAL-013/016 0 PII (source+bundle), 429867 only, "DOI pending", no PMP/SAFe/ROM; EVAL-017 OG ×4 new routes; sitemap /about confirmed; EVAL-002 recruiter path 4 clicks.
  - **Blocker = EVAL-011 (QA-002): crawler false positive.** `tests/e2e/crawler.ts observeButtonEffect()` calls `page.goBack()` on ANY location.href change; ExperienceTimeline TimelineNode uses `history.replaceState()` for deep-link hash sync (no history push) → goBack pops the crawler's own nav → corrupts /about crawl → 2 false "dead" controls. NOT a product bug (timeline.spec verifies the hash sync works). **→ Fix dispatched (sonnet): make observeButtonEffect ignore hash-only replaceState changes (compare path+search / history.length); re-run full EVAL-011 + full eval to confirm criticalFailures:[].** Relates to the M-005/M-007 crawler-robustness carry-forward.
  - **Non-blocking (→ follow-up):** /contact CopyButton lacks a dedicated keyboard e2e (native button, mitigated); bundle 218.7→244.4kB gz > 180 budget (informational → TKT-14/49); mobile LHCI perf swiftshader (env).
- **Crawler fix ✅ commit `a3accca` (sonnet)** — observeButtonEffect ignores hash-only replaceState (compares path+search / history.length) + doi.org/pubs.acs.org added to BOT_BLOCK_HOSTS (Langmuir DOI Cloudflare-challenge on HEAD, genuinely live, same class as LinkedIn/GitHub) + regression test. 2 files only (crawler.ts + eval-011 spec), no product code touched.
- **✅ M-006 phase QA gate RUN 2 (@a3accca) = PASS-WITH-ACCEPTED-RISKS.** Full `pnpm eval` exit 0, **criticalFailures:[]**, EVAL-011 PASS (/about 0 dead), 13 pass/1 fail(EVAL-005 informational bundle)/3 manual, `eval-run-0.2.0-401d19f-4.json`. Report `docs/reports/M006-qa.md` updated to RUN-2 verdict. Accepted-risks: EVAL-005 bundle 244.4kB>180 (→TKT-49), EVAL-004 mobile LHCI 96→92 swiftshader env, EVAL-016 headers/017 inspector need deploy (TKT-50/51), /contact CopyButton keyboard-e2e follow-up.
- **Changed-file audit (git diff main..HEAD --stat, 20 commits/105 files) CLEAN:** all M-006-scoped (app/{about,thinking,playground,contact,not-found,sitemap}, components/{about,contact,playground,thinking,timeline}, data/{experience,skills,impact,writing,credentials,index}, backlog status, lighthouserc, tests, docs). NO untouchable files (cinematic/portfolio-index clean), no secrets/stray config.
- **NEXT: merge m-006-pages → main (--no-ff, local; no remote until TKT-50) → update HANDOFF → report to Tushar at the milestone boundary. Then M-007 (quality sweeps + deploy) / Stages 8–10 remain.**

## Phase 7 = M-007 · Quality sweeps + deploy prep (branch `m-007-quality` from main) — IN PROGRESS 2026-09-22
Branch `m-007-quality` created off `main` (M-006 merged at ca49f01). Entry gate met: Campfire HTTP 200, node v26.7.0 / pnpm 11.25.0 (match). Baseline on branch: typecheck 0, lint 0, build `all routes static (13)`; bundle ~244.4kB (M-006 gate, EVAL-005). Host memory-tight (~108 MB free, load ~2.5) — heavy `pnpm eval`/e2e/LHCI run FOREGROUND with a stop-rule, `workers:1`, check `uptime` first (background Monitor deadlocked twice — do not use).
**Scope reconciliation (tickets.md authoritative over baton's loose labels):** the baton labelled TKT-52 "analytics" and TKT-51 "SEO/OG"; tickets.md is the truth — TKT-51 = link-preview inspector validation (hard-deps TKT-50 preview URL), TKT-52 = evidence pack + HANDOFF (hard-deps TKT-50/51). Analytics is part of TKT-50's code.
**Doable now:** TKT-49 (perf/bundle) → TKT-47 (responsive) → TKT-48 (a11y) → carry-forwards (TC-091 content-guard automation, crawler timeout-robustness) → TKT-50 **code-only** (security headers in next.config, Vercel Analytics+Speed-Insights wiring, pre-deploy check script + unit tests, `pnpm audit` gate) → TKT-51 **local** OG-tag correctness → TKT-52 **local** record eval-run + evidence pack → M-007 QA gate.
**HARD-STOPPED (build up to, leave turnkey — NOT crossed autonomously):** Vercel project/GitHub repo creation, preview + prod deploy, domain buy (TKT-50 deploy step, TKT-53), sanitised resume PDF (TKT-08, `resumeAvailable` stays false), featured-3 demo videos (Tushar records w/ Recordly).
**Env constraint:** LHCI mobile perf is swiftshader/no-GPU-unreliable on this host (documented M-002/M-003/M-006) → TKT-49 Lighthouse-**score** ACs verified at the Vercel preview/CI; the **JS-budget** AC (≤180 kB, measurable via build/EVAL-005) is done now.
**Order rationale:** TKT-49 first (riskiest — known 244>180 gap may be infeasible without threshold change, which EV2 forbids; and its code-splitting changes the DOM/bundle the 47/48 sweeps capture — sweep the FINAL code once). Sequential (Playwright :3000 reuseExistingServer scar + OOM), one fresh subagent per ticket, two-stage review, bounded fix loop, separate QA-tester at the phase gate.
| Ticket | Native | Tier | Status | Notes |
|---|---|---|---|---|
| TKT-49 perf/bundle | TASK-45 | opus | ⚠️ code ACCEPTED, AC2 accepted-risk (In Review) | commits `c383f38` (levers −16.7 kB, design-preserving: NavPill/AskPortfolio/Parallax off `domAnimation`) + `e74906b` (report) + `0655fd4` (EXE-10 gate measurement fix + regression test). 2-stage review by orchestrator = ACCEPT (levers technically sound: `domAnimation` excludes layout so NavPill slide was inert; `useSpring` standalone; reduced-motion via global `animation-duration:1ms`, EVAL-010 e2e pass). **AC2 NOT met: `/` = 189.3 kB gz honest (was 244.4 counting a 38.5 kB noModule polyfill no module-browser downloads — EXE-10) vs 180, still exit 1.** Residual ~9 kB = approved cursor-spring core → EXE-11 provisional B2 (keep spring), Lighthouse-SCORE deferred to Vercel preview (swiftshader), B1 vs B2 + EXE-10 ratification FLAGGED for Tushar. gates: typecheck/lint/build all-static(13), bundle-budget test 5/5 incl EXE-10 case. |
| TKT-47 responsive sweep | TASK-43 | sonnet(QA) | ✅ DONE & ACCEPTED | commits `283bc13` (broaden EVAL-008 to full 22-route sweep) + `be16385` (essay CTA 44px fix) + `8efca42` (88-file screenshot pack docs/screenshots/) + `b95231a` (report). 2-stage review = ACCEPT: coverage broadened (route list now DERIVED from app/sitemap sources — projects `category:personal` + writing — so new pages auto-swept; 14px check now loops ALL routes, was `/`-only), NO threshold weakened, kept out of shared routes.json. Sweep = 309 pass/0 fail across 22 routes×3 checks; typecheck/lint/vitest(227)/build(49 static) clean; foreground, no OOM. **1 defect found+fixed:** 5 `/thinking/<slug>` essay "Related project" CTAs were 27px<44 (slipped past M-006 due to the under-coverage). **SCAR:** EVAL-008 was silently under-covering (7/22 routes, 14px only on `/`) through all prior gates — now DERIVED from sitemap sources so it can't drift. |
| TKT-48 a11y sweep | TASK-44 | sonnet(QA) | ✅ DONE & ACCEPTED | commits `ce83792` (broaden EVAL-006 axe + EVAL-010 reduced-motion to 22 routes, sitemap-derived like TKT-47) + `8092086` (QA-003 heading fix) + `1934c24` (keyboard e2e CopyButton/OverviewToggle/ShowTheThinking) + `e1b5cd7` (docs/a11y-pass.md + report). 2-stage review = ACCEPT. **axe 0 crit/serious all 22 routes @390/1440; 8/8 keyboard flows pass** (closed M-006 CopyButton follow-up); reduced-motion pass all 22 (was `/`-only); contrast via axe color-contrast per route (stronger than spot-check). `pnpm eval --only EVAL-006,007,010` = 166 pass/0 fail; typecheck/lint/build clean. **docs/a11y-pass.md** = AT-tree evidence for /,/work/teachspark,/about; real macOS VoiceOver pass FLAGGED turnkey for Tushar (not faked). **SCARS:** (1) EVAL-006/010 were under-covering like EVAL-008 — now sitemap-derived; (2) **QA-003 (resolved):** all 11 deep-dive case studies skipped h1→h3 (no h2), DecisionCard h4 — invisible to axe wcag2aa (heading-order is best-practice, not AA) — fixed Chapter h3→h2 + DecisionCard h4→h3 (class-sized, no visual change). **Follow-up (→ carry-forwards): add a heading-order regression guard** (enable axe `heading-order` rule or an e2e no-skip assert) so QA-003 can't recur silently. |
| carry-forwards (CF-1/2/3) | — | sonnet | ✅ DONE & ACCEPTED | commits `09932e0` (CF-1 crawler timeout→`warn` via exported `isTimeoutError()` + 6 unit tests, red→green) + `22e12bc` (CF-2 `tests/unit/content-rules.test.ts` 48 assertions/1 skip + `metric-allowlist.json`, TC-091 rules over data/projects.ts, teeth proven by planting 3 violations) + `3bd719e` (CF-3 axe `heading-order` guard across 22 routes, red→green) + `0491c83` (report). 2-stage review = ACCEPT. CF-2 interpretations sound (625 allowed only within 60 chars of "not used"/"could not be reproduced"; solo only as persona/denial — guards the INTENT, not loosened). vitest 280 pass/2 skip/0 fail; EVAL-006/011/013 pass. **CF-3 surfaced a real pre-existing /work h1→h3 skip** → **QA-004 (resolved by orchestrator):** commit `1d57be4` (sr-only "Personal builds" h2 in app/work/page.tsx, no visual change) + un-fixme'd /work in the guard; **full heading-order sweep now 22 passed** (verified via `pnpm run test:e2e`). Briefs + PWA sync committed `1c87e2c`. |
| **LESSON (env):** running Playwright directly, ALWAYS use `pnpm run test:e2e ...` (or `dotenv -e .env.tooling -- playwright test`) — raw `pnpm exec playwright test` misses `PLAYWRIGHT_BROWSERS_PATH=/Volumes/E Drive/Dev/.cache/ms-playwright` and fails "Executable doesn't exist" pointing at the Mac INTERNAL disk cache. 88 phantom "(1ms)" failures were this, not code. | — | — | 📌 | subagents run e2e via the eval harness (which sets it); the orchestrator must remember to. |
| TKT-50 code-only | TASK-46 | sonnet | ✅ CODE ACCEPTED (deploy BLOCKED on Tushar) | commits `f71379e`/`f0d78d4`/`665dbe7`/`1aa54cd`/`59c3a33`. 2-stage review = ACCEPT. **Headers/CSP** in next.config (default-src 'self'; script-src 'self' 'unsafe-inline' va.vercel-scripts.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' [self-hosted, no CDN]; connect-src 'self'; frame-ancestors 'none') + HSTS preload + X-Content-Type-Options + Referrer-Policy + Permissions-Policy + X-Frame-Options DENY. Impl READ the installed @vercel/analytics v2 source (endpoints are same-origin /_vercel/*, covered by connect-src 'self') — corrected my brief's guessed hosts. **Analytics** <Analytics/>+<SpeedInsights/> in layout, all-static(13) preserved, verified in real Chrome (0 CSP violations on /,/work,/work/teachspark). **predeploy-check.ts** (4 failure modes, wired first in `prebuild`, empirically confirmed pnpm auto-runs prebuild) + 18 unit tests (each mode fires; clean state passes). audit exits 0. **docs/deploy.md** turnkey runbook + rollback + 5 preview-only checks deferred. Gates: typecheck/lint/test(298 pass/2 skip)/build green. NO deploy/remote/Vercel action taken. **→ Stage-10 security register:** (a) CSP `'unsafe-inline'` script/style-src = unavoidable Next static-hydration limitation (no nonce on fully-static; minimal XSS surface — deterministic Ask takes data not query); (b) NEW dev-only MODERATE advisory `uuid` via `@lhci/cli` (below the `--audit-level high` gate; not shipped) — not yet in decisions.md, Stage 10 to adjudicate (cf. EXE-2). |
| TKT-51 local OG | TASK-47 | sonnet | ⬜ pending | tag test now; inspector step turnkey-deferred (needs preview URL) |
| TKT-52 evidence pack | TASK-48 | sonnet | ⬜ pending | local record run + pack; preview re-run deferred |
| TKT-51 local OG | TASK-47 | — | ✅ local slice done / ⛔ inspector BLOCKED-on-preview | EVAL-017 OG tags verified locally in built HTML (absolute-HTTPS, 1200×630 PNGs, all page families) via the QA-gate run. The opengraph.xyz + LinkedIn Post Inspector step needs the public preview URL → turnkey-deferred (docs/deploy.md). Campfire → Blocked. |
| TKT-52 evidence pack | TASK-48 | sonnet(QA) | ✅ local slice done / full pack at preview | canonical local record `evals/results/eval-run-v1.0.0-rc.json` produced by the gate (commit `c348d8f`); Stage-8/9/10 packs = the committed docs/reports/* + docs/screenshots/ (88) + docs/a11y-pass.md + docs/deploy.md. Preview-URL record run deferred (hard-stop). HANDOFF→Stage 8 written. |
| **M-007 phase QA gate** | — | sonnet(QA) | ✅ **PASS-WITH-ACCEPTED-RISKS** | commit `c348d8f` (+ `6a78aa4` rc). Independent QA-tester. typecheck/lint/build clean; full `pnpm eval` = 13 pass/1 fail(EVAL-005 informational)/3 manual, **criticalFailures:[]**; vitest 298 pass/2 skip/0 fail; e2e 686 expected + 1 unexpected (=QA-005, now fixed). EVAL-002/006/007/008/010/011/012/013/014/015/017 PASS; EVAL-004 PASS (mobile 91–94 swiftshader informational); EVAL-005 FAIL informational (bundle 191.1 kB gz > 180 — EXE-10/11 accepted-risk, Lighthouse score → preview); EVAL-016 headers + EVAL-017 live-inspector BLOCKED-on-preview. Accepted-risks confirmed, no NEW blockers. eval-run `.gitignore` hygiene done (transient `eval-run-0.2.0-*.json` ignored, canonical rc + baseline kept, none deleted). |
| **QA-005 (resolved by orchestrator)** | — | opus(orch) | ✅ FIXED | commit `06f4eda`. NEW regression the gate found: @vercel/analytics + @vercel/speed-insights inject `/_vercel/*` scripts on `NODE_ENV=production` (incl. local `pnpm start`), not only on Vercel → 404 off-platform → broke the "no console errors" smoke gate. Fixed: gate `<Analytics/>/<SpeedInsights/>` on `process.env.VERCEL` (inlined at SSG build; local build/start excludes them, prod/preview keep them). Verified: analytics absent from local built HTML, smoke 2 passed, all-static(13). NOT a production defect (real Vercel serves the scripts); the "no-op off Vercel" comment/report claim was corrected. |
| **M-007 COMPLETE** (doable scope) | — | — | ✅ | All doable M-007 work done: TKT-47/48/49 accepted, carry-forwards + QA-003/004/005 fixed, TKT-50 code accepted, TKT-51/52 local slices done, phase QA gate PASS-WITH-ACCEPTED-RISKS. **BLOCKED-on-Tushar (turnkey):** TKT-50 preview deploy, TKT-51 inspector, TKT-52 preview record, TKT-53 prod (all need Vercel/GitHub/domain); TKT-08 resume PDF; featured-3 videos. **Decisions awaiting Tushar:** EXE-10 (bundle measurement) ratify, EXE-11 (perf B1 vs B2). Next: Stages 8 (Design Critique)→9→10 before any deploy. |

### M-002 QA-gate fix items (resolve at the gate, after TKT-07b)
- **(a) caption 12–13px vs 14px token:** decide overline exception (uppercase tracked eyebrows/monogram/tile LABELS ok <14px, like the WCAG-2.5.8 inline-link precedent) vs real bug — CHECK whether the 12–13px is a label/overline (exception) or the tile ONE-LINER content (must be ≥14px → fix in TKT-04/05). Record as EXE + encode in eval-008 spec.
- **(b) /contact "email me" link 80×26 (h<44):** fix (≥44 or data-inline-link) — contact stub / TKT-06.
- Both fixme'd in tests/e2e/eval-008 by TKT-07a per EV2 (not faked).
| TKT-08 resume PII gate | TASK-8 | cheap | ⛔ blocked | S08r.01 test infra only (skips loud, resumeAvailable stays false); S08r.02+ need Tushar's sanitised PDF — hard-blocks prod (TKT-53) |

## Conflicts already resolved (§E — read before TKT-01)
E-1 footer "Built with curiosity." · E-6 split card-padding tokens · E-7 ClayButton primary = bg text on accent · E-9 nav = Home·Work·Thinking·About · E-11 featured cards use ClayIcon not imagery · E-12 verify VT export name at S06.01 · E-13 resume both states covered. Full table: technical-plan.md §E.

## Fan-out plan (Tushar asked to parallelize) + M-005 media tool
- **Where fan-out is safe:** M-005 media (TKT-22–27 ffmpeg encodes — no server) ∥ content/trace docs, and M-006 independent pages. NOT TKT-07 (its e2e/LHCI steps share `pnpm start` :3000). Serialize `data/projects.ts` edits (shared file). Use git worktrees with worktree + node_modules pinned to E-Drive, ~2–3 concurrent (bounded by review+merge), per-result review kept.
- **Recordly REVIEWED 2026-09-17 → SAFE TO TRIAL** (`https://github.com/webadderallorg/Recordly`): AGPL Electron screen-recorder, on-machine capture + local ffmpeg export, NO hosted upload / telemetry / credential access; only outbound = changelog + Google Fonts + official whisper model; extensions disabled. Clean. Precautions: test on a throwaway screen first, keep extensions off, treat exported videos as sensitive (whatever's on-screen is baked in). **IMPLICATION:** it's a DESKTOP app Tushar runs — being safe does NOT let the orchestrator produce the videos (needs Tushar's authed logins + interactive recording). Media lane = Tushar records (Recordly ok) OR drops raw files → orchestrator encodes/optimises + forbidden/PII scrubs (TKT-22–27). Featured-3 videos block PROD only (TKT-53). Clone at /Volumes/E Drive/Dev/.scratch/recordly-review/repo (delete when done).

## Open items (defaults applied; Tushar's calls) — do not block execution
TeachSpark metric date (08-24) · RailCite figure policy (live-with-date) · Cubicle deploy (N/A) · domain name · sanitised resume PDF (hard-blocks TKT-53 only) · GitHub repo creation · years wording ("7+"). Full list: §E "Open items carried".

## Carry-forwards (read before the named task)
- **TSK-07 gates:** (a) LCP hint check must be `grep -ic 'fetchpriority="high"'` (≥2: img + preload) — React 19 serialises `fetchPriority` camelCase, so the plan's `grep -c` returns 0 (false negative). (b) Colour-token invariant = `pnpm tokens:check` (13/13) or `grep -cE '^\s*--color-[a-z0-9-]+:'` (13); the crude `grep -c '--color-'` is polluted by var() refs (EXE-4 note). (c) At w1440 the hero frame is now 520 (EXE-4 breakpoint fix).
- **DRAFT copy for Tushar (M-001 gate):** hero eyebrow triad, headline, support line are DRAFT (shipped DRAFT-labelled per plan default). Tagline + 3 tile one-liners are VERIFIED.
- **M-003 (home complete) — REVISIT hero grid balance (EXE-6 follow-up):** the avatar is column-capped and measures only ~252px @1024 / ~384px @1440 because the 65fr min-content headline squeezes the 35fr avatar track. Accepted for the tracer (placeholder FeaturedWork dominates the sparseness). At M-003, with real FeaturedWork content, run the 5-second test and decide whether to widen the avatar track (e.g. 40fr_60fr) or restructure the hero. Do NOT force fixed avatar widths (reintroduces the F1 overflow).
- **TKT-07 flaky test:** `tracer.spec.ts` hero-tiles-offset-ladder failed once under parallel-worker contention (passes 3/3 isolated). At TKT-07 make it robust (await stable layout / serialize the tile-offset measurement) so CI isn't flaky.
- **TKT-07 screenshot-churn fix:** every eval/e2e run re-renders the committed `docs/screenshots/tracer/*.png` (deterministic, visually identical), so they show modified after each ticket and (a) clutter the tree, (b) trip the destructive-command gate on `git checkout`. At TKT-07, route routine-run screenshots to a git-ignored path (e.g. `test-results/screenshots/` or `.eval/`), and keep committed `docs/screenshots/**` only for deliberate milestone/gate captures. Until then the orchestrator just commits the re-renders (harmless).
- **Perf levers (F5) at TKT-14 / TKT-49:** tracer first-load JS 218.7 kB (budget 180), LCP mobile 2858 ms (budget 2500). Pull levers (code-split motion, image tuning, chunk analysis) at TKT-14 (home) and TKT-49 (perf sweep) per A14.
- **TSK-04 / S04.01:** `lib/motion.ts` `easings` object MUST equal the CSS `--ease-*` values just added to globals.css (EXE-3): hover=`cubic-bezier(0.23,1,0.32,1)`, reveal=`cubic-bezier(.2,.7,.2,1)`, panel=`cubic-bezier(0.32,0.72,0,1)`, vt=`cubic-bezier(.77,0,.175,1)`. JS/CSS duplication kept in sync.
- **TSK-06 / S06.01:** `experimental.viewTransition` is GONE in Next 16.3.5 (EXE-1). VT is built into the App Router. S06.01 must still verify the React `<ViewTransition>` export name and behaviour before writing VT code (E-12), using the built-in App Router mechanism (`<Link transitionTypes>` / app-router-context), NOT the removed config flag.
- **Stage 10 / QA-report:** accepted-risk EXE-2 (two dev-only `extract-zip` highs via `ignoreGhsas`) must appear in the accepted-risks register.
- **Minor TSK-01 choices (report, not decisions.md):** `agentRules:false` (stops Next auto-writing AGENTS.md/CLAUDE.md), `allowBuilds.esbuild:true` (tsx/vitest), base-layer colours via `@apply`, explicit-path git staging, `*.tsbuildinfo` added to .gitignore by orchestrator.

## Log
- 2026-09-15 — Stage 7 opened. Read HANDOFF + technical-plan §0/A9/A10/B(M-001)/C/E, verified env. Reconciled stale memory (Solution-PRD was Approved 2026-09-15; memory was pre-sign-off). Setup done. Next: dispatch TSK-01 (opus).
- 2026-09-22 — Resumed M-006. Host healthy (load ~1.5, 31 GiB free E-Drive) vs earlier contention; kept e2e workers:1 per convention. Campfire up (200). TASK-36.2 → In Progress. Dispatched TSK-23 (sonnet) against docs/briefs/TSK-23.md. Pre-wrote docs/briefs/TSK-24.md + TKT-41.md as DAG lookahead. Awaiting TSK-23 report.

---

## Stage 8 · Design Critique — ✅ COMPLETE 2026-09-22 (branch `m-007-quality`)
`bw-design-critique` → `impeccable`. A design-reviewer subagent drove the running build at 1440 (its `resize_window` was pinned — mobile via the automated EVAL-008 sweep); orchestrator **independently verified every finding** (dev `:3000` + a prod `next start :3100`, DOM measurement + the deterministic Ask provider) before acting. Report: `docs/reports/DES-findings.md`; decisions `DC1–DC4`.
**11 findings → 4 fixed · 3 parked · 4 dismissed.**
- **DES-001 (P0) FIXED `557b85e`** — `ExperimentCard` horizontal row overflowed its 184px card by +193–239px (illegible spill); measured on dev AND prod. → vertical connector always + `min-w-0`; re-measured overflow=false.
- **DES-002 (P1) FIXED `557b85e`** — `ArtifactGrid` fixed 3-up in the 60ch column = 184px slivers + empty tracks for 1-artifact chapters → `auto-fit minmax(15rem,1fr)`; lone card fills column (601=601), 2-up at 288px. Deviation from literal "3-up ≥1024" (legibility).
- **DES-004 (P1) FIXED `8175391`** — `/about` Impact 19-card wall (11 self-reported résumé buried the measured numbers) → two tiers: 8 product cards prominent + 11 self-reported in a quieter flat block under an h3. No numbers changed; MetricCard/EVAL-013 + badges preserved.
- **DES-005 (P1) FIXED `7ec03a1`** — Ask returned empty for "tell me about railcite/teachspark/velora" (+ bare names); confirmed via provider → data-only aliases (railcite/teachspark→ai-products, velora→built); no scoring change, no fabrication, EVAL-012 intact.
- **DISMISSED:** DES-007 (deep-link — dev-only React StrictMode double-effect; **prod verified working**: hash stays `#experience-amex`, card open); DES-008 (EditorialGrid IS the spec'd asymmetric layout — measured large card 779×782 = 8col/2row); DES-006 (Draft badge = intentional draft-content honesty flag); DES-010 (lowercase "present" = convention).
- **PARKED:** DES-003 hero-fold (CTA bottom 819px @1440×748; local-display artifact, NOT re-tuned → routed to Tushar's manual EVAL-001 spot-check, see DC4); DES-009 (→ Stage-9 truth pass); DES-011 (spec-satisfied — email is a clipboard-error fallback).
**Gate:** typecheck/lint 0 · build all-static(13) · vitest 298 pass/2 skip · e2e about+eval-006+eval-008 **349 pass/0 fail** (390/768/1024/1440). Web-deliverables: mobile PASS · four-states PASS · link-preview PASS (live inspector deferred). No threshold weakened; no content truth changed.
**New carry-forwards:** regenerate `docs/screenshots/about/*` (predate the Impact retier); DES-009 wording harmonisation at Stage 9.
**NEXT = Stage 9** (`bw-code-review-test-eval`): `/code-review` on `main..m-007-quality` + full `pnpm eval` record run. Do NOT merge/deploy before the Stage-10 QA-report gate.

---

## Deploy · GitHub + Vercel PREVIEW live 2026-09-22 (Tushar-authorized in-session)
Tushar explicitly asked to push to GitHub + deploy to Vercel (lifts the autonomous hard-stop for THIS action). Chose **private repo + preview** (via AskUserQuestion). Pre-push safety verified: no tracked secrets/PII, `.env.tooling`/`.env.example` secret-free, real résumé PDF gitignored, `resumeAvailable:false`.
- **GitHub (private):** `https://github.com/007U5H4R/portfolio-clay` — `main` (M-006) + `m-007-quality` (M-007 + Stage-8) pushed; default branch `main`. No build artifacts tracked.
- **Vercel project:** `portfolio-clay`, id `prj_QrQmCfOiPTcaLV81FVekot4t6zsD`, team `team_pLaStAJybzggE3tGD5ioih5M` (007u5h4r, hobby). Production branch `main`. **SSO protection ON for previews** (viewer must be logged into the Vercel team).
- **Build-command override (IMPORTANT):** project buildCommand = `pnpm run prebuild && next build && pnpm exec tsx scripts/assert-static.ts` (installCommand `pnpm install --frozen-lockfile`). This deliberately SKIPS the `pnpm build` script's `dotenv -e .env.tooling` wrapper, because `.env.tooling` hard-codes E-Drive paths (`TMPDIR=/Volumes/E Drive/...`, Playwright cache) + a placeholder `NEXT_PUBLIC_SITE_URL` that don't exist / are wrong on Vercel's Linux builder. Runbook gap → fix noted (docs/deploy.md §3/§4 assume default `pnpm build`; that would break the build and mis-set OG URLs). Follow-up: reconcile the runbook / consider a Vercel-safe build script.
- **First deploy (API, target defaulted to production):** `dpl_GpHF6v8Crgji...` ERRORED at build step — the PB4 production video gate fired (VERCEL_ENV=production, no demo videos) exactly as designed. Safety net worked; nothing went live. Cancelled/superseded.
- **PREVIEW (live, verified):** `dpl_D6qzqfmrNysM...`, commit `2912000` (empty trigger commit on m-007-quality), state READY. **URL: https://portfolio-clay-git-m-007-quality-tushar-49a6.vercel.app** (stable branch alias). Verified in-browser: renders (not the SSO wall), DES-004 Impact two-tier live (h2 Impact→h3 "From my résumé", 8 product cards), OG url resolved to `https://portfolio-clay.vercel.app` (real host, NOT the `.example` placeholder — build fix confirmed).
- **Still hard-stopped / NOT done:** production deploy (needs Stages 9–10 QA gate + demo videos [PB4] + sanitised résumé [PB5/TKT-08] + a domain); anonymous preview sharing (turn off Vercel Auth or mint a shareable link). Dashboard: `https://vercel.com/tushar-49a6/portfolio-clay`.

**Update (2026-09-22, Tushar-authorized):** Vercel SSO/Deployment-Protection DISABLED on project `portfolio-clay` (`ssoProtection.enabled:false`) — the preview URL is now PUBLICLY shareable (verified: unauthenticated `curl` → 200 + real page). Tushar chose "make current build public" over forcing a production deploy (production still hard-blocked by PB4 videos + PB5 résumé + Stages 9–10 + no domain). This is still the `m-007-quality` PREVIEW target, just no longer login-walled.

---

## Stage 9 · Code Review + Test & Eval Execution — ✅ COMPLETE 2026-09-22 (branch `m-007-quality`, fixed HEAD `76da616`)
`bw-code-review-test-eval` (Fable/low). Three agents in parallel — code-reviewer (`/code-review` low + targeted manual pass), eval-runner (report-only, real output → `docs/reports/stage9-eval.md`), silent-failure-hunter — then orchestrator triage/fix/verify. Consolidated record: **`docs/reports/stage9-review.md`**.
**Findings → 9 CR (5×P2/4×P3) + 6 SF (1×P1) + 4 QA → 15 FIXED / 4 PARKED / 0 open.** Commits: `86c2c36` seo preview host (CR-002/QA-006) · `f95e8bb` forbidden-string gate fail-closed + single PII rule set + CI-portable scratch (SF-1/2/3, CR-004/005/008) · `361adee` vercel.json + runbook (CR-001) · `3d8c2c9` DC5 pin (CR-006) · `f9a8bec` parallax (CR-009) · `9fa9ac1` crawler detail (SF-4) · `5f5c3e7` ask logging (SF-6) · `285adbd` **QA-007** artifact-grid mobile column + EVAL-008 Deep-dive sweep + **DC6 fail-closed eval harness** · `76da616` eval evidence.
**Two bugs of note only this stage caught:** (1) SF-1 — the PII/forbidden-string PRODUCTION gate swallowed readdir/stat/read errors → could report "0 hits" over an unverified tree (fail-open); now `ScanResult.skipped` fails the gate, corrupt `forbidden.local.json` fails loud, regression tests added. (2) DC6 — `pnpm eval` derived status only from tagged specs, so 5 real failing untagged Playwright tests never reached `criticalFailures` and the run exited 0 (QA-005 slipped the same hole at M-007). Now any `unexpected` test → `PLAYWRIGHT-SUITE` critical failure + persisted `playwrightUnexpected`. It immediately caught **QA-007**: my Stage-8 DES-002 `auto-fit`/`min(100%,15rem)` grid made the case-study mobile column intrinsically 601px → 390px overflow on 6 case studies (EVAL-008 never opened Deep-dive). First fix (`min-w-0`) was WRONG and the new scars caught it (327/6); probe-verified root cause; fixed with an explicit single column below `md` (333/0), verified on the deployed build (6/6 slugs scrollWidth 390).
**Live verification on preview `76da616`:** og:image 200 everywhere (was 404 — the runner's "VERCEL_ENV=production" theory refuted empirically), opengraph.xyz 0 errors/3 warnings(45-char DRAFT description)/10 passes = TC-118 PASS, all 6 TP9 headers, deployed-bundle forbidden scan 0 hits, private files 404.
**Eval records:** `eval-run-v1.0.0-rc2.json` (0e1a052: 13 pass/1 info-fail/3 manual, criticalFailures [] — but 5 hidden untagged failures → DC6), `eval-run-preview-0e1a052.json` (live: 12/0/2 skip/3 manual), `stage9-qa007-eval008.json` (RED, scar proof) + `-v2.json` (GREEN), **`eval-run-v1.0.0-rc3.json`** (fixed HEAD: Playwright **711/0** vs rc2 682/5; Lighthouse informational; JSON committed on completion).
**Gate:** typecheck 0 · lint 0 · vitest **306** pass/2 skip (298→306, all new = regression scars) · build predeploy OK / all routes static (13). No threshold weakened (EV2); DC6 strengthened one.
**PARKED → Stage 10:** CR-003 résumé-PDF delivery on Vercel (Tushar decision: commit vs fetch; poppler on builder vs PII gate in CI) · CR-007/QA-008 CSP vs Vercel preview toolbar (security call; recommend scoping the smoke allowlist) · SF-5 `bundle-budget --json` exit 0 (eval gates on `ok`; `--strict` follow-up). CI: Actions enabled, 0 runs (main/PR triggers only) → first green run via the merge PR. Follow-ups: regenerate `docs/screenshots/about/*`; DES-009 wording (fold into DRAFT sign-off); LinkedIn Post Inspector (Tushar).
**NEXT = Stage 10** (`bw-security-review`): `/security-review` on `main..m-007-quality` → SEC-; decide/park CR-003 + CR-007; assemble `QA-report.md` (single release recommendation + accepted-risks register) + `evals/reports/eval-report-v1.0.0-rc3.md`. Do NOT merge/deploy production before the QA-report gate is approved.

---

## Stage 10 · Security Review + consolidated QA gate — ✅ COMPLETE 2026-09-22 (HEAD `420238e`) — AWAITING TUSHAR'S APPROVAL of `QA-report.md`
`bw-security-review` (Fable/low). A security-reviewer agent ran the threat-surface pass + live-origin probing + `pnpm audit` (the `/security-review` command itself refused to run: session shell pinned to the sibling `portfolio` folder — manual equivalent performed, recorded honestly in the QA-report).
**Findings: 0 exploitable.** SEC-001 (Low) résumé PII gate format-narrow → **FIXED `420238e`**: résumé-only `RESUME_PII_PATTERNS` superset (ISO/textual DOB, +cc/parenthesised/spaced phones, Western street keywords, labelled postal codes) applied by `scanResumePii`; shared `PII_PATTERNS.DOB` deliberately kept narrow (content scan must keep accepting `asOf` ISO dates, "7 Sep 2026", patent 429867); 9 positive + 1 negative fixture; vitest 306→**316**; `predeploy OK`. SEC-002 (Info) HSTS `preload`+`includeSubDomains` → Stage-11 custom-domain checklist (`docs/deploy.md` §6a). **DC7:** Vercel preview toolbar DISABLED (`enablePreviewFeedback:false`, reversible) instead of widening CSP → resolves CR-007/QA-008 with header parity (`docs/deploy.md` §6b); console-error verification on the next preview build.
**Verified clean:** headers served byte-identical (6/6), `'unsafe-inline'` justified (0 `dangerouslySetInnerHTML`/`innerHTML`/`eval`), Ask input safe (no RegExp from input), no secrets tracked, `/.env*` `/resume.pdf` `/HANDOFF.md` `/.git/HEAD` etc. → 404, dev routes 404 + robots disallow, sitemap = 22 public routes, prod audit **0/0/0/0/0**, dev advisories unchanged (uuid moderate; extract-zip ×2 ignored — path is `@lhci/cli>lighthouse>puppeteer-core>@puppeteer/browsers>extract-zip`, NOT Playwright as EXE-2's text says: provenance note).
**`QA-report.md` (project root) — the single consolidated gate:** 34 findings across Stages 8–10 → 28 fixed / 6 parked with owner / 0 open; Unified Findings Register U-01…U-21; severity: P0 1 fixed · P1 6 fixed · P2 9 (6 fixed, 3 parked) · P3 5 · Info 1; **Unresolved Critical/High: 0.** Gates: Design PASS · Code Quality PASS · Functional PASS (vitest 316/0, Playwright 711/0) · Evaluation PASS-with-accepted-risks (rc3) · Security PASS · **OVERALL = READY WITH ACCEPTED RISKS** — merge `m-007-quality`→`main` approved on evidence; **production conditional on Tushar-supplied inputs** (PB4 videos, PB5 résumé + CR-003, domain, DRAFT sign-off). `evals/reports/eval-report-v1.0.0-rc3.md` generated from `eval-run-v1.0.0-rc3.json`.
**Accepted-risks register:** EVAL-005 191.1 kB (EXE-11) · EVAL-004 swiftshader · extract-zip highs (EXE-2) + uuid moderate (dev-only) · CSP `'unsafe-inline'` · SF-5 · DC5.
**NEXT = Stage 11** (`bw-deployment-production-monitoring`) **only after Tushar approves `QA-report.md`**: PR `m-007-quality→main` (first CI run), merge, then production gated on videos/résumé/domain. Hard-stops unchanged.
**DC7 verified 2026-09-22 on preview build `420238e` (`portfolio-clay-glnsqka28-…`, created after the toolbar setting change):** headless load of `/` and `/about` → 0 console errors/warnings, scripts only from the site's own origin, no `vercel.live` request — TC-114 "0 CSP violations" sub-clause PASSES on preview; CR-007/QA-008 closed. **PWA sync (honest): Partially** — Campfire was not opened this session; TKT-51 (OG live inspector: opengraph.xyz 0 errors) and TKT-52 (preview record run: `eval-run-preview-0e1a052.json`) are effectively Done and should be moved via `move-ticket.sh` at the start of Stage 11.
