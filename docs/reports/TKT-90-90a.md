# TKT-90a (TASK-85, part of TKT-90) · Residual clay removal — report

Implementer: Claude Opus 5.5 · branch `m009/tkt-90a` from `ff806f5` · code commit `582a2e3`.
Scope comes from the TKT-89 report's "Kept: live consumers" list (FANOUT-E row 90a).

## What changed
| Consumer | Before | After |
|---|---|---|
| `components/projects/DemoVideo.tsx` | `ClayButton` for the play control and "View live"; `--radius-utility` on 3 chips | native `<button>` / `<a target=_blank rel=noopener noreferrer>` + sr-only "(opens in new tab)", styled as the paper secondary pill (the same pill as not-found's "See the work": ivory, `--line` border, `--radius-pill`, `--shadow-paper`, `min-h-11 min-w-11`, 1 px lift that turns off under reduced motion); chips use `--radius-paper` |
| `components/layout/Section.tsx` | `tone` prop → `toneClass` wash | `tone` prop removed (its only caller was the `/dev/primitives` demo) |
| `lib/stages.ts` | `Tone` imported from `components/clay/tiers` | `type Tone = ThinkingStageDef["tone"]` (the schema's zod enum, type-only); stage copy unchanged |
| `components/layout/SkipLink.tsx` | `--radius-clay-sm` / `--shadow-clay-rest` | `--radius-paper` / `--shadow-paper` |
| `app/not-found.tsx` | `--shadow-utility` ×2 | `--shadow-paper` (tokens only) |
| `app/dev/video/VideoDevBoard.tsx` | `/avatar/avatar.webp` poster ×4, clay radius/shadow | `illustration("hero-desk")` from the manifest (`/media/illustrations/hero-poster.webp`, manifest alt/size); paper radius/shadow |
| `app/globals.css` (base `@theme`) | `--radius-clay`, `--radius-clay-sm`, `--radius-utility`, `--shadow-clay-rest/-hover/-press`, `--shadow-utility`, `--gradient-clay-volume` | deleted, and with them the last `rgba()` literals in `@theme` (this closes the TKT-69 carry). The easing comment no longer names Clay* |

**Deleted:** `components/clay/**` (7 files), `public/avatar/**` (11 files), `tests/unit/{clay.test.tsx, ClayButton.test.tsx, ClayCard.test.tsx, ClayPrimitives.test.tsx (+ snapshot), tiers.test.ts}`.

### Deleted-test ledger
| Test | Disposition |
|---|---|
| `clay.test.tsx` "Common primitives — S04.06" (4) | **moved verbatim** to `tests/unit/common-primitives.test.tsx` |
| `clay.test.tsx` ClayCard/ClayButton/ClayPill/ClayTile blocks, `ClayButton.test.tsx`, `ClayCard.test.tsx`, `ClayPrimitives.test.tsx`, `tiers.test.ts` | the behaviour they tested is gone (components deleted). The contract they protected (a 44 px named control) is re-asserted for DemoVideo in the new guard below |

**New regression guard:** `tests/unit/clay-retired.test.tsx` (3 tests). It fails when anything under `app/`, `components/` or `lib/` matches the gate pattern plus `radius-utility|shadow-utility|gradient-clay`, or when `components/clay` or `public/avatar` exists again. It also checks that DemoVideo's play control is a named `type=button` pill with `min-h-11 min-w-11` and paper tokens.

**Unowned files, comment or dev-board edits only (EXE-20 call):** `components/common/CopyButton.tsx` (l.35), `components/layout/SectionHeading.tsx` (l.26), `tests/e2e/eval-008.spec.ts` (l.16, l.75), `tests/e2e/primitives.spec.ts` (l.58) are comment rewording, done so the gate grep reaches 0. `app/dev/primitives/page.tsx` drops `tone="lavender"` and the "tone is…" clause from the demo lead; this is required by the `Section` API change. `layout.spec.ts` only measures padding and aria, so it is unaffected.

## Gate (one locked run, logs in `/Volumes/E Drive/Dev/.scratch/m009/tkt90a/`)
- `grep -rn "components/clay\|ClayButton\|toneClass\|radius-clay\|shadow-clay\|/avatar/" app components lib tests` → **0**
- typecheck 0 · lint 0 · `tokens:check` **13/13 round-trip OK** · build 0
- unit **534 passed / 2 skipped (52 files)**, 0 failed. vs TKT-89's 541: −14 deleted clay tests, +7 new/moved (4 moved + 3 guard)
- bundle `/` first-load **158.6 kB gz** ≤ 180
- `tests/e2e/eval-018-parked.json` = `[]` (untouched)
- **FULL e2e: 922 passed · 1 failed · 1021 skipped.** Baseline `ff806f5` (integ run A) was 921 / 2 failed. Both of those baseline failures (`work.spec:285` filter row at 390, `contact.spec:164` CopyButton) **pass** here.
  - The 1 failure is `eval-019.spec.ts:164` "masked clip stays registered … at 1024, 1440 and 1920" (w1440). It is a `page.goto("/", {waitUntil:"load"})` 30 s test timeout, and it reproduced 3/3 in a `--repeat-each=3` re-run.
  - **It is not caused by this ticket.** Sibling `m009/tkt-90c` fails the same test the same way (31.5 s, `tkt90c/g-eval.log`), and that branch has **no** changes under `app/ components/ lib/`. The integration runs at the same SHA passed it earlier in the day. Host load average was 11–14 during my runs. My only change on `/` is the SkipLink class and deleted unused tokens.
  - This test sits in the hero-banner area (TKT-92r2 / perf ticket), which this row may not touch. Flagged for the orchestrator.
- Screenshot viewed: `docs/screenshots/not-found/390.png` (eyebrow pill + CTAs render correctly with paper shadow). Churned screenshots restored, not committed.

## Merge notes
- **Overlap with `m009/tkt-89`** (not in `ff806f5`). TKT-89 deletes part of the same clay tree and tokens, and edits some of the same files. Expected conflicts and how to resolve them:
  - `tests/unit/clay.test.tsx`: TKT-89 modifies it, 90a deletes it. Take the deletion (common block lives in `common-primitives.test.tsx`).
  - `tests/unit/tiers.test.ts` and `ClayButton.test.tsx`: same, take the deletion.
  - `app/globals.css` `@theme` radius/shadow lines and comments: keep neither side's clay lines (all 8 clay/utility tokens gone).
  - `tests/e2e/primitives.spec.ts`: TKT-89 renames it to `paper-board.spec.ts`. Carry my one-line comment edit, or drop it.
  - Files both branches delete merge cleanly.
- `scripts/avatar*.ts` and the `media:avatar` scripts write into `public/avatar`. TKT-89 deletes them, so I left them alone (they are outside the gate dirs).
- `data/schema.ts` Media regex still allows `/avatar/` paths (a data file, not in the gate dirs). Harmless, left for D7.
- The brief lists `components/case-study/DemoVideo.tsx`. The file actually lives at `components/projects/DemoVideo.tsx`.
