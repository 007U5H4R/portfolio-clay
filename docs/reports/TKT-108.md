# TKT-108 — Home hero copy block rebuilt to Tushar's reference (Campfire TASK-105)

**Direction:** Tushar, 2026-09-26: "change the hero section to this". Reference: `docs/redesign-mockups/m-009/tushar-2026-09-26/hero-copy-target.png` (1672×941). Orchestrator interpretation: the full-scene banner above stays (TKT-96/105); only the copy block below it is rebuilt. Branch `m009/tkt-108` from `ec55aaa` + the two reference commits. Spend: none new — the Tushky head sticker is the already-generated Higgsfield job `36837247-25de-4830-8f58-c069b00d2188` (0.5 cr, Tushar-approved).

## What changed
| Reference item | Build |
|---|---|
| Eyebrow | `data/hero.ts` "SENIOR PRODUCT MANAGER · ENTERPRISE AI · AI-NATIVE BUILDER", Inter 14 px / 500, 0.26em tracking (0.16em < 640 so it wraps to 2 lines at 390), navy. |
| h1 | "I turn messy problems into AI-native products people actually use." — `headline.before / highlight / after` are the reference's three lines; each is a shrink-to-fit centred block ≥ 768 (inline below, natural wrap). Fraunces `clamp(34px, 5.6vw, 80px)`, line-height 1.08. Rust draw-in underline under "people actually use."; the two rust accent strokes are a CSS `::after` mask on the same underline host (≥ 768). |
| Hand line | New `hero.handLine` "Same curiosity. Bigger problems. Better products." — the existing `Annotation` (Caveat, `aria-hidden`), −1°, balanced wrap; faint navy stroke under "problems." is CSS on a span inside the annotation. |
| Support | "7+ years across product, cloud, data and AI — from enterprise platforms at Godrej, Quantiphi, Shellkode and American Express to independently built AI products tested with real users." Inter 17–20 px, max 40em; still hidden < 768 (390 fold). |
| CTAs | Fraunces pills 52 px (64 px ≥ 1024): rust "View my work →" → `/projects` (TKT-101's route — see Open items); ivory "✦ Ask Tushky" → `#ask` (same behaviour as before; the ✦ is `aria-hidden`, accessible name "Ask Tushky", `aria-describedby` the caption). Caption "My AI portfolio assistant" 13 px `data-micro-label`, navy-2 on paper. Tushky head sticker 64 px (`public/media/illustrations/tushky-head.webp`, 128×128 webp alpha, 7.9 kB, `alt=""`, group `aria-hidden`, lazy — not LCP) + a small pointer arrow, ≥ 640. |
| Marginalia + backdrop | ONE `<div data-decor="collage" aria-hidden>` in the sheet: Caveat notes "real / problems / real people / bigger impact" (curved arrow to the h1) and "problem → insight → build → learn" (faint underline) ≥ 1024; two pale torn paper patches, a grid-paper corner bottom-right, a leaf silhouette bottom-left and a hairline loop top-right ≥ 768. All CSS/inline SVG on paper tokens via `color-mix` (no raster, no filters), absolutely positioned (no layout effect). |

CSS: appended `/* TKT-108 … */ … /* end TKT-108 */` block at the end of `app/globals.css`; it overrides the TKT-93 `.hero-*` copy rules and the TKT-92r3 eyebrow/h1 box rules by order (the earlier rules were left in place, not edited). Hero-only: shared `.hero-btn*` rules used by About/Contact are untouched (overrides are scoped `.hero-copy .hero-btn*`).

## Decoration accounting (EVAL-018) — Design.md §11 **Dev-50** (confirm at review)
The reference would be 8 objects in the one hero unit (budget ≤ 4). Resolution, recorded as Dev-50:
- **(a)** the paper sheet is now a nested `<section id="hero-copy">` — a §3.2 rule-1 counting unit, exactly as nested chapter sections are. Banner section = 1 (postmark); `section#hero-copy` = 4 (torn · h1 underline sketch · hand-line annotation · collage) at 390 and 1440. `home.spec` exact-count test updated to these numbers; `eval-018-parked.json` stays `[]`.
- **(b)** the `collage` kind (Dev-41) now also carries `aria-hidden` Caveat marginalia (still counted once).
- **(c)** h1 accent strokes and the hand-line stroke are CSS on their hosts (same objects — the Dev-41 CSS-tape precedent).
- **(d)** the Tushky sticker + pointer is a decorative illustration (`alt=""`, `aria-hidden`), not a `data-decor` object (like the polaroid photos).
If Tushar rejects (a), the alternative is dropping two of: margin notes, backdrop, hand-line stroke.

## CLS scar (TKT-92r3)
The eyebrow and h1 now `justify-self: stretch` (box width = container, independent of the font) and ≥ 768 the h1's three lines are forced blocks, so the fallback→Fraunces swap cannot change the line count there; < 768 the existing `Fraunces Hero Fallback` / `Inter Eyebrow Fallback` fits keep the wraps. No re-fit of the `@font-face` overrides was needed — measured fallback vs loaded boxes (probe, woff2 blocked vs loaded) are identical at 1440 / 1024 / 768 / 390 (e.g. 1440: eyebrow `[124,761,1192,22]`, h1 `[124,826,1192,259]` both frames). `hero-fold.spec` "TKT-92r3 … keep their boxes across the font swap" passes at all 4 projects.

## Tests changed (none weakened)
- `tests/e2e/hero-fold.spec.ts`, `home.spec.ts`, `hero-scene.ts`, `lenis.spec.ts`, `tracer.spec.ts`: secondary CTA name "Ask my portfolio" → "Ask Tushky"; tracer asserts href `/projects` and the caption as the accessible description; `home.spec` EVAL-018 exact counts for the split units.
- New `hero-fold.spec` test "TKT-108 hero copy block" (all widths): h1 text = data sentence, 3 lines ≥ 768, hand line text, caption visible, sticker `alt=""` inside `aria-hidden`, margin notes visible ≥ 1024 / hidden below, no horizontal overflow.
- New `tests/unit/hero-copy.test.ts`: the copy verbatim, the three-line split and the "problems." split the component relies on, and every TKT-108 row citing the reference.

## Gates (through `heavy.sh`)
Logs in `/Volumes/E Drive/Dev/.scratch/m009/tkt108/{g1,g2,g3}/`.
- **g1** (full targeted run): `pnpm typecheck` 0 · `pnpm lint` 0 · `pnpm tokens:check` 0 · `pnpm test` 583 passed / **1 failed** (the pre-existing TornEdge snapshot, Open item 2; new `hero-copy.test.ts` 3/3) · `pnpm build` 0 · e2e (hero-fold, home, eval-019, eval-008, eval-010, eval-018, eval-006, ask-panel, ask-inline, tracer, lenis × 4 projects): 630 passed, 9 failed — 7 are the `/projects` prefetch `networkidle` timeouts (Open item 1); 2 were `/` loads timing out at 1440/1920 (`eval-019` clip registration, polaroid placement).
- **g2**: removed the two CSS `filter`s (leaf blur, sticker drop-shadow — the TKT-99 finding: filters over clip-pathed layers stall large-viewport loads). Rebuilt; `/` load at 1920 is 67–184 ms. Rerun of eval-019, hero-fold, home, eval-006, ask-inline, ask-panel, tracer: 193 passed, 7 failed — only the Open item 1 set.
- **g3** (final CSS): build 0 · hero-fold + eval-018 + eval-019, 4 projects: **76 passed, 0 failed** (incl. "TKT-92r3 … keep their boxes across the font swap" and the new TKT-108 test).
- **Lighthouse (local, lhci, `/`, 3 runs each, g1 build):** desktop CLS **0.0007 / 0.0008 / 0.0009** (≤ 0.05), perf 70 / 99 / 99, LCP 841–1339 ms, a11y 100; mobile CLS 0.0009 / 0.0010 / 0.0012, perf 74 / 81 / 83, LCP 3914–4608 ms, a11y 100. LCP element in every run = the banner `<img>`.
- The g1 static gates ran before the g2/g3 edits, which were CSS-only (two `filter` lines removed, the accent strokes nudged right).

## Screenshots
`docs/screenshots/m-009/tkt-108/`: `hero-copy-1440.png`, `hero-copy-1024.png`, `hero-copy-390.png` (the sheet only, header hidden), `fold-390.png` (first viewport at 390 — eyebrow, h1, hand line and both CTAs above the fold), `pair-1440.png` (reference left, build right).

## Open items / not mine
1. **`/projects` does not exist on this branch** (TKT-101 adds it). Next's `<Link>` prefetch of `/projects?_rsc=…` never completes against the 404, so `networkidle` never fires on `/` at 390/768 (where the CTA is in the initial viewport) — that is the only cause of 7 axe-spec timeouts (`eval-006 /`, `home.spec:131` ×2, `tracer` ×2, `ask-inline`/`ask-panel` axe at w390). Post-load network log: every request finishes except `GET /projects?_rsc=…`. Resolves when TKT-101's route merges; if it does not, change the href to `/work`.
2. Pre-existing, unrelated: `tests/unit/paper.test.tsx` TornEdge `fill=terracotta` snapshot fails on the base — `43d5f73` (TASK-95 band tooth halving) changed the path without updating the snapshot. Not touched here.
3. Lighthouse mobile `/` LCP ≈ 3.9–4.6 s locally (LCP element = the unchanged banner `<img>`, not the sticker); desktop CLS well under 0.05. No base-branch mobile comparison was run (lock time).
