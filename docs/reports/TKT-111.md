# TKT-111 — Polaroids pinned onto the banner's blank papers

Campfire TASK-108 · M-009 · branch `m009/tkt-111` (from `036880f`) · Design.md §11 **Dev-80**

Tushar's direction (2026-09-26, verbatim): "move polaroids to blank canvases behind, if there's a blank canvas left then create a new polaroid and move to that blank canvas."

## Credits spent

**0 of the 3 approved.** The wall has three blank papers and the site already has three polaroids, so no paper was left over and nothing was generated.

## Measured paper boxes (`content/media/illustrations/hero-banner.webp`, 3168×1344 px)

| Paper | Box (x0–x1 × y0–y1) | Polaroid | Placement (canvas px) | Tilt |
|---|---|---|---|---|
| Tall cream sheet, left of the character | 765–1165 × 60–665 | `scene-about` | x 795–1135, y 145–497 (below the sheet's own tape) | −1.6° |
| Square yellow note, top-right | 2058–2368 × 58–335 | `scene-playground` | x 2103–2313, y 92–314 | +2° |
| Large cream sheet, right | 2382–2745 × 100–495 | `scene-work` | x 2418–2708, y 150–452 | −1.2° |
| Mountain photo | ≈ 1690–2030 × 30–420 | stays as is | — | — |

Keep-clear zones asserted in e2e: character's face 1400–1730 × 170–610, book titles 430–910 × 940–1260.

## Implementation

- `components/hero/Hero.tsx`: the POLAROIDS list was reordered and re-tilted, each with a centre tape. `sizes` went from 18vw to 11vw because the polaroids are now at most about 10.7 % of the canvas wide. The copy block was not touched (TKT-108's area).
- `app/globals.css` `.hero-polaroid*`: position, size, padding and tape are all in banner-canvas pixels. `--cpx: calc(100cqw / 3168)`, where `.hero-banner` is the ≥ 768 inline-size container from TKT-96 and the box is the whole canvas, so each photo tracks its paper exactly at every width ≥ 768. The old `max(136px, …)` floors are gone. The polaroids are still in the parallax image layer.
- **< 768 (round 1):** hidden (`MediaGate min={768}`); superseded by round 2 below.

## Decisions

- **Corkboard left bare.** It has been clean since TKT-105 (coloured blank stickies on cork) and reads as intentional, with nothing garbled to cover. Moving all three polaroids to the wall also balances the scene. The partly cropped white papers at the far-left edge are fragments cut by the frame, not target canvases.
- **No new polaroid:** three papers and three polaroids (see above).
- **Postmark:** unchanged. At 768 its cancellation waves just touch the right polaroid's corner (multiply blend, which reads like a postmark). At ≥ 1024 it is clear of the polaroid.

## Guards updated

- `tests/e2e/hero-fold.spec.ts`: the TKT-98 test "cover the corkboard / clear the journey note" is replaced by "hero polaroids sit on the banner's blank papers, clear of the face and the book titles". It checks that there is one polaroid per paper and that each rotated rect is inside its paper's box ± 4 × 8 canvas px, with no overlap with the face or book zones. It runs at 1024/1440/1920 in w1440 and at 768 in w768. No other assertion was changed.
- `evals/results/eval-021-ff806f5.md`: the "garbled corkboard text must stay under the polaroids" note is marked obsolete, with the new rule.
- `tests/unit/eval-021.test.ts` was not changed, because no asset was added (the three scenes still declare `/`).

## Gates (through `heavy.sh`)

typecheck ✓ · lint ✓ · tokens:check 13/13 ✓ · vitest 592 passed ✓ · build (14 static routes) ✓ · bundle `/` 158.9 kB gz ≤ 180 ✓ · e2e hero-fold + home + eval-019 + eval-018 + eval-008, all 4 projects: **391 passed, 121 skipped, 0 failed**. The eval-019 clip registration is still green.
The polaroids use lazy `next/image` loading without `priority`; the banner `<img fetchpriority=high>` is still the LCP (eval-019 static-HTML test).

Screenshots: `docs/screenshots/m-009/tkt-111/home-banner-{1920,1440,1024,768,390}.png`. All were read: the three photos sit taped inside their papers at every width ≥ 768 and none shows at 390.

## Round 2 — polaroids on phones (Tushar 2026-09-26: "yes show polaroids there too")

- The `MediaGate min={768}` wrapper is gone, so the polaroids render at every width and are in the static HTML (`aria-hidden`, lazy `next/image`, `sizes="(max-width: 767px) 20vw, 11vw"`).
- **Below 768** the same canvas-pixel placement applies to the 4:3 art-directed crop. `--cpx: calc(75vw / 1344)` (the canvas is the box height tall), and `--ox: calc(50% − 0.49 × 3168 × --cpx)` is the canvas's left edge from `.scene-banner-canvas` with focal 0.49 (TKT-92r2). The crop shows canvas x ≈ 656–2448.
  - Tall cream sheet: 100 % inside the crop, so its polaroid is shown.
  - Yellow note: 100 % inside, so its polaroid is shown.
  - Large cream sheet: only ≈ 18 % inside (x 2382–2448 of 2382–2745), under the 80 % rule, so its polaroid stays **hidden** below 768 (`display: none`).
- **e2e guard:** the test now runs in every project (w390 checks 390 and 414). Rects are measured against `.scene-banner-canvas`. The shown/hidden set must match the 80 % rule for the crop, and each shown polaroid must be inside its paper, inside the crop, and clear of the face and the book titles.
- **LCP:** at 768–1920 the LCP is the banner `<img>`. In the non-touch 390/414 screenshot context it is the clip's poster `<video>`, because a fine pointer mounts the clip (EVAL-019). On real phones (coarse pointer) the clip never mounts, so it is the banner. In neither case is it a polaroid: all three are `loading="lazy"`, and the eval-019 static-HTML test (banner `<img fetchpriority=high>`) passes.
- **Open issue for Tushar:** on phones the postmark (`.hero-stamp`, top-right, `clamp(120px, 11vw, 160px)`) sits almost exactly on the yellow note, so it mostly covers that polaroid (see `home-banner-390.png` / `-414.png`). I did not change the postmark because its position is an earlier design decision. The smallest fix would be a < 768 rule that shrinks the stamp (for example to about 88 px) or moves it clear of x ≈ 64–75 % of the canvas.
- **Gates (round 2):** typecheck ✓ · lint ✓ · tokens 13/13 ✓ · vitest 592 ✓ · build ✓ · bundle `/` 158.9 kB ✓ · e2e hero-fold/home/eval-019/eval-018/eval-008, all 4 projects: **392 passed, 120 skipped, 0 failed**. Screenshots were refreshed at 1920/1440/1024/768/414/390 and read.

## Round 3: postmark on phones (orchestrator decision)

- Below 768, `.hero-banner .hero-stamp` is 88 px wide, 8 px from the right edge, and `top: 19vw`, which is just below the yellow note's paper (canvas y ≈ 335). It stays top-right, right of the character's face, and clear of both visible polaroids. The ≥ 768 postmark is unchanged.
- **e2e:** at 390 and 414 the placement test also asserts that the postmark's box doesn't intersect any visible polaroid or the face zone.
- **Gate:** typecheck ✓ · lint ✓ · tokens ✓ · build ✓ · e2e hero-fold/home/eval-018/eval-008 on w390 + w1440: **222 passed, 18 skipped, 0 failed**. Screenshots at 390/414 (and the rest) were refreshed and read.
