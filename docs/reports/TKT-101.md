# TKT-101 (Campfire TASK-97) · `/work` → Experience (Work Experience + Education collage) · new `/projects`

Implementer: Claude Opus 5.5 · branch `m009/tkt-101` from integration `43d5f73` (+ 2 reference commits).
Direction: Tushar, 2026-09-26 (verbatim in the ticket). References:
`docs/redesign-mockups/m-009/tushar-2026-09-26/work-experience-target.png` and `education-target.png`.

## What changed
| Area | Change |
|---|---|
| IA / routes | `/work` is now the **Experience** page. The project index (WorkHero, FilterTabs, WorkIndex, ExperienceStrip and the `scene-work` opener) moved unchanged to **`/projects`** (`app/projects/page.tsx`, with its own metadata and the "Selected Work" OG card). The h1 changed from "Work" to "Projects". Case studies stay at `/work/<slug>`. |
| Nav | Six items: Home · **Experience** (`/work`) · **Projects** (`/projects`) · Thinking · About · Playground (`lib/nav.ts`). MobileMenu reads the same list. |
| Links | `filterHref` → `/projects?filter=`; `lib/anchors` route set (a stale `/work?filter=` now fails the link gate); sitemap and `STATIC_ROUTES`; home "View my work →" → `/projects`; case-study crumb "Projects" → `/projects`; 404 "See the work" → `/projects`; Ask evidence ("Projects", "Projects — Enterprise"); manifest `scene-work.usedOn`. The Featured section has no "all work" link, so nothing changed there. |
| `/work` page | `app/work/page.tsx` + `app/work/opengraph-image.tsx` (the new "Experience" card). `components/experience/CollageTimeline.tsx` renders both sections; `Doodles.tsx` holds the per-row collage pieces; `logos.ts` holds the logo and vignette maps; `deckle.ts` is a copy of the TKT-99/100 helper. CSS is in the `/* TKT-101 */` block of `app/globals.css`. |
| Opener | **`/work` has no SceneOpener.** Tushar's reference starts at the title, and the pinboard scene illustrates the project index, so it moved to `/projects`. **Orchestrator: remove `/work` from TKT-103's `scene-openers.spec.ts` route list and add `/projects` instead.** `/projects` keeps the TKT-95 opener untouched and sets no `--banner-h`. |
| Parallax | Education (`paper-2`, `TornEdge` top = the paper cut-out) slides up over Work. While `#education` enters the viewport, the Work content drifts down at half speed (`xp-drift` on a `view-timeline` shared through `timeline-scope`). This is the TKT-96 mechanism: CSS only, behind `@supports`, and off under reduced motion. |
| Data | `data/schema.ts` + `data/experience.ts` gain an optional `location` (cities from CONTENT_INVENTORY §4.5, the résumé). `EducationEntry.highlights?` is optional and left empty. No existing string was edited. |
| Logos | Official files from Wikimedia, svgo-optimised: American Express, Godrej, NIT Calicut, and the AWS and Google Cloud marks. Provenance is in `content/media/logos/README.md`. Shellkode, Quantiphi and BIT Durg have **no official Commons file**, so their names are set in type. |
| Vignettes | Six generated building/campus images (Higgsfield, spend approved by Tushar) sit behind the logo cards: `public/media/illustrations/{office-*,campus-*}.webp`, 480 px, 37–39 kB each. Provenance is in `content/media/illustrations/README.md` under "TKT-101". They are decorative and hidden below 768 px. The collage sprite sheets are **not used**: neither reference has scraps. |
| Design.md | §3.3 rows, §4.1 nav, §7.2 rewritten, §11 **Dev-90…45**. |

## Decisions (recorded in Design.md §11)
- **Dev-90:** `/work` becomes Experience, the index moves to `/projects`, and the nav has six items.
- **Dev-91:** Collage treatment. Each section counts 3 (Work) or 4 (Education) decorations: torn · title `note` · aside annotation · one `collage`. `eval-018-parked.json` stays `[]`. The Caveat title is a decoration and the real `<h2>` is sr-only. Dates, roles and cities are set in **Fraunces italic**, not the reference's handwriting, because §3.4 bans Caveat for dates and headings.
- **Dev-92:** Real logos are an EVAL-021 exception.
- **Dev-93:** Cities are shown. `site.showLocation` stays `false`.
- Card bullets = `context`, `responsibility`, `scale` (only when recorded, i.e. AmEx), `whatChanged`, followed by the résumé outcomes as chips under "Outcomes · self-reported". Work roles run newest first.

## ⚠ For Tushar
1. **NIT Calicut logo licence.** The file is *fair-use on English Wikipedia*, not on Commons, and its rationale does not license reuse. It ships under your logo decision. To drop it, delete `public/media/logos/nit-calicut.svg` and its `ORG_LOGOS` entry; the name then shows in type.
2. **Missing official logos:** Shellkode, Quantiphi, BIT Durg. Drop an official SVG in `public/media/logos/` and add an `ORG_LOGOS` line.
3. **Education has no bullets.** `data/credentials.ts` has none. Add sourced lines to `highlights` and the cards render them.

## Data vs the reference images (not fixed — data wins; fix the data if the image is right)
### Work Experience
| # | Entry | Image says | Data says (`data/experience.ts`) |
|---|---|---|---|
| W1 | Order | newest first | stored oldest first. The page sorts newest first, so no issue |
| W2 | American Express, bullet 1 | "Owned the roadmap for Accounts Receivable transaction capabilities and migration from Triumph to MARS microservices." | responsibility: "Owns the Accounts Receivable transaction-capability roadmap: requirements, backlog, and Devin GenAI integration." The migration is in `context`: "Legacy Triumph platform being migrated to the cloud-native MARS microservices platform." |
| W3 | American Express, bullet 2 | "Prioritized 180+ user stories across four Agile teams, helping reduce delivery cycle time by 30%." | scale: "35+ capabilities, 180+ user stories, 4 Agile teams, 40+ microservices/APIs"; outcome "-30% feature delivery cycle time (self-reported)". "Prioritized" is not in data |
| W4 | American Express, bullet 3 | "Enabled Devin GenAI adoption to improve developer productivity and accelerate AR modernization." | whatChanged: "Legacy retirement accelerated; AI-assisted development (Devin) adopted across the MARS engineering ecosystem."; outcomes "-30% development effort…", "+25% developer productivity (self-reported)" |
| W5 | Shellkode, role | "Technical Project Manager – AWS Division" (dash) | "Technical Project Manager, AWS Division" (comma) |
| W6 | Shellkode, bullets | "Directed end-to-end program delivery from solution strategy to production deployment." · "Established Agile delivery frameworks, DevOps, and CI/CD practices to improve execution efficiency." · "Drove adoption of Shellkode's internal project management platform, Pulse, with better reporting and governance." | responsibility: "End-to-end program delivery; Agile/DevOps/CI-CD frameworks; executive and client engagement."; whatChanged: 'Org-wide adoption of the internal PM platform "Pulse"; standardized stories, acceptance criteria, docs, and repos.' None of "solution strategy to production deployment", "improve execution efficiency" or "better reporting and governance" is in data |
| W7 | Quantiphi, name | "Quantiphi Analytics Solutions Pvt. Ltd." | company: "Quantiphi Analytics" (`credentials.ts` awards say "Quantiphi Analytics Solutions") |
| W8 | Quantiphi, role | "Technical Project Manager – GCP Division" | "Technical Project Manager, GCP Division" |
| W9 | Quantiphi, bullets | "Led enterprise cloud-native programs across data engineering, API modernization, and Generative AI initiatives." · "Spearheaded modernization initiatives such as DynamoDB-to-Spanner migration and other data platform programs." · "Delivered HIPAA-compliant healthcare migration and GCP capability-building programs across strategic engagements." | context: "Enterprise cloud-native programs spanning data engineering, API modernization, and GenAI initiatives."; whatChanged: "DynamoDB→Cloud Spanner migrations; SQL Server transformation frameworks; HIPAA-compliant healthcare data migration; a GCP capability-building program." The data also has responsibility (governance, charters, risk) and two unquantified outcomes the image omits |
| W10 | Godrej, bullet 2 | "Delivered 12 high-impact features in 11 months." | "12 features shipped in 11 months". "high-impact" is not in data |
| W11 | Godrej, bullet 3 | "Improved service monitoring by 25% and team productivity by 30%." | outcomes "+25% service-monitoring effectiveness", "+30% team productivity", plus "-20% turnaround time", which the image omits |
| W12 | Cities | Bengaluru ×3, Mumbai | not in data before this ticket. Added as `location` from CONTENT_INVENTORY §4.5; they match the image |
| — | Dates | Jun 2026–Present · Apr–Jun 2026 · Aug 2022–Apr 2026 · Sep 2016–Dec 2018 | match |

### Education
| # | Entry | Image says | Data says (`data/credentials.ts`) |
|---|---|---|---|
| E1 | NIT Calicut, bullets | 3 bullets (nanotechnology/materials expertise; biosensors, nanoparticle monolayers; published in *Langmuir* and *Soft Matter* + granted patent for a portable electrochemical biosensor) | **no bullets** in the education record. The papers and the patent exist in `papers` / `patent` (patentee "NIT–Calicut"), but nothing ties the papers to the degree |
| E2 | NIT Calicut, label | "NIT Calicut" (short name on the emblem card) | "National Institute of Technology Calicut, Kozhikode". The logo card shows the official emblem |
| E3 | BIT, city | "Durg, Chhattisgarh" | "Bhilai Institute of Technology, **Durg**". The state is not in data |
| E4 | BIT, bullets | 3 bullets (engineering foundation; systems thinking; teamwork) | **no bullets** in data |
| E5 | BIT, label | "BIT Durg" | "Bhilai Institute of Technology". There is no official logo file, so the name is set in type |
| — | Degrees / years | M.Tech., Nanotechnology 2022 · B.E., Mechanical Engineering 2016 | match |

Sticky and tag copy in the images ("From legacy to modern.", "Build Deploy Scale", "Data APIs AI Scale", "From ideas to impact", Research / Publications / Patent, Engineering foundation / Problem solving) and the two asides are used as decoration (aria-hidden), as in the references.

## Shared pieces to dedupe at merge (orchestrator)
- `components/experience/deckle.ts` is a verbatim copy of TKT-99 `components/home/deckle.ts` and TKT-100 `components/timeline/deckle.ts`.
- The deckled-card CSS (`.ct-paper*`) mirrors `.aj-paper*` / `.hit-*`.
- The `collage` decor kind is defined by TKT-99 (Dev-40, §3.1/§3.2). This branch uses it and documents it only in §3.3/§11.

## Header fit (six items, QA-010)
The first run failed at w1024 on every route: horizontal overflow, plus the header overlap test at 1024 and 1280. Measured at 1024, the header needed 973 px of content in an 896 px row. At 1280 the Container is capped at 1200 px, and Playground overlapped the pill by 11.9 px. Fix, in the unlayered part of the TKT-101 CSS block:
- **1024–1279:** 32 px header gutters, 16 px nav type, 16 px nav gap.
- **1280–1439:** 24 px nav gap.

Items, order and the 44 px hit areas are unchanged, and ≥ 1440 is untouched. Recorded in Dev-90. QA-010 is green at 1024, 1280 and 1440.

## Gates (through `heavy.sh`; logs in `/Volumes/E Drive/Dev/.scratch/m009/tkt101/`)
- `pnpm typecheck` ✓ · `pnpm lint` ✓ · `pnpm tokens:check` 13/13 ✓.
- `pnpm test`: 586 passed, 2 skipped, **1 failed, and not from this branch.** `paper.test.tsx > TornEdge fill=terracotta … matches its snapshot` is stale since integration commit `43d5f73` ("halve the footer torn-edge tooth width"), which changed `sketch-paths.ts` without updating the snapshot. This branch does not touch it. **Orchestrator: run `vitest -u` for that one snapshot on integration.** New file `tests/unit/experience-page.test.tsx` (6 tests) passes.
- `pnpm build` ✓, all routes static (14 incl. `/work`, `/projects`, `/projects/opengraph-image`). First-load JS: `/` 158.8 kB, `/work` 153.0 kB, `/projects` 160.9 kB (budget 180).
- Targeted e2e (orchestrator's reduced gate), 4 viewport projects, 1 worker:
  - Run 1, 19 specs (work, projects, layout, tracer, eval-002/006/008/010/011/015/017/018, not-found, case-study, scene-opener, hero-fold, regressions-m009, sweep, home): 801 passed. The 35 failures were all the w1024 header overflow and the QA-010 1280 overlap.
  - After the header fix, the 7 affected specs (layout, eval-008, tracer, home, work, projects, regressions-m009) were rerun: **426 passed, 0 failed.**
  - `eval-018-parked.json` stays `[]`.
- No horizontal overflow on `/work` at 390, 768, 1024 and 1440.
- Screenshots in `docs/screenshots/m-009/tkt-101/`: `work-1440.png` and `work-390.png` (full page), `work-768.png`, `projects-1440.png`, `pair-experience-1440.png` and `pair-education-1440.png` (target left, build right). Other churned screenshots were restored.

**Visual status:** the build is structurally close to both references: title strip, aside, dashed rail with pins, date blobs, vignettes behind taped logos, deckled cards, city chips, role strokes, doodle and sticky column, torn cut-out. The remaining differences are content: data bullets instead of the image copy, outcome chips, and no Education bullets. The last three logos are set in type.

**Not verified:** the scroll-driven parallax was checked by computed style and the reduced-motion test (`work.spec.ts`), not watched in a browser by eye. There was no Safari or Firefox run: Firefox has no scroll timelines, so it gets plain scroll by design.

## Round 2: Tushar's answers (2026-09-26)
His words: "1. Generate the education bullets, 2. image wins, 3. Keep the NIT Calicut logo".

**1. Education bullets.** The three NIT Calicut and three BIT bullets are transcribed verbatim from `education-target.png` into `data/credentials.ts` `highlights`.
- Consistency check against /about's research and patent data: *Langmuir* and *Soft Matter* (`papers`: "Langmuir"; "Soft Matter, RSC") match, and "a granted patent for a portable electrochemical biosensor" matches `patent` (IN 429867, granted 24 Apr 2023, patentee NIT–Calicut). A new unit test locks this in: `credentials.test.ts`.
- One note, not a contradiction: the Langmuir paper's year is 2025, after the 2022 M.Tech. The bullet doesn't date the paper, so I left it as is.

**2. Image wins (W1–W11).** Changes to `data/experience.ts`:
- New `highlights` field: the image's three bullets per role, verbatim. The schema adds `highlights` as optional, at most 4.
- Figure-bearing bullets keep the site's label: "…reduce delivery cycle time by 30% (self-reported).", "Delivered 12 high-impact features in 11 months (self-reported).", "Improved service monitoring by 25% and team productivity by 30% (self-reported)."
- `company` for Quantiphi → "Quantiphi Analytics Solutions Pvt. Ltd.".
- Both division titles → "Technical Project Manager – AWS/GCP Division" (en dash).
- Dates already matched.
- `/work` renders `highlights` and drops the outcome chips, as in the image.
- The structured fields (context, responsibility, scale, whatChanged, outcomes) are **unchanged**. /about's "Where I've built" story cards still render them, so the only visible /about changes are the Quantiphi company name and the two dashed titles. The /about journey cards and the AboutHero don't read these strings.
- No about/timeline assertion pinned the old wording. The content, forbidden-string and schema gates pass unchanged; no scanner rule blocked any claim.

**3. NIT Calicut logo:** kept. The provenance row now reads "Fair-use file kept at Tushar's direction 2026-09-26".

**Still differs from the image (data wins, not in W1–W11):** BIT's city is "Durg" in the résumé; the image says "Durg, Chhattisgarh".

**`scene-openers.spec.ts`:** this file does not exist on `m009/tkt-101` (it belongs to TKT-103 on integration), so I couldn't edit it. At merge, swap `/work` for `/projects` in its route list.

**Nav:** untouched in round 2. The round-1 header-fit CSS (Dev-90, the 1024–1439 rules in the TKT-101 block) may be superseded by the orchestrator's nav decision.
