# TKT-90c (`TASK-85`, part of TKT-90) · QA sweep evidence: report

Implementer: Claude Opus 5.5. Branch `m009/tkt-90c`, cut from `ff806f5` (`m009/integration-ab`). Scope: test and
document only. I did not change any product code; each finding below names its fix. Logs are in
`/Volumes/E Drive/Dev/.scratch/m009/tkt90c/`.

## AC checklist (FANOUT-E row 90c · S90.02 / S90.03 · TC-174 steps 2–3, TC-175 steps 1, 3, 5)

- [x] **Reduced-motion sweep: every §8 row collapses on every route.** `tests/e2e/eval-010.spec.ts` has a new
  `TKT-90c · §8 reduced-motion row sweep` block that covers 22 public routes + `/definitely-missing`, at w1440. The file
  header maps each §8 row to the test that proves it.
  - **Row sweep (per route).** No autoplaying `<video>`, and the `/` banner image is visible (hero poster only). Every
    `[data-drawin]` has `animation: none` and `stroke-dashoffset: 0`. After a full scroll, every `.reveal` has an
    opacity-only transition, `data-revealed` set, opacity 1 and no transform. `document.getAnimations()` shows no
    running animation longer than the 1 ms floor (this catches CSS, transitions and WAAPI/motion). Lenis is not mounted.
    **23/23 pass.**
  - **Hover sweep (per route).** The spec reads every `:hover` rule that sets `transform`, `translate`, `rotate` or
    `scale` from the live CSSOM (nested `@media (hover)` blocks and Tailwind variants included). It hovers the centre
    of each restyled element and compares the canonicalised transform family before and after. A positive control runs
    the same sweep on `/` with motion allowed and must see ≥ 1 lift; it passes. **22/23 pass. `/playground` fails on a
    real defect (QA-A below).**
  - **Interaction rows.** Experience-strip chevron (instant, still flips), `/work` filter change (no inline transform
    and no transform/height keyframes for 20 frames), overview folder tabs (no running animation after the swap), Ask
    panel (in place one frame after opening). **4/4 pass.** Ask inline expand and Show-the-thinking are already covered
    in `ask-inline.spec.ts` (TC-149) and the TKT-83 block. They are referenced, not duplicated.
- [x] **Contrast pairs.** `tests/unit/contrast-pairs.test.ts` has a new `used text/background pairs — Phase A/B/C`
  block with 34 pairs (4.5 for text, 3 for large text), a composite pair (the status chip: ivory 55 % over note), a
  "large rows are really ≥ 24 px" guard, and 5 negative controls (the corrected mockup pairs really fail).
  - **Enumeration.** I built the list from the ticket reports and Design §2.1/§11 (Dev-13/29/35/36), then
    **cross-checked it against a rendered-pair probe**: every visible text node on 23 routes × 390/1440, with the
    effective background composited from its ancestors. The probe found 38 distinct pairs. It also surfaced six pairs
    that were in no report: ink-soft on note (4.52, the thinnest margin on the site), paper on rust (the 404 button),
    DraftTag terracotta on paper/ivory/note, and navy on the status chip.
  - **Drift guard.** Each row names its evidence: either the `globals.css` rule that must still declare
    `color: var(--color-<fg>)`, or `src:<file>` for a Tailwind `text-<fg>` utility. If the CSS stops using that colour,
    the row fails. I mutation-checked this guard: changing 2 rows gave 2 failures, and the file passed again after restore.
  - **Result: 50/50 pass.** No pair in use fails AA.
- [x] **EVAL-021 per-asset checklist**, drafted for Stage 8 in `evals/results/eval-021-ff806f5.md`. It covers 10 assets,
  each opened at full resolution (clip: frames 0/30/59). 5 are clean, 5 need a Stage-8 call (⚠, each with a
  recommendation), and 0 fail. The one item that could fail: the outpaint's **garbled corkboard text** in `hero-banner`
  must stay under the polaroids at every width.
- [x] **Reading-order notes**, in `docs/a11y-pass.md`, for `/`, `/work/teachspark` (30-sec + deep dive) and `/about`. They
  come from `ariaSnapshot()` (Playwright 1.63 no longer has `page.accessibility.snapshot()`) plus the first 40 Tab
  stops. The file replaces the stale M-007 clay-era TKT-48 pass, which stays in git history. It has 5 findings
  (A11Y-1…5).
- [x] **`docs/eval.md` final state.** EVAL-018: parked list `[]` recorded as final. EVAL-019: clip-mask row. EVAL-020:
  clay-leftover note (TKT-89 + 90a grep gate). EVAL-021: 10/10 ids including `hero-banner`, the `hero-clip-mask.png`
  note, and a pointer to the manual checklist. `primitives.spec.ts` → `paper-board.spec.ts` (TKT-89 S89.02).
- [x] `tests/e2e/eval-018-parked.json` stays `[]`, untouched.

## Findings (not fixed: product code is out of this row's scope)

| id | Sev | Finding | Fix (one line) |
|---|---|---|---|
| **QA-A** (EVAL-010) | High (EVAL-010 gate) | `/playground` card hover still lifts 3 px under reduced motion. `.pg-board .pg-ex:hover { translate: 0 -3px }` (TKT-88 block, `globals.css` ~l.5694). Its reduced-motion block only sets `transition: none`, so the card jumps instead of staying put (§8: "Card hover → none (shadow change only)"). **This makes `eval-010.spec.ts` › `no hover rule moves anything · /playground` fail on this branch. I did not weaken the test.** | In the TKT-88 block's `@media (prefers-reduced-motion: reduce)`, add `.pg-board .pg-ex:hover { translate: none; }` |
| A11Y-1 | Medium | `.reveal` keeps `visibility: hidden` until scrolled into view. The How-I-think stages on `/` and the product journey on `/about` are missing from the accessibility tree and the heading rotor on load. | See `docs/a11y-pass.md`. Keep the content in the tree (visual-only hide, or reveal on `focusin` / under reduced motion) |
| A11Y-2…4 | Low | The "●" kind dot is read aloud (`/about`). The insight figure's bare "“" is read aloud. The decision label reads "Why:The…" with no space. | `aria-hidden` on the glyphs; add a space |
| A11Y-5 | Info | The `/about` experience lead says "open any node", but the cards are always open (Dev-11). The copy comes from data (D7). | Tushar's content call |
| EVAL-021 ⚠ | Stage 8 | Garbled pseudo-text on the `hero-banner` corkboard must stay covered by the polaroids at 390/768/1024/1440. The other four ⚠ rows are recommended ✓. | Stage-8 visual check |

Observations, no action: `/about` has visible 11 px text outside `aria-hidden` (probe `minSize`; for example kind
badges `.metric-kind` 11 px). That is EVAL-008 / sweep territory (TKT-85 / 90b). `eval-019` registration hit one `goto`
timeout in the harness run under host load. It passed on the rerun (below), so I treat it as load, not a regression.

## Gate outputs

Run 1 (locked, on the final product tree; the test files changed after it, see run 2):
- `pnpm typecheck` 0 · `pnpm lint` 0 (0 problems) · `pnpm tokens:check` **13/13 tokens round-trip OK**
- `pnpm test` **54 files passed, 1 skipped · 609 passed, 2 skipped**
- `pnpm build` 0 · `bundle-budget --json`: `/` first-load **158.6 kB gz** ≤ 180
- `pnpm eval --only EVAL-010,EVAL-018,EVAL-019,EVAL-020,EVAL-021 --skip-build` (the spec already had the escaped-`:hover`
  fix): **EVAL-018 PASS** (23 routes × 2 widths, 292 units, max 4/4, 0 unparked, 0 parked, 0 stale) · **EVAL-020 PASS** ·
  **EVAL-021 PASS** · EVAL-010 FAIL on `/playground` (QA-A) and on `/definitely-missing` (a test false positive: `translate: 0px`
  vs `none`, fixed below) · EVAL-019 FAIL (goto timeout, a flake).

Run 2 (locked fix rerun after canonicalising identity transforms): typecheck 0 · lint 0 ·
`test:e2e --project=w1440 --project=w390 eval-010.spec.ts eval-019.spec.ts` → **106 passed · 1 failed (QA-A /playground) · 105
skipped** (per-width skips by design). EVAL-019 passed in full, including registration.

The harness JSON from run 1 is kept at `.scratch/m009/tkt90c/tkt90c-paper-gates.json` and not committed. The phase-D
record run (`eval-run-m009-phase-d-<sha>`, TC-175 step 2) belongs on the merged tree.

## Files changed
- `tests/e2e/eval-010.spec.ts`: appended the TKT-90c block (row map, row sweep ×23, hover sweep ×23 + positive control,
  4 interaction tests, `hoverSweep()` helper). Existing tests are untouched.
- `tests/unit/contrast-pairs.test.ts`: appended the used-pairs block. The TKT-72 band block is untouched.
- `evals/results/eval-021-ff806f5.md` (new) · `docs/a11y-pass.md` (rewritten for M-009) · `docs/eval.md` (EVAL-018…021
  sections + the dev-routes spec name) · this report.
- **Shared-file edits:** none (`globals.css`, `eval-018-parked.json` and the manifest are untouched).
- Screenshots: none produced. I did read the 10 illustration sources (and 3 clip frames) for the EVAL-021 checklist.

## Calls made (EXE-20)
1. **QA-A left red rather than parked.** No park mechanism exists for EVAL-010, and inventing one would weaken the gate.
   The fix is one CSS line in the TKT-88 block, which I don't own. The orchestrator applies it at merge (or routes it to
   the TKT-88 owner).
2. Hover targets come from the CSSOM, not a hand list, so rules added later are covered automatically. I hover the
   restyled element's centre instead of parsing selectors, because Lightning CSS rewrites `:has()` into `:is()` forms.
3. For `docs/a11y-pass.md` I replaced the stale M-007 content. It described removed UI (avatar hero, footer nav,
   "Still curious?" sections).

## Merge notes
- Needs the QA-A one-liner in `app/globals.css` (TKT-88 block) before the merged EVAL-010 is green.
- `docs/eval.md` EVAL-020 note describes TKT-90a's end state. If 90a lands with exceptions, adjust that bullet.
- There is no overlap with 90a/90b files.
