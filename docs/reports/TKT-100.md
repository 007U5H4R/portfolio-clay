# TKT-100 — /about product journey, collage restyle (Campfire TASK-96)

Tushar direction 2026-09-26: "change this image to this image" —
`docs/redesign-mockups/m-009/tushar-2026-09-26/about-journey-current.png` → `about-journey-target.png`.
Branch `m009/tkt-100` from integration `43d5f73`. Design.md §11 **Dev-41**.

## What changed
- `components/timeline/ProductJourney.tsx` — the head block, the lead and the closing line sit on torn paper strips (CSS `::before` on their own boxes, seeded `clip-path`). Each card is a deckled torn-edge sheet: two `aria-hidden` rim and face layers, as in TKT-99. The range kicker sits on a tinted highlighter strip (sage, blue-grey, sage, peach). It keeps `data-micro-label` and 12 px, and has a solid background so EVAL-008 checks its contrast against the tint. The DraftTag looks like an outlined stamp. Copy is unchanged and still comes from `JOURNEY_STAGES`.
- `components/timeline/JourneyCollage.tsx` (new) is ONE `data-decor="collage"` `aria-hidden` layer with pointer-events off and no motion. It holds:
  - the generated paper and botanical pieces (`public/media/illustrations/collage-*.webp`: scraps, leaves, fern, baby's-breath, wildflower, stamp, postmarks);
  - thin CSS grid, ledger and tape strips;
  - inline-SVG doodles at the card feet: a generic building, cloud + database with "Cloud. Data. GenAI.", a generic building, and a lightbulb with "TeachSpark / RailCite / Cubicle". The Caveat notes sit inside the hidden layer.

  Below 1025 px the doodles and most pieces are hidden, but the object stays in the DOM.
- `components/timeline/deckle.ts` is a verbatim copy of TKT-99's `components/home/deckle.ts`.
- `app/globals.css` gets an appended `/* TKT-100 … */ … /* end TKT-100 */` block.
- `content/media/illustrations/README.md` gets a provenance table for the 11 collage files (job ids `6eab8569…`, `8fddab59…`). There is no manifest entry: these are decorative `alt=""` pieces, and EVAL-021's manifest is fixed at ten ids.
- `Design.md`:
  - §3.1 `collage` row and §3.2 rule 2 are byte-identical to TKT-99's edits, so they merge cleanly;
  - §3.3 /about row: 3 → 4 decorations at 1440, 1 → 2 at 390;
  - a §7.4 note;
  - Dev-41.

## Decisions
- **EVAL-018:** the whole backdrop, doodles included, counts as ONE decoration: torn + collage + path + "start here" = 4 at ≥ 900, and torn + collage = 2 below. That is within the budget of 4, and `eval-018-parked.json` stays `[]`.
- **EVAL-021:** the target's "Godrej" and "AMERICAN EXPRESS" building sketches are not reproduced. Cards 1 and 3 get a generic, unbranded line-art office building instead.
- **Card tilt** stays within the Sheet's ±0.9° cap. The target tilts further.
- **Generated pieces:** the orchestrator relayed Tushar's approval to use these. This ticket made no new generations. Four files are byte-identical copies of TKT-102's exports (`collage-scrap-sage`, `collage-scrap-kraft`, `collage-leaf-1`, `collage-fern`). Seven are new crops at their native size. `collage-babys-breath-1.webp` is 26.7 kB at q44, slightly over the ≈ 25 kB target.

## Dedupe at merge (orchestrator)
- `components/timeline/deckle.ts` duplicates TKT-99's `components/home/deckle.ts`. Move it to one shared module.
- The `.ajc-*` scrap tones mirror TKT-99's `.hit-*`.
- The README "Shared collage pieces" sections overlap with TKT-102's. Drop the duplicate rows.
- `Design.md` Dev-41 depends on Dev-40 (TKT-99) for the `collage` kind.

## Gates (through `heavy.sh`)
- Static checks: `pnpm typecheck` ✓ · `pnpm lint` ✓ · `pnpm tokens:check` ✓ (13/13).
- `pnpm test`: 579 passed, 1 failed. The failure is `paper.test.tsx › TornEdge fill=terracotta snapshot`. It was already broken at the base: `43d5f73` changed the TornEdge path without updating the snapshot. The band footer is outside this ticket's scope, so it is left for the orchestrator.
- `pnpm build` ✓. `/about` first-load JS is 153.5 kB gz (budget 180).
- e2e, per the orchestrator's revised gate (touched specs, all 4 projects, `--workers=1`):
  - specs: `about`, `a11y-90d`, `eval-008`, `eval-010`, `eval-018`, `scene-opener`;
  - result: 496 passed, 0 failed (400 skipped by the specs' own width guards).
- New tests:
  - unit: collage count, aria-hidden, no brand names, micro-label and strips;
  - e2e `TKT-100 journey collage`: one hidden decoration, content exposed, opaque highlighter strips, doodles only at ≥ 1025, no horizontal overflow (measured 0 at 390, 768, 1024 and 1440).

## Screenshots
`docs/screenshots/m-009/tkt-100/{1440,1024,768,390}.png` and `pair-1440.png` (target left, build right). The site header is hidden for capture only. At 1440 the build matches the target's structure: strips, torn pinned cards on the dashed path, highlighter kickers, foot doodles, stamp top-right, leaves and flowers, and the stamp-style closing tag. The target's backdrop is denser and its type is larger relative to the frame.
