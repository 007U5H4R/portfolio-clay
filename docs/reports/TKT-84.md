# TKT-84 (TASK-79) — `/thinking` opener + essays ruled sheet, `/thinking/[slug]` essay page, S18 double-prefix fix

Branch `m009/tkt-84` · implementation commit `8f96a2e` · model Opus 5.5 (standard tier).

## AC checklist
| AC | Status | Evidence |
|---|---|---|
| 1 · 5 entries in `data/writing.ts` order, each linking to its slug; the empty-state line appears while 0 are published and disappears otherwise | PASS | unit `writing.test.ts` "renders the 5 essays in data order…", "shows the empty-state line… hides it once one is" (fixture with one `publishedOn`), "empty collection…"; e2e `ThinkingList renders…` (order via `toHaveText(titles)`) |
| 2 · essay: `DraftTag` once in the meta row, prefix once in the paragraph, pull quotes ≤ 240 chars with cites | PASS | **TC-164** unit (5 slugs, `split(prefix).length - 1 === 1`, tag count 1 inside `.essay-meta`) + e2e (5 slugs, `#main` innerText count, `[data-paper="tag"]` 1). Negative control: a test double with the span re-added counts 2. Mutation check: re-adding the span to `EssayBody` failed all 5 unit cases, then I reverted it |
| 3 · EVAL-018 counts | PASS (with deviations, see below) | unit: essays SSR 3 (margin annotation absent), opener 2, essay 1, `.essay-prose` 0; e2e: margin annotation present + `aria-hidden` at 1440, absent at 390; `eval-018.spec.ts` green on `/thinking` + 5 essays at w390/w1440 |
| 4 · pager prev/next resolve | PASS | e2e "TC-164 · pager links resolve 200 on every essay" (prev → `/thinking`, next in data order, none on the last essay) |
| 5 · no overflow at 390; margin moves under the header below 900 px | PASS | `no-overflow + screenshot` at 4 widths; e2e "TC-163 · < 900 the essay margin sits under the header" |
| 6 · reduced motion: underline drawn complete | PASS | e2e "TC-163 · reduced motion: the h1 and h2 underlines…" (`stroke-dashoffset` 0) |
| D9 quiet close | Not built (default) | — |

## Deviations (for the orchestrator/Tushar)
- **Dev-24 supersedes the two in-page scene photos.** TKT-95's `SceneOpener` already shows `scene-thinking` above both pages. So:
  - `ThinkingHero` leaves out the §7.5 taped photo and its caption annotation. The opener unit is **2**, not the planned 3.
  - `EssayMargin` leaves out the §7.6 pinned photo and its caption. The essay unit is **1**, not the planned 2.
  - Without the photo, the margin column only holds an `aria-hidden` sticky. It is therefore a plain `div`, not an `aside aria-label="Pinned to the margin"` landmark, because an empty landmark would only add noise for screen-reader users.
- **Generic essay sticky copy** is "Notes first. Essays later." It reuses the `/thinking` hand-sub, so it carries no information that isn't elsewhere (§3.2 rule 6). The mockup's line ("the tests all passed…") is specific to essay 01 and not generic.
- **Where the tests live.** The TC-163/164 e2e tests are in `tests/e2e/thinking-page.spec.ts`, not `thinking.spec.ts`. `thinking.spec.ts` is TKT-21's ShowTheThinking spec, and TC-164 itself names `thinking-page.spec.ts`.
- **Empty-state rule.** It now uses `publishedOn` (was `!draft`), per the plan.

## Files changed
`components/thinking/{ThinkingHero,ThinkingList,EssayBody}.tsx`, `components/thinking/EssayMargin.tsx` (new), `app/thinking/page.tsx`, `app/thinking/[slug]/page.tsx`, `tests/unit/writing.test.ts` (+14 cases), `tests/e2e/thinking-page.spec.ts` (+10 tests; row locator moved to the `li`; related-link `.first()`, because the essay now links the project twice), `docs/screenshots/thinking/{list,essay}-{390,768,1024,1440}.png` (regenerated by the spec).

## Shared-file edits
- `app/globals.css`: one appended block `/* TKT-84 · thinking … */ @layer components { … } /* end TKT-84 */`. It uses tokens and derived vars only. The essay cite is 14 px (the shared `.hand-cite` is 13 px, which fails the EVAL-008 content floor), overridden inside my block only.
- No edits to eval-007/010, the parked JSON, the manifest, or any other ticket's files.

## Gate outputs (all run through heavy.sh)
- typecheck: clean. lint: clean. `tokens:check`: 13/13 OK.
- Full unit suite: 50 files passed (1 skipped); 486 tests passed, 2 skipped.
- `pnpm build`: exit 0, all routes static (13).
- e2e `thinking-page.spec.ts` (4 widths): 29 passed, 51 skipped (width-gated), 0 failed. `scene-opener.spec.ts`: green.
- e2e w390/w1440, `-g thinking`, across eval-018/006/008/010/layout: 78 passed, 0 failed. The first run had 10 EVAL-008 failures from the 13 px cite; I fixed it and re-ran.
- `pnpm eval --only EVAL-006,EVAL-011,EVAL-013,EVAL-018,EVAL-021`: 5 pass · 0 fail (`evals/results/eval-run-0.2.0-6366c7b.json`, gitignored).

## Screenshots (I viewed them)
`list-1440` (opener, Essays h2 + underline, empty line, notebook sheet with 5 rows, sticky, margin annotation), `essay-1440` (crumb, header, meta with one tag, two pull quotes, framing with a single prefix, pager, kraft sticky in the margin), `essay-390` (sticky under the header, pager stacked, no overflow).

## Merge notes
- I needed no files owned by other tickets.
- `.essay-pull-cite` overrides `.hand-cite`'s 13 px. If another ticket's hand-quote cite hits EVAL-008, the shared `.hand-cite` size may be worth raising globally. That change belongs in the TSK-34 block, not mine.
