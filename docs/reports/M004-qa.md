# M-004 Phase QA Gate Report

**Tester:** independent QA-tester subagent (did not author the M-004 code)
**Branch:** `m-004-work` @ `ab36d19`
**Date:** 2026-09-17
**Host caveat:** this machine was memory-contended throughout (~60 MB free typically). Per QA brief, ran targeted/per-file Playwright specs instead of one giant `pnpm test:e2e` run. No OOM kills occurred during this gate; no host-contention failures needed to be triaged.

## 1. Baseline suite (fresh run)

| Check | Result |
|---|---|
| `pnpm typecheck` | PASS — 0 errors |
| `pnpm lint` | PASS — 0 errors/warnings |
| `pnpm test` (unit, Vitest) | **188 passed, 1 skipped** (37 files) — skip is `resume-pii.test.ts` (`resumeAvailable:false`, TKT-08 still BLOCKED — pre-existing, not M-004 scope) |
| `pnpm exec tsx scripts/validate-content.ts` | PASS — `content OK (projects:14 experience:0 skills:0 writing:0 knowledge:11 thinking:6)` |
| `scripts/forbidden-strings.ts` | PASS — `0 hits in 104 files`; sandbox-join-code sub-rule SKIPped (no local `forbidden.local.json` on this machine) — documented, not silent |
| `pnpm tokens:check` | PASS — `13/13 tokens round-trip OK` |

## 2. EVAL gate — `pnpm eval --only EVAL-002,006,007,008,010,011,013,014,015,017`

Fresh run (full `pnpm build`), result `evals/results/eval-run-0.2.0-ab36d19.json` (left unstaged per instructions). **0 assertion failures** across 444 executed Playwright specs (111 × 4 viewports) plus Vitest/content-gate.

| EVAL | Status | Evidence |
|---|---|---|
| **EVAL-002** | **PASS** | Recruiter-path hops 1–3 (`/` → `/work` → `/work/teachspark`, all 200 + correct heading) green at 390/1440 (`eval-002.spec.ts:12`). `/contact` hop also 200. The full 6-click journey through `/about` + resume is `test.fixme` (documented, blocked on TKT-42/TKT-08 — not M-004 scope, correctly not faked). |
| **EVAL-006** | PASS | 92 specs / 45 runs, axe WCAG2.1AA clean (incl. `/work`, all 11 `/work/[slug]`, `/dev/video`) |
| **EVAL-007** | PASS | 56 specs / 16 runs — FilterTabs roving tabindex, desktop nav focus ring, AskPanel keyboard, all green |
| **EVAL-008** | PASS | 80 specs / 71 runs — no horizontal overflow, 44px targets, FilterTabs 390 peek scroll, all green |
| **EVAL-010** | PASS | 28 specs / 5 runs — reduced-motion collapses filter-switch lift/slide |
| **EVAL-011** | PASS | 178 controls · 152 ok · 26 WARN (all owner-tagged: LinkedIn 429 bot-block, `/about` and `/thinking` not-yet-built routes owned by TKT-40/41 — pre-existing, not M-004) · **0 dead controls** |
| **EVAL-013** | PASS | `validate-content OK · 0 hits in 149 files (incl. .next bundle) · fixture proof present` |
| **EVAL-014** | **PASS (verified with real assertions, not just the SKIP path)** | The tagged `--only` run SKIPped all 6 state-matrix tests because they require an `ALLOW_DEV_ROUTES=1` build (by design — `/dev/video` 404s on a plain build, and every test `test.skip`s rather than fails). I rebuilt with `ALLOW_DEV_ROUTES=1` and reran `playwright test --grep 'eval-014\|video'`: **17/17 passed, 0 failed** — no-video badge, pre-intent no-`<video>`, valid-src loading→playing, 404 error overlay + `console.warn`, throttled loading→error→"Demo coming" fallback, pointer:fine 50%-in-view auto-reveal, and the ≤4 MB/≤120 kB file-size sweep all genuinely exercised and green. |
| **EVAL-015** | PASS | 44 specs / 19 runs — VT-off fallback (`/work` card → case study, identical end state) and JS-off static HTML (content + NextProject) both green; case-study.spec.ts's own EVAL-015 pair also green at 390/1440. Missing-hero-image placeholder test is `test.skip`'d pending TKT-18/19 wiring of that specific fixture (pre-existing scope note, not a new gap). |
| **EVAL-017** | PASS | SEO/OG/Twitter tag set (incl. `/work`, `/work/teachspark`) green; robots.txt, sitemap.xml, font licence all pass |

**Static-build confirmation:** `next build` + `assert-static.ts` → `all routes static (9)`; build output shows `/work` as `○ (Static)` and all 11 `/work/[slug]` (`teachspark`, `railcite`, `velora`, +8 more) as `● (SSG)` via `generateStaticParams`. Re-confirmed identical with the `ALLOW_DEV_ROUTES=1` build.

## 3. M-004 test-case execution (`test-cases.md` TC-062–TC-085)

| TC | Title | Status | Evidence |
|---|---|---|---|
| TC-062 | 14 records, gate/status/filter/grid data integrity | PASS | `schema.test.ts`: 14 records, 11 personal/3 professional, unique slugs, professional entries carry no `links.live`/`demoVideo`/`featured`, exactly 1 `gridSize:'large'`. Per-field CONTENT_INVENTORY diff not independently re-walked row-by-row (relies on existing schema/content gates + TKT-15 sign-off) |
| TC-063 | EditorialGrid spans at 4 widths | PASS | `work.spec.ts` hero-span math measured and correct at 768 (full-width) and 1440 (≈8/12 vs medium ≈4/12); 390/1024 covered by no-overflow + grid-count assertions rather than exact ratio math (minor gap, not a defect) |
| TC-064 | FilterTabs URL sync, history, tab semantics | PASS | Click→URL update, Back/Forward restore, keyboard arrow/Home/End roving tabindex all green |
| TC-065 | Filter → exact SITEMAP.md set | PASS | `EVAL-011` deep-link tests for all/ai/enterprise/cloud/experiments all resolve to the correct card set |
| TC-066 | FilterTabs horizontal peek at 390 | PASS | w390-only scroll/peek assertion green |
| TC-067 | Per-filter empty state | PASS | `work-grid.test.ts` (Vitest jsdom) — injected empty dataset renders honest copy + live "Show all" |
| TC-068 | Filter transition motion + VT wrappers | PASS | Reduced-motion "no lift/slide on filter switch" green; VT wrapper/card-click-to-study behaviour covered by EVAL-015 |
| TC-069 | `/work` axe + Lighthouse | PASS (axe) / **informational, not re-run** (Lighthouse) | axe 0 critical/serious on `/work` confirmed. Per QA-brief instruction, did not run the full LHCI suite this gate — see §4 note |
| TC-070 | ExperienceStrip: flat, labelled, no product affordance | PASS | Verbatim label/rows/tags asserted; no external `<a>`/DemoVideo/arrow/VT-name/img inside rows; one-open-expand keyboard-operable |
| TC-071 | DemoVideo four states (EVAL-014) | PASS | See §2 EVAL-014 — real assertions, not the SKIP path |
| TC-072 | `<video>` mounts only on intent + 50%-in-view rule | PASS | No `<video>` in initial DOM; mounts on click (touch) and on 50%-in-view scroll (pointer:fine desktop), `preload="none"` until played |
| TC-073 | DemoVideo play control accessibility | PASS | 44px target floor + axe-clean on `/dev/video` confirmed; full "accessible name includes project name" + focus-move-to-controls detail not independently re-measured beyond the axe/target gates |
| TC-074 | All 11 case-study routes render, thin-content honest | PASS | `case-study.spec.ts`: all 11 slugs return 200, correct `<h1>`, "Hero media coming" placeholder, "Deep dive coming" note, no `ChapterNav` while chapters empty, valid `NextProject` link, no-overflow + min-targets + axe-clean at 390 & 1440 — **47/47 passed, 0 failed** on a dedicated full-file run |
| TC-075 | CaseStudyHeader metrics (5 fields, badge, asOf, media) | **BLOCKED (known/accepted)** | All 14 records currently have `deepDive:false` / empty metrics (M-005 scope, real content not yet written) — there is no live record to exercise this component against yet. Component-level guard (no naked numbers) is separately covered by TC-081 |
| TC-076 | OverviewToggle default/keyboard/crossfade | **BLOCKED (known/accepted)** | Same root cause — no deep-dive record exists yet. `eval-007.spec.ts` documents this fixme explicitly (TKT-10/16/17/45) |
| TC-077 | ChapterNav rail/pills, anchor scheme | **BLOCKED (known/accepted)**, thin-content invariant PASSES | The honest half of this TC — "no `ChapterNav` renders while every chapter is empty, so no anchor is ever dead" — is asserted and green in `case-study.spec.ts`. The anchor-resolution half needs M-005 content |
| TC-078 | NextProject cycle + ProgressBar | **PARTIAL PASS** | NextProject band presence + correct next-slug target verified for all 11 slugs (green). Hover-thumbnail-slide and `ProgressBar` monotonic-scroll behaviour were not independently exercised in this gate (no dedicated e2e assertion found beyond `ProgressBar.test.tsx`'s unit-level `aria-valuenow` check, which is green) |
| TC-079 | `/work/teachspark` Lighthouse + OG image | PASS (OG) / **informational, not re-run** (Lighthouse) | `eval-017.spec.ts` confirms `/work/teachspark` carries a distinct, correct OG/Twitter tag set. Lighthouse run 2 (post-TKT-28) is explicitly deferred to M-005 per the TC's own precondition |
| TC-080 | ArtifactRenderer maps all 8 types | PASS | `artifacts.test.tsx` (Vitest, renderer mapping incl. `generic`) + `artifacts.spec.ts` (Playwright, `/dev/artifacts` no-overflow/min-targets/axe) both green |
| TC-081 | MetricCard sourcing guard | PASS | Compile-time `@ts-expect-error` on a partial `MetricCardProps`, plus runtime `toThrow(/asOf \+ source/)` for missing `asOf`/context — both real, both green |
| TC-082 | Artifact layout stays in chapter column, tier locks | PASS | `/dev/artifacts` no-overflow + min-targets green at all 4 widths |
| TC-083 | ShowTheThinking 8-node chain / hidden when empty | PASS | `thinking.spec.ts`: fixture chain renders 8 ordered nodes; empty-chain fixture renders no toggle at all |
| TC-084 | ShowTheThinking ARIA, DOM presence, keyboard, no auto-play | PASS | 8 nodes present in DOM before open; 2s dwell with no auto-play; Enter opens + focus stays on toggle; axe-clean open and closed |
| TC-085 | ShowTheThinking reveal timing, reduced motion, single column | PASS | Reduced-motion collapses to instant opacity-only reveal; verified green |

**Tally: 20 PASS (2 of those PASS-with-a-noted-partial-gap: TC-063, TC-078), 3 BLOCKED (TC-075/076/077 — all known/accepted, M-005 scope), 0 FAIL, 0 NOT APPLICABLE.**

## 4. Known/accepted items (per QA brief — not new findings)

- EVAL-004/005 (Lighthouse perf) are informational on this host (Chromium/swiftshader, no GPU) — not re-run this gate per instruction. The only committed eval-run with real Lighthouse numbers predates M-004 (`eval-run-0.2.0-7a658d6.json`, stub `/work`) and is stale for `/work`/`/work/[slug]`; real perf validation is owned by TKT-49.
- Metrics/chapters are intentionally thin — real content is M-005 scope; case pages correctly show a labelled "Deep dive coming" note rather than inventing content.
- TKT-08 (resume PDF) remains BLOCKED — `resume-pii.test.ts` correctly skips.
- The e2e timing/measurement tests were hardened in commit `9ee9a7b` (settle-waits + `toPass()` retry). No flakiness or host-contention failures were observed in this gate's runs.

## 5. Gate verdict

**PASS**

All baseline gates green (typecheck/lint/188 unit tests/content-gate/forbidden-strings/tokens). All 10 requested EVAL suites PASS, including both suites this brief called out as needing real (not SKIP-path) evidence:
- **EVAL-002 hops 1–3** (home → `/work` → case study) — genuinely exercised and green.
- **EVAL-014** (DemoVideo four states) — the tag-only run only SKIPped these (dev-route-gated by design); I rebuilt with `ALLOW_DEV_ROUTES=1` and reran them for real: 17/17 passed.
- **EVAL-015** (VT fallback + JS-off) — green across the 11 case slugs (JS-off spot-checked on `velora` at w1440, per the spec's own "runs once" design; VT-off fallback verified end-to-end).

`/work` and all 11 `/work/[slug]` confirmed statically prerendered. 20 of 23 applicable M-004 test cases PASS outright (2 with a minor non-blocking coverage gap noted, not a functional defect); the remaining 3 (TC-075/076/077) are correctly BLOCKED pending M-005 real content — this is the documented, accepted scope boundary of M-004, not a build defect.

**No real (non-accepted, non-environmental) defects found.**

## 6. Recommendations (non-blocking)

1. When M-005 lands real chapter/metric/thinking content, add the still-open e2e coverage for TC-075 (CaseStudyHeader metrics), TC-076 (OverviewToggle), TC-077 (ChapterNav anchor resolution), and the ProgressBar/hover-thumbnail half of TC-078 — all currently untestable for lack of a live deep-dive record.
2. Consider adding an explicit 1024-width hero-span assertion to `work.spec.ts` (TC-063 currently measures 768/1440 only) for full 4-width parity.
