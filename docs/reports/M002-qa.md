# M-002 Phase QA Gate Report

**Tester:** independent QA-tester subagent (did not author the M-002 code)
**Branch:** `m-002-foundations` @ `7734391` (backlog/ledger bookkeeping only, no functional diff from the code under test at `9571acb`)
**Date:** 2026-09-16

## 1. Suite results (fresh run)

| Check | Result |
|---|---|
| `pnpm typecheck` | PASS — 0 errors |
| `pnpm lint` | PASS — 0 errors/warnings |
| `pnpm test` (unit, Vitest) | **117 passed, 1 skipped** (24 files) — the 1 skip is `resume-pii.test.ts`, correctly SKIP because `resumeAvailable:false` and no PDF exists yet (TKT-08 BLOCKED, expected) |
| `pnpm test:e2e` (Playwright, default build, dev routes OFF) | **105 passed, 0 failed, 167 skipped** — skips are by design: each spec runs at one representative viewport and `/dev/*`-dependent specs (primitives/layout boards) correctly skip when dev routes are off in a production build |
| `/dev/*`-dependent specs re-run against an `ALLOW_DEV_ROUTES=1` build (`primitives.spec.ts` + `layout.spec.ts`) | **20 passed, 0 failed** (20 skipped, one viewport per test as designed) |
| `pnpm exec tsx scripts/validate-content.ts` | PASS — `content OK (projects:1 …)`, exit 0 |
| `CONTENT_FIXTURE=invalid pnpm exec tsx scripts/validate-content.ts` | PASS — reports 3 issues (`metrics.0.asOf`, `sources`, forbidden `statusLabel`), exit 1 |
| `pnpm exec tsx scripts/forbidden-strings.ts` | PASS — `0 hits in 57 files`; sandbox-join-code rule SKIPped (no local `forbidden.local.json` on this machine) — expected/documented, not a silent pass |
| `pnpm tokens:check` | PASS — `13/13 tokens round-trip OK` |

No functional-code regression introduced by the running orchestrator commit (`7734391`) landing mid-QA — diffed it against the commit I tested (`9571acb`): only `backlog/tasks/task-8…md` and `docs/ledger.md` changed.

## 2. M-002 test-case execution (`test-cases.md` TC-013, TC-019–TC-041)

| TC | Title | Status | Evidence |
|---|---|---|---|
| TC-013 | Resume control state machine (placeholder vs download) | **PASS** (false-branch); **BLOCKED** (true-branch) | Curled `/`: label "Resume — updating", `href="/contact#resume"`, hidden note "Sanitised resume coming — email me for a copy" present; `GET /resume.pdf` → 404; `public/resume.pdf` absent. True-branch blocked on TKT-08 (no sanitised PDF) — matches the case's own note |
| TC-019 | Content schema shape | PASS | `schema.test.ts` 12/12 tests green (table-driven mutations) |
| TC-020 | Build-time gate reports id+path, fails `next build` | PASS | `content-gate.test.ts` 2/2 green; manual `validate-content.ts` run confirmed exit codes both ways |
| TC-021 | Deliberate failing fixture proves the gate | PASS | `evals/results/content-gate-proof.txt` names all 3 violations, exit 1 |
| TC-022 | Forbidden-string scan + positive control | PASS | `forbidden-strings.test.ts` 4/4 green; script: 0 hits/57 files; sandbox-code rule SKIP is the documented local-file-absent state, not silent |
| TC-023 | Clay tier→token mapping + flat-tier type guard | PASS | `tiers.test.ts` 8/8; type guard implemented via `@ts-expect-error` (in `tiers.test.ts`, not a separate `.test-d.ts` — functionally equivalent, verified by clean `tsc --noEmit`) |
| TC-024 | `ClayPill` filter vs tag variants distinct | PASS | `primitives.spec.ts` "filter pill changes bg on hover; tag pill does not" green (re-run w/ dev routes on) |
| TC-025 | Interactive primitives hit-area/focus/press/reduced-motion | PASS | `primitives.spec.ts` "no-overflow + min-targets + screenshots" green at all 4 widths |
| TC-026 | `/dev/*` excluded from sitemap/prod build; axe clean | PASS | Default build: `GET /dev/primitives` → 404, `sitemap.xml` has 0 `/dev/` entries, `robots.txt` disallows `/dev/`; `ALLOW_DEV_ROUTES=1` build: route 200, axe clean (`primitives.spec.ts` axe test green). Only `/dev/primitives` exists yet — `/dev/artifacts`/`/dev/thinking` are TKT-20/21 (M-004), correctly not yet present |
| TC-027 | `Container`/`Section` measurements at 4 widths | PASS | `layout.spec.ts` "Container gutters…" + "Section vertical rhythm…" green at all 4 widths |
| TC-028 | `Reveal` fires once, opacity-only under reduced motion | PASS | `layout.spec.ts` Reveal tests green (default + reduced-motion) |
| TC-029 | Footer content, links, safe-area | PASS | `layout.spec.ts` Footer tier-1/tier-2 tests green |
| TC-030 | Per-route metadata absolute HTTPS | PASS | Curled `/`, `/work`, `/work/teachspark`, `/contact`: unique titles, canonical/OG/Twitter all absolute `https://portfolio-clay.example…`, no relative/`http://`/`localhost` |
| TC-031 | OG images 1200×630, ≤300kB, per-slug distinct, contrast | PASS | All 4 fetched OG images: 1200×630 PNG, 40–203 kB (well under 300 kB); home + TeachSpark images visually inspected — legible, high-contrast text |
| TC-032 | `sitemap.xml` / `robots.txt` | PASS (for routes built so far) | Sitemap = `/`, `/work`, `/work/teachspark`, `/contact` (exact match to what's built); `/about`, `/thinking`, `/playground` not yet built (M-006/M-005) — correctly absent, not a defect; robots allows `/`, disallows `/dev/`, has `Sitemap:` line |
| TC-033 | Base-URL fallback (Vercel preview) | PASS | `seo.test.ts` covers all 3 env permutations, 9/9 green |
| TC-034 | `pnpm eval` orchestrator output/provenance/exit code | PASS (evidenced, not re-run) | Read `evals/results/eval-run-0.2.0-7a658d6.json`: has `provenance` (version/commit/branch/node/pnpm/os/timestamp/config), `totals`, `regressions[]`, `criticalFailures`, `baseline` — schema matches. Per QA-brief instruction, did **not** re-run the full Lighthouse-inclusive `pnpm eval` on this host (OOM/software-render risk) |
| TC-035 | `eval-cases.json` mirrors plan §3 | PASS | 17/17 ids match `evaluation-plan.md`; automation flags for 001/003/009 correctly `false` |
| TC-036 | Playwright skeletons exist per EVAL id | PASS | `--list` shows specs for EVAL-002/006/007/008/010/011/014/015; 4 viewport projects + fixtures confirmed |
| TC-037 | Dead-control crawler + positive control | PASS | Full e2e run: `@EVAL-011 no dead controls` PASS, `crawler self-test: a dead button is reported, a live one is not` PASS (0 dead, 32 WARN — all owner-tagged: LinkedIn 429 bot-block, not-yet-built `/about`/`/thinking`, allowlisted `Ask AI` disabled button) |
| TC-038 | Lighthouse config + bundle-budget enforce thresholds | PASS | `lighthouserc.mobile.json`/`.desktop.json` confirmed: 4 routes (documented `/contact` stand-in for unbuilt `/about`), 3 runs, exact thresholds (perf .90/a11y .95/BP .95/SEO .95/LCP 2500/CLS .05); `bundle-budget.test.ts` 4/4 covers the failing scenario |
| TC-039 | CI workflow runs `pnpm eval` on PR | **BLOCKED** | `.github/workflows/eval.yml` exists and is self-documented as "pending a GitHub remote" — confirmed `git remote -v` empty. Exact command to complete this case once a remote exists: `gh repo create <name> --private --source=. --push` then open a PR touching `data/` and watch the `eval` workflow run |
| TC-040 | Resume PDF PII/patent test + negative control | **BLOCKED** | No sanitised PDF supplied yet (TKT-08). `resume-pii.test.ts` correctly SKIPs (loud, reasoned skip, fails closed if `pdftotext` is missing) rather than silently passing. The negative-control assertion against the original unsanitised resume is not yet wired into the test file — acceptable at this stage since the whole ticket is blocked on the sanitised export; flag for TKT-08 pickup, not a new defect |
| TC-041 | Site-wide resume flip regression | **BLOCKED** | Depends on TKT-08 landing (same reason as TC-040) |

**Tally: 21 PASS, 3 BLOCKED (all pre-known/documented), 0 FAIL, 0 N/A.**

## 3. EVAL status (authoritative source: `evals/results/eval-run-0.2.0-7a658d6.json`, not re-run)

| EVAL | Status | Note |
|---|---|---|
| EVAL-001 | MANUAL | human/inspector review, tracked in gate-tracer |
| EVAL-002 | PASS | |
| EVAL-003 | MANUAL | human/inspector review |
| EVAL-004 | PASS (informational) | swiftshader/software-render caveat, thresholds unchanged |
| EVAL-005 | **FAIL (informational, accepted)** | first-load JS 219.8 kB gz vs 180 kB budget — owned by TKT-14/49, threshold not lowered |
| EVAL-006 | PASS | |
| EVAL-007 | PASS | |
| EVAL-008 | PASS | (both prior EVAL-008 items fixed under EXE-7, confirmed) |
| EVAL-009 | MANUAL | human/inspector review |
| EVAL-010 | PASS | |
| EVAL-011 | PASS | |
| EVAL-012 | SKIP (accepted) | provider suite lands in M-003/TKT-09 |
| EVAL-013 | PASS | content gate proven |
| EVAL-014 | SKIP (accepted) | DemoVideo lands in M-004/TKT-18 |
| EVAL-015 | PASS | |
| EVAL-016 | PASS | 0 forbidden/PII strings, 0 high/critical deps |
| EVAL-017 | PASS | |

Gate-critical set (EVAL-006/007/008/010/013/016/017): **all PASS.** EVAL-005 FAIL is informational/accepted (TKT-14/49). EVAL-012/014 SKIP are accepted (out of M-002 scope).

## 4. Gate verdict

**PASS-WITH-ACCEPTED-RISKS**

- EVAL-013 proven (content gate + fixture proof) ✓
- EVAL-016 clean (0 hits) ✓
- EVAL-006/007/008/010/017 all PASS ✓
- No Critical regression in this run ✓
- Accepted, pre-documented risks only: EVAL-005 (bundle-budget miss, informational, owned by TKT-14/49), TKT-08 resume PDF still BLOCKED (TC-040/041/TC-013-true, and CI-on-PR TC-039 blocked on a GitHub remote not yet existing)
- **No new defects found** during this independent pass — all failing/blocked states match the QA brief's known/expected list exactly.

## 5. Notes for the record

- One process-level observation, not a code defect: mid-QA, another session committed `7734391` (backlog/ledger bookkeeping only) on this same branch while I was testing `9571acb`. Diffed the two commits — no functional files changed, so all results above stand.
- TC-023's type-level flat-tier guard lives in `tests/unit/tiers.test.ts` via `@ts-expect-error` rather than a separate `.test-d.ts` file the case text describes — same enforcement, verified by a clean `tsc --noEmit`; noting the naming difference for traceability, not as a defect.
