# Implementer brief — TKT-07b · Eval harness part 2: crawler + Lighthouse + orchestrator + first run (M-002, TASK-7)

Fresh implementer. Execute **TSK-10 + TSK-11 + TSK-12** of ticket TKT-07 (part 2 of 2; TKT-07a did TSK-08/09). Model tier: most-capable. Work in `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay/` on branch `m-002-foundations`. Standard guardrails (E-Drive only via `.env.tooling`; don't touch main/backlog/docs ledger+briefs/sibling portfolio; explicit-path staging, never `git add -A`; leave re-rendered `docs/screenshots/**` unstaged, don't `git checkout` them). Chromium already on E-Drive.

## Read first
- `technical-plan.md` §B M-002 → **TSK-10 (S10.01–03), TSK-11 (S11.01–02), TSK-12 (S12.01–04)** (lines ~597–610) — *Files / Contract / Gate* authoritative.
- `technical-plan.md` §A16 (eval orchestrator + provenance + baseline diff + exit codes), §A9 (LHCI/bundle), §A11/§A10 (headers, CI). `scripts/eval.ts` (minimal — TSK-12 makes it the full A16 orchestrator), `scripts/eval-cases.ts` + `tests/e2e/routes.json` (from TKT-07a), `tests/e2e/crawler-allowlist.json` (tracer entry `ask-ai-disabled`).

## Steps (each has a hard Gate)
- **S10.01–03 (EVAL-011 crawler):** `tests/e2e/crawler.ts` (per route × {390,1440}: enumerate visible controls; internal href → 200 + hash target exists; external → HEAD 200–399 cached ≤2 concurrent, 403 from LinkedIn/GitHub = WARN not FAIL; button → asserts a state change; `aria-disabled` allowlisted) + `tests/e2e/eval-011-dead-controls.spec.ts` + allowlist. Gate: `pnpm test:e2e --grep @EVAL-011` → `0 dead` on current routes; `.eval/dead-controls.json` written; unit test with a fixture dead-button → reported. (Nav prefetches unbuilt `/thinking` + `/about` → 2 benign resource 404s per TKT-07a — handle: internal links to not-yet-built routes are WARN with owner, not FAIL, until those tickets land.)
- **S11.01–02 (EVAL-004/005):** enforce `lighthouserc.{mobile,desktop}.json` (4 routes, median, level error) + `enforceLighthouse:true` in eval config; `scripts/bundle-budget.ts` (per-chunk gz for `/`, exit 1 >180 kB) + `tests/unit/bundle-budget.test.ts`. Gate: LHCI assertions evaluated per route; bundle-budget prints `first-load JS (/) = NN.N kB gz (budget 180)` — RECORD the value (F5: tracer was 218.7 kB > 180; report whether it still exceeds — do NOT lower the threshold; if over, it stays a recorded FAIL for the TKT-14/49 perf levers, per EV2/A14).
- **S12.01 (orchestrator):** `scripts/eval.ts` full A16 — layers, `--only`/`--baseline`/`--base-url`/`--label`/`--skip-build`, no-overwrite, `regressions[]`/`improvements[]` vs `baseline-v1.json`, exit codes (0 clean/1 critical FAIL/2 regression/3 runner error) + `scripts/security-headers.ts` (fetch HEAD vs TP9 set; SKIP without `--base-url`). **Dev-route handling (carry-forward from TKT-07a):** the orchestrator runs the `/dev/*` specs (`@primitives` etc.) only in the `ALLOW_DEV_ROUTES=1` build/job, and those specs must SKIP (not FAIL) when dev routes are 404 — fix so a plain `pnpm test:e2e` is green (no 14 primitives failures).
- **S12.02 CI:** `.github/workflows/eval.yml` (Node 26/pnpm 11, install frozen, typecheck+lint+test, playwright install chromium, build, `pnpm eval --label ci-<sha>`, upload artifacts). Gate: `actionlint`/`action-validator` passes; note "CI pending remote" (no GitHub remote yet — do NOT create one).
- **S12.03 docs:** `docs/eval.md` (how to run, flags, schema, add-a-case, manual protocol, external WARN policy, never-overwrite/never-lower-thresholds). Gate: a unit test greps `--help` flags appear in the doc.
- **S12.04 first full run:** `pnpm eval` → `evals/results/eval-run-0.2.0-<sha>.json` (totals: automated evaluated/SKIP-with-reason, MANUAL) with `regressions: []` vs baseline-v1. Gate: file committed; matches TKT-07 files.

## Rules
- Everything on E Drive; colour DEFINITIONS 13. **The 2 EVAL-008 defects TKT-07a fixme'd (12–13px captions; /contact link h<44) STAY fixme'd — do NOT fix them here (orchestrator will rule at the M-002 gate); do NOT weaken any threshold.** If the first full run has a real FAIL (e.g. bundle >180, or a Lighthouse miss), REPORT it honestly with numbers — never hide/skip a case to make the run green.
- Commit(s): `feat(m002): TKT-07b eval harness pt2 (crawler + lighthouse + orchestrator)` + `eval: first full M-002 run vs baseline` + Co-Authored-By trailer. Explicit paths only.

## Finish
`pnpm typecheck && lint && test && build` green; plain `pnpm test:e2e` green (dev specs skip); first full `pnpm eval` written. Write `docs/reports/TKT-07b.md` (per-step gates, bundle-JS value + over/under budget, EVAL-011 result, first-run totals + any real FAILs, `git diff --stat`). Final 5-line summary: steps done, bundle kB vs 180, EVAL-011 dead count, first-run totals/regressions, blockers, commit SHA(s).
