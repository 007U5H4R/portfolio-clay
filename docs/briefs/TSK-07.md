# Implementer brief — TSK-07 · Screenshots + minimal `pnpm eval` + baseline (M-001 / TKT-01)

Fresh implementer. Execute **TSK-07 only** (steps S07.01–S07.07). Model tier: most-capable. This is the LAST TKT-01 task: it installs Chromium (on the E Drive), runs every Playwright gate deferred by TSK-02/04/05/06, builds the minimal eval orchestrator, and writes the **baseline** the visual gate depends on. Prereqs done: TSK-01–06 (full tracer: Hero, Header, ProjectCard, `/work/teachspark` stub, primitives, avatar). Work in the repo root on branch `m-001-tracer`.

## Read first
- `technical-plan.md` §B → **TSK-07 steps S07.01–S07.07** (lines ~524–530) — *Files / Contract / Gate* authoritative.
- `technical-plan.md` §A9 (Playwright/LHCI/Vitest config detail), §A16 (evaluation wiring — the eval orchestrator + provenance), §A12 (bundle budget).
- `evaluation-plan.md` (EVAL-001…017 — which are PASS/FAIL vs MANUAL vs SKIP on the tracer) and `evals/eval-cases.json` (the machine-readable authority).
- The deferred Playwright checks to fold into `tracer.spec.ts`: header compaction 96→68 (S04.03), NavPill (S04.04), MobileMenu focus-trap/Esc/axe at w390 (S04.05), SkipLink (S04.02), AskAIButton tab-order (S04.06), Hero frame widths + tile offsets + reduced-motion (S05.02/03/06), VT + noViewTransitions + reducedMotion fallback (S06.05), avatar-edge re-shot (S02.02), resume placeholder `a[href="/contact#resume"]` "Resume — updating" + `/resume.pdf`→404 (E-13).

## Steps
- **S07.01** `playwright.config.ts` (4 projects w390/768/1024/1440, `webServer: pnpm start` on 127.0.0.1:3000 reuseExistingServer, json reporter → `.eval/playwright.json`, `outputDir: test-results`) + `tests/e2e/fixtures.ts` (`axe` wcag2a/2aa/21aa no critical|serious, `noOverflow`, `reducedMotion`, `noViewTransitions`, `minTargets` ≥44×44 with allowlist). Gate: `pnpm exec playwright test --list` prints 4 projects × specs; `ls ~/Library/Caches | grep -c ms-playwright` → 0 (nothing on internal disk).
- **S07.02** Install Chromium on the E Drive: `dotenv -e .env.tooling -- pnpm exec playwright install chromium`. Gate: files under `/Volumes/E Drive/Dev/.cache/ms-playwright/chromium-*`; `df -h /` free space unchanged within 50 MB vs before (record both).
- **S07.03** `tests/e2e/tracer.spec.ts` (tags `@EVAL-006 @EVAL-008 @EVAL-010 @EVAL-015`): for `/` and `/work/teachspark` at all 4 widths → screenshot `docs/screenshots/tracer/{home,case}-{w}.png` (8 files) + `noOverflow` + `minTargets`; `axe` at w390/w1440; plus all the deferred checks listed above. Gate: `pnpm test:e2e` green; `ls docs/screenshots/tracer/*.png | wc -l` → 8 (+ the avatar-edge shot). If avatar-edge shows a light halo, rerun `tsx scripts/avatar.ts --erode 1` and re-shoot (note it).
- **S07.04** `lighthouserc.mobile.json` + `lighthouserc.desktop.json` (A9; tracer collects `/` only; assertions present but informational until TKT-07). Gate: `dotenv -e .env.tooling -- pnpm exec lhci autorun --config lighthouserc.mobile.json --collect.url=http://127.0.0.1:3000/` completes 3 runs → `.lighthouseci/*.json`.
- **S07.05** `scripts/eval.ts` (schema v1: loads `evals/eval-cases.json`, runs Playwright + LHCI mobile+desktop, collects provenance [git HEAD, branch, dirty, node, pnpm, next version from lockfile, os.release, timestamp, sha256 of eval-cases.json], maps → `cases[]` with status PASS/FAIL for 006/008/010/015, MANUAL for 001/003/009/017, SKIP `reason:'not built yet'` for the rest, totals, writes `evals/results/<label>.json`, never overwrites; flags `--label --only --skip-build --informational`; exits non-zero on FAIL among `critical` cases unless `--informational`). Also `scripts/bundle-budget.ts` (first-load JS gzip for `/`, ≤180kB, EVAL-005). Gate: `pnpm eval --label baseline-v1 --informational` writes `evals/results/baseline-v1.json`; `node -e "const r=require('./evals/results/baseline-v1.json');console.log(r.schemaVersion,r.provenance.commit.length,r.totals.cases)"` → `1 40 17`.
- **S07.06** Baseline captured + committed. Gate: `baseline-v1.json` shows EVAL-006 PASS (0 critical/serious @390&1440), EVAL-008 PASS (0 overflow, 0 sub-44 targets), EVAL-010 PASS, EVAL-015 PASS, EVAL-004/005 measured (mobile+desktop scores for `/`, LCP, CLS, first-load JS kB). Commit `eval: baseline-v1 on tracer bullet`.
- **S07.07** Ticket wrap. Gate: `pnpm typecheck && pnpm lint && pnpm test && pnpm build` green; write `docs/reports/TSK-07.md`. **AC-9 clarification:** the plan's "`git diff main..HEAD --stat` lists only TKT-01 files" applies to APP/COMPONENT/LIB/SCRIPT/TEST files only — orchestration artifacts (`docs/`, `backlog/`, `decisions.md`, `technical-plan.md`, the EXE-3 `globals.css` easing tokens) are EXPECTED and not violations. Verify no stray component/route beyond the TKT-01 list exists (`tree -I node_modules -L 2`).

## Rules
- Everything on E Drive — CRITICAL for this task (Chromium install, traces, LHCI). Use `.env.tooling` / `scripts/env.sh` for every browser/temp-writing command. Record `df -h /` before and after the Chromium install.
- **Commit staging: explicit paths only** (`git add playwright.config.ts tests/e2e scripts/eval.ts scripts/bundle-budget.ts lighthouserc.*.json evals/results/baseline-v1.json docs/screenshots/tracer docs/reports/TSK-07.md` + any config). NEVER `git add -A`. Do NOT commit `.lighthouseci/`, `test-results/`, `.eval/` (gitignored).
- If EVAL-006/008/010/015 FAIL on the tracer, that is a real finding — report it (do not hide or delete the failing case); the orchestrator decides fixes before the visual gate.

## Finish
1. Baseline written + the trio+build green.
2. Two commits allowed: `feat(tracer): TSK-07 playwright + eval harness` and `eval: baseline-v1 on tracer bullet` (or one combined) — Co-Authored-By trailer.
3. `docs/reports/TSK-07.md`: Chromium install path + df before/after; the 4 EVAL PASS/FAIL results with numbers; EVAL-004/005 measured values; screenshot list; avatar-edge halo verdict; `git diff --stat`.
4. Final 5-line summary: EVAL-006/008/010/015 results, EVAL-004/005 numbers, screenshot count, avatar-edge verdict, blockers, commit SHA(s).
