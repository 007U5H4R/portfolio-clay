# Brief — TKT-93 · Hero banner restyle (hero-gate change request, EXE-15)

**Ticket:** TKT-93 (Backlog `TASK-87`) · M-009 · Feature · P0 · sp:5 · **Depends on:** TKT-73 (done). **Next:** TKT-95 reuses your banner component for every page — build it reusable.
**Worktree:** `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay-redesign/` · **Branch:** `m-009-redesign` (verify; never `main`, **never push**).
**Model tier:** most-capable (Fable 5.1). **Co-Authored-By trailer:** your session's actual model.

## Objective
Rebuild the `/` hero in the style of Tushar's reference (style only — never its content/branding): a **full-bleed illustrated banner** under the header, a **torn paper bottom edge**, the **copy block centred below**, **2–3 taped polaroids** overlapping the banner's bottom-left, a **postmark stamp** top-right — while keeping everything the hero contract guarantees (poster/LCP in SSR, the once-and-hold clip, the four EVAL-019 modes).

## Read first
1. `decisions.md` **EXE-15**, **EXE-16**, **EXE-17**, **TP13**, **D10**, **S14** (search the IDs).
2. `Design.md` **§11 Dev-21, Dev-23** (the change), then **§5** (5.2–5.4 modes/lifecycle still apply; 5.1's grid is superseded), **§3.1–3.4** (decoration counting — the hero must stay ≤ 4), **§2** tokens, **§8** motion.
3. `docs/reports/TSK-37.md` (how the current hero + `HeroClip` were built) and `components/hero/{Hero,HeroClip}.tsx`.
4. The reference description in EXE-15. Current look: `docs/screenshots/m-009/tracer/home-1440.png`.

## Assets (prototype already proven by the orchestrator — reuse, don't re-derive)
- Banner master (outpaint, 3168×1344, 7 MB PNG): `/Volumes/E Drive/Dev/Code/Claude/Portfolio-illustration/animation/export/banner/hero-banner-outpaint-a1.png`. Encode with `sharp` → `content/media/illustrations/hero-banner.webp` (long edge 3168, quality tuned so the file is ≤ 400 kB — report bytes) and import it statically so `next/image` produces AVIF/WebP + srcset.
- Clip alpha mask (1280×684, feathered, white = show clip): `/Volumes/E Drive/Dev/.scratch/m009/clip-mask-1280.png` → `public/media/illustrations/hero-clip-mask.png` (optimise with `sharp` png palette/compression; report bytes).
- **Registration (measured):** the 1280×684 clip sits on the 3168×1344 banner at scale 1.912, x 362, y 6 ⇒ as percentages of the banner box: **left 11.4268 %, top 0.4464 %, width 77.2525 %, height 97.3071 %**. The `<video>` is absolutely positioned in those percentages inside a wrapper whose `aspect-ratio: 3168 / 1344`, with `mask-image: url(/media/illustrations/hero-clip-mask.png); mask-size: 100% 100%` (+ `-webkit-mask-*`). Offline composites of the first/mid/last frames were seamless (`/Volumes/E Drive/Dev/.scratch/m009/c4-mid-small.png` — Read it).
- **Responsive crop without breaking registration:** never `object-fit` the img and the video separately. Put img + video inside one inner "canvas" (`aspect-ratio: 3168/1344`, position relative) and crop the *canvas* with the outer banner box (`overflow:hidden`; canvas sized to cover the box, centred on the character at ≈ 49 % x). Banner box height ≈ `clamp(300px, 42vw, 620px)` at ≥ 768; on < 768 a taller crop (e.g. `aspect-ratio: 4/3`) centred on the character — touch devices are poster-only anyway (EVAL-019), but a narrow desktop window must still register.

## Scope
- `components/hero/Hero.tsx` rebuilt: banner (new reusable component e.g. `components/paper/SceneBanner.tsx` with props like `{ id, priority, focalX, children? }` so TKT-95 can use it on six pages) → `TornEdge` (fill paper) as the banner's bottom edge → centred copy block (eyebrow, Fraunces h1 + the rust underline `Sketch` on "people can use.", `Annotation` hand line "Same curiosity. Bigger problems." in large Caveat — decoration, `aria-hidden`; support paragraph hidden < 768 as today; CTAs "View my work →" / "Ask my portfolio"). Copy stays verbatim from `data/hero.ts` (D7). h1 stays **Fraunces** (EXE-15).
- Polaroids: 2–3 `Sheet variant="photo"` + `Tape` fasteners, rotations within caps, showing crops of `scene-work`, `scene-about`, `scene-playground` (via `lib/illustrations.ts`), overlapping the banner's bottom-left so they **cover the garbled corkboard** (left ~21 % of the banner, its sticky notes) at ≥ 1024; decorative → `alt=""` + `aria-hidden="true"` on the group (Dev-23). Hide or reposition them < 768 if they would cover the character.
- Postmark stamp: a small authored SVG (circular postmark + wavy cancellation lines, navy/rust tokens, short text such as "TP · BUILD · LEARN") top-right of the banner, `data-decor="sketch"`, `aria-hidden`. Hero section total decorations **≤ 4** (underline sketch, hand line, stamp, + optional figcaption annotation — drop the figcaption if needed).
- `HeroClip`: state machine unchanged (TP13); only placement/classes change — video absolutely positioned per the registration, masked, `poster` = the existing `hero-poster.webp`. No new effects/listeners.
- Manifest + provenance (Dev-23): new entry `hero-banner` (file `hero-banner.webp`, alt exactly the Dev-23 string, `usedOn: ["/"]`); README row: model `outpaint` (Higgsfield), input media `fc45d649-4038-4558-bd41-b659db56a9d3`, job `6d94caf5-1a14-4151-9864-050b6a193615`, date 2026-09-25, credits 2. `hero-desk` keeps its entry (poster still ships for the clip's `poster`) — update its `usedOn` honestly. EVAL-021 green both ways; polaroid scenes' `usedOn` gain `/` if EVAL-021 requires it.
- CSS under a `/* TKT-93 · hero banner */` banner in `app/globals.css`; tokens/derived only (EVAL-020).
- Tests: update `tests/e2e/eval-019.spec.ts`, `tests/unit/hero-clip.test.tsx`, `tests/e2e/home.spec.ts`, `tests/e2e/tracer.spec.ts` for the new SSR image (the **banner** `<img>` is now the LCP `priority` image with `fetchpriority="high"`; no `<video>` in SSR; default mode mounts the masked video; the other three modes mount none). Add an e2e assertion that the video's bounding box equals the registration percentages of the canvas (±2 px) at 1024, 1440 and 1920 — the registration regression guard. Never weaken EVAL-019's assertions; update selectors only.

## Gates
`pnpm typecheck && pnpm lint && pnpm tokens:check && pnpm test && pnpm build`; **FULL** `pnpm test:e2e` on a freshly restarted prod server (baseline 0 failures); `pnpm eval --only EVAL-018,EVAL-019,EVAL-020,EVAL-021 --skip-build` all PASS (hero unit ≤ 4); `pnpm exec tsx scripts/bundle-budget.ts --route / --json` (≤ 180). Screenshots from a prod build **after the clip ends** (`--wait-for-timeout=4500`): `docs/screenshots/m-009/hero-banner/{390,768,1024,1440,1920}.png` — **Read every one** and check: no seam/halo around the character, polaroids cover the garbled notes, stamp and copy readable, nothing overflows. Plus one mid-clip screenshot at 1440 (`--wait-for-timeout=1200`) to check the masked overlay mid-motion.

## Constraints
Everything on `/Volumes/E Drive` (temp `/Volumes/E Drive/Dev/.scratch/m009`). GateGuard may deny the first Edit/Write per file — state the facts and retry. Stage explicit paths only; never stage `docs/ledger.md`, `docs/briefs/*`, `backlog/**`. The tree currently has ~35 churned PNGs from the preview eval run — first run `git checkout -- docs/screenshots/about docs/screenshots/contact docs/screenshots/not-found docs/screenshots/playground docs/screenshots/thinking docs/screenshots/tracer docs/screenshots/m-009/tracer/hero-end-1440.png`, and restore churn again at the end. No Higgsfield spend. After every `pnpm build`, restart `pnpm start` (Playwright reuses a live :3000). Commits (1–3) + trailer, e.g. `feat(hero): full-bleed banner with masked clip, polaroids and stamp (TKT-93)`.

## Output — `docs/reports/TKT-93.md` (commit it)
AC checklist (Backlog TASK-87) with evidence; asset bytes; registration test result; EVAL statuses; bundle; e2e counts; screenshot notes (per width, what you saw); anything that needs Tushar's eye. Final chat reply ≤ 10 lines.
