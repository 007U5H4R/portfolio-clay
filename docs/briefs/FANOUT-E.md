# Fan-out E — TKT-90 split across 3 parallel implementers — read `FANOUT-AB.md` first

Same rules as `FANOUT-AB.md` (own worktree + branch, never push/merge, `heavy.sh` lock for every heavy command, report + short reply) and `FANOUT-D.md` (branches start from the integration head — here **`ff806f5`** on `m009/integration-ab`, which includes Phase 0 + all Phase A/B/C tickets + post-merge fixes; new CSS goes in an appended `/* <ID> … */ … /* end <ID> */` block). Other agents run in parallel (integration fixes, TKT-92 round 2 on the hero, TKT-79/85/89 waiting to merge) — **don't touch** hero/banner files, `playwright.config.ts`, or files another row owns. Tushar is AFK with delegated authority (EXE-20): decide by plan/Design precedence; record calls in your report. Model: Opus 5.5. Worktrees `/Volumes/E Drive/Dev/Code/Claude/Portfolio-m009-tkt-<id>/`, branch `m009/tkt-<id>`.

Plan of record: `technical-plan.md` "##### TKT-90" (S90.01–S90.03), `tickets.md` TKT-90 (TASK-85), test-cases TC-173/174/175, Design §8, §10, §11.

| id | Scope | Owns |
|---|---|---|
| 90a | **Residual clay removal** (carried from TKT-89 — see `docs/reports/TKT-89.md` on branch `m009/tkt-89` for the list: `ClayButton` in `DemoVideo`, `Tone`/`toneClass` in `components/layout/Section.tsx` + `lib/stages.ts`, `public/avatar/avatar.webp` on `/dev/video`, tokens `--radius-clay-sm` `--shadow-clay-rest` `--radius-utility` `--shadow-utility` in SkipLink / not-found / DemoVideo). Replace each use with the paper equivalent (paper buttons/pills from the existing home/band CSS, paper radius/shadow tokens, an illustration from the manifest for the dev board), then delete the clay files/tokens/avatar. Gate: `grep -rn "components/clay\|ClayButton\|toneClass\|radius-clay\|shadow-clay\|/avatar/" app components lib tests` → 0 (report exceptions with reason). | `components/case-study/DemoVideo.tsx` (+ its tests), `components/layout/Section.tsx`, `lib/stages.ts` (tone only — don't change stage copy), `components/navigation/SkipLink.tsx`, `app/not-found.tsx` (tokens only), `app/dev/video/**`, `components/clay/**`, `public/avatar/**`, the clay tokens in `app/globals.css` base. |
| 90b | **Bug fixes from the TKT-85 sweep** (`docs/reports/TKT-85.md` on branch `m009/tkt-85`): (1) `/work` filter tabs cut mid-word at 390 ("Exp…") — must wrap or scroll with a visible affordance, ≥ 44 px targets, no overflow; (2) `ChapterNav` highlights "08 What I learned" instead of "01 Context" after scrolling back to top — reproduce first (Playwright, w1440), then fix the active-item logic (IntersectionObserver/Lenis interplay); (3) the intermittent chapter-link click → no scroll at w1024 (TKT-83 note; Lenis path in `lib/focus.ts`) — reproduce with `--repeat-each=10`, fix the root cause. Each fix gets a regression test. | `components/projects/FilterTabs.tsx` (+ its CSS block), `components/case-study/ChapterNav.tsx`, `lib/focus.ts` (bug 3 only), new `tests/e2e/regressions-m009.spec.ts`. |
| 90c | **QA sweep evidence** (S90.02/S90.03, excluding what 90a/90b fix): `tests/e2e/eval-010.spec.ts` — every §8 motion row collapses under `reducedMotion:"reduce"` across all routes (hero poster only); `tests/unit/contrast-pairs.test.ts` — extend to every text/background pair the Phase A/B/C tickets introduced (collect from their reports, e.g. terracotta links on paper-2, award years on kraft, Tegaki numeral navy-2 on kraft); `evals/results/eval-021-<sha>.md` — the per-asset manual checklist (each illustration: depicts no metric/logo/product UI/claim) drafted for Stage 8; `docs/a11y-pass.md` — reading-order notes for `/`, `/work/teachspark`, `/about` (from the accessibility tree via Playwright `page.accessibility.snapshot()`); `docs/eval.md` — update EVAL-018…021 sections for the final state (parked list `[]`, hero banner + clip mask, S89.02 doc updates). | `tests/e2e/eval-010.spec.ts`, `tests/unit/contrast-pairs.test.ts`, `evals/results/eval-021-*.md`, `docs/a11y-pass.md`, `docs/eval.md`. |

## Gates (each)
`pnpm typecheck && pnpm lint && pnpm tokens:check && pnpm test && pnpm build`; your specs + any spec your change could affect (90a: FULL e2e — removals touch shared chrome); `eval-018-parked.json` stays `[]`; bundle `/` ≤ 180 kB. Never weaken a test or threshold. Report `docs/reports/TKT-90-<id>.md` (committed). Reply ≤ 10 lines.
