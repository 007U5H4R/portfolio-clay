# Fan-out D — remaining parallel work (4 implementers) — read `FANOUT-AB.md` first

Everything in `docs/briefs/FANOUT-AB.md` applies (own worktree + branch, never push/merge, the `heavy.sh` lock for every heavy command, report + short reply) **except**: these branches start from the integration head `86054cc` (branch `m009/integration-ab` — Phase 0 + TKT-92 round 1 + all 14 Phase A/B/C tickets + post-merge fixes), so you see the whole redesigned site. The per-ticket `globals.css` block rule still applies for **new** CSS (append a `/* <ID> … */ … /* end <ID> */` block); TKT-89 is the only one allowed to delete from the base blocks. An integration QA agent is running the full e2e suite in parallel — expect lock waits. Tushar is AFK with delegated authority (EXE-20): decide by plan/Design precedence and record calls in your report. Worktrees: `/Volumes/E Drive/Dev/Code/Claude/Portfolio-m009-tkt-<id>/`, branch `m009/tkt-<id>`, id ∈ {79, 92r2, 89, 85}. Model: Opus 5.5 (no Fable credits).

## Ownership and scope
| id | Ticket | Scope (plan section) | Owns |
|---|---|---|---|
| 79 | TKT-79 (`TASK-74`) home assembly + Phase A evidence | `technical-plan.md` "##### TKT-79", `tickets.md` "### TKT-79", Design §7.1 + §8 (`Reveal` on home sections below the hero — opacity + 12 px, never on the hero/LCP), TC-151 | `app/page.tsx`, `tests/e2e/home.spec.ts`, `docs/screenshots/m-009/home/**` + `pairs/home-*`, `evals/results/eval-001-m009-home.md`. **Do not** change hero/banner files (TKT-92r2 owns them). |
| 92r2 | TKT-92 round 2 (`TASK-88`) | Preview Lighthouse (EXE-17, ledger): mobile `/` perf 91 but **LCP 2712 ms > 2500**; LCP element = the hero banner `<img>`; teachspark/desktop pass. Target the **mobile hero image**: `sizes` vs the displayed box at 412 px (4:3 crop on < 768), a mobile-specific source (smaller/cropped rendition for < 768 via `<picture>` art direction or tighter `sizes`), AVIF quality, `fetchPriority`/preload correctness, no competing eager images above the fold (polaroids, poster). Read the LCP breakdown from the committed preview LHRs (`evals/results/lighthouse-m009-tracer/preview-tkt92/mobile/`) before changing anything. | `components/paper/SceneBanner.tsx`, `components/hero/*`, `next.config.ts` (images only), `content/media/illustrations/hero-banner*` (+ new mobile rendition + manifest/README provenance row if you add a file), `tests/e2e/eval-019.spec.ts` (only if selectors change). Append a "Round 2" section to `docs/reports/TKT-92.md`. Local Lighthouse is informational only; the orchestrator re-measures on the preview. |
| 89 | TKT-89 (`TASK-84`) dead-code removal (S11) | `technical-plan.md` "##### TKT-89" (S89.*), `tickets.md` "### TKT-89", TC-172 | deletions per S89.*: `components/clay/*` + `tiers.ts`, aurora/glow leftovers, avatar system (`public/avatar/*`, `content/media/avatar/**`, `scripts/{avatar,avatar-poses}.ts`, the `media:avatar`/`media:poses` scripts), `assets/fonts/Manrope-*`, `components/layout/Footer.tsx` if still present, `scripts/codemod-tokens.ts`, clay radius/shadow/gradient tokens + the clay `rgba()` shadows in the `globals.css` base (ledger TKT-69 carry), `DESIGN_DIRECTION.md`/`COMPONENT_ARCHITECTURE.md` updates, `tests/e2e/paper-board.spec.ts`. Prove every deletion unused by grep first; delete-only commit separate from doc updates. **Do not** touch files owned by 79/92r2/85. |
| 85 | TKT-85 (`TASK-80`) Phase B evidence | `technical-plan.md` "##### TKT-85", `tickets.md` "### TKT-85", TC-165 | `tests/e2e/sweep.spec.ts` (new: all 11 case-study slugs + 5 essays + /work + /thinking at 390 and 1440 — no overflow, axe 0 critical/serious, EVAL-018 unit counts, console clean), `docs/screenshots/m-009/{phase-b,pairs}/**`. One rich (teachspark) + one thin (e.g. tegaki) case-study screenshot pair vs `docs/redesign-mockups/m-009/case-study.html` — record differences as Stage-8 inputs; don't fix product code. |

## Gates (each)
Your ticket's plan gates + `pnpm typecheck && pnpm lint && pnpm tokens:check && pnpm test && pnpm build`, your specs and any spec your change could affect (89: the FULL e2e suite — deletions touch everything), `tests/e2e/eval-018-parked.json` stays `[]`, bundle `/` ≤ 180 kB. Report `docs/reports/<ID>.md` (committed; 92r2 appends to TKT-92.md). Reply ≤ 10 lines.
