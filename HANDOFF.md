# HANDOFF — Portfolio (M-009 · Illustrated editorial redesign)

Updated 2026-09-24 · **Stage 4 (UI/UX Design) for M-009 COMPLETE — awaiting Tushar's sign-off at the gate.** Next = **Stage 5 Problem Breakdown** (`bw-problem-breakdown`, Fable / Medium) — not yet started.

**Where things live.** Build worktree: `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay-redesign/` (branch `m-009-redesign`, forked from `m-008-visual-wow` @ `7a8b60c`; `main` = M-001–M-007 merged, prod still gated). **Stage 2 + 3 + 4 edits are uncommitted in the worktree** (`Discovery-PRD.md`, `Solution-PRD.md`, `decisions.md`, `evaluation-plan.md`, `evals/eval-cases.json`, `scripts/eval.ts`, `scripts/eval-cases.ts`, `docs/eval.md`, `Design.md`, `docs/redesign-mockups/m-009/**`, `HANDOFF.md`) — commit when Tushar approves the Stage-4 gate. Design reference of record now **in the repo**: `docs/redesign-mockups/m-009/` (8 mockups + briefs + scene JPEGs + `README.md` index; the character sheet, scene PNG masters and hero clip exports stay on disk under `Portfolio-illustration/` until Stage 7 — paths in that README). Obsidian: `Obsidian Vault/Portfolio-illustration/Progress.md` (redesign log) + `Obsidian Vault/Portfolio-clay/` (artifact mirrors). Auto-memory: `illustrated-redesign-direction.md`. PWA: Campfire (`campfire`), project `portfolio-clay` — **M-009 milestone/tickets not yet created (Stage 5/6)**.

> Start the next session by reading the global `~/.claude/CLAUDE.md`, this file, `Design.md` (all of it — it is the spec Stage 5 breaks down), `Solution-PRD.md` §12, `evaluation-plan.md` §8, `decisions.md` S11–S21 · EV3–EV6 · D6–D12, then continue at "NEXT".

---

## 1. What M-009 is (one paragraph)
Same product, new skin. The routes, `data/*.ts` content model, zod truth gate, `pnpm eval` harness, tests, SEO and headers all carry forward untouched. The presentation is replaced wholesale: 13 paper colour tokens (1:1 for the 13 clay tokens, gate stays 13/13), Fraunces + Inter + Caveat, paper primitives (torn edges, tape, sticky, notebook, postcard, sketch) instead of clay/aurora, an illustrated desk-scene hero with a 2.5 s clip-A animation that plays once and holds, and a terracotta band footer on every page replacing the home Final-CTA. M-008's visual layer is **removed**, not restyled; `m-008-visual-wow` never merges on its own (S11).

## 2. Stage 4 outputs (this session, 2026-09-24)
- **`Design.md` rewritten** for the paper system (11 sections): §1 strategy (mockups = benchmark, Higgsfield assets inventory, no re-run of Mobbin/Higgsfield); §2 tokens — the 13 paper tokens with **culori-exact OKLCH** and a contrast table (two mockup pairs fail and are corrected: band `.dim` → kraft, kraft tag text → navy), derived values via `color-mix` only, Fraunces/Inter/Caveat scale, spacing/radius/rotation caps; **§3 the EV5 decoration contract** — `data-decor` (counts) / `data-paper` (content, never counts) / `data-fastener` (tape + pins on a host, never count, D6) / `data-flat` (0 decorations) / `data-hand="quote|cta|label"` (the S13 Caveat exemptions with measurable limits), nearest-section ownership, and a **per-section planned count table for every route (all ≤ 4)**; §4 header (no compaction, D12) + band footer markup + 404; **§5 hero contract** — static poster `<img>` + client-mounted `<video>` in default mode only (D10), the full EVAL-019 mode matrix and lifecycle rules; **§6 illustrations** — `content/media/illustrations/manifest.ts` shape, README provenance columns, the nine exact alt strings, placement forms; §7 per-page layouts for all 8 pages + 404 with four screen states per data-backed view; §8 motion (reveals + draw-ins only); §9 OG paper skin (D11); §10 a11y/QA checklist mapped to EVAL rows; **§11 Deviations Dev-01…17** (EVAL-022 appends at Stage 8).
- **`decisions.md` D6–D12 appended**: D6 contract details (fasteners don't count, `data-hand` exemptions, nearest-section — *refines EV5, flagged*) · D7 copy precedence (data over mockup) · **D8 Playground in the header nav (proposed)** · **D9 Thinking/Playground quiet-close sections dropped (proposed)** · D10 hero markup · D11 OG re-skin · D12 header without compaction.
- **`docs/redesign-mockups/m-009/`** created (S17): `home/work/case-study/about/thinking/essay/playground/contact.html`, `MOCKUP-BRIEF.md`, `content-brief.md`, `assets/*.jpg` (6.5 MB), `README.md` (index + on-disk paths for the character sheet and hero exports).
- **Verified:** OKLCH values regenerated with the repo's `culori` and round-trip to the authoritative hex 13/13; WCAG contrast computed for 21 token pairs; the eight mockups' decorations counted section by section against the §3 contract (home Featured 6→4 and playground bench 5→3 trimmed; every other section ≤ 4 as designed). **Not run:** any build/test — Stage 4 changes docs only (`git status` shows no source changes beyond Stage 3's).

## 3. Gate for Tushar (Stage 4 → 5)
Approve `Design.md` as written, or note changes. Decisions that need his word:
1. **D6 fastener rule** — tape/pins that attach a card or photo don't count toward the 4-per-section budget (free-standing objects do). Without it the approved mockups fail their own budget on 5 sections.
2. **D8** — add `Playground` to the header nav (the mockups show it; the band has no nav links).
3. **D9 / Dev-08** — drop the Thinking + Playground "quiet close" sections (two stacked closing CTAs above the band).
4. **Dev-01/02** — data wins over mockup copy (h1 keeps "AI-native products"; "A product journey, not a process."; "Ask my portfolio"; essays h2 "Essays"; Velora card loses "10/10 unit tests").
5. **Dev-07** — the playground's Caveat notebook sheet and the retired clay "tone" lines go.
6. Still open from Stage 2: hiring line copy (DRAFT), "Bengaluru, India" in the band (public already; confirm), keep/drop the global `AskPanel` (S21), remainder of the master prompt (→ change requests).

## 4. Reference assets
- In repo: `docs/redesign-mockups/m-009/` (see its `README.md` for the artifact links).
- On disk until Stage 7: character sheet `Portfolio-illustration/illustrations/character-sheet/character-ref-LOCKED.png`; scene masters `illustrations/scenes/*.png`; hero exports `animation/export/hero-animation.{webm,mp4}` (176 / 312 kB), `hero-poster.webp` (87 kB), `hero-end.webp`, 1280×684, 2.5 s. Higgsfield balance 69 credits; no further spend approved (§12.4).

## 5. NEXT — Stage 5 · Problem Breakdown (`bw-problem-breakdown`, Fable / Medium) — only after §3 approval
Inputs: `Design.md`, `Solution-PRD.md` §12.3/§12.9 (phases), `evaluation-plan.md` §8, `decisions.md` D6–D12, `evals/eval-cases.json` (v1.1.0). Do:
1. `milestones.md`: add **M-009** with the §12.9 phases as the dependency spine — Phase 0 tracer (tokens + fonts + header + band + hero on `/`, `pnpm eval --label baseline-m009-tracer`, **Tushar's hero gate on the Vercel preview** = EVAL-022 sub-gate) → A home → B work + case-study template (11 slugs) + thinking/essay → C about, playground, contact, 404 → D QA + dead-code removal + `Design.md` deviations + PWA sync.
2. `tickets.md`: vertical-slice tickets (provisional `TKT-`/`TSK-` ids; Backlog allocates native ids at Stage 6) with Type · Priority · AC · DoD · `TC-` · `EVAL-` links. Must exist: paper primitives + the `data-decor`/`data-paper`/`data-fastener`/`data-flat`/`data-hand` contract (Design.md §3); tokens swap + `tokens-check` AUTHORITATIVE (S12/EVAL-020); fonts (S13); header + `MobileMenu` + nav (D8 variant flagged); band footer (S16); `HeroClip` + poster + asset copy (D10/EVAL-019); illustration manifest + README + alt strings (EVAL-021); OG re-skin + font TTFs (D11/EVAL-017); per-page tickets per §7; the **S18 fixes as regression tests** (double DRAFT prefix, timeline lead, `learnings[]` "What I learned", `hero.tagline` in the band); the derived **Sources** section on case studies; `EmptyState` conditional (Dev-05); dead-code removal (clay primitives, aurora, avatar system, `heroMotion.ts`, `usePointerParallax`, `AvatarScene`, `HeroActivationContext`, header hysteresis, Manrope, `public/avatar/*`); Stage-7-owed specs `tests/e2e/eval-018.spec.ts`, `eval-019.spec.ts`, `tests/unit/eval-020.test.ts`, `eval-021.test.ts` (remove EVAL-018/019 from `DEFERRED_SPECS` when they land).
3. Replace the `"M-009"` placeholders in `evals/eval-cases.json` `related_tickets` with the ticket ids.
4. Then Stage 6 (`technical-plan.md`, `test-cases.md`, Campfire onboarding), Stage 7 Phase 0 tracer first.

## 6. Guardrails (carry forward, amended)
13-token gate (paper palette, S12; EVAL-020) · content truth + **never publish PII** (real contact = email/LinkedIn per EXE-8; no phone/DOB/address; "Bengaluru, India" is already public — confirm before rendering in the band) · reduced-motion + touch + Save-Data → poster-only hero (EVAL-019; D10 markup) · poster is the LCP `priority` image · all routes stay statically prerendered (TP1) · don't break the build (`pnpm typecheck && pnpm lint && pnpm tokens:check && pnpm test && pnpm build`) · commit attribution per the session reminder · cinematic site + `portfolio/index.html` untouchable · decoration budget ≤ 4 per section under the **D6 definitions** (S15/EV5/EVAL-018) · Caveat only via `data-decor`/`aria-hidden`/`data-hand` · illustrations decorative with provenance, alts exactly as `Design.md` §6.3 (S20/EVAL-021) · **thresholds are never lowered** (EV2/EV6) · mockups in `docs/redesign-mockups/m-009/` are frozen; changes are `Design.md` §11 rows · everything on `/Volumes/E Drive` (scratch `/Volumes/E Drive/Dev/.scratch`).

## 7. Tooling notes
- Playwright screenshots from the clay repo: `PLAYWRIGHT_BROWSERS_PATH="/Volumes/E Drive/Dev/.cache/ms-playwright" TMPDIR="/Volumes/E Drive/Dev/.scratch" pnpm exec playwright screenshot --browser=chromium --viewport-size=1440,900 --full-page --wait-for-timeout=2500 <url> out.png`.
- Verify hero/poster against a **prod build or the Vercel preview**, not `next dev` (image-cache scar, M-008).
- GateGuard "Fact-Forcing Gate" denies the first Edit/Write per file — state importers/API/schemas/verbatim instruction, retry. Plain `tsc --noEmit` reports a missing `LayoutProps` in `app/layout.tsx`; use `pnpm typecheck` (runs `next typegen` first).
- OKLCH/contrast for the paper tokens: a 20-line `culori` script run from inside the repo (`node_modules` resolution) — values are in `Design.md` §2.1; `pnpm tokens:check --write` regenerates them once `AUTHORITATIVE` is swapped.
- Higgsfield: on-disk media via `media_upload` → curl PUT → `media_confirm`; `gpt_image_2_5` with `image_references`; Seedance 2.5 via single `generate_video` (16:9 1280×720 source). Don't chain hops for multi-beat character sequences (lesson, 2026-09-24).

## 8. Persistence
Mirror `Design.md`, `decisions.md`, `HANDOFF.md` (plus the Stage-2/3 `Solution-PRD.md`, `Discovery-PRD.md`, `evaluation-plan.md`, `evals/eval-cases.json`) to `Obsidian Vault/Portfolio-clay/`; log progress in `Obsidian Vault/Portfolio-illustration/Progress.md`; keep memory in sync (Obsidian wins on conflict). Rewrite this baton in place at each stage boundary.
