# HANDOFF — Portfolio (M-009 · Illustrated editorial redesign)

Updated 2026-09-24 · **Stage 5 (Problem Breakdown) for M-009 COMPLETE — awaiting Tushar's sign-off on granularity + blocking edges (tickets.md "M-009 granularity & blocking-edge questions", 5 items).** Next = **Stage 6 Technical Planning** (`bw-technical-planning`, Fable / Medium) — not yet started.

**Where things live.** Build worktree: `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay-redesign/` (branch `m-009-redesign`, forked from `m-008-visual-wow` @ `7a8b60c`; `main` = M-001–M-007 merged, prod still gated). **Stages 2–4 are committed** (`b0f6f8d` "docs(m-009): Stages 2–4 …", 28 files incl. `docs/redesign-mockups/m-009/**`). **Stage 5 edits are uncommitted** (`milestones.md`, `tickets.md`, `evals/eval-cases.json`, `HANDOFF.md`) — commit when Tushar approves the Stage-5 gate. Design reference of record: `docs/redesign-mockups/m-009/` (frozen; changes = `Design.md` §11 rows). On-disk build inputs until Stage 7: paths in that folder's `README.md`. Obsidian: `Obsidian Vault/Portfolio-illustration/Progress.md` (log) + `Obsidian Vault/Portfolio-clay/` (artifact mirrors). Auto-memory: `illustrated-redesign-direction.md`. PWA: Campfire (`campfire`), project `portfolio-clay` — **M-009 milestone/tickets not yet created (Stage 6)**; M-008 lives there as milestone `m-7` / TASK-50…63.

> Start the next session by reading the global `~/.claude/CLAUDE.md`, this file, `milestones.md` (M-009 section), `tickets.md` (M-009 section + Appendix D), `Design.md` §3 · §5 · §6 · §11, `evaluation-plan.md` §8, `decisions.md` D6–D12, then continue at "NEXT".

---

## 1. What M-009 is (one paragraph)
Same product, new skin. Routes, `data/*.ts`, zod truth gate, `pnpm eval`, tests, SEO and headers carry forward untouched. The presentation is replaced wholesale: 13 paper tokens (1:1 for the 13 clay tokens, gate stays 13/13), Fraunces + Inter + Caveat, paper primitives under the `data-decor`/`data-paper`/`data-fastener`/`data-flat`/`data-hand` contract, an illustrated desk-scene hero with a 2.5 s clip that plays once and holds, and a terracotta band footer on every page. M-008's visual layer is **removed**, not restyled (S11).

## 2. Stage 5 outputs (this session, 2026-09-24)
- **`milestones.md`**: header/table updated (`M-001`…`M-009`); **M-008 ledger stub** (superseded, Campfire-only, TKT-55…68 / TASK-50…63 so numbers are never reused); **M-009** section — objective, scope by phase (0 tracer → A home + OG → B work + case-study template + thinking → C about/playground/contact/404 → D dead-code + QA + record run + hand-off), deliverables, tickets, dependencies, entry/exit, DoD (EV6 baseline rule, hero gate, 180 kB binding), risks, blockers.
- **`tickets.md`**: addendum header; §0.4 M-009 mapping table (TKT-69…91, TSK-30…47 → `_Stage 6_`); the **M-009 section** = 23 vertical-slice tickets, 94 sp, each with Type · Priority · Milestone · sp · Dependencies · Description · Objective · Product requirement · AC · DoD (**Paper DoD** = Base + EVAL-018 counts per §3.3 + verbatim copy + tokens gate + reduced motion) · Notes · Related TC (`TC-TBD`) · Related EVAL · Blockers · Phase · Owner; 18 sub-tasks where a ticket has ≥ 3 separable pieces; **Appendix D** (edges, critical path 69→70→73→74→77→79→81→83→85→86→87→89→90→91 = 60 sp, sp by phase, the four S18 regression tests mapped to TKT-72/82/84/87); Appendix C M-009 not-ticketed list; **5 granularity questions** for Tushar at the end of the section.
- **`evals/eval-cases.json`**: every `"M-009"` placeholder replaced with ticket ids (EVAL-017 → TKT-78/91 · EVAL-018 → 19 tickets · EVAL-019 → 73/74/90/91 · EVAL-020 → 69/74/78/89/90 · EVAL-021 → 73/80/81/84/86/88/90 · EVAL-022 → 74/79/85/90/91); schema note updated. **Verified:** `loadCases()` = 22 cases, 0 placeholders, 9 required Playwright specs (018/019 still deferred by design); `tests/unit/eval-docs.test.ts` green.
- **Ticket map (provisional ids):** Phase 0 — TKT-69 tokens + fonts + repo-wide codemod + `eval-020.test.ts` · TKT-70 paper primitives + contract + `eval-018.spec.ts` (EVAL-018 leaves `DEFERRED_SPECS`) · TKT-71 header (D12, D8 variant, `MobileMenu`, reading progress, `AskAIButton` ghost) · TKT-72 `BandFooter` + `hero.tagline` regression + `FinalCTA` removed + "Bengaluru" flag · TKT-73 illustration manifest/README/assets + hero + `HeroClip` + `eval-019.spec.ts` + `eval-021.test.ts` + hero motion system deleted · TKT-74 tracer assembly + `baseline-m009-tracer` + Vercel preview + **Tushar's hero gate**. Phase A — TKT-75 Featured Work · TKT-76 How I think · TKT-77 Ask notebook + panel · TKT-78 OG re-skin (runs across the gate) · TKT-79 home assembly + gate. Phase B — TKT-80 `/work` · TKT-81 case-study header/metrics/overview/next · TKT-82 What I learned + Sources (S18) · TKT-83 deep dive/artifacts/Show the thinking · TKT-84 thinking + essay (double-prefix fix, D9) · TKT-85 gate. Phase C — TKT-86 about part 1 · TKT-87 about part 2 (timeline lead fix, Dev-11) · TKT-88 playground + contact + 404. Phase D — TKT-89 dead-code removal · TKT-90 QA sweep + Stage-8 packs · TKT-91 preview record run + OG inspectors + deviations + PWA sync + hand-off.

## 3. Gate for Tushar (Stage 5 → 6) — answer the five questions in `tickets.md`
1. **Phase gates strict?** TKT-74/79/85 hard-block the next phase (only TKT-78 OG crosses the Phase-0 gate). Default: strict.
2. **Token codemod in the tracer (TKT-69)?** One-commit repo-wide rename keeps the 13-count gate green from day one; legacy pages render in paper colours until their phase. Alternative: a temporary 26-token exemption (needs a decision entry). Default: codemod.
3. **Case-study split** TKT-81 / 82 / 83 — merge 81+82? Default: keep 82 separate (S18 scope + its regression tests).
4. **Phase C bundling** — split TKT-88 (playground · contact · 404) into three? Default: one ticket, three tasks.
5. **Pending decisions as variants** — D8 five-item nav (TKT-71), D9 quiet-close dropped (TKT-84/88), "Bengaluru, India" flag off by default (TKT-72), `AskPanel` kept (TKT-77). Confirm the defaults now, or per ticket at dispatch?
Also still open from Stage 4: D6 fastener rule (applied in `Design.md` §3 and in every ticket's counts), hiring-line copy (stays DRAFT-tagged).

## 4. Reference assets
- In repo: `docs/redesign-mockups/m-009/` (see its `README.md`).
- On disk until TKT-73: character sheet `Portfolio-illustration/illustrations/character-sheet/character-ref-LOCKED.png`; scene masters `illustrations/scenes/*.png`; hero exports `animation/export/hero-animation.{webm,mp4}` (176 / 312 kB), `hero-poster.webp` (87 kB), `hero-end.webp`, 1280×684, 2.5 s. Higgsfield balance 69 credits; no further spend approved (§12.4).

## 5. NEXT — Stage 6 · Technical Planning (`bw-technical-planning`, Fable / Medium) — only after §3 approval
Inputs: `tickets.md` M-009 + Appendix D, `milestones.md` M-009, `Design.md`, `evaluation-plan.md` §8, `evals/eval-cases.json`, `decisions.md`. Do:
1. Commit the Stage-5 edits (`milestones.md`, `tickets.md`, `evals/eval-cases.json`, `HANDOFF.md`) once approved; record any granularity changes as `PB6+` decisions and walk the sync chain (tickets → eval-cases `related_tickets` → this file).
2. `technical-plan.md`: per-ticket atomic plans for TKT-69…91 in dependency order (Phase 0 first, including the codemod design for TKT-69, the `HeroClip` lifecycle for TKT-73, the `eval-018` counting algorithm for TKT-70), model/effort per task, the Vercel preview procedure for TKT-74, and the orchestration playbook for Phase 0.
3. `test-cases.md`: `TC-###` rows back-filled into every M-009 ticket's "Related TC" — including the four **S18 regression tests** (band tagline · learnings · single DRAFT prefix · timeline lead/order), the EVAL-018 violating fixture, the EVAL-019 four-mode matrix, EVAL-020 greps, EVAL-021 both-ways provenance.
4. Campfire (`campfire`, verify it **loaded**): create the M-009 milestone (next after `m-7`), onboard TKT-69…91 + TSK-30…47 via `backlog task create` (Backlog allocates native ids — never hand-pick), record the mapping in `tickets.md` §0.4 + `backlog/id-map.json`, set types/priorities/`sp:`/dependencies/phases; Gantt reflects Appendix D.
5. Then Stage 7 Phase 0 (`bw-execution-orchestration`): TKT-69 → 70 → {71 ∥ 72 ∥ 73} → 74 tracer + hero gate; nothing past Phase 0 until Tushar approves the hero on the preview.

## 6. Guardrails (carry forward)
13-token gate (paper palette, S12; EVAL-020) · content truth + **never publish PII** (real contact = email/LinkedIn per EXE-8; no phone/DOB/address; "Bengaluru, India" behind a flag until confirmed) · reduced-motion + touch + Save-Data → poster-only hero (EVAL-019; D10 markup) · poster is the LCP `priority` image · all routes statically prerendered (TP1) · don't break the build (`pnpm typecheck && pnpm lint && pnpm tokens:check && pnpm test && pnpm build`) · commit attribution per the session reminder · cinematic site + `portfolio/index.html` untouchable · decoration budget ≤ 4 per section under the **D6 definitions** (S15/EV5/EVAL-018), planned counts in `Design.md` §3.3 · Caveat only via `data-decor`/`aria-hidden`/`data-hand` · illustrations decorative with provenance, alts exactly as `Design.md` §6.3 (S20/EVAL-021) · **thresholds are never lowered** (EV2/EV6) · mockups frozen; changes are `Design.md` §11 rows · stable IDs: M-, TKT-, TSK-, TC-, EVAL- never renumbered; M-008's TKT-55…68 never reused · everything on `/Volumes/E Drive` (scratch `/Volumes/E Drive/Dev/.scratch`).

## 7. Tooling notes
- Playwright screenshots: `PLAYWRIGHT_BROWSERS_PATH="/Volumes/E Drive/Dev/.cache/ms-playwright" TMPDIR="/Volumes/E Drive/Dev/.scratch" pnpm exec playwright screenshot --browser=chromium --viewport-size=1440,900 --full-page --wait-for-timeout=2500 <url> out.png`.
- Verify hero/poster against a **prod build or the Vercel preview**, not `next dev` (image-cache scar, M-008).
- GateGuard "Fact-Forcing Gate" denies the first Edit/Write per file — state importers/API/schemas/verbatim instruction, retry. Plain `tsc --noEmit` reports a missing `LayoutProps` in `app/layout.tsx`; use `pnpm typecheck` (runs `next typegen` first).
- `pnpm eval --check-specs` starts a full Playwright run (minutes); for a quick loader check use `pnpm exec tsx -e 'import("./scripts/eval-cases.ts").then(m=>console.log(m.loadCases().length))'`.
- OKLCH/contrast for the paper tokens: `Design.md` §2.1 (culori-exact); `pnpm tokens:check --write` regenerates once `AUTHORITATIVE` is swapped (TKT-69).
- Higgsfield: on-disk media via `media_upload` → curl PUT → `media_confirm`; `gpt_image_2_5` with `image_references`; Seedance 2.5 via single `generate_video`. No further spend approved.

## 8. Persistence
Mirror `milestones.md`, `tickets.md`, `evals/eval-cases.json`, `HANDOFF.md` (plus the committed Stage-2/3/4 artifacts) to `Obsidian Vault/Portfolio-clay/`; log progress in `Obsidian Vault/Portfolio-illustration/Progress.md`; keep memory in sync (Obsidian wins on conflict). Rewrite this baton in place at each stage boundary.
