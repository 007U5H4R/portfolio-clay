# Technical Plan — Clay Portfolio (Tushar Pathak · Senior Product Manager)

Stage 6.1 (Technical Planning) · 2026-09-15 · **living plan** (see §0.3). Consumes `Solution-PRD.md`, `Design.md` (tokens/components/motion/deviations — implemented verbatim, never re-decided here), `COMPONENT_ARCHITECTURE.md`, `SITEMAP.md`, `evaluation-plan.md`, `milestones.md` (M-001…M-007), `tickets.md` (49 tickets / 29 tasks, dependency order), `decisions.md` (S1–S10, EV1–EV2, D1–D5, PB1–PB5 — all binding), `CONTENT_INVENTORY.md` (§8 packs, §9 KnowledgeIndex). Companions produced in Stage 6: `evals/eval-cases.json` (17 EVAL rows as data), `test-cases.md` (TC-### — authored by a sibling agent; every `TC-TBD` below is back-filled when it lands), Campfire PWA onboarding (6.2/6.3, separate step). Technical decisions taken here are appended to `decisions.md` as **TP1…TP9**.

---

## 0. How to read this plan

### 0.1 Sections
- **A. Technical approach** — architecture, module map, data model (full zod schema + the rules that fail the build), Ask adapter, View Transitions, motion, media pipeline, SEO/OG, tooling, git, deployment, observability, failure modes, risks, rollback, evaluation wiring (workflow 6.5).
- **B. Per-ticket atomic plans** — every ticket in **M-001, M-002, M-003** broken into 2–5-minute atomic steps (file paths · contract · verification gate). **M-004 → M-007** carry ticket-level plans that are atomised at their phase boundary.
- **C. Sequencing & phases** — entry criteria, DAG order, QA gate, human checkpoints per phase.
- **D. Traceability** — ticket → files → EVAL IDs (TC IDs `TC-TBD` until `test-cases.md` lands).
- **E. Conflicts register** — every inconsistency found between the upstream artifacts and how it is resolved (Execution must read this before TKT-01).

### 0.2 Conventions
- Paths are relative to the project root `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay/` unless absolute. Everything (repo, `node_modules`, pnpm store, Playwright browsers, Next cache, ffmpeg scratch, screenshots) stays on `/Volumes/E Drive` — never the internal disk (global CLAUDE.md scar).
- A **step** is `S<ticket>.<n>` (e.g. `S01.07`). Each has *Files*, *Contract* (the interface/behaviour it creates) and *Gate* (a command + expected output, or a named screenshot check). A step that fails its gate is not "done"; the implementer fixes it or reports the breaker.
- **Gate shorthands:** `TYPECHECK` = `pnpm typecheck` exits 0 · `LINT` = `pnpm lint` exits 0 · `UNIT` = `pnpm test` exits 0 · `BUILD` = `pnpm build` exits 0 and `scripts/assert-static.ts` prints `all routes static` · `EVAL(<ids>)` = `pnpm eval --only <ids>` writes a results file with those cases `PASS` · `SHOT(<name>)` = Playwright screenshot saved under `docs/screenshots/…` and eyeballed by the orchestrator.
- **Model assignment per dispatch** (Execution rule): transcription/data-entry steps → cheap tier; integration steps → standard tier; design-judgement or highest-risk steps (tokens, Hero, View Transitions, AskPanel focus trap, eval orchestrator) → most-capable tier. Named per ticket in §B.

### 0.3 Living plan (change-management rule)
This plan is consumed by Execution and by the QA stages. When implementation reveals the plan is wrong, Execution does **not** follow the stale plan: it names the new evidence, updates the affected step/ticket here, walks the chain (`tickets.md` → `milestones.md` → PWA/Gantt → `test-cases.md`/`evals/eval-cases.json`), and records the deviation in `decisions.md` as `EXE-n`. Ticket-level plans for M-004…M-007 are **deliberately not atomised yet** — they are atomised at each phase boundary (after the previous phase's QA gate) so the atomic steps reflect the real codebase rather than a guess made seven milestones early.

---

## A. Technical approach

### A1. Architecture

**Shape.** A statically generated Next.js 16 App Router site. Every route is prerendered at build time to HTML + RSC payload; there is no server-side logic at request time in v1 (no API routes, no middleware, no ISR). Content lives in typed TypeScript data modules (`data/*.ts`) validated by zod at build time; pages are server components that import data and render; interactivity is confined to client leaves.

```
                    build time                                   request time (Vercel CDN)
┌──────────────┐  zod  ┌────────────────┐  RSC  ┌──────────────┐        ┌─────────────────────┐
│ data/*.ts    │──────▶│ app/**/page.tsx │──────▶│ .next static │──deploy▶ HTML + RSC + assets │
│ (typed lit.) │ fail  │ (server comps)  │       │ HTML/RSC/OG  │        │  + images (/_next/  │
└──────────────┘ build └───────┬────────┘       └──────────────┘        │    image optimiser) │
                               │ props                                   └─────────┬───────────┘
                        ┌──────▼────────┐                                          │ hydrate
                        │ client leaves │  Header · MobileMenu · Parallax · FilterTabs · AskPortfolio
                        │ ("use client")│  AskPanel · DemoVideo · ShowTheThinking · Timeline · Reveal
                        └───────────────┘  ViewTransitionLink · CopyButton · OverviewToggle · HowIThink
```

**Rendering mode — decision TP1.** Default Next build with **every route statically prerendered** (SSG), deployed on Vercel — *not* `output: 'export'`. Rationale: static export disables `next/image` optimisation (DESIGN_DIRECTION §9 requires AVIF/WebP), `headers()` in `next.config` (TKT-50 security headers), and makes the future `/api/ask` (RagProvider, S7) a deployment-mode change. SSG on Vercel gives the same "static output" guarantee (CDN-served HTML, no functions invoked) while keeping those features. The guarantee is **enforced mechanically**: `scripts/assert-static.ts` reads `.next/prerender-manifest.json` + `.next/app-path-routes-manifest.json` after `next build` and fails if any app route is not prerendered (the only permitted dynamic entries are Next's internal `/_not-found` handling and OG image routes that carry `generateStaticParams`). `pnpm build` = `next build && tsx scripts/assert-static.ts`.

**Server/client boundary.** Server components by default. `"use client"` only on: `Header` (scroll state), `MobileMenu`, `AskAIButton` (opens panel), `Parallax`, `ViewTransitionLink`, `FilterTabs` + `WorkGrid` wrapper, `AskPortfolio`, `AskPanel` (+ `AskProvider` context), `DemoVideo`, `ShowTheThinking`, `ExperienceTimeline`, `HowIThink`, `OverviewToggle`, `ChapterNav` (IO highlight), `Reveal`, `ProgressBar`, `CopyButton`. Data never crosses to the client as a whole module — pages pass the exact props a leaf needs (e.g. `AskPortfolio` receives `prompts: string[]`, not `knowledge.ts`; the provider is constructed client-side from a tree-shaken `knowledge.ts` import inside `lib/ask/local-provider.ts` — acceptable because the 11 entries are ~6 kB and the Ask feature is client-only by nature). Fonts via `next/font/google` (Manrope 500–800, Caveat 500–600, `display: 'swap'`, self-hosted at build).

**Search params on `/work` — decision TP7.** `?filter=ai` is read client-side (`useSearchParams` inside a `<Suspense>` boundary in `WorkGrid`), so `/work` stays static. The full grid (all personal cards) is in the prerendered HTML; the filter is applied on hydration. On a deep link there can be a single-frame flash from "all" to the filtered set — accepted (rare path; documented in E-4). Playwright asserts the post-hydration state.

**404.** `app/not-found.tsx` is prerendered; Vercel serves it with HTTP 404 for unknown paths.

**Environment.** `NEXT_PUBLIC_SITE_URL` (absolute HTTPS base for canonical/OG; falls back to `https://${VERCEL_PROJECT_PRODUCTION_URL}` then `https://${VERCEL_URL}` then `http://localhost:3000`). `RAG_ENDPOINT` reserved, unused, documented in `.env.example`. No secrets exist in v1.

### A2. Module map (matches COMPONENT_ARCHITECTURE §1; additions marked ➕)

```
Portfolio-clay/
  app/
    layout.tsx                 fonts, metadata defaults, SkipLink, Header, Footer, AskProvider (+ lazy AskPanel), <main id="main">
    page.tsx                   Home: Hero · AskPortfolio · FeaturedWork · HowIThink · FinalCTA
    globals.css                @import "tailwindcss"; @theme { tokens from Design.md §2 }; base styles; VT + reduced-motion rules
    work/page.tsx              WorkHero · FilterTabs+WorkGrid (client) · ExperienceStrip
    work/[slug]/page.tsx       generateStaticParams (11) · CaseStudyHeader · OverviewToggle · ChapterNav · Chapter×n · ShowTheThinking · NextProject
    work/[slug]/opengraph-image.tsx   per-slug 1200×630
    about/page.tsx · thinking/page.tsx · thinking/[slug]/page.tsx · playground/page.tsx · contact/page.tsx
    opengraph-image.tsx        home OG; each page family has its own opengraph-image.tsx using lib/og.tsx ➕ template
    sitemap.ts · robots.ts · not-found.tsx
    dev/primitives/page.tsx ➕  dev/artifacts/page.tsx ➕  dev/thinking/page.tsx ➕   (excluded from sitemap; 404 in production via lib/dev-only.ts ➕)
  components/
    layout/      Container, Section, SectionHeading, Footer, SkipLink
    navigation/  Header, NavPill, MobileMenu, AskAIButton
    hero/        Hero, AvatarStage, FloatingTiles, Annotation
    clay/        ClayCard, ClayButton, ClayPill, ClayTile, ClayFrame, ClayIcon, tiers.ts ➕ (tier→token map, type-level guard)
    projects/    FeaturedWork, ProjectCard, EditorialGrid, WorkGrid ➕, FilterTabs, StatusBadge, DemoVideo, ExperienceStrip, WorkHero ➕, EmptyState ➕
    case-study/  CaseStudyHeader, OverviewToggle, Chapter, ChapterNav, NextProject,
                 artifacts/ ArtifactRenderer ➕, ArtifactCard, InsightCard, HypothesisCard, MetricCard, DecisionCard, EvaluationCard, ExperimentCard, PrototypeFrame
    timeline/    ExperienceTimeline, TimelineNode, StoryCard, ProductJourney
    interactions/ ShowTheThinking, ThinkingNode, Reveal, Parallax, ViewTransitionLink, ProgressBar
    ai/          AskProvider ➕ (context + useAsk), AskPortfolio, AskPanel, AnswerView, SuggestedPrompts, EvidenceLinks
    thinking/    ThinkingList, EssayBody
    about/ ➕     AboutHero, CapabilityClusters, Impact, Awards, Research, Education
    home/ ➕      HowIThink, FinalCTA
    common/      Icon, Tag, ExternalLink, CopyButton, VisuallyHidden, Prose
  data/
    schema.ts        zod schemas + inferred types (A3)
    projects.ts      11 personal + 3 professional
    experience.ts    4 roles · skills.ts 4 clusters · writing.ts 5 DRAFT essays · knowledge.ts 11 entries · thinking-framework.ts 6 stages
    index.ts ➕       re-exports + `validateAll()` used by scripts/validate-content.ts and Vitest
  lib/
    ask/adapter.ts · ask/local-provider.ts · ask/rag-provider.ts · ask/normalise.ts ➕ · ask/synonyms.ts ➕
    motion.ts (variants, springs, useReducedMotionSafe) · format.ts · seo.ts · og.tsx ➕ · site.ts ➕ (resumeAvailable flag, identity constants) · anchors.ts ➕ (chapter id ↔ anchor) · filters.ts ➕ · dev-only.ts ➕
  scripts/
    tokens-check.ts (D2 culori round-trip) · validate-content.ts · assert-static.ts · avatar.ts (sharp) · encode-video.sh (ffmpeg) · bundle-budget.ts · predeploy-check.ts · eval.ts (orchestrator) · forbidden-strings.ts
  tests/
    unit/  (Vitest)   e2e/ (Playwright: fixtures.ts, crawler.ts, eval-0xx.spec.ts, work.spec.ts, case-study.spec.ts, tracer.spec.ts) · fixtures/ (invalid-project.fixture.ts, ask-provider mocks)
  evals/  evaluation-plan.md (pointer/copy) · eval-cases.json · results/ · reports/
  content/media/<slug>/ (sources + SOURCES.md) · public/ (avatar/, media/, video/) · docs/ (screenshots/, eval.md, anchors.md, deploy.md, a11y-pass.md, og/)
  next.config.ts · postcss.config.mjs · tsconfig.json · eslint.config.mjs · vitest.config.ts · playwright.config.ts · lighthouserc.mobile.json · lighthouserc.desktop.json · .github/workflows/eval.yml · .env.example · .gitignore · .npmrc
```

### A3. Data model — `data/schema.ts` (full)

zod 4 (`zod@^4`). One file; imported by data modules for types, by `scripts/validate-content.ts` (prebuild) and by Vitest. **Never imported by client components** (types only via `import type`).

```ts
import { z } from 'zod';

/* ── primitives ─────────────────────────────────────────────── */
export const Slug      = z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/);
export const IsoDate   = z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'asOf must be YYYY-MM-DD');
export const YearMonth = z.string().regex(/^\d{4}-\d{2}$/);
export const InternalHref = z.string().regex(/^\/(?!\/)[^\s]*$/);            // "/work/railcite#05-what-i-built"
export const Href      = z.union([InternalHref, z.url().startsWith('https://'), z.string().startsWith('mailto:')]);
export const Tone      = z.enum(['neutral','lavender','sky','mint','blush','peach','butter']);
export const RichText  = z.array(z.string().min(1));                          // paragraphs; inline markdown links only

/* Every public claim points at one of these. `ref` (inventory path/line) is never rendered; `label` is. */
export const SourceRef = z.object({
  id: z.string().min(2),                       // 'TS-README-3'
  label: z.string().min(3),                    // 'TeachSpark README' — the only field the UI shows
  ref: z.string().min(1),                      // 'TS/README.md:3' or a URL (traceability only)
  inventory: z.string().regex(/^§\d+(?:\.\d+)*$/), // CONTENT_INVENTORY section, e.g. '§8.1'
  url: z.url().startsWith('https://').optional(),   // rendered as a link when present
});

export const Media = z.object({
  src: z.string().regex(/^\/(avatar|media|video)\//),
  alt: z.string().min(8),
  width: z.number().int().positive(),
  height: z.number().int().positive(),
  kind: z.enum(['image','video','placeholder']).default('image'),
  caption: z.string().optional(),
  source: z.string().optional(),               // SourceRef.id
});

export const Metric = z.object({
  value: z.string().min(1),                    // keep units/formatting: '17', '37.5 min', '5,760'
  label: z.string().min(2),
  context: z.string().min(12),                 // 'joined the WhatsApp pilot in week 1, test handsets excluded'
  asOf: IsoDate,
  kind: z.enum(['measured','structural','self-reported']),
  source: z.string().min(2),                   // SourceRef.id (must exist in the owning entity's sources)
});

/* ── artifacts (discriminated) ──────────────────────────────── */
const ArtifactBase = { id: z.string().min(2), source: z.string().min(2), caption: z.string().optional() };
export const InsightArtifact    = z.object({ ...ArtifactBase, type: z.literal('insight'),    quote: z.string().min(12), attribution: z.string().min(2) });
export const HypothesisArtifact = z.object({ ...ArtifactBase, type: z.literal('hypothesis'), believe: z.string().min(12), knowWhen: z.string().min(12),
                                             status: z.enum(['validated','partially-validated','invalidated','unmeasured']).default('unmeasured') });
export const MetricArtifact     = z.object({ ...ArtifactBase, type: z.literal('metric'),     metric: Metric });
export const DecisionArtifact   = z.object({ ...ArtifactBase, type: z.literal('decision'),   title: z.string().min(4), chosen: z.string().min(8), rejected: z.array(z.string().min(4)).min(1), reason: z.string().optional() });
export const EvaluationArtifact = z.object({ ...ArtifactBase, type: z.literal('evaluation'), method: z.string().min(8), result: z.string().min(4), limitation: z.string().min(8) });
export const ExperimentArtifact = z.object({ ...ArtifactBase, type: z.literal('experiment'), setup: z.string().min(8), result: z.string().min(4), learning: z.string().min(8) });
export const PrototypeArtifact  = z.object({ ...ArtifactBase, type: z.literal('prototype'),  media: Media });
export const GenericArtifact    = z.object({ ...ArtifactBase, type: z.literal('generic'),    title: z.string().min(3), kind: z.enum(['prd','deck','ledger','doc','link']), href: Href.optional(), note: z.string().optional() });
export const Artifact = z.discriminatedUnion('type', [InsightArtifact, HypothesisArtifact, MetricArtifact, DecisionArtifact, EvaluationArtifact, ExperimentArtifact, PrototypeArtifact, GenericArtifact]);

/* ── chapters (fixed 8, fixed order) ────────────────────────── */
export const CHAPTER_IDS = ['context','problem','discovery','bet','built','evaluation','outcome','learned'] as const;
export const ChapterId = z.enum(CHAPTER_IDS);
export const Chapter = z.object({ id: ChapterId, title: z.string().min(3), body: RichText, artifacts: z.array(Artifact).max(3) });
export const Chapters = z.array(Chapter).length(8).superRefine((chs, ctx) => {
  chs.forEach((c, i) => { if (c.id !== CHAPTER_IDS[i]) ctx.addIssue({ code: 'custom', path: [i, 'id'], message: `chapter ${i} must be '${CHAPTER_IDS[i]}'` }); });
});

/* ── show-the-thinking (exactly 8 or none) ──────────────────── */
export const THINKING_STAGES = ['observation','user-problem','insight','hypothesis','product-decision','prototype','evaluation','outcome'] as const;
export const ThinkingNode  = z.object({ stage: z.enum(THINKING_STAGES), text: z.string().min(20), source: z.string().min(2), href: Href.optional() });
export const ThinkingChain = z.union([
  z.array(ThinkingNode).length(8).superRefine((ns, ctx) => ns.forEach((n, i) => { if (n.stage !== THINKING_STAGES[i]) ctx.addIssue({ code: 'custom', path: [i, 'stage'], message: `node ${i} must be '${THINKING_STAGES[i]}'` }); })),
  z.tuple([]),                                  // thin projects: chain hidden entirely (TKT-21 AC 1)
]);

/* ── project ────────────────────────────────────────────────── */
export const ProjectStatus = z.enum(['live','pilot','prototype','research','archived']);
export const Filter = z.enum(['ai','enterprise','cloud','experiments']);
export const Tag = z.string().min(2).max(18);

export const Project = z.object({
  slug: Slug, name: z.string().min(2), tagline: z.string().min(20),        // tagline = the one-sentence proposition
  category: z.enum(['personal','professional']),
  tags: z.array(Tag).min(1).max(3), filters: z.array(Filter).min(1),
  status: ProjectStatus, statusLabel: z.string().min(3),                   // badge colour from status, badge text from statusLabel ('Live pilot', 'Built, not launched')
  statusAsOf: IsoDate.optional(),                                          // dated live-status check (TKT-22 AC 3)
  featured: z.union([z.literal(1), z.literal(2), z.literal(3)]).optional(),
  gridSize: z.enum(['large','medium','small']),
  icon: z.string().min(2),                                                 // lucide icon name (cards use ClayIcon, no product imagery)
  role: z.string().min(4), dates: z.object({ start: YearMonth, end: YearMonth.optional() }), duration: z.string().min(2),
  links: z.object({
    live: z.url().startsWith('https://').optional(),
    demoVideo: z.object({ src: z.string().regex(/^\/video\/[a-z0-9-]+\.mp4$/), poster: z.string().regex(/^\/video\/[a-z0-9-]+-poster\.webp$/), durationSec: z.number().int().min(5).max(60) }).optional(),
    github: z.url().startsWith('https://github.com/').optional(),
    repoPublic: z.boolean(),
  }),
  hero: z.object({ image: Media.optional(), prototype: Media.optional() }),
  metrics: z.array(Metric),
  overview: z.object({ thirtySecond: RichText.min(1), deepDive: z.boolean() }),
  chapters: Chapters,
  thinking: ThinkingChain,
  learnings: z.array(z.string().min(8)),
  sources: z.array(SourceRef).min(1),
}).superRefine((p, ctx) => {
  const ids = new Set(p.sources.map(s => s.id));
  const need = (id: string, path: (string|number)[]) => { if (!ids.has(id)) ctx.addIssue({ code: 'custom', path, message: `source '${id}' not declared in sources[]` }); };
  p.metrics.forEach((m, i) => need(m.source, ['metrics', i, 'source']));
  p.chapters.forEach((c, ci) => c.artifacts.forEach((a, ai) => { need(a.source, ['chapters', ci, 'artifacts', ai, 'source']); if (a.type === 'metric') need(a.metric.source, ['chapters', ci, 'artifacts', ai, 'metric', 'source']); }));
  p.thinking.forEach((n, i) => need(n.source, ['thinking', i, 'source']));
  if (p.category === 'professional' && (p.links.live || p.links.demoVideo || p.featured)) ctx.addIssue({ code: 'custom', path: ['links'], message: 'professional entries never carry live/demo/featured (Solution-PRD §5)' });
  if (p.links.repoPublic && !p.links.github) ctx.addIssue({ code: 'custom', path: ['links', 'repoPublic'], message: 'repoPublic requires github' });
  if (p.overview.deepDive && p.chapters.filter(c => c.body.length > 0).length < 4) ctx.addIssue({ code: 'custom', path: ['overview', 'deepDive'], message: 'deepDive needs ≥4 non-empty chapters' });
  if (p.featured && p.category !== 'personal') ctx.addIssue({ code: 'custom', path: ['featured'], message: 'only personal builds are featured' });
});

/* ── about ──────────────────────────────────────────────────── */
export const Outcome = z.object({ text: z.string().min(8), kind: z.enum(['measured','self-reported']), source: z.string().min(2) });
export const Experience = z.object({
  id: Slug, company: z.string().min(2), companyNote: z.string().optional(),  // 'via IntraEdge'
  title: z.string().min(2), dates: z.object({ start: YearMonth, end: YearMonth.optional() }),
  context: z.string().min(20), responsibility: z.string().min(20),
  scale: z.union([z.string().min(10), z.literal('not recorded')]),           // MISSING renders 'Scale: not recorded' (TKT-41 AC 1)
  whatChanged: z.string().min(20), outcomes: z.array(Outcome).min(1), sources: z.array(SourceRef).min(1),
});
export const SkillCluster = z.object({ id: Slug, name: z.string().min(3), tone: Tone, items: z.array(z.string().min(2)).min(3).max(6), source: z.string().min(2) });

/* ── thinking (essays) ──────────────────────────────────────── */
export const Essay = z.object({
  slug: Slug, title: z.string().min(6), dek: z.string().min(12), draft: z.boolean(), readingMinutes: z.number().int().min(1).max(20),
  passages: z.array(z.object({ quote: z.string().min(20), source: z.string().min(2) })).min(1),
  framing: z.string().min(40),                                                 // the one clearly-marked DRAFT paragraph
  relatedProject: Slug.optional(), publishedOn: IsoDate.optional(), sources: z.array(SourceRef).min(1),
}).refine(e => e.draft || !!e.publishedOn, { message: 'non-draft essays need publishedOn', path: ['publishedOn'] });

/* ── ask ────────────────────────────────────────────────────── */
export const KnowledgeEntry = z.object({
  id: Slug, prompt: z.string().min(8),
  aliases: z.array(z.string().min(4)).default([]),                            // whole-prompt paraphrases
  keywords: z.array(z.object({ term: z.string().min(2), weight: z.number().min(0.5).max(3) })).min(2),  // canonical terms (after synonyms)
  answer: z.string().min(40).max(600),                                        // ≤3 sentences, verbatim from CONTENT_INVENTORY §9 / VERIFIED rows
  evidence: z.array(z.object({ label: z.string().min(3), href: Href })).min(2).max(3),
  sources: z.array(SourceRef).min(1), draft: z.boolean(),
  surface: z.array(z.enum(['home','panel'])).min(1),                           // home shows 5, panel shows 6 (PB3: 11 total, disjoint)
});

/* ── how-i-think ────────────────────────────────────────────── */
export const ThinkingStageDef = z.object({
  id: z.enum(['problem','insight','bet','build','evaluate','impact']), label: z.string().min(3), principle: z.string().min(12), tone: Tone,
  example: z.object({ quote: z.string().min(20), attribution: z.string().min(2), project: Slug, href: z.string().regex(/^\/work\/[a-z0-9-]+(?:#\d{2}-[a-z-]+)?$/) }),
  source: z.string().min(2),
});

export type Project = z.infer<typeof Project>; export type Metric = z.infer<typeof Metric>; export type Artifact = z.infer<typeof Artifact>;
export type Experience = z.infer<typeof Experience>; export type Essay = z.infer<typeof Essay>; export type KnowledgeEntry = z.infer<typeof KnowledgeEntry>;
export type ThinkingStageDef = z.infer<typeof ThinkingStageDef>; export type SkillCluster = z.infer<typeof SkillCluster>;
```

**Content-integrity rules that fail the build (EVAL-013)** — enforced by `scripts/validate-content.ts` (runs in `prebuild` and as `tests/unit/schema.test.ts`):
1. Every entity in `data/*.ts` parses against its schema; a failure prints `<entity>.<id> → <zod path>: <message>` and exits 1.
2. Every `Metric` has `value/label/context/asOf/kind/source`; every `source` id resolves inside the owning entity's `sources[]` (superRefine above).
3. Every `Project` has ≥1 `SourceRef`; professional entries never carry live/demo/featured; `tags ≤ 3`; chapters are the fixed 8; thinking is exactly 8 or empty.
4. Cross-entity checks in `validate-content.ts`: `KnowledgeEntry.evidence[].href` and `ThinkingStageDef.example.href` internal links must match a known route or `route#anchor` (routes from `lib/anchors.ts` + slugs; essays from `writing.ts`) — a dangling internal link is a build failure, not a crawler finding later; exactly 3 `featured` personal projects with distinct ranks; exactly one `gridSize:'large'`; `surface` counts: 5 home + 6 panel; `data/knowledge.ts` answers must not contain digits that do not appear in some `Metric.value` or `SourceRef`-backed text? — *no*: too clever; numbers in answers are covered by the reviewer trace table (tickets "Truth" DoD). Keep the mechanical gate to structure + links.
5. **Forbidden strings** (`scripts/forbidden-strings.ts`, runs in Vitest and `pnpm eval`): scans `data/**`, `content/**/*.md`, `app/**`, `components/**`, `public/**/*.{txt,json,svg}` and, when present, `.next/server/app/**/*.html` + `.next/static/**/*.js` for: `/\bPMP\b/`, `/SAFe (Agilist|certif)/i`, DOB patterns (`/\b(0?[1-9]|[12]\d|3[01])[\/\-.](0?[1-9]|1[0-2])[\/\-.](19|20)\d{2}\b/`, `/\bDate of Birth\b/i`), phone patterns (`/\+91[\s-]?\d{5}[\s-]?\d{5}/`, `/\b\d{10}\b/` inside `data/**` and `content/**` only), the TeachSpark sandbox join code (value read from git-ignored `tests/forbidden.local.json`; test skips with a loud `SKIP` line if the file is missing so CI never silently passes), `/AI Product Manager/` (any case; S8), `.env` key names (`ANTHROPIC_API_KEY|SUPABASE_|TWILIO_|VOYAGE_`), local filesystem paths (`/\/Volumes\/E Drive\//`) in anything under `app/`, `components/`, `public/`, `.next/` (source `ref` fields may hold them inside `data/**` but they must never reach the bundle — the scan of `.next/static` catches leakage), and `public/resume.pdf` presence while `resumeAvailable === false` (PB5).
6. The **deliberate failing fixture** `tests/fixtures/invalid-project.fixture.ts` (metric without `asOf`, project without `sources`, `statusLabel: 'AI Product Manager'`) is asserted to throw in Vitest **and** is wired once into a real `pnpm build` (`CONTENT_FIXTURE=invalid pnpm build`) whose non-zero exit + output is saved to `evals/results/content-gate-proof.txt` (S03.08).

### A4. Ask adapter — `lib/ask/*` (decision TP3)

```ts
// lib/ask/adapter.ts
export type Evidence = { label: string; href: string };
export type Answer =
  | { kind: 'answer'; text: string; evidence: Evidence[]; matched: string[]; score: number }
  | { kind: 'empty';  text: string; evidence: Evidence[]; matched: []; suggestions: string[] };
export type AskContext = { route?: string; surface?: 'home' | 'panel' };
export interface AnswerProvider { readonly name: string; ask(query: string, ctx?: AskContext): Promise<Answer>; }
export class AskError extends Error { constructor(msg: string, readonly cause?: unknown) { super(msg); } }
```

**`LocalKnowledgeProvider`** (`lib/ask/local-provider.ts`, pure, no I/O, deterministic):
1. **Normalise** (`lib/ask/normalise.ts`): NFKC → lowercase → strip punctuation except `+`/`#` inside tokens → collapse whitespace → tokenise on spaces → drop stopwords (`a an the of to in on for me my your you what which how show tell about do does did is are can could would please`) → **canonicalise** each token via the synonym table → drop duplicates, keep order. Also compute bigrams of canonical tokens.
2. **Synonym table** (`lib/ask/synonyms.ts`, one canonical term per row; extend only with evidence): `built ← build|builds|building|built|shipped|ship|made|created|projects|project|products|product|work|portfolio` · `ai ← ai|genai|llm|llms|ml|gpt|claude|rag|agent|agents|agentic` · `enterprise ← enterprise|corporate|amex|americanexpress|american|express|quantiphi|godrej|shellkode|company|employer|job` · `technical ← technical|architecture|stack|engineering|code|system|backend|pipeline|hardest|complex|deepest` · `impact ← impact|results|outcomes|metrics|numbers|achieved|achievements|kpi|traction` · `discovery ← discovery|research|interviews|interview|users|customer|problem|validate|validation|assumptions` · `skills ← skills|strengths|strength|good|best|competencies|capabilities|superpower` · `evaluate ← evaluate|evaluation|evals|eval|test|tests|testing|measure|quality|qa` · `learned ← learned|learn|lesson|lessons|failed|failure|wrong|mistake|mistakes|assumption|killed|kill|pivot` · `research ← patent|paper|papers|publication|published|phd|academic|biosensor|research-background` · `pm ← pm|product-manager|manager|management|why|makes|qualifies` · `experience ← experience|career|background|history|timeline|years|worked` · `railcite ← railcite|railway|circulars|citation|citations` · `teachspark ← teachspark|teachers|teacher|whatsapp|worksheets` · `velora ← velora|nuptis|vendor|vendors|onboarding|apparel|wedding|weddings|sourcing`. Multi-word inputs like "american express", "product manager", "research background" are joined before tokenising via a small phrase list.
3. **Score** each `KnowledgeEntry`: if the normalised query equals the normalised `prompt` or any `alias` → `score = 1`. Otherwise `score = Σ weight(matched keyword) / Σ weight(all entry keywords)` + `0.1 × (matched bigrams)` capped at 1. Entry keywords are the canonical terms with weights (e.g. the "most technical project" entry: `technical:2, built:1, railcite:1.5`).
4. **Decide**: pick max score; ties broken by array order in `knowledge.ts` (stable, documented). `score ≥ 0.34` → `{kind:'answer', text: entry.answer, evidence: entry.evidence, matched: [entry.id], score}` — `text` is **the entry's answer string, unmodified** (the no-fabrication invariant; Vitest asserts byte-equality). Below threshold, or query with zero canonical tokens → `{kind:'empty', text: FALLBACK, suggestions: 3 prompts of the current surface not equal to the query}`. `FALLBACK` = CONTENT_INVENTORY §1.3: "I only answer from the sourced facts on this site — try one of the prompts, or email me."
5. **Latency**: 11 entries × ≤12 keywords → sub-millisecond; `ask()` still returns a Promise and the UI still shows the ≤150 ms skeleton (Design.md).
6. **PII probes** ("phone number", "salary", "address"): none of these tokens exist in any synonym row or entry keyword → score 0 → empty. Vitest fixes the 5 off-topic probes from TKT-09 AC 5.

**`RagProvider` stub** (`lib/ask/rag-provider.ts`, not wired): `class RagProvider implements AnswerProvider { constructor(private endpoint = process.env.RAG_ENDPOINT) {} async ask(q, ctx) { if (!this.endpoint) throw new AskError('RAG_ENDPOINT not configured'); const r = await fetch(this.endpoint, { method:'POST', body: JSON.stringify({ q, ctx }) }); if (!r.ok) throw new AskError(`RAG ${r.status}`); return Answer.parse(await r.json()); } }` with a zod `Answer` schema so a future backend cannot return a shape the UI does not understand. `AskProvider` context chooses the provider once: `const provider = useMemo(() => new LocalKnowledgeProvider(knowledge), [])`; swapping to RAG is a one-line change plus the EV1 "add AI evals" rule.

**`useAsk(provider)`** (in `components/ai/AskProvider.tsx`): state machine `idle → loading → answer | empty | error`; `submit(q)` sets `loading`, awaits `Promise.all([provider.ask(q), delay(150)])` (skeleton floor), catches into `error` with `retry()`; `reset()` → `idle`. Shared by `AskPortfolio` and `AskPanel`; the panel is `next/dynamic` imported on first open (EVAL-005).

### A5. View Transitions strategy (decision TP2)

- `next.config.ts`: `experimental: { viewTransition: true }` (Next 16 wires `document.startViewTransition` around App Router navigations when present).
- `components/interactions/ViewTransitionLink.tsx`: `next/link` wrapped so the card's `ClayFrame`/icon carry `<ViewTransition name={`project-${slug}`}>` / `name={`icon-${slug}`}` (React 19.2 export — **verify the export name at S06.01**: `node -e "console.log(Object.keys(require('react')).filter(k=>/ViewTransition/i.test(k)))"`; use `unstable_ViewTransition as ViewTransition` if that is what 19.2 ships, behind a single re-export in `lib/motion.ts` so a rename touches one line). The case-study header media declares the same names. Default timing from Design.md §4: `::view-transition-group(*) { animation-duration: 450ms; animation-timing-function: cubic-bezier(.77,0,.175,1) }` in `globals.css`.
- **Fallback (EVAL-015):** browsers without `document.startViewTransition` (Firefox stable, older Safari) simply navigate; nothing in layout depends on the transition. Playwright's `noViewTransitions` fixture sets `document.startViewTransition = undefined` via `addInitScript` and asserts the identical end state (URL, `h1`, header media present).
- **Reduced motion:** `@media (prefers-reduced-motion: reduce) { ::view-transition-group(*), ::view-transition-old(*), ::view-transition-new(*) { animation: none !important } }` → plain navigation.
- Not used anywhere else (no page-level cross-fades) — keeps the first-paint budget and avoids Safari's VT+scroll quirks.

### A6. Motion architecture (decision TP6)

- **Two mechanisms only**, per Design.md §4: CSS transitions (`transform`/`opacity`/`clip-path`) for hover/press/reveal/compaction/node-reveal/annotation wash; `motion` 13 (`motion/react`) only where a spring or FLIP layout animation is specified — cursor parallax (`useSpring` 120/20), filter change (layout 260/28 + `AnimatePresence`), Ask inline expand (layout 210/26), StoryCard expand (layout 240/30), NavPill slide (layout).
- **Bundle:** `LazyMotion features={domAnimation} strict` at the root of each client subtree that uses `m.*`; never import the `motion` default proxy (~20 kB extra). `useSpring`/`useMotionValue` import from `motion/react` directly (tree-shaken).
- **`lib/motion.ts`** exports: `springs = { parallax:{stiffness:120,damping:20,mass:1}, filter:{stiffness:260,damping:28}, askExpand:{stiffness:210,damping:26}, story:{stiffness:240,damping:30} }`, `durations = { hover:200, press:90, lift:180, reveal:500, header:250, panel:320, node:220 }`, `easings = { reveal:'cubic-bezier(.2,.7,.2,1)', hover:'cubic-bezier(0.23,1,0.32,1)', panel:'cubic-bezier(0.32,0.72,0,1)', vt:'cubic-bezier(.77,0,.175,1)' }`, `useReducedMotionSafe()` (wraps `motion`'s `useReducedMotion`, SSR-safe: returns `true` until mounted so the first client frame never animates), `usePointerFine()` (`matchMedia('(hover:hover) and (pointer:fine)')`), and the `ViewTransition` re-export.
- **CSS side:** `globals.css` declares `@media (prefers-reduced-motion: reduce) { *, ::before, ::after { transition-duration: 1ms !important; animation-duration: 1ms !important; } }` plus explicit per-component opt-outs where Design.md keeps opacity (`.reveal { transition-property: opacity }`). Every CSS transition lists its properties explicitly (`transition: transform 200ms var(--ease-hover), box-shadow 200ms …`), never `all`.
- **Client boundaries** exactly as A1; `Reveal` is a 40-line IO wrapper (no `motion` dependency) so section reveals cost nothing extra.

### A7. Media pipeline (decision TP4)

**Avatar** (`scripts/avatar.ts`, `sharp`): input `content/media/avatar/avatar-cutout.png` (1856×2304, alpha — already background-removed; `avatar-source.png` stays untouched). Outputs: `public/avatar/avatar.webp` (long edge 1800, `webp({quality:82, alphaQuality:90, effort:6})`, target ≤300 kB — if over, step quality 78/74 and log), `public/avatar/avatar@2x.webp` (original 2304 long edge, quality 80), `public/avatar/avatar-poster.webp` (flattened on `#FAF9FF`, 1200×1500, for OG/fallback), and `public/avatar/avatar-blur.txt` (base64 16px blurDataURL). Script prints sizes; the gate checks them. Halo check is visual (S02.04): 200 % zoom at 1440 on the hair/beard edge over the sky/lavender frame.

**Video** (`scripts/encode-video.sh <in> <slug> <start> <end>`, ffmpeg 9.0.1, scratch in `/Volumes/E Drive/Dev/.scratch/portfolio-clay/`):
```
ffmpeg -y -ss $START -to $END -i "$IN" -an -vf "scale='min(1280,iw)':-2,fps=30,format=yuv420p" \
  -c:v libx264 -profile:v high -level 4.0 -preset slow -crf 26 -maxrate 1000k -bufsize 2000k -movflags +faststart \
  "public/video/$SLUG.mp4"
ffmpeg -y -ss 1 -i "public/video/$SLUG.mp4" -frames:v 1 -vf "scale=1280:-2" -c:v libwebp -quality 80 "public/video/$SLUG-poster.webp"
stat -f %z public/video/$SLUG.mp4   # must be ≤ 4194304; poster ≤ 122880
```
A 30 s clip at ≤1000 kbps is ≤3.75 MB by construction; the script exits 1 above 4 MB (drop to `-maxrate 850k` or shorten the trim). Audio is dropped (players are `muted`). No AV1 secondary in v1 (adds encode time, no Lighthouse benefit with `preload="none"`). Posters are the LCP candidate where above the fold.

**`next.config.ts` images:** `images: { formats: ['image/avif','image/webp'], deviceSizes: [390,768,1024,1440,1920], imageSizes: [56,120,180,280,360,480,520], minimumCacheTTL: 31536000 }`. All `next/image` uses declare `width/height` (from `Media`) or `fill` + `sizes`; hero avatar gets `priority` + `placeholder="blur"`; posters/screenshots `loading="lazy"`.

**Product screenshots:** sources in `content/media/<slug>/` with a `SOURCES.md` (URL/commit, date, viewport); web copies produced by `scripts/optimise-images.ts` (sharp → WebP ≤200 kB, max 1600 px) into `public/media/<slug>/`. Nothing under `Game/neogeo/`, no corporate screenshots.

### A8. SEO / OG

- `lib/seo.ts`: `siteUrl()` (A1 env chain), `buildMetadata({ title, description, path, ogFamily, image? }): Metadata` → `<title>` template `%s · Tushar Pathak`, description, `alternates.canonical`, `openGraph {type:'website'|'article', url, title, description, images:[{url,width:1200,height:630,alt}]}`, `twitter {card:'summary_large_image', title, description, images}`. `metadataBase = new URL(siteUrl())` in `app/layout.tsx` guarantees absolute URLs.
- `lib/og.tsx`: one `ImageResponse` template `OgCard({ eyebrow, title, subtitle, tone, badge?, avatar?: boolean })` — clay palette from a hard-coded hex map (ImageResponse cannot read CSS variables), Manrope 700/800 loaded once from `next/font`'s font files via `fs.readFile` (or `fetch(new URL('../public/fonts/Manrope-Bold.ttf', import.meta.url))`), 1200×630, contrast ≥4.5:1 (ink on bg/lavender). Families: home (avatar poster + headline), work, case study (`name + tagline + statusLabel`, per slug via `generateStaticParams` re-use), about, thinking (+ essay), playground, contact. Each `opengraph-image.tsx` exports `size`, `contentType='image/png'`, `alt`. Budget ≤300 kB each (S06.05 gate measures).
- `app/sitemap.ts`: static routes + 11 slugs + 5 essay slugs, `lastModified` = build date; excludes `/dev/*`. `app/robots.ts`: allow all, `sitemap: ${siteUrl()}/sitemap.xml`, `disallow: ['/dev/']`.
- Tag test (`tests/unit/seo.test.ts` + `tests/e2e/eval-017.spec.ts`): renders each family and asserts the tag set from TKT-06 AC 1 with absolute `https://` URLs.

### A9. Tooling (decision TP5)

`package.json` scripts (pnpm 11.25, Node 26.7; `.npmrc`: `engine-strict=true`, `auto-install-peers=true`):

| script | command | purpose |
|---|---|---|
| `dev` | `next dev` | local |
| `prebuild` | `tsx scripts/validate-content.ts` | EVAL-013 gate before every build |
| `build` | `next build && tsx scripts/assert-static.ts` | SSG + static guarantee (TP1) |
| `start` | `next start -p 3000` | used by Playwright/LHCI `webServer` |
| `typecheck` | `tsc --noEmit` | strict TS |
| `lint` | `eslint .` (flat config, `eslint-config-next`, `eslint-plugin-jsx-a11y` strict) | |
| `test` | `vitest run` | unit + schema + provider + seo + forbidden-strings |
| `test:e2e` | `playwright test` | all specs, 4 projects |
| `tokens:check` | `tsx scripts/tokens-check.ts` | D2 hex↔oklch round trip |
| `media:avatar` / `media:video` | `tsx scripts/avatar.ts` / `bash scripts/encode-video.sh` | A7 |
| `eval` | `tsx scripts/eval.ts [--only EVAL-0xx,…] [--baseline <file>] [--base-url <url>] [--label <name>]` | the single command (A16) |
| `predeploy` | `tsx scripts/predeploy-check.ts` | PB4/PB5 guards (TKT-50) |

- **Vitest 4** (`vitest.config.ts`): two projects — `node` (schema, providers, format, seo builders, forbidden strings, predeploy-check, bundle-budget) and `jsdom` (component tests with Testing Library: ArtifactRenderer, MetricCard guards, useAsk state machine). Coverage not gated (evidence comes from EVAL, not %).
- **Playwright 1.5x** (`playwright.config.ts`): Chromium only; projects `w390` (390×844, `isMobile`, `hasTouch`), `w768`, `w1024`, `w1440`; `webServer: { command: 'pnpm start', url: 'http://127.0.0.1:3000', reuseExistingServer: true }` (tests run against the production build, matching Lighthouse); `outputDir: 'test-results'`; `use.reducedMotion` default `'no-preference'`; fixtures `reducedMotion` (new context with `reducedMotion:'reduce'`), `noViewTransitions` (`addInitScript`), `axe` (`@axe-core/playwright`, tags `wcag2a wcag2aa wcag21aa`, assert no `critical|serious`), `noOverflow` (`document.documentElement.scrollWidth <= clientWidth` at each viewport), `minTargets` (every visible `a,button,[role=button],[role=tab],input` has bounding box ≥44×44 or a documented allowlist entry). **Env:** `PLAYWRIGHT_BROWSERS_PATH=/Volumes/E Drive/Dev/.cache/ms-playwright` and `TMPDIR=/Volumes/E Drive/Dev/.scratch/tmp` exported by `scripts/env.sh` (sourced by the `test:e2e`/`eval` scripts through `dotenv -e .env.tooling` — a committed, secret-free file) so browsers and traces never touch the internal disk.
- **Lighthouse CI** (`@lhci/cli`): `lighthouserc.mobile.json` (default mobile emulation) and `lighthouserc.desktop.json` (`settings.preset:'desktop'`); both: `collect.url = [/, /work, /work/teachspark, /about]` (base from `--base-url`), `numberOfRuns: 3`, `startServerCommand: 'pnpm start'`; `assert.assertions`: `categories:performance ≥0.90`, `categories:accessibility ≥0.95`, `categories:best-practices ≥0.95`, `categories:seo ≥0.95`, `largest-contentful-paint maxNumericValue 2500`, `cumulative-layout-shift maxNumericValue 0.05` — all with `aggregationMethod:'median'` (EV2). `upload.target:'filesystem'` into `.lighthouseci/` (git-ignored) so `scripts/eval.ts` can read the median JSON.
- **Bundle budget** (`scripts/bundle-budget.ts`): reads `.next/app-build-manifest.json` + `.next/build-manifest.json`, sums gzip sizes of the first-load JS for `/` (via `zlib.gzipSync` over the files) and asserts ≤ 180 kB (EVAL-005).
- **Lint/typecheck:** TS `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`; ESLint flat config with `next/core-web-vitals`, `jsx-a11y/strict`, `@typescript-eslint/recommended-type-checked`; Prettier not added (match existing style rule — repo starts with the Next defaults).
- **Dev routes:** `lib/dev-only.ts` → `if (process.env.NODE_ENV === 'production' && !process.env.ALLOW_DEV_ROUTES) notFound()` at the top of every `app/dev/*/page.tsx`; sitemap excludes them; Playwright runs them with `ALLOW_DEV_ROUTES=1 pnpm start` in the QA job only.

### A10. Git strategy

- `git init` in `Portfolio-clay/` at S01.01 (the folder is not a repo today); default branch `main`; first commit = the Stage 2–6 artifacts as they stand (`docs: stage 2–6 planning artifacts`). Remote: private GitHub repo `007U5H4R/portfolio-clay` (Tushar creates or approves; needed for Vercel git integration at TKT-50 — until then local-only is fine).
- Branch per milestone: `feat/m001-tracer`, `feat/m002-foundations`, … `feat/m007-release`; PR into `main` at each phase's QA gate (squash-merge, one logical change per commit inside the branch). Execution works in a **worktree** per milestone branch: `/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay-wt/m00X/` (sibling directory, never inside the repo), created with `git worktree add`; the ledger (`docs/ledger.md`, git-ignored is *not* acceptable — it is committed) tracks task status.
- Media tickets (TKT-22…27) commit binaries ≤4 MB each directly (no LFS; total budget ≤ 40 MB of video in-repo). Sources >4 MB never enter the repo (they stay under `content/media/**` only when ≤4 MB; otherwise referenced by absolute path in `SOURCES.md`).
- `.gitignore`: `node_modules/ .next/ out/ test-results/ playwright-report/ .lighthouseci/ .vitest/ coverage/ .env .env.*.local public/resume.pdf tests/forbidden.local.json .DS_Store content/media/**/*.mov content/media/**/raw/`.
- Push/PR hygiene: `git diff main..HEAD --stat` reviewed before every PR (global rule); worktree contamination check is part of the phase QA gate.

### A11. Deployment

- **Vercel project `portfolio-clay`** (new; never the cinematic site's project — S1). Created at TKT-50 via the Vercel MCP plugin (`create_git_project` on the GitHub repo) or `npx vercel link` (CLI not installed globally; `npx vercel@latest` works). Framework preset Next.js, Node 24.x runtime setting irrelevant (no functions), build `pnpm build`, install `pnpm install --frozen-lockfile`. Production branch `main`; every PR gets a preview.
- **Env:** `NEXT_PUBLIC_SITE_URL` set on production only (`https://<domain>`); previews fall back to `VERCEL_URL` (A1) so OG validates on previews. `RAG_ENDPOINT` reserved (documented, unset). No secrets.
- **Security headers (decision TP9)** in `next.config.ts` `headers()` for `/(.*)`: `Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self'; font-src 'self'; connect-src 'self' https://vitals.vercel-insights.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests` (`'unsafe-inline'` for scripts is required by Next's static-page inline bootstrap without a nonce — a nonce needs per-request rendering, which TP1 excludes; there are zero third-party scripts, so the residual risk is XSS-only and there is no user input reaching the DOM except the Ask field, which renders answers from data, never the query as HTML), `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()`, `X-Frame-Options: DENY`. Verified by `curl -sI` in TKT-50 and by Stage 10.
- **Pre-deploy guard** (`scripts/predeploy-check.ts`, runs as the first step of `build` on Vercel via `vercel-build`/`build` script — i.e. `prebuild` chain): fails when `public/resume.pdf` exists and `tests/unit/resume-pii.test.ts` would fail (runs `pdftotext` if available, else fails closed with "cannot verify PDF"); when `resumeAvailable === true` without a passing PDF; when any of `public/video/{teachspark,railcite,velora}.mp4` is missing or >4 MB **only when `VERCEL_ENV === 'production'`** (PB4: previews may ship without them); when forbidden strings hit. Each failure mode has a Vitest case (TKT-50 AC 8).
- **Analytics:** `@vercel/analytics` + `@vercel/speed-insights` components in `layout.tsx` (basic, cookie-less); nothing else.
- **Domain:** Tushar's new domain attached at TKT-53; `NEXT_PUBLIC_SITE_URL` updated; OG re-validated.

### A12. Observability ("how would we know at 3 AM?")

- **Build-time schema failures are the primary guard** — a bad claim never reaches production because `prebuild` fails the Vercel build and the previous deployment stays live.
- **Vercel Analytics + Speed Insights** dashboards (traffic, Web Vitals per route); Vercel deployment logs/alerts for build failures; 404 volume visible in Analytics "paths" (an unexpected 404 spike = broken internal link).
- **Custom 404/500:** `not-found.tsx` (TKT-46) and `app/error.tsx` (client error boundary with "Something broke — email me" + link home; no stack traces) + `app/global-error.tsx`.
- **Eval evidence trail:** `evals/results/eval-run-*.json` per phase + `eval-run-v1.0.0.json` on production; proposed weekly GitHub Actions cron running `pnpm eval --base-url https://<domain> --only EVAL-002,EVAL-004,EVAL-011,EVAL-016` (dead links, Lighthouse drift, headers) — enabled or explicitly deferred at TKT-53.
- **Client errors:** none of the Ask/DemoVideo/CopyButton failure paths are silent — each has a visible error state (Design.md) and a `console.warn` with a stable prefix (`[ask]`, `[video]`, `[copy]`) so Playwright's console capture surfaces them in the QA runs.

### A13. Failure modes & error handling

| Failure | Where | Behaviour | Guard |
|---|---|---|---|
| Unsourced metric / missing asOf / dangling anchor | build | `prebuild` fails, deploy aborted | A3 rules 1–4, EVAL-013 |
| Forbidden string / PII in data or bundle | build + eval | Vitest + eval FAIL; predeploy blocks | A3 rule 5, EVAL-016 |
| `startViewTransition` absent (Firefox/old Safari) | runtime | plain navigation, identical end state | A5, EVAL-015 |
| JS disabled | runtime | all content in HTML; Ask/filters degrade to static (prompts visible, filters show all); `<noscript>` note on Ask | EVAL-015 |
| Video 404 / network error | runtime | `DemoVideo` `error` → "View live →" or "Demo coming" | TKT-18, EVAL-014 |
| Missing video in data | build/runtime | `no-video` state (never a broken `<video>`) | schema optional + TKT-18 |
| Missing hero image | runtime | labelled placeholder `Media.kind:'placeholder'` (never a broken `<img>`) | TKT-19 |
| Ask provider throws | runtime | `error` state (ink on blush + retry); query never echoed as HTML | A4 `useAsk` |
| Clipboard API blocked | runtime | `CopyButton` `error` → selectable text fallback | TKT-14/45 |
| Featured video missing at production deploy | deploy | predeploy fails (PB4) | A11 |
| Resume PDF with PII present | deploy | predeploy fails; `resumeAvailable:false` placeholder | PB5 |
| Lighthouse flake | eval | 3-run median; rerun once before failing the gate; never lower thresholds | EV2 |
| Route accidentally dynamic | build | `assert-static.ts` fails | TP1 |
| Dev routes in production | runtime | `notFound()` + sitemap exclusion | A9 |

### A14. Technical risks & mitigations

| Risk | Likelihood | Mitigation (owner step) |
|---|---|---|
| React 19.2 `ViewTransition` export/API differs from assumption | medium | S06.01 verifies export name first; single re-export in `lib/motion.ts`; fallback = `next/link` + CSS-only (no shared element) with an `EXE-n` note |
| Clay shadows (multi-layer box-shadow on many cards) hurt paint/LCP on `/work` | medium | cards are static (no `will-change`); hover lifts via `transform`; measured at M-004 gate (EVAL-004 on `/work`); fallback = single-layer shadow on `small` cards |
| Home JS > 180 kB gz with `motion` + AskPanel | medium | `LazyMotion`, dynamic `AskPanel`, CSS-first motion; bundle-budget runs from TKT-01 baseline; first lever documented in TKT-14 |
| `ImageResponse` fonts / sizes under SSG | low | S06.03 proves one OG image at build; font loaded from a local TTF copy in `assets/fonts/` |
| `useSearchParams` CSR bailout hides the grid from static HTML | medium | grid rendered server-side with all cards; only the *filter application* is client (TP7); Suspense boundary wraps `FilterTabs` not the cards |
| Focus trap / `inert` on AskPanel bottom sheet + iOS Safari scroll lock | medium | native `<dialog>` where possible; `inert` on `#main` + `overflow:hidden` on `html`; tested in Playwright `w390` with `hasTouch` |
| Playwright/LHCI writing to the internal disk | high (scar) | `.env.tooling` + `scripts/env.sh` set `PLAYWRIGHT_BROWSERS_PATH`, `TMPDIR` on E Drive; S01.02 verifies with `df` before/after `pnpm exec playwright install chromium` |
| pdftotext absent on Vercel build image | high | predeploy PII test runs locally + CI (macOS/Linux with poppler); on Vercel it fails **closed** only when a PDF is present and cannot be verified — so the PDF is committed only after the local test passes (TKT-08) |
| Content tickets drift into invention | medium | schema + anchors gate + reviewer trace table (Truth DoD); reviewers get the pack section, not the whole inventory |
| TeachSpark Railway app down when recording | medium | TKT-22 re-verifies first; fallback = re-encode the 08-23 mp4 + status copy "pilot concluded" |

### A15. Rollback

- **Production:** Vercel "Instant Rollback" to the previous production deployment (one click / `vercel rollback <deployment>`); documented in `docs/deploy.md` (TKT-50 AC 7) and exercised once at TKT-53 AC 5. Because every route is static, rollback is atomic and has no data migration surface.
- **Content:** a bad claim cannot deploy (A12); if one is discovered post-release, fix the data, `pnpm eval --only EVAL-013`, push → new deployment; rollback in the meantime if severity warrants.
- **Code:** revert PR on `main`; worktrees mean `main` is never edited in place.

### A16. Evaluation wiring (workflow 6.5)

**Runner.** `pnpm eval` = `scripts/eval.ts` (tsx). It (1) reads `evals/eval-cases.json`; (2) builds if `.next` is stale (or `--skip-build`); (3) starts `pnpm start` on 127.0.0.1:3000 unless `--base-url` is given; (4) runs the layers, mapping outputs to EVAL ids:

| Layer | Command | Cases |
|---|---|---|
| Vitest | `vitest run --reporter=json --outputFile .eval/vitest.json` | EVAL-012 (provider suite), EVAL-013 (schema + forbidden strings + resume-pii), EVAL-017-tags (seo unit) |
| Playwright | `playwright test --reporter=json` (`--grep @EVAL-0xx` when `--only`) | EVAL-002, 006, 007, 008, 010, 011, 014, 015, 017 (tags on served HTML) |
| Lighthouse CI | `lhci autorun --config lighthouserc.mobile.json` then `.desktop.json` | EVAL-004 (median scores per route × form factor), EVAL-005 (LCP/CLS from LHCI + JS from `bundle-budget.ts`) |
| Security | `pnpm audit --json --audit-level high`, `forbidden-strings.ts --bundle`, header check (`--base-url` only) | EVAL-016 |
| Manual slots | recorded, not executed: status `MANUAL` with a pointer to the evidence file (`evals/results/gate-tracer.md`, `docs/screenshots/home/`, `docs/og/`, `test-cases.md` traceability) | EVAL-001, 003, 009, 017 (inspectors) |

(5) writes `evals/results/eval-run-<pkgVersion>-<shortSha>.json` (or `--label baseline-v1` → `baseline-v1.json`); **never overwrites** an existing file (appends `-2`, `-3`); (6) compares against `--baseline` (default `evals/results/baseline-v1.json` when present): a case that was PASS and is now FAIL, or a Lighthouse median that dropped >3 points, or JS that grew >10 kB, is a **regression**; (7) exits non-zero on any Critical FAIL or regression (Critical per `eval-cases.json`).

**Result schema (v1)** — every field generated from real output:
```json
{
  "schemaVersion": 1, "runId": "eval-run-0.1.0-a1b2c3d", "label": "baseline-v1",
  "provenance": { "version": "0.1.0", "commit": "a1b2c3d…", "branch": "feat/m001-tracer", "dirty": false,
                  "node": "v26.7.0", "pnpm": "11.25.0", "next": "16.3.5", "os": "darwin 25.6.0", "timestamp": "2026-09-16T10:00:00Z",
                  "baseUrl": "http://127.0.0.1:3000", "evalCasesSha256": "…",
                  "config": { "viewports": [390,768,1024,1440], "lighthouseRuns": 3, "thresholds": { "lighthouse": [90,95,95,95], "lcpMs": 2500, "cls": 0.05, "jsKb": 180, "axe": "0 critical/serious" } } },
  "totals": { "cases": 17, "passed": 0, "failed": 0, "skipped": 0, "manual": 0 },
  "cases": [ { "id": "EVAL-004", "status": "PASS|FAIL|SKIP|MANUAL", "priority": "critical", "measured": { "/": { "mobile": [92,98,100,100], "desktop": [99,98,100,100] } }, "threshold": [90,95,95,95], "details": "", "artifacts": [".lighthouseci/…"] } ],
  "criticalFailures": [], "regressions": [], "improvements": [], "runtimeMs": 0, "baseline": "evals/results/baseline-v1.json"
}
```
**Baseline:** captured on the tracer bullet (S07.06) as `baseline-v1.json` — informational for Lighthouse (thresholds enforced from TKT-07), gating for axe/overflow. **Regression triggers** (`pnpm eval` mandatory before a ticket is "done"): any change to `data/schema.ts` or `data/*`; navigation/header/menu; FilterTabs/grid; Ask (data, provider, UI); motion tokens or `lib/motion.ts`; clay tokens/tiers; `next.config`/headers; media additions on the four Lighthouse routes; every phase QA gate; TKT-52 record run; TKT-53 production run. **CI:** `.github/workflows/eval.yml` runs `pnpm typecheck && pnpm lint && pnpm test && pnpm eval` on PR (Ubuntu, Playwright's Chromium, LHCI in headless), uploads `evals/results/*.json` + `playwright-report` as artifacts; results from CI are labelled `ci-<sha>` and are evidence, not the phase record (the phase record is run locally against the production build or the preview URL).

`evals/evaluation-plan.md` is a one-line pointer to the root `evaluation-plan.md` (one fact, one place); `evals/eval-cases.json` is authoritative for ids/priorities/automation flags and is what the runner reads.

---

## B. Per-ticket atomic plans

### B.0 Dispatch rules for every step
- One fresh implementer subagent per **task** (TSK) or per ticket when a ticket has no tasks; steps inside are executed in order; the brief is a file (`docs/briefs/<ticket>.md` generated from this section), the report is a file (`docs/reports/<ticket>.md`). Model tier per ticket is stated in its header.
- Before the first step of any ticket: `git status` clean in the worktree, `pnpm install --frozen-lockfile`, `pnpm typecheck && pnpm lint && pnpm test` green (baseline sanity). After the last step: the same trio + `pnpm build` + the ticket's `pnpm eval` regression + `git diff --stat` reviewed against the ticket's *Files*.
- Copy is **always** taken from the CONTENT_INVENTORY row named in the ticket — implementers never paraphrase; if a row is DRAFT it is rendered and flagged in the report.

---

### M-001 · Tracer bullet & visual direction approved

#### TKT-01 · Tracer bullet — *most-capable tier for TSK-01/05/06/07 (tokens, hero, VT, runner); standard tier for TSK-02/03/04*

**TSK-01 · Scaffold Next 16 project + token file**

- **S01.01 — Initialise the repo.** *Files:* `.git/`, `.gitignore` (A10 list), `README.md` (3 lines: what, how to run, link to `technical-plan.md`). *Contract:* `main` branch exists; the Stage 2–6 artifacts are the first commit `docs: stage 2–6 planning artifacts`. *Gate:* `git -C "/Volumes/E Drive/Dev/Code/Claude/Portfolio-clay" log --oneline | wc -l` → `1`; `git status --porcelain` empty.
- **S01.02 — Pin tooling to the E Drive.** *Files:* `.env.tooling` (`PLAYWRIGHT_BROWSERS_PATH=/Volumes/E Drive/Dev/.cache/ms-playwright`, `TMPDIR=/Volumes/E Drive/Dev/.scratch/tmp`, `LHCI_DIR=.lighthouseci`), `scripts/env.sh` (exports the same, `mkdir -p` both dirs), `.npmrc` (`engine-strict=true`, `auto-install-peers=true`). *Contract:* every later script that launches a browser or writes temp files is prefixed `dotenv -e .env.tooling --`. *Gate:* `source scripts/env.sh && echo $PLAYWRIGHT_BROWSERS_PATH` → the E Drive path; `pnpm store path` → `/Volumes/E Drive/Dev/.pnpm-store/v11`; `df -h / | tail -1` recorded in the report (internal-disk free space before any install).
- **S01.03 — Scaffold Next 16.3.5 into the existing folder.** *Files:* `package.json`, `tsconfig.json` (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `paths @/*`), `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `next-env.d.ts`. *Contract:* scaffold generated by `pnpm dlx create-next-app@16.3.5 /Volumes/E\ Drive/Dev/.scratch/portfolio-clay-scaffold --ts --app --tailwind --eslint --no-src-dir --import-alias "@/*" --use-pnpm` then `rsync -a --exclude README.md --exclude .git` into the repo (create-next-app refuses a non-empty directory); App Router, no `src/`. *Gate:* `pnpm install` completes; `pnpm dev` → `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000` → `200`; `pnpm typecheck` exits 0.
- **S01.04 — Pin the dependency set + minimal Vitest.** *Files:* `package.json` (deps: `next@16.3.5 react@19.2 react-dom@19.2 motion@13.3.0 lucide-react@1.46.0 zod@^4 @vercel/analytics @vercel/speed-insights sharp culori`; dev: `tailwindcss@4.3.3 @tailwindcss/postcss typescript @types/react @types/node eslint eslint-config-next eslint-plugin-jsx-a11y @typescript-eslint/* vitest@^4 @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @playwright/test @axe-core/playwright @lhci/cli tsx dotenv-cli @types/culori`), `vitest.config.ts` (node + jsdom projects, `tests/unit/**`), `tests/unit/smoke.test.ts`. *Contract:* the scripts table from A9 exists (eval/predeploy point at files created later — `tsx` prints "not yet implemented" and exits 1 until then, so a missing script is never silently green). *Gate:* `pnpm ls --depth 0 | grep -E 'next@16.3.5|motion@13.3.0|lucide-react@1.46.0|tailwindcss@4.3.3'` → 4 lines; `pnpm audit --audit-level high` → 0 high/critical; `pnpm test` → 1 passed.
- **S01.05 — Token file (`@theme`) verbatim from Design.md §2.** *Files:* `app/globals.css`. *Contract:* `@import "tailwindcss"; @theme { … }` containing all 13 colour tokens (hex values as `oklch()` placeholders from Design.md, corrected in S01.06), `--font-display/--font-hand`, the 6 fluid text tokens + 3 tracking/leading tokens, `--space-1…13`, section-gap ×3, container ×2, gutter ×3, `--radius-clay/-sm/-pill/-utility`, the 3 clay shadows + `--shadow-utility` + `--gradient-clay-volume`, plus `--card-padding: var(--space-7)` and `--card-padding-hero: var(--space-9)` (Design.md's `40px to 56px` range split into two valid tokens — E-6). Base layer: `html { color-scheme: light; background: var(--color-bg); color: var(--color-ink); font-family: var(--font-display) }`, focus ring utility `.focus-ring:focus-visible { outline: 3px solid var(--color-accent); outline-offset: 3px }`, reduced-motion global rule (A6), VT timing rules (A5). *Gate:* `grep -c -- '--color-' app/globals.css` → `13`; `grep -c -- '--shadow-clay-' app/globals.css` → `3`; `pnpm build` exits 0.
- **S01.06 — OKLCH regeneration guard (D2).** *Files:* `scripts/tokens-check.ts`, `app/globals.css` (values rewritten). *Contract:* script holds the 13 authoritative hex values from DESIGN_DIRECTION §2, converts with `culori` (`formatCss(oklch(hex))`, 3-decimal precision), `--write` mode rewrites the `@theme` colour lines, default mode parses `globals.css`, converts each oklch back to hex and asserts equality to the authoritative hex (sRGB 8-bit round-trip, 0 diff). *Gate:* `pnpm tokens:check --write && pnpm tokens:check` → `13/13 tokens round-trip OK`; `git diff --stat app/globals.css` shows only colour lines changed.
- **S01.07 — Layout, fonts, site constants, config.** *Files:* `app/layout.tsx` (`Manrope` 500–800 + `Caveat` 500–600 via `next/font/google`, `display:'swap'`, CSS variables `--font-manrope/--font-caveat` mapped into `@theme` `--font-display/--font-hand`; `metadataBase`, default `title.template`, `lang="en"`, `<body>` with `<main id="main">`), `lib/site.ts` (`export const site = { name:'Tushar Pathak', title:'Senior Product Manager', tagline:'Product Thinker · AI Builder · Problem Solver', email:'Tushar_Pathak@outlook.com', linkedin:'https://www.linkedin.com/in/pathaktushar', github:'https://github.com/007U5H4R', priorSite:'https://tushar-pathak.vercel.app/', resumeAvailable:false as boolean }` + `export function resumeAction(): { label:string; href:string; download:boolean; note?:string }` returning `{label:'Resume — updating', href:'/contact#resume', download:false, note:'Sanitised resume coming — email me for a copy'}` while `false` and `{label:'Download Resume ↓', href:'/resume.pdf', download:true}` when `true`), `.env.example` (`NEXT_PUBLIC_SITE_URL=`, `RAG_ENDPOINT=` commented "reserved, unused in v1"), `next.config.ts` (`images` per A7, `experimental.viewTransition:true`, `reactStrictMode:true`, `poweredByHeader:false`). *Contract:* `resumeAction()` is the only source of truth for every resume control (PB5). *Gate:* `TYPECHECK`, `LINT`; `curl -s http://localhost:3000 | grep -o '/_next/static/media/[^"]*woff2' | head -2` → two self-hosted font files (no `fonts.googleapis.com` in the HTML); `tests/unit/site.test.ts` asserts both `resumeAction()` shapes.
- **S01.08 — Static-route guarantee (TP1).** *Files:* `scripts/assert-static.ts`, `package.json` (`build` script). *Contract:* after `next build`, reads `.next/prerender-manifest.json` and `.next/app-path-routes-manifest.json`; every app route must appear in `routes`/`dynamicRoutes` with prerendered HTML, else prints the offending route and exits 1; prints `all routes static (N)`. *Gate:* `BUILD` → `all routes static (1)` for `/`.

**TSK-02 · Avatar asset pipeline**

- **S02.01 — Export script.** *Files:* `scripts/avatar.ts`. *Contract:* per A7 — reads `content/media/avatar/avatar-cutout.png`, writes `public/avatar/avatar.webp` (long edge 1800, ≤300 kB, alpha), `avatar@2x.webp`, `avatar-poster.webp` (flattened on `#FAF9FF`, 1200×1500), `avatar-blur.txt`; prints each path + bytes; exits 1 if `avatar.webp` > 307200 bytes after the quality ladder 82→78→74. *Gate:* `pnpm media:avatar` → 4 files listed; `stat -f %z public/avatar/avatar.webp` ≤ 307200; `sharp` metadata shows `hasAlpha: true`, width or height ≥1600.
- **S02.02 — Edge inspection frame.** *Files:* `tests/e2e/avatar-edge.spec.ts` (tagged `@manual-shot`). *Contract:* renders `avatar.webp` on a `sky`→`lavender` gradient at 1440, `deviceScaleFactor:2`, clips a 400×400 region around hair/beard boundary → `docs/screenshots/tracer/avatar-edge@2x.png`. *Gate:* `SHOT(avatar-edge@2x)` — orchestrator confirms no light halo / no clipped beard; if halo: rerun `scripts/avatar.ts --erode 1` (1px alpha erosion option) and re-shoot.
- **S02.03 — Provenance.** *Files:* `content/media/avatar/candidates/README.md` (append: source file, script version, sharp version, sizes, date; "no paid tool calls" — background removal already existed, S4 spend rule respected). *Gate:* `git diff content/media/avatar/candidates/README.md` shows one appended block; `content/media/avatar/avatar-source.png` sha unchanged (`shasum` before/after equal).
- **S02.04 — Alt text constant.** *Files:* `lib/site.ts` (`avatarAlt: 'Clay illustration of Tushar Pathak at a laptop'`). *Contract:* the only place the alt string lives (EVAL-013 alt rule). *Gate:* `grep -rn "Clay illustration of Tushar" app components lib | wc -l` → `1`.

**TSK-03 · Minimal clay primitives for the slice**

- **S03.01 — Tier map + type guard.** *Files:* `components/clay/tiers.ts`. *Contract:* `export type Tier = 'hero'|'card'|'utility'|'flat'`; `export const tierClass: Record<Tier,string>` mapping to Tailwind classes that read only the allowed tokens (hero: `rounded-[34px] shadow-[var(--shadow-clay-rest)] bg-[image:var(--gradient-clay-volume)]`, card: `rounded-[var(--radius-clay)] …`, utility: `rounded-[var(--radius-utility)] shadow-[var(--shadow-utility)]`, flat: `''`); `export const toneClass: Record<Tone,string>`; discriminated prop type `ClayProps = { tier:'flat'; tone?:'neutral'; interactive?:false } | { tier:Exclude<Tier,'flat'>; tone?:Tone; interactive?:boolean }` so `tier:'flat'` + `tone:'lavender'` is a **type error** (D1). *Gate:* `tests/unit/tiers.test.ts` — `flat` class string contains no `shadow`/`gradient`; `// @ts-expect-error` line for `{tier:'flat', tone:'lavender'}` compiles only because it errors (`TYPECHECK`).
- **S03.02 — `ClayCard`.** *Files:* `components/clay/ClayCard.tsx`. *Contract:* polymorphic `as` (default `div`), `ClayProps` + `padding?: 'card'|'hero'`, `interactive` adds `transition-[transform,box-shadow] duration-200 ease-[var(--ease-hover)] hover:-translate-y-[5px] hover:shadow-[var(--shadow-clay-hover)] active:translate-y-px active:scale-[.98] active:shadow-[var(--shadow-clay-press)] motion-reduce:hover:translate-y-0` + `.focus-ring`. *Gate:* `TYPECHECK`; jsdom test renders 4 tiers and asserts class presence/absence.
- **S03.03 — `ClayButton`.** *Files:* `components/clay/ClayButton.tsx`. *Contract:* `variant: 'primary'|'secondary'|'ghost'`, renders `<a>` when `href`, else `<button type="button">`; `min-h-11 min-w-11 px-6 rounded-[var(--radius-clay-sm)]`; primary = `bg-accent text-bg hover:bg-accent-deep` (**`bg` text on accent — 4.9:1; `ink` on accent is 3.4:1 and fails AA; resolves Design.md's ambiguous primitives row — E-7**), secondary = `border border-ink/15 text-ink bg-surface`, ghost = transparent icon-only (`aria-label` required by type when `iconOnly`); press 90 ms `scale(.98)`, hover lift 180 ms `translateY(-3px)` (removed under reduced motion), `.focus-ring`, optional `trailingIcon`, `download` passthrough. *Gate:* jsdom test: iconOnly without `aria-label` is a type error; rendered button has `min-height: 44px` computed in Playwright later (S07.03 checks all `button,a` ≥44).
- **S03.04 — `ClayTile`, `ClayFrame`, `ClayIcon`.** *Files:* `components/clay/ClayTile.tsx` (`size: 56|120|140|180`, tone, tier utility|card), `components/clay/ClayFrame.tsx` (`ratio: '4/5'|'16/9'`, tier hero|card, duotone `tone`/`tone2` gradient, `children` slot, forwards `style` for VT names), `components/clay/ClayIcon.tsx` (`size: 40|56`, lucide `icon` component prop, stroke 1.75, inside a utility `ClayTile`). *Gate:* `TYPECHECK`; jsdom snapshot of each at default props.
- **S03.05 — `Icon`, `Tag`, `StatusBadge`.** *Files:* `components/common/Icon.tsx` (lucide wrapper, `size 20|24`, `strokeWidth 1.75`, `aria-hidden` unless `label`), `components/common/Tag.tsx` (static utility pill, `ink-2` text, **no hover**), `components/projects/StatusBadge.tsx` (`status → {tone, icon}`: live→mint/`Radio`, pilot→sky/`FlaskConical`, prototype→peach/`Hammer`, research→lavender/`BookOpen`, archived→neutral/`Archive`; text = `statusLabel`; icon + text always). *Gate:* jsdom: `StatusBadge` renders an `svg` and the label for all 5 statuses; `Tag` has no `hover:` class (`expect(className).not.toMatch(/hover:/)`).
- **S03.06 — Primitive smoke page (tracer-only).** *Files:* `app/dev/primitives/page.tsx` (minimal grid of the above; extended in TKT-04), `lib/dev-only.ts`. *Gate:* `pnpm dev` → `/dev/primitives` renders; `NODE_ENV=production pnpm start` → `/dev/primitives` returns 404 (`curl -o /dev/null -w '%{http_code}'`).

**TSK-04 · Header + NavPill + MobileMenu + SkipLink**

- **S04.01 — Motion helpers.** *Files:* `lib/motion.ts` (A6 constants, `useReducedMotionSafe`, `usePointerFine`, `useScrollY(threshold)` returning boolean past-threshold via passive scroll listener + rAF). *Gate:* `TYPECHECK`; jsdom test: `useReducedMotionSafe` returns `true` on first render, then matchMedia value.
- **S04.02 — `Container` + `SkipLink`.** *Files:* `components/layout/Container.tsx` (`max-w-[var(--container-max)] 2xl:max-w-[var(--container-max-wide)] px-[var(--gutter-mobile)] md:px-[var(--gutter-tablet)] lg:px-[var(--gutter-desktop)] mx-auto`, `as` prop), `components/layout/SkipLink.tsx` (`sr-only focus:not-sr-only` fixed top-left, `href="#main"`). *Gate:* Playwright later (S07.03) — `Tab` once from load focuses the skip link and `Enter` moves focus to `#main`.
- **S04.03 — `Header` shell + compaction.** *Files:* `components/navigation/Header.tsx` (`"use client"`; `useScrollY(24)` → `data-compact`; rest `py-7` (28 px → 96 px tall) → compact `py-3.5` (68 px) + `backdrop-blur-[12px] bg-bg/80`; `transition-[padding,background-color,backdrop-filter] duration-250 ease-in-out motion-reduce:transition-none`; `position: sticky; top: 0; padding-top: env(safe-area-inset-top)`; left: `ClayIcon` "TP" mark 40 + wordmark stack (subtitle `hidden md:block` — Deviation 5); centre-right nav from `lib/nav.ts` (`Home · Work · Thinking · About`), each link `min-h-11 px-4 flex items-center`), `lib/nav.ts`. *Gate:* Playwright (S07.03): height at scrollY 0 = 96±1 px, at scrollY 40 = 68±1 px; `backdrop-filter` computed `none` at rest, `blur(12px)` compact.
- **S04.04 — `NavPill`.** *Files:* `components/navigation/NavPill.tsx` (`m.span layoutId="nav-pill"` inside `LazyMotion features={domAnimation}`; lavender utility fill behind the active item, `aria-current="page"` on the link, not the pill; reduced motion: `layout={false}` → instant). *Gate:* Playwright: navigating `/` → `/work` moves `aria-current` and the pill's bounding box overlaps the active link.
- **S04.05 — `MobileMenu`.** *Files:* `components/navigation/MobileMenu.tsx` (`"use client"`; hamburger `ClayButton ghost` `aria-controls aria-expanded` (`md:hidden`); native `<dialog>` `showModal()` full-screen sheet; nav rows 56 px; bottom row `AskAIButton` + resume action (`resumeAction()`); `Esc`/backdrop click closes; focus returns to the hamburger; `overflow:hidden` on `html` while open). *Gate:* Playwright `w390`: open → `document.activeElement` inside dialog; `Tab` cycles inside; `Escape` closes and focus is on the hamburger; axe 0 critical/serious with the menu open.
- **S04.06 — `AskAIButton` (disabled state) + assemble.** *Files:* `components/navigation/AskAIButton.tsx` (`ClayButton secondary`, icon `Sparkles` + "Ask AI"; tracer: `aria-disabled="true"` + `title="Ask AI — coming in this build"` + visible tooltip on focus/hover, still focusable, `onClick` no-op — **never a dead control** per TKT-01 AC 3), `app/layout.tsx` (SkipLink → Header → `<main id="main">` → children). *Gate:* `LINT` (jsx-a11y clean); Playwright: the button is in the tab order and has an accessible description; crawler allowlist entry `ask-ai-disabled` added with the reason (removed in TKT-11).

**TSK-05 · Hero + AvatarStage + FloatingTiles + Annotation**

- **S05.01 — `Parallax` leaf.** *Files:* `components/interactions/Parallax.tsx` (`"use client"`; props `depth: number` (0.5/1/1.5), `maxPx` (6 for avatar, 6×depth for tiles); `useMotionValue` for pointer offset relative to the stage centre, `useSpring(springs.parallax)`; active only when `usePointerFine() && !useReducedMotionSafe()`; otherwise renders children unmoved with no listeners). *Gate:* jsdom: with `matchMedia('(pointer:fine)')` false the component adds no `pointermove` listener (spy on `addEventListener`).
- **S05.02 — `AvatarStage`.** *Files:* `components/hero/AvatarStage.tsx` (`ClayFrame ratio 4/5 tier hero tone sky tone2 lavender`; sizes via CSS: `w-[280px] md:w-[360px] lg:w-[480px] 2xl:w-[520px]` (heights follow the 4:5 ratio → 350/450/600/650); `next/image src="/avatar/avatar.webp" alt={site.avatarAlt} priority placeholder="blur" blurDataURL sizes="(min-width:1440px) 520px, (min-width:1024px) 480px, (min-width:768px) 360px, 280px" width=1800 height=…`; ≤3 supporting `ClayTile`s (laptop/plant/books — icons `Laptop`, `Sprout`, `BookMarked`) at frame corners at depths 0.5/1/1.5, wrapped in `Parallax`; avatar itself in `Parallax depth={-1} maxPx={6}` (moves opposite the cursor)). *Gate:* `TYPECHECK`; Playwright (S07.03) frame width at 1440 = 520, 1024 = 480, 768 = 360, 390 = 280 (±1).
- **S05.03 — `FloatingTiles`.** *Files:* `components/hero/FloatingTiles.tsx`, `data/hero.ts` (typed literal: eyebrow, headline parts, support, tagline, three tiles — **verbatim CONTENT_INVENTORY §1.2 rows**, each with `source` label; migrated into the schema world in TKT-03 if a `HeroCopy` schema is added, else stays a typed literal with a comment naming the rows). *Contract:* 3 `ClayTile size 180` (≥1024, ~180×140 via `aspect-[9/7]`) with labels AI Products · People · Progress + the sourced one-liner, vertical offsets `-24/0/+24` px, depths 0.5/1/1.5 via `Parallax`; `<1024` single column, 16 px gap, after the CTA row. *Gate:* Playwright: tiles' `y` offsets differ by 24 px at 1440; single column at 390 (each tile `x` equal).
- **S05.04 — `Annotation`.** *Files:* `components/hero/Annotation.tsx` (Caveat, `aria-hidden`, `rotate-[-4deg]`, copy `ideas → impact`, placed near the CTA row; the "AI-native products" wash is a `span.hero-highlight` with `background: linear-gradient(90deg, transparent 50%, var(--color-lavender) 50%) 100%/200%` animating `background-position` to `0` over 700 ms once (`@keyframes wash`, `animation-fill-mode: forwards`, removed entirely under reduced motion)). *Gate:* `LINT`; Playwright: `[aria-hidden="true"]` annotation count in hero = 1; under `reducedMotion` the highlight has `animation-name: none`.
- **S05.05 — `Hero` composition + home page.** *Files:* `components/hero/Hero.tsx` (server; grid `lg:grid-cols-[35fr_65fr] gap-16 2xl:gap-24`, `pt-32 pb-24`; right column order eyebrow → h1 (`text-[length:var(--text-hero)] font-extrabold tracking-[var(--tracking-hero)] leading-[var(--leading-hero)]`) → lead (`max-w-[44ch] text-ink-2`) → CTA row (`ClayButton primary` "View My Work →" `href="/work"`, + resume control from `resumeAction()` as `ClayButton secondary`) → `FloatingTiles`; `<lg`: stacked avatar → headline → CTAs → tiles, all left-aligned, CTAs full-width `<md`), `app/page.tsx` (renders `<Hero/>` + one `<FeaturedWork/>` placeholder section holding the single card from TSK-06), `app/work/page.tsx` stub ("Work — coming in this build", `Container`, so the CTA is never dead), `app/contact/page.tsx` stub with `<section id="resume">` + the "email me" note (so `/contact#resume` resolves — PB5). *Gate:* `BUILD` → `all routes static (4)`; `SHOT(tracer/home-1440)`; Playwright: DOM order of `h1`, CTA row, tiles at 390 matches the fixed mobile order; `h1` contains a `span.hero-highlight` with text "AI-native products".
- **S05.06 — Halo + reduced-motion + LCP check.** *Files:* none (verification). *Gate:* `SHOT(tracer/avatar-edge@2x)` from S02.02 re-shot on the real Hero; Playwright `reducedMotion`: `Parallax` leaves `transform: none` after synthetic `pointermove`; `curl -s localhost:3000 | grep -c 'fetchpriority="high"'` ≥ 1 (avatar is the LCP candidate with `priority`).

**TSK-06 · One ProjectCard + ViewTransitionLink + stub `/work/teachspark`**

- **S06.01 — Verify the React VT export and enable it.** *Files:* `lib/motion.ts` (append `export { unstable_ViewTransition as ViewTransition } from 'react'` **or** `ViewTransition` — whichever the check prints), `next.config.ts` (`experimental.viewTransition: true` already set in S01.07). *Gate:* `node -e "console.log(Object.keys(require('react')).filter(k=>/ViewTransition/i.test(k)))"` prints a non-empty array and the chosen name; `pnpm dev` starts without an "unknown experimental option" warning. If the array is empty → stop, report the breaker (fallback plan A14 row 1), do not improvise.
- **S06.02 — `ViewTransitionLink`.** *Files:* `components/interactions/ViewTransitionLink.tsx` (`"use client"`; thin wrapper over `next/link` that renders `children` inside `<ViewTransition name={props.transitionName}>` when `typeof document !== 'undefined' && 'startViewTransition' in document && !reducedMotion`, else plain `Link`; `prefetch` default). *Gate:* jsdom: with `document.startViewTransition` undefined the wrapper renders no VT element (query by `data-vt` attribute the wrapper sets).
- **S06.03 — `ProjectCard` (featured mode) fed by a typed literal.** *Files:* `components/projects/ProjectCard.tsx` (`mode: 'featured'|'grid'` — grid mode implemented in TKT-16; anatomy fixed: `ClayIcon 56` (VT name `icon-{slug}`) → `h3` name → proposition `line-clamp-2` → ≤3 `Tag` → `StatusBadge` → ghost arrow `ClayButton` 44×44 bottom-right (`aria-hidden`, presentational — the whole card is the link via `ViewTransitionLink` with `transitionName="project-{slug}"` on the card's `ClayFrame`-less body; single accessible name = project name); hover: rise 5 px / icon `scale(1.03)` / arrow `translateX(4px)` / gradient +8 % opacity, 200 ms), `data/tracer.ts` (TeachSpark literal: name, tagline from §1.4, tags `AI · WhatsApp · EdTech`, `status:'pilot', statusLabel:'Live pilot — uptime unverified since 2026-09-09'`, icon `MessageSquareText`). *Gate:* Playwright: card has exactly one `a` whose accessible name includes "TeachSpark"; hover changes `transform` on the card (desktop project) and not under `reducedMotion`.
- **S06.04 — `CaseStudyHeader` shell + stub routes.** *Files:* `components/case-study/CaseStudyHeader.tsx` (flat 60/40 grid ≥1024; left: `h1` name, lead = one-line problem, meta chips role/duration/status (`StatusBadge`); right: `ClayFrame ratio 16/9 tier card` **VT target** `project-{slug}` containing a labelled placeholder (`Media.kind:'placeholder'` → "Hero media coming"); icon slot `icon-{slug}`), `app/work/[slug]/page.tsx` (`generateStaticParams` → `[{slug:'teachspark'}]`, `notFound()` otherwise; renders header + "Case study — coming in this build" note), `app/work/[slug]/loading.tsx` **not** created (static). *Gate:* `BUILD` → `all routes static (5)`; `curl -o /dev/null -w '%{http_code}' localhost:3000/work/teachspark` → `200`; `/work/nope` → `404`.
- **S06.05 — Transition timing + fallback verification.** *Files:* `app/globals.css` (VT rules from A5). *Gate:* Playwright `w1440` (Chromium supports VT): click card → URL `/work/teachspark`, `h1` "TeachSpark", header media present; same test under `noViewTransitions` fixture and under `reducedMotion` → identical assertions pass; `docs/screenshots/tracer/` gets `case-1440.png`.

**TSK-07 · Screenshots + minimal `pnpm eval` + baseline**

- **S07.01 — Playwright config.** *Files:* `playwright.config.ts` (A9: 4 projects, `webServer: pnpm start`, `reporter: [['list'],['json',{outputFile:'.eval/playwright.json'}]]`, `outputDir:'test-results'`), `tests/e2e/fixtures.ts` (`axe`, `noOverflow`, `reducedMotion`, `noViewTransitions`, `minTargets` — implemented now, reused by TKT-07). *Gate:* `pnpm exec playwright test --list` prints the 4 projects × specs; no `~/Library/Caches/ms-playwright` created (`ls ~/Library/Caches | grep -c ms-playwright` → 0).
- **S07.02 — Install Chromium on the E Drive.** *Gate:* `dotenv -e .env.tooling -- pnpm exec playwright install chromium` → files under `/Volumes/E Drive/Dev/.cache/ms-playwright/chromium-*`; `df -h /` free space unchanged within 50 MB versus S01.02.
- **S07.03 — Tracer spec.** *Files:* `tests/e2e/tracer.spec.ts` (tags `@EVAL-006 @EVAL-008 @EVAL-010 @EVAL-015`): for `/` and `/work/teachspark` at all 4 projects → screenshot `docs/screenshots/tracer/{home,case}-{w}.png` (8 files) + `noOverflow` + `minTargets`; at `w390`/`w1440` → `axe`; header compaction, mobile menu (S04.05), skip link (S04.02), VT + fallback (S06.05), resume placeholder (`a[href="/contact#resume"]` text "Resume — updating" present, `/resume.pdf` → 404), reduced-motion checks (S05.06). *Gate:* `pnpm test:e2e` green; `ls docs/screenshots/tracer/*.png | wc -l` → `8` (+ the edge shot).
- **S07.04 — Lighthouse CI configs.** *Files:* `lighthouserc.mobile.json`, `lighthouserc.desktop.json` (A9; for the tracer, `collect.url = ['/']` only; assertions present but the runner records them as informational until TKT-07 flips `enforce:true` in `eval-cases.json` config). *Gate:* `dotenv -e .env.tooling -- pnpm exec lhci autorun --config lighthouserc.mobile.json --collect.url=http://127.0.0.1:3000/` completes 3 runs and writes `.lighthouseci/*.json`.
- **S07.05 — Minimal orchestrator (schema v1).** *Files:* `scripts/eval.ts` (loads `evals/eval-cases.json`, runs Playwright + LHCI (mobile+desktop), collects provenance (`git rev-parse HEAD`, branch, `git status --porcelain` → `dirty`, `process.version`, `pnpm -v`, next version from lockfile, `os.release()`, timestamp, sha256 of `eval-cases.json`), maps results → `cases[]` with `status` (`PASS/FAIL` for 006/008/010/015; `MANUAL` for 001/003/009/017; `SKIP` with `reason:'not built yet'` for the rest), computes totals, writes `evals/results/<label|eval-run-…>.json`, never overwrites; `--label`, `--only`, `--skip-build` flags; exits non-zero on any FAIL among cases marked `critical` unless `--informational` (tracer only)). *Gate:* `pnpm eval --label baseline-v1 --informational` writes `evals/results/baseline-v1.json`; `node -e "const r=require('./evals/results/baseline-v1.json');console.log(r.schemaVersion,r.provenance.commit.length,r.totals.cases)"` → `1 40 17`.
- **S07.06 — Baseline captured + committed.** *Gate:* `baseline-v1.json` shows EVAL-006 PASS (0 critical/serious at 390 & 1440), EVAL-008 PASS (0 overflow, 0 sub-44 targets), EVAL-010 PASS, EVAL-015 PASS, EVAL-004/005 `measured` populated (mobile+desktop scores for `/`, LCP, CLS, first-load JS kB from `next build` output parsed by a first `scripts/bundle-budget.ts`); committed with message `eval: baseline-v1 on tracer bullet`.
- **S07.07 — Ticket wrap.** *Gate:* `pnpm typecheck && pnpm lint && pnpm test && pnpm build` green; `git diff main..HEAD --stat` lists only files named in TKT-01 tasks; `tree -I node_modules -L 2` shows **no** component/route beyond the TKT-01 list (AC 9); report written to `docs/reports/TKT-01.md` (DRAFT copy flagged: hero support line, eyebrow triad).

#### TKT-02 · Visual-direction review gate — *orchestrator + Tushar; no implementer*

- **S02g.01 — Present.** Start `pnpm start`; open `/` and `/work/teachspark` at 390 and 1440 in Chrome; show the 8 screenshots + `avatar-edge@2x.png` + `baseline-v1.json` numbers. *Gate:* Tushar has seen ≥2 widths live (AC 1).
- **S02g.02 — Score.** *Files:* `evals/results/gate-tracer.md` (EVAL-001 checklist 6 items × {390,1440} scored by Claude then confirmed/overridden by Tushar; EVAL-009 hero rubric 6 items × 0–2; avatar-likeness line; DRAFT copy items listed). *Gate:* file committed; every row has a score and a note.
- **S02g.03 — Decide and write back.** *Files:* `decisions.md` (`## EXE-1 · Tracer visual direction — accepted|accepted with changes`), `Design.md` (a Deviation entry per change request), `HANDOFF.md`. *Gate:* Tushar's words are quoted ("approved" / "approved with changes: …"); silence is not recorded as approval; TKT-04/05/06 remain blocked until this file exists.

---

### M-002 · Foundations & quality harness

#### TKT-03 · Content schema + zod gate + failing fixture + forbidden strings — *most-capable tier (S03.01–S03.02, S03.07); standard for the rest*

- **S03.01 — `data/schema.ts`.** *Files:* `data/schema.ts` = A3 verbatim. *Gate:* `TYPECHECK`; `node --import tsx -e "import('./data/schema.ts').then(m=>console.log(Object.keys(m).length))"` ≥ 25.
- **S03.02 — Validator + prebuild hook.** *Files:* `data/index.ts` (`export const collections = { projects, experience, skills, writing, knowledge, thinkingFramework }` + `validateAll(): {ok:true} | {ok:false; issues: string[]}` applying each schema and the cross-entity checks from A3 rule 4 — internal hrefs must resolve against `routes()` from `lib/anchors.ts` + slugs + essay slugs; featured ranks 1/2/3 distinct; one `large`; knowledge `surface` counts 5/6), `scripts/validate-content.ts` (`CONTENT_FIXTURE=invalid` swaps in the fixture; prints `<entity>.<id> → path: message` per issue; exit 1), `package.json` (`prebuild`). *Gate:* `pnpm exec tsx scripts/validate-content.ts` → `content OK (projects:1 experience:0 skills:0 writing:0 knowledge:0 thinking:0)`; empty collections are allowed until their tickets land (`min(0)` at collection level, entity-level rules strict).
- **S03.03 — Anchors module.** *Files:* `lib/anchors.ts` (`CHAPTER_ANCHORS: Record<ChapterId,{anchor:string; title:string}>` = context→`01-context`/"Context", problem→`02-problem`/"Problem", discovery→`03-discovery`/"Discovery", bet→`04-product-bet`/"Product bet", built→`05-what-i-built`/"What I built", evaluation→`06-evaluation`/"Evaluation", outcome→`07-outcome`/"Outcome", learned→`08-what-i-learned`/"What I learned"; `PAGE_ANCHORS = { about: ['experience','impact','capabilities','research'], contact: ['resume'] }`; `routes()` builder), `docs/anchors.md` (the table — TP8, E-3). *Gate:* unit test: every CONTENT_INVENTORY §1.5 and §9 href (hard-coded list in the test) resolves through `routes()`.
- **S03.04 — Migrate the tracer literal into `data/projects.ts`.** *Files:* `data/projects.ts` (TeachSpark at card fidelity: slug/name/tagline/tags/filters `['ai']`/status/statusLabel/featured 1/gridSize large/icon/role/dates `{start:'2026-08'}`/duration/links `{live, repoPublic:false, github}`/hero `{}`/metrics `[]`/overview `{thirtySecond:[…§8.1 problem sentence…], deepDive:false}`/8 empty chapters/`thinking: []`/`learnings: []`/`sources` ≥1 (`TS-README-3`, `CS4-FINAL-PRD`)), delete `data/tracer.ts`, `components/projects/ProjectCard.tsx` + `app/work/[slug]/page.tsx` + `app/page.tsx` read from `projects`. *Gate:* `BUILD` (prebuild passes); Playwright tracer spec still green.
- **S03.05 — Schema unit tests.** *Files:* `tests/unit/schema.test.ts` (all current data parses; targeted negatives: metric without `asOf`, metric `source` not in `sources`, `tags` length 4, chapter order swapped, thinking length 7, professional with `links.live`, `repoPublic:true` without github, `deepDive:true` with 2 chapters — each asserts the zod path in the error). *Gate:* `UNIT` → ≥ 10 passed.
- **S03.06 — Deliberate failing fixture.** *Files:* `tests/fixtures/invalid-project.fixture.ts` (a copy of TeachSpark with: one metric missing `asOf`, `sources: []`, `statusLabel: 'AI Product Manager'`), `tests/unit/content-gate.test.ts` (asserts `validateAll()` with the fixture returns `ok:false` with exactly those 3 issue paths). *Gate:* `UNIT` green; the fixture file is imported **only** by the test and by `validate-content.ts` under `CONTENT_FIXTURE=invalid` (grep confirms 2 importers).
- **S03.07 — Forbidden strings.** *Files:* `scripts/forbidden-strings.ts` (A3 rule 5 pattern list; `--bundle` adds `.next/**`; reads `tests/forbidden.local.json` for the sandbox code, prints `SKIP: forbidden.local.json missing` when absent), `tests/forbidden.local.example.json` (`{"strings":["<teachspark sandbox join code>"]}` placeholder text only), `tests/unit/forbidden-strings.test.ts`, `.gitignore` (`tests/forbidden.local.json`). *Gate:* `pnpm exec tsx scripts/forbidden-strings.ts` → `0 hits in N files`; planting `PMP` in a temp data file makes it exit 1 (test does this in a tmp dir).
- **S03.08 — Prove the gate against a real build (EVAL-013).** *Gate:* `CONTENT_FIXTURE=invalid pnpm build 2>&1 | tee evals/results/content-gate-proof.txt; echo "exit=$?"` → the file contains the 3 issue lines and `exit=1` (pipefail on) and **no** `.next` output was produced for the fixture (`prebuild` fails before `next build`); then `pnpm build` (no env) → green. File committed.
- **S03.09 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-013,EVAL-006,EVAL-008` → EVAL-013 `PASS` (schema + forbidden + proof file present), no regression vs baseline; `git diff --stat` matches ticket files.

#### TKT-04 · Clay primitive system complete + common primitives — *most-capable tier (S04.01–S04.05); standard (S04.06–S04.09)*

- **S04.01 — Tier system finalised.** *Files:* `components/clay/tiers.ts` (hero radius 34 px; card 28; utility 14 + `--shadow-utility` only + no press state; flat none; `glass` exported separately as `headerGlassClass` — not a tier; `interactive` disallowed on `utility` unless `variant:'filter'`), `tests/unit/tiers.test.ts` extended. *Gate:* `TYPECHECK` with `@ts-expect-error` cases for `flat+tone`, `flat+interactive`, `utility+interactive` (non-filter).
- **S04.02 — `ClayCard` full contract.** *Files:* `components/clay/ClayCard.tsx` (tone × tier × interactive × `as` × `padding`; tone gradient overlay `after:` pseudo with `--gradient-clay-volume`; 6 % darker bottom edge via inset shadow token). *Gate:* jsdom matrix test 7 tones × 4 tiers renders; flat has zero `shadow`/`bg-[image` classes.
- **S04.03 — `ClayButton` full.** *Files:* `components/clay/ClayButton.tsx` (add `size:'md'|'lg'`, `trailingIcon`, `loading` (spinner + `aria-busy`), `download`, `external` → `target=_blank rel=noopener` + "opens in new tab" `VisuallyHidden`). *Gate:* jsdom: external variant renders the hidden note; `LINT`.
- **S04.04 — `ClayPill`.** *Files:* `components/clay/ClayPill.tsx` (`variant:'filter'` → `<button role="tab">`-compatible props, hover `bg-lavender/40`, active `bg-lavender text-ink`, 40–44 px height; `variant:'tag'` → static `<span>`, no hover; `variant:'link'` → `<a>` pill with trailing arrow for `EvidenceLinks`). *Gate:* Playwright (S04.08) asserts computed `background-color` differs on hover for `filter`, identical for `tag`.
- **S04.05 — `ClayTile` / `ClayFrame` / `ClayIcon` complete + glass utility.** *Files:* `components/clay/ClayTile.tsx` (sizes 56–180, `tone`, `tier`, `interactive` for playground), `components/clay/ClayFrame.tsx` (`ratio 4/5 | 16/9`, `bezel` width token), `components/clay/ClayIcon.tsx` (40/56), `app/globals.css` (`.glass { backdrop-filter: blur(12px); background: color-mix(in oklch, var(--color-bg) 80%, transparent) }`). *Gate:* `grep -n "glass" components/clay/*.tsx` → 0 (glass never combined with clay); Header uses `.glass` only in compact state.
- **S04.06 — Common primitives.** *Files:* `components/common/Icon.tsx` (final), `Tag.tsx` (→ `ClayPill variant="tag"`), `ExternalLink.tsx` (inline text + 12 px `ArrowUpRight`, new tab, hidden note), `CopyButton.tsx` (skeleton: props `value`, states typed `idle|copied|error`, behaviour completed in TKT-14), `VisuallyHidden.tsx`, `Prose.tsx` (`max-w-[60ch] text-[length:var(--text-body)] leading-relaxed [&_p+p]:mt-5 [&_a]:underline [&_a]:text-accent`, flat — no clay props exist on it). *Gate:* `TYPECHECK`; jsdom: `Prose` root has `max-width` class and no tier prop in its type.
- **S04.07 — `/dev/primitives` board.** *Files:* `app/dev/primitives/page.tsx` (every primitive × tier × tone, states rendered via `data-state` classes for rest/hover/press/focus/disabled; sections labelled), `lib/dev-only.ts` (guard). *Gate:* `ALLOW_DEV_ROUTES=1 pnpm start` → page renders; production start → 404.
- **S04.08 — Tests + screenshots.** *Files:* `tests/unit/clay.test.tsx`, `tests/e2e/primitives.spec.ts` (hover-style assertions, axe at 390/1440, `minTargets`, screenshots → `docs/screenshots/primitives/{390,1440}.png`). *Gate:* `UNIT`, `pnpm test:e2e --grep primitives` green; 2 PNGs present.
- **S04.09 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-006,EVAL-007,EVAL-008,EVAL-010` no regression; `git diff --stat` matches.

#### TKT-05 · Layout system + Reveal + motion lib + Footer shell — *standard tier*

- **S05.01 — `Container` / `Section` / `SectionHeading`.** *Files:* `components/layout/Container.tsx` (final), `components/layout/Section.tsx` (`py-[var(--section-gap-mobile)] md:py-[…tablet] lg:py-[…desktop]`, optional `tone` (one accent per section — the prop is the mechanism), `id`, `aria-labelledby` wiring), `components/layout/SectionHeading.tsx` (eyebrow + h2 + optional lead, `max-w-[44ch]`). *Gate:* Playwright computed paddings at 390/768/1440 = 72/96/128 px; container `max-width` 1200 at 1024, 1320 at 1440; gutters 24/40/64.
- **S05.02 — `Reveal`.** *Files:* `components/interactions/Reveal.tsx` (`"use client"`; IO `threshold 0.2`, once; children get `data-revealed`; CSS in `globals.css`: `.reveal { opacity:0; transform: translateY(20px); transition: opacity 500ms var(--ease-reveal), transform 500ms var(--ease-reveal) } .reveal[data-revealed] { opacity:1; transform:none }` + `--stagger` custom property × 70 ms; reduced motion → `transition-property: opacity; transition-duration: 1ms`; SSR renders children **visible** when JS is off via `<noscript>`-free approach: the class is added only after mount, so static HTML is fully visible — EVAL-015). *Gate:* Playwright: element is visible in static HTML (`page.route` blocking JS → still `visible`); under `reducedMotion` computed `transform` never changes between frames.
- **S05.03 — `lib/motion.ts` variants + `ProgressBar`.** *Files:* `lib/motion.ts` (add `fadeUp`, `staggerChildren` variant objects for `m.*`, `LazyMotionRoot` wrapper component), `components/interactions/ProgressBar.tsx` (reading progress, `transform: scaleX`, `role="progressbar"`, hidden under reduced motion? — **no**: kept, it is opacity/scale of a 3 px bar; `aria-valuenow` updated at most 4×/s). *Gate:* `TYPECHECK`; jsdom: `ProgressBar` exposes `aria-valuenow`.
- **S05.04 — `Footer` (two tiers).** *Files:* `components/layout/Footer.tsx` (server; tier 1 headline "Still curious? Let's build what's next." + actions: resume (`resumeAction()`), LinkedIn (`ExternalLink` button), "Let's Talk" → `/contact`; tier 2 caption: name + title, nav Work · Thinking · About · Contact, GitHub profile link, prior-site link, **"Built with Claude Code"** credit (CONTENT_INVENTORY §1.6 / TKT-05 AC 3 — E-1), `pt-24 pb-10 pb-[calc(40px+env(safe-area-inset-bottom))]`), `app/layout.tsx` (mount). *Gate:* Playwright: all footer links resolve (`HEAD` internal 200; external allowlisted); text "Built with Claude Code" present.
- **S05.05 — Layout assertions spec.** *Files:* `tests/e2e/layout.spec.ts` (S05.01/S05.02 assertions, 4 widths). *Gate:* green.
- **S05.06 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-008,EVAL-010,EVAL-011` no regression; `git diff --stat` matches.

#### TKT-06 · SEO: metadata builders · OG generator · sitemap · robots — *standard tier (S06.03 most-capable: ImageResponse under SSG)*

- **S06.01 — `lib/seo.ts` + route metadata.** *Files:* `lib/seo.ts` (A8 `siteUrl()`, `buildMetadata()`), `app/layout.tsx` (`metadataBase`, defaults), `app/page.tsx`, `app/work/page.tsx`, `app/work/[slug]/page.tsx` (`generateMetadata` from project), `app/contact/page.tsx` (each exports metadata via the builder). *Gate:* `tests/unit/seo.test.ts`: `buildMetadata` output has absolute `https://` OG/Twitter URLs for `NEXT_PUBLIC_SITE_URL=https://example.test` and falls back to `https://<VERCEL_URL>`.
- **S06.02 — OG font assets.** *Files:* `assets/fonts/Manrope-Bold.ttf`, `assets/fonts/Manrope-ExtraBold.ttf`, `assets/fonts/OFL.txt` (from the Manrope repo, OFL licence). *Gate:* `ls -la assets/fonts` → 3 files; licence present.
- **S06.03 — `lib/og.tsx` template + home OG.** *Files:* `lib/og.tsx` (`OgCard` per A8; hex palette map; fonts loaded with `fs.readFile(path.join(process.cwd(),'assets/fonts/…'))`; avatar poster embedded as base64 for the home family), `app/opengraph-image.tsx` (`export const size={width:1200,height:630}; contentType; alt; default async function`). *Gate:* `BUILD` → `assert-static` lists `/opengraph-image` as prerendered; `curl -s localhost:3000/opengraph-image -o /tmp/og.png && file /tmp/og.png` → `PNG image data, 1200 x 630`; size ≤ 300 kB; `SHOT(og/home)` — text legible, ink on bg.
- **S06.04 — Work + case-study OG.** *Files:* `app/work/opengraph-image.tsx`, `app/work/[slug]/opengraph-image.tsx` (`generateStaticParams` re-used; `name + tagline + statusLabel + tone by status`). *Gate:* `BUILD` → `all routes static` includes `/work/teachspark/opengraph-image`; PNG 1200×630 ≤300 kB. (About/thinking/playground/contact OG images are added by TKT-42/43/44/45 using `OgCard` — one line each.)
- **S06.05 — Sitemap + robots.** *Files:* `app/sitemap.ts` (static routes + `projects.filter(personal).map(slug)` + essays; `/dev/*` excluded by construction), `app/robots.ts`. *Gate:* `curl -s localhost:3000/sitemap.xml | grep -c '<loc>'` → static routes + 1 (teachspark) now; `curl -s localhost:3000/robots.txt` contains `Sitemap: https://…/sitemap.xml` and `Disallow: /dev/`.
- **S06.06 — Tag tests.** *Files:* `tests/e2e/eval-017.spec.ts` (`@EVAL-017`: for every page family route currently built, assert `<title>`, `meta[name=description]`, `link[rel=canonical]`, `og:title/description/image/url/type`, `twitter:card=summary_large_image`, `twitter:image`, all URLs `^https://`; fetch each `og:image` → 200, PNG, 1200×630 via `sharp` metadata). *Gate:* green for `/`, `/work`, `/work/teachspark`, `/contact`.
- **S06.07 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-017,EVAL-004` (SEO category ≥95 informational until TKT-07) no regression; `git diff --stat` matches.

#### TKT-07 · Test & evaluation harness — *most-capable tier for TSK-10 and TSK-12; standard for TSK-08/09/11*

**TSK-08 · Vitest layer + eval-cases.json**
- **S08.01 — Vitest projects.** *Files:* `vitest.config.ts` (projects `node` (`tests/unit/**/*.test.ts`) and `jsdom` (`tests/unit/**/*.test.tsx`), `setupFiles` for jest-dom, `reporters: ['default','json']`, `outputFile: '.eval/vitest.json'`). *Gate:* `pnpm test` runs both projects; `.eval/vitest.json` written.
- **S08.02 — `eval-cases.json` loader.** *Files:* `scripts/eval-cases.ts` (zod schema for the case file — id/feature/category/method/threshold/priority/automated/tags/related_tickets/viewport/accessibility_requirement — and a `loadCases()` used by `scripts/eval.ts`; also asserts 17 unique ids EVAL-001…017 and that every `automated:true` case has a runner mapping), `evals/evaluation-plan.md` (pointer line to the root file). *Gate:* `pnpm exec tsx scripts/eval-cases.ts` → `17 cases OK · 14 automated · 3 manual` (EVAL-017 counts as automated for its tag-level part; its inspector part is a `MANUAL` sub-result).
- **S08.03 — `lib/format.ts` + tests.** *Files:* `lib/format.ts` (`formatAsOf('2026-08-24') → 'as of 24 Aug 2026'`, `formatRange({start,end}) → 'Aug 2026 – present'`, `formatNumber` with `tabular-nums` expectations, `readingTime(words)`), `tests/unit/format.test.ts`. *Gate:* `UNIT` green.

**TSK-09 · Playwright project: viewports, fixtures, per-EVAL specs**
- **S09.01 — Fixtures finalised.** *Files:* `tests/e2e/fixtures.ts` (from S07.01, add `keyboardOnly` helper (`page.keyboard` navigation with a focus-visible assertion after each Tab: `:focus-visible` element has `outline-width: 3px` and `outline-color` = accent), `consoleErrors` collector failing a test on `console.error`). *Gate:* `pnpm test:e2e --grep @smoke` green.
- **S09.02 — Per-EVAL spec skeletons.** *Files:* `tests/e2e/eval-002.spec.ts` (recruiter path — `test.fixme` until `/about`, resume; hops implemented incrementally: `/ → /work → /work/teachspark` live now), `eval-006.spec.ts` (axe every route in `routes.json` at 390 & 1440 — live), `eval-007.spec.ts` (keyboard scripts: nav + MobileMenu live; Ask/filters/timeline/thinking `fixme`), `eval-008.spec.ts` (overflow + targets + font-size ≥14 px sweep — live), `eval-010.spec.ts` (reduced-motion: no `transform` transitions animate — live for header/hero), `eval-014.spec.ts` (`fixme` → TKT-18), `eval-015.spec.ts` (VT off + JS off (`page.route('**/*.js', r=>r.abort())`) → content + nav links present — live). Each `test` carries `@EVAL-0xx` in its title. *Gate:* `pnpm test:e2e --list | grep -c '@EVAL-'` ≥ 8; no EVAL id from the automated list is absent (`scripts/eval-cases.ts --check-specs`).
- **S09.03 — Route list.** *Files:* `tests/e2e/routes.json` (static list: `/`, `/work`, `/work/teachspark`, `/contact`, `/dev/primitives` (QA job only)) with a loader that prefers `http://127.0.0.1:3000/sitemap.xml` when it returns 200 (PB1: crawler never waits on the visual gate; sitemap exists since TKT-06 anyway). *Gate:* unit test for the loader's fallback order.

**TSK-10 · Dead-control crawler (EVAL-011)**
- **S10.01 — Crawler core.** *Files:* `tests/e2e/crawler.ts` (for each route × {390,1440}: open; also open `MobileMenu` at 390 and any `[aria-haspopup]`/`[aria-expanded]` toggles once; enumerate visible `a[href], button, [role=button], [role=tab], [role=link]`; classify: internal `href` → `HEAD`/`GET` 200–299 (hash targets must exist: `#id` present on the target page); external → `HEAD` 200–399 with 10 s timeout, cached per run, ≤2 concurrent, `User-Agent: portfolio-clay-crawler`; `mailto:` → valid address; button → click then assert one of: URL changed, `aria-expanded`/`aria-pressed`/`aria-selected` toggled, a `dialog[open]` appeared, DOM mutation count > 0 within 500 ms, or a `data-testid`'d toast/state appeared; `aria-disabled="true"` controls must be allowlisted). *Gate:* unit test with a fixture HTML page containing one dead button → crawler reports it.
- **S10.02 — Allowlist + spec.** *Files:* `tests/e2e/crawler-allowlist.json` (`[{selector, reason, expires?}]` — tracer entry `ask-ai-disabled` until TKT-11), `tests/e2e/eval-011-dead-controls.spec.ts` (`@EVAL-011`, prints a table: route · width · control · result). *Gate:* `pnpm test:e2e --grep @EVAL-011` → `0 dead` on current routes; report artifact `.eval/dead-controls.json` written.
- **S10.03 — External-link hygiene.** *Files:* `tests/e2e/crawler.ts` (retry once on 429/5xx after 2 s; treat 403 from LinkedIn/GitHub bot-blocking as `WARN` with the status recorded, not `FAIL` — documented in `docs/eval.md`). *Gate:* running twice in a row hits the cache (log shows `cached` for externals on the second run).

**TSK-11 · Lighthouse CI + bundle budget (EVAL-004/005)**
- **S11.01 — Enforce thresholds.** *Files:* `lighthouserc.mobile.json`, `lighthouserc.desktop.json` (4 routes; `assert.assertions` per A9 with `aggregationMethod:'median'`, `level:'error'`), `evals/eval-cases.json` (unchanged — the runner reads `automated`; enforcement switch lives in `scripts/eval.ts` config `enforceLighthouse:true` from this step). *Gate:* `pnpm exec lhci autorun --config lighthouserc.mobile.json` on the current build → assertions evaluated (pass/fail printed per route); `/work` and `/about` are still stubs and pass trivially — recorded, not celebrated.
- **S11.02 — Bundle budget.** *Files:* `scripts/bundle-budget.ts` (A9; prints per-chunk gz sizes for `/` and the total; exit 1 > 180 kB), `tests/unit/bundle-budget.test.ts` (fixture manifest). *Gate:* `pnpm exec tsx scripts/bundle-budget.ts` → `first-load JS (/) = NN.N kB gz (budget 180)`; value recorded in the report.

**TSK-12 · `pnpm eval` orchestrator + provenance + baseline diff + CI**
- **S12.01 — Full orchestrator.** *Files:* `scripts/eval.ts` (A16 complete: layers, `--only` (comma list, maps to Vitest `-t`/Playwright `--grep`/LHCI skip), `--baseline <file>` (default `baseline-v1.json`), `--base-url` (skips `pnpm start`, adds header check), `--label`, `--skip-build`, no-overwrite, `regressions[]`/`improvements[]` computed per A16, exit codes: 0 clean · 1 critical FAIL · 2 regression · 3 runner error), `scripts/security-headers.ts` (curl-equivalent via `fetch` HEAD; expects the TP9 header set — `SKIP` when `--base-url` absent). *Gate:* `pnpm eval --only EVAL-006 --label smoke` writes `evals/results/smoke.json`; second identical call writes `smoke-2.json` (no overwrite); `pnpm eval --only EVAL-999` exits 3 with a clear message.
- **S12.02 — CI workflow.** *Files:* `.github/workflows/eval.yml` (Ubuntu, pnpm 11, Node 26, cache pnpm store, `pnpm install --frozen-lockfile`, `pnpm typecheck && pnpm lint && pnpm test`, `pnpm exec playwright install --with-deps chromium`, `pnpm build`, `pnpm eval --label ci-${{ github.sha }}`, upload `evals/results/ci-*.json`, `playwright-report/`, `.lighthouseci/` as artifacts; `concurrency` cancel-in-progress). *Gate:* `pnpm dlx action-validator .github/workflows/eval.yml` (or `actionlint`) passes; workflow runs green on the M-002 PR once the GitHub remote exists (else recorded as "CI pending remote" in the report — not claimed).
- **S12.03 — `docs/eval.md`.** *Files:* `docs/eval.md` (how to run, flags, where results go, result schema, how to add a case (edit `eval-cases.json` → add a spec with the `@EVAL-` tag → map in `scripts/eval.ts`), the manual-case protocol, the external-link WARN policy, the "never overwrite / never lower thresholds" rules). *Gate:* every flag in `scripts/eval.ts --help` appears in the doc (a unit test greps both).
- **S12.04 — First full run vs baseline.** *Gate:* `pnpm eval` → `evals/results/eval-run-0.1.0-<sha>.json` with `totals` (13 automated evaluated or SKIP with reason, 4 MANUAL), `regressions: []` against `baseline-v1.json`; file committed; `git diff --stat` matches TKT-07 files.

#### TKT-08 · Sanitised resume PDF + PII gate — *cheap tier for the test; Tushar supplies the file*

- **S08r.01 — PII test against a scratch path.** *Files:* `tests/unit/resume-pii.test.ts` (reads `process.env.RESUME_PATH ?? 'public/resume.pdf'`; skips with a loud `SKIP` when the file is absent **and** `resumeAvailable === false`; fails when the file is absent and the flag is `true`; runs `pdftotext -layout` (spawnSync; fails closed if the binary is missing while a file exists), asserts: no DOB pattern, no `+91`/10-digit phone, no street-address keywords (`Road|Street|Nagar|Layout|Apartment|Flat No`), contains `429867`, does not contain `044152784`, contains "Senior Product Manager" **or** the mismatch is recorded in `decisions.md` (test reads for `EXE-` block containing "resume title")). *Gate:* `RESUME_PATH="/Volumes/E Drive/Dev/.scratch/portfolio-clay/resume-candidate.pdf" pnpm test -t resume-pii` → PASS on Tushar's sanitised export (FAIL on the current PDF — confirmed once and recorded, the current PDF is never copied into the repo).
- **S08r.02 — Commit the PDF + flip the flag.** *Files:* `public/resume.pdf` (≤2 MB), `.gitignore` (remove the `public/resume.pdf` line), `lib/site.ts` (`resumeAvailable: true`). *Gate:* `pnpm test -t resume-pii` PASS without `RESUME_PATH`; `stat -f %z public/resume.pdf` ≤ 2097152.
- **S08r.03 — Both control states verified.** *Files:* `tests/e2e/resume.spec.ts` (`@EVAL-002 @EVAL-011`: every `a[data-resume]` → `href="/resume.pdf"`, `download` attribute, `HEAD` 200 `application/pdf`; text "Resume — updating" absent site-wide). *Gate:* green at 390 & 1440; `pnpm predeploy` (once it exists, TKT-50) passes.
- **S08r.04 — Decision + regression.** *Files:* `decisions.md` (`EXE-n · Resume title — updated PDF | accepted mismatch`). *Gate:* `pnpm eval --only EVAL-002,EVAL-013,EVAL-016` no regression.

---

### M-003 · Home complete

#### TKT-09 · Ask knowledge data + `AnswerProvider` + `LocalKnowledgeProvider` + coverage tests — *most-capable tier (S09.02, S09.05); cheap tier for transcription (S09.03); standard otherwise*

- **S09.01 — Adapter contract.** *Files:* `lib/ask/adapter.ts` (A4 types + a zod `AnswerSchema` mirroring `Answer` for the RAG boundary), `tests/unit/ask-adapter.test.ts` (schema accepts both `Answer` kinds, rejects an answer without evidence). *Gate:* `UNIT`.
- **S09.02 — Normalisation + synonyms.** *Files:* `lib/ask/normalise.ts` (`normalise(q): { tokens: string[]; bigrams: string[]; raw: string }` per A4 step 1; phrase joiner list), `lib/ask/synonyms.ts` (A4 table as `Record<canonical, string[]>` + `canonicalise(token)`), `tests/unit/ask-normalise.test.ts` (case/punctuation/stopwords; "What products have you built?" → `['built']`; "american express" → `['enterprise']`; "phone number" → `[]`). *Gate:* `UNIT` ≥ 8 cases.
- **S09.03 — Transcribe the 8 KnowledgeIndex entries.** *Files:* `data/knowledge.ts` (8 `KnowledgeEntry` objects: `prompt`/`answer` **verbatim** from CONTENT_INVENTORY §9, except "Ten years" → "7+ years" (the §9 note) and evidence `/work?tab=enterprise` → `/work?filter=enterprise` (E-2); `evidence` labels as in §9 (`View RailCite →` style labels per Design.md); `sources` from the §9 status column; `draft:true`; `keywords` with weights and `aliases` (2–3 paraphrases each); `surface`: home = `built`, `discovery`, `ai-products`, `impact`, `most-technical` (5); panel = the remaining 3 + the 3 new ones (6)). *Gate:* `pnpm exec tsx scripts/validate-content.ts` → `knowledge:8`; diff of each `answer` against a copy of the §9 cell = identical apart from the two sanctioned edits (reviewer trace in the report).
- **S09.04 — Author the 3 PB3 entries from VERIFIED rows.** *Files:* `data/knowledge.ts` (+3: "What did you learn when an assumption failed?" (TeachSpark `is_test` retro, Nuptis kill, RailCite threshold — §1.5/§8.1/§8.4/§8.2), "How do you evaluate an AI product?" (RailCite calibration + validator + refusal; TeachSpark QA gates + instrumentation — §8.2/§8.1), "What is your research background?" (patent IN 429867, Langmuir 2025, Soft Matter 2023 by title, co-inventor 2nd of 5 — §4.7/§8.8); each ≤3 sentences, ≥2 evidence links, `sources` listed, `draft:true`). *Gate:* `validate-content` → `knowledge:11`, `surface` counts `home:5 panel:6`; every number in the 3 answers appears in the cited pack line (report trace table).
- **S09.05 — `LocalKnowledgeProvider`.** *Files:* `lib/ask/local-provider.ts` (A4 steps 3–4; constructor `(entries: KnowledgeEntry[], opts?: { threshold?: number })`, `THRESHOLD = 0.34`, `suggestionsFor(surface, exclude)`), `lib/ask/index.ts` (`createDefaultProvider()`), `FALLBACK` constant. *Gate:* `UNIT`: exact prompt → score 1; paraphrase "which AI things did you build" → the AI-products entry; tie case with two entries equal → first in array.
- **S09.06 — `RagProvider` stub.** *Files:* `lib/ask/rag-provider.ts` (A4; not imported anywhere in `app/`/`components/`), `tests/unit/rag-provider.test.ts` (throws `AskError` when unconfigured; parses a valid mocked response; rejects a malformed one). *Gate:* `grep -rn "rag-provider" app components | wc -l` → `0`.
- **S09.07 — EVAL-012 suite.** *Files:* `tests/unit/eval-012.test.ts` (`@EVAL-012` in describe title: 11/11 prompts → `kind:'answer'`, `evidence.length ≥ 2`, `text === entry.answer` (byte-identical), `matched` = `[entry.id]`; each `alias` also resolves to its entry; 5 off-topic (`weather in Paris`, `write me a poem`, `what is your salary`, `phone number`, `lorem ipsum`) → `kind:'empty'` with `text === FALLBACK` and 3 suggestions; 200 random word-soup queries never return `kind:'answer'` with a score < threshold (property check); p95 `ask()` < 20 ms over 1 000 calls). *Gate:* `pnpm test -t EVAL-012` → all green; runtime printed.
- **S09.08 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-012,EVAL-013` → EVAL-012 `PASS` with `measured: {answered:11, empty:5, fabricated:0}`; `git diff --stat` matches.

#### TKT-10 · `AskPortfolio` inline field + `useAsk` + views — *most-capable tier (S10.01, S10.04); standard otherwise*

- **S10.01 — `AskProvider` + `useAsk`.** *Files:* `components/ai/AskProvider.tsx` (`"use client"`; context `{ provider, panelOpen, openPanel(), closePanel(), triggerRef }`; `useAsk(surface)` state machine per A4 with the 150 ms skeleton floor, `AbortController`-free (pure provider), `submit/reset/retry`; `provider` prop defaults to `createDefaultProvider()` — tests inject mocks), `tests/unit/use-ask.test.tsx` (idle→loading→answer; loading lasts ≥150 ms with an instant provider (fake timers); provider throw → `error` → `retry()` re-submits; `reset()` → idle). *Gate:* `UNIT`.
- **S10.02 — `SuggestedPrompts` + `EvidenceLinks`.** *Files:* `components/ai/SuggestedPrompts.tsx` (`ClayPill variant="filter"` buttons in a `ul`, wraps to 2 rows <768, `aria-label="Suggested questions"`), `components/ai/EvidenceLinks.tsx` (`ClayPill variant="link"` per evidence, trailing arrow, `next/link` for internal, `ExternalLink` semantics for https). *Gate:* jsdom: 5 prompts render 5 buttons; evidence with `https://` gets `rel="noopener"`.
- **S10.03 — `AnswerView` (answer / empty / error).** *Files:* `components/ai/AnswerView.tsx` (`answer`: `h3` "Answer" (focus target, `tabIndex=-1`) + `p` ≤65ch + `EvidenceLinks` + ghost "Ask another"; `empty`: FALLBACK text + 3 fresh prompts; `error`: `ink` text on `blush` flat surface + `AlertTriangle` icon + "Try again"; microcopy line **"Answers come from this portfolio's content — nothing generated."** in idle and answer states (S7); skeleton: 2 shimmer lines, `aria-busy`, `role="status"` "Looking through the portfolio…"). *Gate:* jsdom renders all four sub-states from props; the S7 microcopy exists in idle + answer, not in error.
- **S10.04 — `AskPortfolio` card with in-place expansion.** *Files:* `components/ai/AskPortfolio.tsx` (`"use client"`; `ClayCard tier card` max-w 720 centred; `LazyMotionRoot` + `m.div layout transition={springs.askExpand}` around the card body; `form` with `input` (56–64 px, placeholder "Ask about my work…", `aria-label`), submit button 44×44; tone switches `neutral → lavender` when not idle; `onSubmit` → `preventDefault()` (never navigates), `submit(q)`, then `requestAnimationFrame(() => answerHeading.focus())`; reduced motion: `layout={false}`, content `opacity` 150 ms; `min-h-[240px]` when expanded), `app/page.tsx` (mount below Hero inside `Section` with `id="ask"`; passes `prompts` from `knowledge.filter(surface home)`). *Gate:* Playwright `w1440`: type + Enter → URL unchanged, `document.activeElement` is the answer heading, page still scrolls (`window.scrollY` changes on `mouse.wheel`), card height grows from ~64 to ≥240 px.
- **S10.05 — Dev fixture route for the 5 states.** *Files:* `app/dev/ask/page.tsx` (guarded; `?mode=answer|empty|error|slow` mounts `AskProvider` with a mock provider: `error` throws, `slow` resolves after 2 s, `empty` returns empty, default local). *Gate:* `ALLOW_DEV_ROUTES=1 pnpm start` → each mode renders its state; `SHOT(ask/{idle,loading,answer,empty,error}-{390,1440})` (10 PNGs into `docs/screenshots/ask/`).
- **S10.06 — Playwright spec.** *Files:* `tests/e2e/ask-inline.spec.ts` (`@EVAL-007 @EVAL-008 @EVAL-010 @EVAL-011 @EVAL-012`: 5 states via `/dev/ask`; keyboard order field → pills → answer → evidence → Ask another; skeleton visible ≥150 ms (`slow` mode timing + instant mode using `page.clock`); pills wrap to 2 rows at 390 (`y` of 5th pill > 1st); `noOverflow`; axe 390/1440 in idle + answer). *Gate:* green.
- **S10.07 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-006,EVAL-007,EVAL-008,EVAL-010,EVAL-011,EVAL-012` no regression; `bundle-budget` value recorded (delta vs baseline in the report).

#### TKT-11 · `AskPanel` drawer / bottom sheet + `AskAIButton` wiring — *most-capable tier (S11.01–S11.03); standard otherwise*

- **S11.01 — Panel shell + lazy mount.** *Files:* `components/ai/AskPanel.tsx` (`"use client"`; `<dialog>` positioned `fixed inset-y-4 right-4 w-[400px] 2xl:w-[480px]` (768–1023 also 400 px — D3 clarification, E-8), `role="dialog" aria-modal="true" aria-labelledby`; header "Ask AI" + 44×44 close; body reuses `useAsk('panel')` + `SuggestedPrompts` (6) + `AnswerView`; input pinned to the panel bottom; scrim `fixed inset-0 bg-ink/20`; slide-in `transform` 320 ms `var(--ease-panel)` from `translateX(100%)`; reduced motion: instant, scrim opacity 150 ms), `components/ai/AskPanelLazy.tsx` (`next/dynamic(() => import('./AskPanel'), { ssr:false })` mounted by `AskProvider` only after the first `openPanel()`), `app/layout.tsx` (`AskProvider` wraps children; `AskPanelLazy` mounted once). *Gate:* `bundle-budget` for `/` unchanged within 2 kB after this step (panel chunk is separate — `.next/static/chunks` shows a new chunk loaded only on open, verified via Playwright `page.on('request')`).
- **S11.02 — Focus trap, inert, scroll lock, return focus.** *Files:* `components/ai/AskPanel.tsx` (`showModal()` gives the trap; additionally `#main`, `header`, `footer` get `inert` while open; `html { overflow:hidden }` via a class; `Esc`, scrim click, close button → `close()` → `triggerRef.current?.focus()`), `lib/focus.ts` (helper `withInert(elements, fn)`). *Gate:* Playwright `w1440`: open → `Tab` ×20 never leaves the dialog; `Escape` → focus on the header button; `document.body.scrollHeight` scroll attempt while open does not change `scrollY`; content behind is visible (screenshot shows the page at 80 % brightness, not blank).
- **S11.03 — Bottom sheet <768.** *Files:* `components/ai/AskPanel.tsx` (`max-md:inset-x-0 max-md:bottom-0 max-md:top-auto max-md:h-[90vh] max-md:w-auto max-md:rounded-t-[var(--radius-clay)]`, slide from `translateY(100%)`, `padding-bottom: env(safe-area-inset-bottom)`, input in the lower half). *Gate:* Playwright `w390`: dialog `top` ≥ 10 % of viewport height; input `y` > viewport/2; `noOverflow`; axe open.
- **S11.04 — Wire the triggers + panel prompts.** *Files:* `components/navigation/AskAIButton.tsx` (remove disabled state; `onClick={openPanel}`; `aria-haspopup="dialog" aria-expanded`), `components/navigation/MobileMenu.tsx` (its Ask action closes the menu then opens the panel — focus return target becomes the hamburger), `tests/e2e/crawler-allowlist.json` (remove `ask-ai-disabled`), `data/knowledge.ts` (confirm `surface:['panel']` on 6). *Gate:* crawler: header Ask button now classified `opens dialog` (not allowlisted); panel shows 6 prompts, none duplicating the home 5.
- **S11.05 — Keyboard-only script + axe.** *Files:* `tests/e2e/eval-007.spec.ts` (un-`fixme` the Ask section: open via keyboard → type → Enter → read answer (`role=status` then heading focused) → Tab to first evidence → Enter → new route → panel closed → focus sane; run at 390 and 1440), `tests/e2e/ask-panel.spec.ts` (widths per breakpoint: 400 at 1024, 480 at 1440, sheet at 390; `ctx.route` is passed — spy via `/dev/ask?mode=echo` mock that returns the ctx in `details`). *Gate:* green at both widths; axe 0 critical/serious with the panel open.
- **S11.06 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-005,EVAL-006,EVAL-007,EVAL-010,EVAL-011` no regression; JS budget for `/` still ≤180 kB (value in report).

#### TKT-12 · Featured Work (3 data-driven cards) + generalised transition — *standard tier; cheap tier for S12.01 transcription*

- **S12.01 — RailCite + Velora records at card fidelity.** *Files:* `data/projects.ts` (RailCite: tagline §1.4, tags `AI · RAG · GovTech`, `status:'live', statusLabel:'Live'`, `statusAsOf:'2026-09-15'`, `featured:2`, `gridSize:'medium'`, filters `['ai','cloud']`, live URL, `repoPublic:false`, sources ≥2; Velora: name "Nuptis → Velora" (display) with `slug:'velora'`, tagline "Two vendor-onboarding products in nine days — and the decision to kill the first.", tags `B2B · Marketplace · PM craft`, `status:'live', statusLabel:'Live (mock data)'`, `featured:3`, `gridSize:'medium'`, filters `['enterprise','experiments']`, live URL, sources ≥2; both with empty chapters/thinking, `deepDive:false`, `thirtySecond` = the pack's one-line problem). *Gate:* `validate-content` → `projects:3`, featured `1,2,3`, one large.
- **S12.02 — `FeaturedWork` + generalised `ProjectCard`.** *Files:* `components/projects/FeaturedWork.tsx` (server; `projects.filter(p => p.featured).sort()`; `Section id="work-featured"` + `SectionHeading` "Featured work"; grid `lg:grid-cols-3 gap-8`, stacked `gap-6` below; equal heights via grid), `components/projects/ProjectCard.tsx` (VT names from `slug`; TeachSpark status caveat rendered from `statusLabel` — no special-casing). *Gate:* Playwright: exactly 3 cards in DOM order TeachSpark, RailCite, Nuptis → Velora; card heights equal at 1440 (±1 px).
- **S12.03 — Stub routes for the new slugs.** *Files:* `app/work/[slug]/page.tsx` (`generateStaticParams` now maps `projects.filter(personal)`; the stub renders `CaseStudyHeader` from data for any slug). *Gate:* `BUILD` → `all routes static` includes `/work/railcite`, `/work/velora`; each 200.
- **S12.04 — Transition + fallback for all three.** *Files:* `tests/e2e/featured.spec.ts` (`@EVAL-002 @EVAL-011 @EVAL-015`: click each card → correct slug; VT names present on card + header (`style` attribute or `data-vt`); `noViewTransitions` + `reducedMotion` variants identical end state; hover/press/focus states; reduced motion removes the rise). *Gate:* green at 390 & 1440.
- **S12.05 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-001,EVAL-002,EVAL-011,EVAL-013,EVAL-015` (001 MANUAL slot recorded) no regression; `SHOT(home/featured-{390,1440})`.

#### TKT-13 · How I Think module — *standard tier; cheap tier for S13.01 transcription*

- **S13.01 — Stage data + tone map.** *Files:* `data/thinking-framework.ts` (6 `ThinkingStageDef`: Problem→lavender?, — **tones per Design.md §3**: Insight→butter, Build→peach, Evaluate→mint, Impact→mint, Problem→sky, Bet→lavender (the two unspecified stages take the remaining calm tones; recorded in the file header); `principle` one line each (DRAFT copy, flagged); `example` quote/attribution/project/href **verbatim** CONTENT_INVENTORY §1.5 with anchors `#02-problem`, `#03-discovery`, `#04-product-bet`, `#05-what-i-built`, `#06-evaluation`, `#07-outcome`; Insight attributed "Week 4 team research"), `lib/stages.ts` (`stageTone` export reused by artifacts/badges). *Gate:* `validate-content` → `thinking:6`, all 6 hrefs resolve via `routes()`.
- **S13.02 — `HowIThink` component.** *Files:* `components/home/HowIThink.tsx` (`"use client"`; `ol` of 6 `ClayTile size 160 tier utility` buttons (`aria-expanded`, `aria-controls`), connector line (`::before` 1 px `ink-3` beneath the row ≥1024, down the left edge <1024), roving `tabindex` with ArrowLeft/Right/Up/Down, `Escape` closes, outside click closes (`pointerdown` on document); hover emphasises the principle (`text-ink` from `text-ink-2`, 200 ms, `@media (hover:hover)` only); one open at a time). *Gate:* jsdom: arrow keys move focus; `Escape` collapses and keeps focus on the tile.
- **S13.03 — Expanded card.** *Files:* `components/home/HowIThink.tsx` (expansion region `ClayCard tier card tone={stage.tone}` below the row: quote + attribution + `ClayPill variant="link"` "See how I tested this in {Project} →" → `example.href`; height change via `m.div layout` (springs.filter) — instant under reduced motion; the module is the section's single accent — `Section` has no `tone`). *Gate:* Playwright: opening a second stage closes the first; link `href` equals the data href; following content shifts down (next section's `y` increases).
- **S13.04 — Spec.** *Files:* `tests/e2e/how-i-think.spec.ts` (`@EVAL-007 @EVAL-010 @EVAL-011 @EVAL-013`: keyboard path, reduced motion (no `transform` animation), links resolve (route root now, anchors later — crawler treats `#anchor` on a not-yet-built page as `WARN(anchor pending)` until TKT-19 lands, then `FAIL`), `noOverflow` at 390, axe). *Gate:* green.
- **S13.05 — Regression + wrap.** *Gate:* `pnpm eval --only EVAL-006,EVAL-007,EVAL-010,EVAL-011,EVAL-013` no regression; `SHOT(home/how-i-think-{390,1440})`.

#### TKT-14 · Final CTA + home assembly + home OG + 5-second test pack — *standard tier; most-capable for S14.04 scoring*

- **S14.01 — `CopyButton` complete + `FinalCTA`.** *Files:* `components/common/CopyButton.tsx` (`navigator.clipboard.writeText` → `copied` 2 s with a 160 ms icon morph `Copy → Check`, `role="status"` toast "Copied"; catch → `error`: renders the email as selectable text `<output>` + "Select to copy"; `console.warn('[copy]', err)`), `components/home/FinalCTA.tsx` (server + client button; "Building something AI-native? Let's talk." + `CopyButton value={site.email}` + "Let's Talk" → `/contact` + resume control), `tests/unit/copy-button.test.tsx` (clipboard mocked resolved/rejected). *Gate:* `UNIT`; Playwright grants `clipboard-write` and asserts the toast; a second context denies it and asserts the fallback text.
- **S14.02 — Assemble `app/page.tsx` in the fixed order.** *Files:* `app/page.tsx` (Hero → `Section#ask` AskPortfolio → `Section#work-featured` FeaturedWork → `Section#how-i-think` HowIThink → `Section#cta` FinalCTA; `Reveal` wraps each section's heading + content with stagger; Footer from layout). *Gate:* Playwright: DOM order of the 5 landmarks/sections matches; section vertical gaps 72/96/128 at 390/768/1440 (computed margins); at 390 the visual order is avatar → headline → CTAs → tiles → Ask → projects → How I Think → CTA (assert `boundingBox().y` monotonic).
- **S14.03 — Accent discipline + home OG copy check.** *Files:* `app/opengraph-image.tsx` (headline copy from `data/hero.ts`), `tests/e2e/home.spec.ts` (`@EVAL-008 @EVAL-011 @EVAL-017`). *Gate:* per-section screenshot at 1440 shows ≤1 tinted tone family (orchestrator eyeballs `docs/screenshots/home/sections-1440.png` — automated proxy: count distinct `data-tone` values per `section` ≤ 1); OG tag test green for `/`.
- **S14.04 — EVAL-001 review pack.** *Files:* `docs/screenshots/home/{390,1440}-first-viewport.png` (full-page too), `evals/results/eval-001-<sha>.md` (6-item checklist × 2 widths scored by Claude with the pixel evidence — each item cites the element and its `y` < viewport height; DRAFT copy list; presented to Tushar). *Gate:* file committed; Claude's score is 6/6 at both widths or the report names the failing item and the fix ticket.
- **S14.05 — Budgets on `/`.** *Gate:* `pnpm eval --only EVAL-004,EVAL-005` → mobile+desktop ≥90/95/95/95 on `/`, JS ≤180 kB gz, LCP ≤2.5 s, CLS <0.05 (median of 3); if JS fails, first lever = confirm `AskPanel` is a separate chunk and `LazyMotion` is in use (A6), never remove a section.
- **S14.06 — Full regression + phase hand-off.** *Gate:* `pnpm eval` (full) → no Critical FAIL, no regression; `evals/results/eval-run-0.3.0-<sha>.json` committed; `git diff main..HEAD --stat` reviewed; M-003 QA gate (§C) dispatched.

---

### M-004 · Work page & case-study system — *ticket-level plans (atomised at the M-003 → M-004 boundary)*

> Living-plan note: the steps below are deliberately at ticket granularity. At the M-004 phase boundary the orchestrator atomises each ticket into `S15.xx…S21.xx` steps in the same format as §B M-001–M-003, using the then-real codebase (primitive APIs, `useAsk`, anchors, eval runner) — not this document's assumptions.

| Ticket | Approach | Files (new/changed) | Interfaces / contracts | Verification | Risks |
|---|---|---|---|---|---|
| **TKT-15** Expand `data/projects.ts` to 14 (Task, sp:3, cheap tier) | Transcribe 8 personal + 3 professional records at card fidelity from CONTENT_INVENTORY §2.2/§2.3 + §8 headers; chapters empty, `deepDive:false`, `thinking:[]`; filters per SITEMAP mapping (Token Toli/Pratyasa/Bhakti-Vilas → `experiments` default, `EXE-n` when Tushar answers); `repoPublic:true` only cinematic + dino-arcade | `data/projects.ts`, `tests/unit/schema.test.ts` (+ per-record trace comments) | `Project` schema; `gridSize` = large teachspark / medium railcite, velora / small rest; professional: no live/demo/featured, tags per §2.3 ("Platform") | `validate-content` → `projects:14`; `generateStaticParams` → 11; BUILD; `pnpm eval --only EVAL-013` | drift into paraphrase → reviewer trace table per record |
| **TKT-16** `/work` page (Feature, sp:5; TSK-13/14/15; most-capable for TSK-13/14) | `WorkHero` flat; `FilterTabs` (client) `role=tablist`, `?filter=` via `useSearchParams` in Suspense (TP7), `LayoutGroup` indicator `springs.filter`; `WorkGrid` (client wrapper) receives all personal cards as server-rendered children keyed by slug and filters with `AnimatePresence` (fade 150/200; opacity-only reduced); `EditorialGrid` CSS grid 12-col: `[0]` `col-span-8 row-span-2`, `[1..2]` `col-span-4`, rest `col-span-4` in 3-up; 768–1023 large full + 2-up; <768 single column, large media 4:3; `EmptyState` + "Show all"; FilterTabs <768 `overflow-x-auto snap-x` with 16–24 px peek via `scroll-padding` and a trailing `pr-6` | `app/work/page.tsx`, `components/projects/{WorkHero,FilterTabs,WorkGrid,EditorialGrid,EmptyState,ProjectCard(grid mode)}.tsx`, `lib/filters.ts`, `app/work/opengraph-image.tsx` (exists), `tests/e2e/work.spec.ts` | `lib/filters.ts`: `FILTERS`, `applyFilter(projects, f)`, `parseFilter(param)`; grid mode card = featured anatomy + `DemoVideo` slot (TKT-18) | Playwright per-filter slug sets = SITEMAP; deep link `/work?filter=ai` → filtered after hydration; back/forward restores; arrow keys move tabs; peek visible at 390 (last tab partially in view, page `noOverflow`); Lighthouse `/work` ≥90/95/95/95; axe | one-frame flash on deep link (TP7, accepted); grid math at 768–1023 → screenshot gate |
| **TKT-17** `ExperienceStrip` (Feature, sp:2, standard) | Flat bordered strip under the grid, label "Professional experience — corporate work, not a public product."; rows (title · company · dates · tags) with `aria-expanded` inline summary, one open; "See my experience →" `/about#experience`; strip rows respond to the active filter | `components/projects/ExperienceStrip.tsx`, `app/work/page.tsx`, `tests/e2e/work.spec.ts` | reads `projects.filter(professional)`; no `DemoVideo`, no VT, no external link | Playwright: no `a[target]`/arrow inside the strip; expand/collapse keyboard; filter Enterprise shows 3 rows, AI shows mars only; axe | none material |
| **TKT-18** `DemoVideo` (Feature, sp:3, most-capable) | Poster `next/image` + 56×56 play `ClayButton`; `<video>` mounted only on intent (click, or IO 50 % + `pointer:fine`), `preload="none" muted playsInline controls`; states `no-video / loading / playing / error`; error overlay → live URL or "Demo coming"; duration caption | `components/projects/DemoVideo.tsx`, `app/dev/video/page.tsx` (fixtures: missing src / 404 src / throttled / valid), `tests/e2e/eval-014.spec.ts` | props `{ video?: Project['links']['demoVideo']; poster fallback; liveUrl?; name }`; emits `console.warn('[video]')` on error | EVAL-014 four states screenshot-tested via `/dev/video`; no `<video>` before intent; play button `aria-label="Play demo: {name}"`; Lighthouse `/work` unaffected | IO-triggered mount on desktop vs Lighthouse → poster remains LCP; verify |
| **TKT-19** Case-study shell (Feature, sp:5; TSK-16/17/18; most-capable for TSK-16/17) | Replace stub: `CaseStudyHeader` 60/40 with 2–3 inline `MetricCard`s; hero media = image / `DemoVideo` / labelled placeholder inside `ClayFrame` (VT target); `OverviewToggle` (`role=radiogroup`, 30-sec default, crossfade 200 ms + layout; hidden when `deepDive:false`); `ChapterNav` sticky rail ≥1024 / pill row <1024 (IO active state); `Chapter` flat + `Prose` + `ArtifactRenderer` (1/2/3-up inside the column); empty chapters omitted + "Deep dive coming — documented as {statusLabel}" note; `NextProject` band; `ProgressBar`; per-slug OG (exists) | `app/work/[slug]/page.tsx`, `components/case-study/{CaseStudyHeader,OverviewToggle,Chapter,ChapterNav,NextProject}.tsx`, `components/case-study/artifacts/MetricCard.tsx` (inline variant), `docs/anchors.md` (final), `tests/e2e/case-study.spec.ts` | anchors from `lib/anchors.ts` (`#01-context…#08-what-i-learned`; `#04-product-bet`, `#05-what-i-built` — E-3); `NextProject` order = `/work` grid order | all 11 slugs build + axe; metrics show value/label/context/`as of`/kind badge; VT + fallback from any card; `OverviewToggle` keyboard; anchors resolve (crawler upgrades `WARN(anchor pending)` → must be `PASS` now); prose ≤600 px at 768; Lighthouse `/work/teachspark` ≥90/95/95/95 | anchor ids are load-bearing for TKT-09/13 — fixed in `lib/anchors.ts`, not re-decided |
| **TKT-20** Artifact components (Feature, sp:5; TSK-19/20/21; standard) | One `ClayCard tier card` DNA; `ArtifactRenderer` maps `type → component`; shapes per Design.md §3; `MetricCard` type-level guard (props are `Metric`, so context/asOf/kind are mandatory); source caption renders `SourceRef.label` (+ link when `url`) — never `ref` | `components/case-study/artifacts/*.tsx`, `app/dev/artifacts/page.tsx` (TeachSpark/RailCite pack fixtures), `tests/unit/artifacts.test.tsx` | `ArtifactRenderer({ artifact, sources })` resolves `source` id → label; unknown type = TS error (exhaustive switch) | unit: renderer mapping, MetricCard refuses partial props (type test); `/dev/artifacts` screenshots 390/1440; axe; grep: no `/Volumes/` string in rendered HTML | artifact sprawl → props only, no per-type styling forks |
| **TKT-21** `ShowTheThinking` (Feature, sp:3, most-capable) | Toggle `ClayButton` (`aria-expanded aria-controls`); 8 `ThinkingNode`s in an `ol` present in DOM (`hidden` until open; a `VisuallyHidden` summary "8-step reasoning chain, expand to read" keeps AT discoverability — chosen pattern documented in the component header); reveal 220 ms/120 ms stagger via CSS `transition-delay: calc(var(--i) * 120ms)`; connector `clip-path: inset(0 0 100% 0) → inset(0)`; reduced motion → all visible, opacity only; hidden entirely when `thinking.length === 0`; vertical at every width (D4) | `components/interactions/{ShowTheThinking,ThinkingNode}.tsx`, `app/dev/thinking/page.tsx`, `tests/e2e/thinking.spec.ts` | props `{ chain: ThinkingChain; sources: SourceRef[] }`; node `href` optional link to the artifact anchor | keyboard: toggle → nodes' source links in order, focus stays on toggle after open; reduced-motion assertion; single column at 4 widths; axe; only `transform/opacity/clip-path` in the transition list (unit test greps the CSS) | none material |

**M-004 phase QA gate:** `pnpm eval` full; EVAL-002 hops `/ → /work → /work/[slug]` green; EVAL-014 four states; EVAL-015 VT fallback on 11 slugs; EVAL-004 on `/work` and `/work/teachspark`; screenshots `docs/screenshots/work/` and `case-study/` at 4 widths; human checkpoint: editorial grid read ("never nine identical rectangles") at 1440 and 768.

### M-005 · Case-study content (11) & media — *ticket-level plans (atomised at the M-004 → M-005 boundary)*

| Ticket | Approach | Files | Interfaces / contracts | Verification | Risks |
|---|---|---|---|---|---|
| **TKT-22** TeachSpark media (Chore, sp:2) | Verify Railway URL (date it); `scripts/encode-video.sh` on the 42.85 MB source with a 20–40 s trim chosen by viewing the file (A7 settings, target ≤3.75 MB); poster; screenshots via `git -C <teachspark> fetch origin && git show origin/main:docs/screenshots/<file> > content/media/teachspark/<file>` (no checkout into this tree); optimise to `public/media/teachspark/`; manual frame check for the sandbox join code | `public/video/teachspark.mp4`, `public/video/teachspark-poster.webp`, `content/media/teachspark/{*.webp,SOURCES.md}`, `public/media/teachspark/*`, `data/projects.ts` (`statusAsOf`, `statusLabel`, `links.demoVideo`) | `SOURCES.md` line per file: origin (URL/commit), date, viewport, "join code frame check: none visible" | `stat` ≤4 MB / poster ≤120 kB; `ffprobe` shows `yuv420p`, `faststart` (moov before mdat: `ffprobe -v trace 2>&1 \| grep -m1 moov`); plays in Chromium (Playwright `video.readyState ≥ 3` after play); EVAL-014 fixture swapped to the real file | Railway down → status copy "Pilot concluded (last verified 2026-09-09)", video from the 08-23 source |
| **TKT-23** RailCite media (Chore, sp:2) | Playwright `recordVideo` context at 1440 on `https://railcite.vercel.app`: a real cited query + a refusal; capture 4–6 screenshots at 1440 & 390; record `/api/stats` numbers with date; space queries ≥20 s apart (Voyage 3 RPM); encode + poster; no tokens/emails in frames (crop/blur check) | `public/video/railcite.mp4` (+poster), `content/media/railcite/*`, `SOURCES.md`, `data/projects.ts` | auth path: only a demo account; if none, document the blocker **and escalate before TKT-50** (PB4 — poster-only does not satisfy the deploy gate) | as TKT-22 + a frame-by-frame token scan note in `SOURCES.md` | auth blocks recording → Tushar decision, early |
| **TKT-24** Nuptis + Velora media (Chore, sp:2) | Two `recordVideo` sessions following the flows in the ticket; reuse existing screenshots (§8.4/§8.5) unless live UI diverged; encode + posters; captions state "mock data" | `public/video/{nuptis,velora}.mp4` (+posters), `content/media/{nuptis,velora}/SOURCES.md`, `data/projects.ts` | — | as TKT-22 | none |
| **TKT-25** Bhakti-Vilas media (Chore, sp:2) | Record the verified flow at 390 + 1440 with an obviously fake phone (`9999999999`) + OTP; 4–6 screenshots; encode + poster | `public/video/bhakti-vilas.mp4`, `content/media/bhakti-vilas/*`, `SOURCES.md` | — | as TKT-22 + grep of `SOURCES.md` for the fake number only | none |
| **TKT-26** Pratyasa · Tegaki · dino-arcade · cinematic media (Chore, sp:3) | Pratyasa: re-encode existing `demo.mp4`; Tegaki: record landing → how → pricing → sign-in screen (stop); dino-arcade: record cabinet UI, **no ROM**, + screenshot; cinematic: 20–30 s scroll recording | 4 × `public/video/*.mp4` (+posters), `content/media/*/SOURCES.md`, `content/media/dino-arcade/screenshot.webp` | — | as TKT-22; `SOURCES.md` states "no ROM/BIOS loaded", "no sign-in performed", "no checkout" | none |
| **TKT-27** Cubicle media (Chore, sp:2, conditional) | Only with a live URL from Tushar by 2026-09-16: record the real debate → artifacts run (label any speed-up), screenshots, then `featured` swap in data + `EXE-n`; else close N/A with date | `public/video/cubicle.mp4` or none; `data/projects.ts`; `decisions.md` | S3 swap rule is data (`featured` field) | media gates as above, or the N/A record | most likely N/A |
| **TKT-28…33, TKT-54** Content tickets (Task; P0 for 28/29) | One implementer per ticket (TKT-54: one per TSK-25…29 sequentially); brief = the exact §8 pack section + `docs/anchors.md` + the artifact component API; author `chapters[8]` (empty bodies where the pack is silent), artifacts by type, `metrics` with all six fields, `thinking` 8 nodes or `[]`, `learnings`, `sources`; render `deepDive` accordingly; TeachSpark uses one metric snapshot date (Tushar's decision, default 2026-08-24); RailCite figures "live as of <date>" (Tushar's policy, default recommended); every MISSING item omitted or a labelled placeholder | `data/projects.ts` (one record each), `content/media/<slug>/SOURCES.md`, `docs/trace/<slug>.md` (reviewer trace table: rendered sentence → pack line) | `Project` schema (no extensions without evidence — extend `schema.ts` only with an `EXE-n` note) | `validate-content`; `/work/<slug>` axe 390/1440; anchors resolve; `pnpm eval --only EVAL-011,EVAL-013,EVAL-014`; reviewer trace table complete; specific ACs from each ticket checked line by line (e.g. "625 tests" absent; "by construction" label; "team of 6; my named role: {pending}") | invention under pressure → trace table is a hard DoD; Lighthouse on `/work/teachspark` re-run after TKT-28 (real media + full content) |
| **TKT-39** EVAL-003 traceability (Docs, sp:1) | Walk the 8 product-leader questions (`test-cases.md` table) against the rendered TeachSpark/RailCite/Velora pages; record `URL#anchor` per question; gaps → sourced artifact or explicit QA finding | `test-cases.md` (table), `evals/results/eval-003-<sha>.md` | manual EVAL slot in the runner points at this file | 8/8 mapped; Tushar confirms | gaps with no sourced fix → logged, never invented |

**M-005 phase QA gate:** `pnpm eval` full incl. EVAL-013/014; EVAL-003 8/8; EVAL-004 on `/work/teachspark` with real media; truth review (trace tables for TeachSpark/RailCite/Velora read by the QA-tester agent); human checkpoint: Tushar reads the three featured case studies once.

### M-006 · About · Thinking · Playground · Contact · 404 — *ticket-level plans (atomised at the M-005 → M-006 boundary; may interleave with M-005 — disjoint files)*

| Ticket | Approach | Files | Interfaces / contracts | Verification | Risks |
|---|---|---|---|---|---|
| **TKT-40** About part 1 (Feature, sp:5; TSK-22/23/24) | `experience.ts` (4 roles; `scale:'not recorded'` where MISSING; outcomes `self-reported` unless measured), `skills.ts` (4 clusters, tones); `AboutHero` (flat Hero variant: headline per §4.1, bio without "AI Product Manager", avatar); `ProductJourney` (connector pattern, `Reveal` only, 4 stages; gap note only after Tushar's framing); `CapabilityClusters` 4 `ClayTile` (2×2 / 4×1); `Impact` = `MetricCard`s with kind labels per TKT-40 AC 2 | `data/{experience,skills}.ts`, `components/about/{AboutHero,CapabilityClusters,Impact}.tsx`, `components/timeline/ProductJourney.tsx`, `app/about/page.tsx`, `app/about/opengraph-image.tsx` | `Experience`/`SkillCluster` schemas; `Impact` consumes `Metric[]` from a small `data/impact.ts` (sources = `RESUME`, packs) | `validate-content`; forbidden strings (SAFe only as methodology); axe; Lighthouse `/about`; defaults for IntraEdge / "7+ years" applied and noted | wall-of-resume → `Prose` ≤600 px, tiles, MetricCards |
| **TKT-41** `ExperienceTimeline` (Feature, sp:5, most-capable) | Horizontal line + 4 `TimelineNode` buttons ≥1024; vertical <1024; `StoryCard` (`ClayCard card lavender`, 2-col dl ≥768) opens via `m.div layout` (springs.story), one at a time, `#experience-{id}` hash sync (`history.replaceState`), deep link opens on load; hover scale 1.1 + segment brighten; keyboard: Enter/Space, arrows, `Esc` returns focus | `components/timeline/{ExperienceTimeline,TimelineNode,StoryCard}.tsx`, `app/about/page.tsx` (`#experience` anchor), `tests/e2e/timeline.spec.ts` | props `{ roles: Experience[] }`; hash ids from `Experience.id` (`godrej`, `quantiphi`, `shellkode`, `amex`) | keyboard path (EVAL-007), reduced motion (EVAL-010), one open, deep link `/about#experience-amex`, vertical at 390/768, horizontal 1024/1440, axe | tablet layout animation → screenshot gate at 768 |
| **TKT-42** About part 2 + assembly (Feature, sp:2) | Awards (3, text), Research (patent IN 429867 + inventors + rights line; Langmuir 2025 DOI link; Soft Matter 2023 "DOI pending"), Education; assemble in SITEMAP order; "Let's talk" + resume | `components/about/{Awards,Research,Education}.tsx`, `app/about/page.tsx`, `data/about.ts` | text-only, `SourceRef` labels | copy verbatim §4.6–4.8; `429867` everywhere; Lighthouse `/about`; axe; OG tag test | none |
| **TKT-43** `/thinking` + essays (Feature, sp:3) | `writing.ts` 5 DRAFT essays (passages quoted + framing); `ThinkingHero`, `ThinkingList` (numbered rows, hover per Design.md), `EssayBody` (`Prose`, DRAFT `Tag` instead of date, related-project `ExternalLink` card), empty-state line above the list; sitemap picks up essay slugs automatically (S06.05) | `data/writing.ts`, `components/thinking/{ThinkingList,EssayBody}.tsx`, `app/thinking/page.tsx`, `app/thinking/[slug]/page.tsx`, `app/thinking/opengraph-image.tsx`, `app/thinking/[slug]/opengraph-image.tsx` | `Essay` schema (`draft:true` ⇒ no `publishedOn`) | DRAFT tag on rows and pages; no dates; ≤600 px measure; crawler; axe; OG | none |
| **TKT-44** `/playground` (Feature, sp:2) | Flat hero + 4 `ClayTile interactive` (butter/peach/blush/mint) from `projects` (slugs pratyasa, tegaki, dino-arcade, cinematic-portfolio), external new-tab links with the hidden note; deeper hover shadow permitted here | `app/playground/page.tsx`, `components/playground/PlaygroundGrid.tsx`, `app/playground/opengraph-image.tsx` | reads `Project.links.live` | 4 tiles, copy §6; ≥44×44; crawler external HEAD; axe; `noOverflow` | none |
| **TKT-45** `/contact` (Feature, sp:2) | `ContactCard` (`ClayCard tier hero tone lavender`, max 640): "Still curious?" → `CopyButton` → mailto → LinkedIn → resume control (`resumeAction()`), `#resume` section keeps the PB5 note; 2×2 ≥768 / stacked; city line "Bengaluru, India" | `app/contact/page.tsx` (replaces the tracer stub), `components/contact/ContactCard.tsx`, `app/contact/opengraph-image.tsx`, `tests/e2e/contact.spec.ts` | resume control both states (PB5) | copy §7, no phone; 4 controls ≥44 + 12 px gaps; CopyButton states incl. denied clipboard; `#resume` exists; axe; crawler | none |
| **TKT-46** 404 (Feature, sp:1) | `not-found.tsx` flat page, one clay tile, links `/`, `/work`, `/contact`; header/footer present via layout | `app/not-found.tsx`, `app/error.tsx`, `app/global-error.tsx` | — | `/nope` → 404 status + page; links resolve; axe 390/1440 | none |

**M-006 phase QA gate:** `pnpm eval` full; EVAL-002 complete path `/ → /work → /work/[slug] → /about → resume → /contact` ≤6 clicks (resume hop = placeholder link until TKT-08, recorded); EVAL-007 timeline + ShowTheThinking + AskPanel keyboard scripts; PII grep on repo + bundle; screenshots for all new routes at 4 widths.

### M-007 · Quality sweeps, deployment & production verification — *ticket-level plans (atomised at the M-006 → M-007 boundary)*

| Ticket | Approach | Files | Interfaces / contracts | Verification | Risks |
|---|---|---|---|---|---|
| **TKT-47** Responsive sweep (Task, sp:3, QA-tester agent) | Playwright screenshot pack for every route (11 slugs, 5 essays, all statics) × 4 widths; automated asserts overflow / <44 px / <14 px / missing intrinsic image sizes; manual review pass; fixes as small commits | `tests/e2e/sweep.spec.ts`, `docs/screenshots/<route>/<width>.png`, `QA-###` entries in `test-cases.md` | uses `routes.json`/sitemap loader | `pnpm eval --only EVAL-008` green; pack committed | none |
| **TKT-48** Accessibility sweep (Task, sp:3, QA-tester agent) | axe on every route × {390,1440}; full keyboard scripts (all flows listed in the ticket); reduced-motion run; VoiceOver pass on `/`, `/work/teachspark`, `/about` (manual notes); contrast audit of every token pairing actually used (`culori` WCAG contrast over the pairs enumerated from `/dev/primitives`) | `tests/e2e/eval-006.spec.ts` (all routes), `docs/a11y-pass.md`, `scripts/contrast-audit.ts` | — | EVAL-006/007/010 green; notes persisted | none |
| **TKT-49** Performance pass (Task, sp:3, most-capable) | Measure first (`pnpm eval --only EVAL-004,EVAL-005`, 3-run median, before JSON kept); fix largest offenders in order: font subsetting (`next/font` `subsets:['latin']`, weights actually used), image `sizes`/`priority`, chunking (`AskPanel`, `motion` features), shadow paint on long pages (`contain: paint` on cards), posters ≤120 kB; after JSON | `evals/results/perf-before-<sha>.json`, `perf-after-<sha>.json`, touched components | never lowers thresholds (EV2) | ≥90/95/95/95 mobile+desktop on 4 routes; `/` JS ≤180 kB, LCP ≤2.5 s, CLS <0.05 | Vercel-vs-local variance → record run repeats on the preview URL (TKT-50) |
| **TKT-50** Vercel project + preview + headers + Analytics + deploy guard (Chore, sp:2) | Tushar creates/approves private GitHub repo `007U5H4R/portfolio-clay`; `git remote add` + push; create Vercel project `portfolio-clay` via the Vercel MCP `create_git_project` (or `npx vercel link`) **after confirming team/project scope with Tushar**; `headers()` per TP9; `@vercel/analytics` + Speed Insights; `scripts/predeploy-check.ts` wired into `prebuild` (PB4/PB5 modes per A11) with a Vitest case per failure mode; `pnpm audit` gate; deploy preview; `pnpm eval --base-url <preview>` | `next.config.ts` (headers), `app/layout.tsx` (analytics), `scripts/predeploy-check.ts`, `tests/unit/predeploy.test.ts`, `docs/deploy.md` (rollback path, env, project ids), `vercel.json` (only if needed for `cleanUrls`/`trailingSlash` — default none) | env `NEXT_PUBLIC_SITE_URL` unset on preview (fallback chain), set on production | preview URL: every route + sitemap + robots 200; resume: 200 or placeholder per flag; `curl -sI` headers persisted in `docs/deploy.md`; audit 0 high/critical; PII grep on the deployed bundle 0; 3 featured videos play from preview; `eval-run-preview-<sha>.json` | featured videos missing → hard block (PB4); wrong Vercel scope → confirm before linking (blast radius) |
| **TKT-51** Link-preview validation (Task, sp:2) | For 7 families on the preview: tag test + opengraph.xyz + LinkedIn Post Inspector (human; Tushar logs in if needed); fix sizing/absolute URL issues; produce the re-validation checklist for TKT-53 | `docs/og/<family>-{opengraph,linkedin}.png`, `docs/og/checklist.md` | — | 7/7 render title + description + image on both; tag test green | LinkedIn caching → use `?v=` cache-buster during iteration |
| **TKT-52** Hand-off to review (Task, sp:2) | Full `pnpm eval --base-url <preview> --label eval-run-v1.0.0-rc`; Stage-8 pack (screenshots + `/dev/primitives`/`/dev/artifacts` boards rendered with `ALLOW_DEV_ROUTES=1` locally); Stage-9 pack (diff stats, results, open `QA-###`); Stage-10 inputs (headers, audit, PII grep); update `tickets.md` statuses, `HANDOFF.md`, Obsidian + auto-memory; Session-Clearing block | `evals/results/eval-run-v1.0.0-rc.json`, `HANDOFF.md`, `tickets.md`, `docs/handoff/stage-8-pack.md` | — | every Critical PASS, no High unaddressed (or parked with reason); provenance complete; `git diff main..HEAD --stat` reviewed | none |
| **TKT-53** Production deployment + verification + monitoring (Chore, sp:3) | **Only after Stage 8–10 approval + `QA-report.md` = READY and TKT-08 done**; attach Tushar's domain; set `NEXT_PUBLIC_SITE_URL`; promote; verify evidence-first: `pnpm eval --base-url https://<domain> --label eval-run-v1.0.0`; EVAL-002 on production; OG re-validation (TKT-51 checklist); Analytics receiving; rollback exercised once (redeploy previous) or documented one-click; weekly `pnpm eval` cron proposed (enabled or deferred); release provenance in `HANDOFF.md` + `decisions.md` `EXE-n`; confirm the cinematic site + `portfolio/index.html` untouched (`git -C <those repos> status`, live URLs unchanged) | `evals/results/eval-run-v1.0.0.json`, `HANDOFF.md`, `decisions.md`, `.github/workflows/eval-cron.yml` (if enabled), `docs/deploy.md` | release provenance = commit, build id, domain, date, eval run id | all TKT-53 ACs; externally visible → Tushar approves promotion explicitly | resume still unsanitised → hard block (PB5); OG absolute URLs change with the domain → re-validate |

---

## C. Sequencing & phases

Phase = milestone. The PWA Gantt (Campfire, onboarded in 6.2/6.3) renders the same DAG from `tickets.md` Appendix A; this section is the narrative the orchestrator reads at each boundary. Every phase ends with a **QA-tester subagent** (separate from the implementers, standard-or-capable model) executing the phase's `test-cases.md` cases + the listed EVAL ids, reporting PASS/FAIL/BLOCKED/N-A per case, then a **human checkpoint** with Tushar, then the `HANDOFF.md` rewrite + Session-Clearing block. No phase starts on its own initiative.

| Phase | Entry criteria | Tickets in DAG order (parallel lanes in braces) | Phase QA gate (EVAL ids) | Human checkpoints |
|---|---|---|---|---|
| **1 · M-001** Tracer bullet | Stage 6 complete (this file + `test-cases.md` + `evals/eval-cases.json`); PWA onboarded; repo initialised (S01.01); worktree `feat/m001-tracer` | TKT-01 (TSK-01 → {TSK-02, TSK-03} → {TSK-04, TSK-05, TSK-06} → TSK-07) → TKT-02 | EVAL-006, 008, 010, 015 PASS on the tracer; EVAL-004/005 measured (informational); `baseline-v1.json` persisted; 8 screenshots + edge shot | **Tracer visual gate (TKT-02):** Tushar sees `/` + `/work/teachspark` live at ≥2 widths; EVAL-001 6×2 scored; EVAL-009 hero rubric; **avatar likeness confirmed on the rendered page** (hard gate); DRAFT copy acknowledged; written "approved / approved with changes" → `EXE-1` |
| **2 · M-002** Foundations & harness | TKT-01 merged (for TKT-03/07/08 — PB1); TKT-02 approved with token file locked (for TKT-04/05/06); Tushar's `forbidden.local.json` value supplied (or SKIP recorded) | Lane A (starts on TKT-01 merge): TKT-03 → TKT-07 (TSK-08 → TSK-09 → {TSK-10, TSK-11} → TSK-12) · Lane B (starts on TKT-02 approval): TKT-04 → {TKT-05, TKT-06} · TKT-08 any time Tushar delivers the PDF · media TKT-22/23/24 may start as filler | EVAL-013 proven (`content-gate-proof.txt`); EVAL-016 forbidden-string scan over repo + bundle; EVAL-006/007/008/010 on `/dev/primitives` + existing routes; EVAL-017 tags on built families; first full `eval-run-*.json` compared to baseline with no Critical regression; `pnpm typecheck && lint && test && eval` green | Review `/dev/primitives` boards at 390/1440 with Tushar (tier system reads premium, not toy — D1); confirm the failing-fixture proof; resume status (TKT-08) recorded as delivered or blocked |
| **3 · M-003** Home complete | M-002 exit; Tushar's stance on the 8 + 3 Ask answers recorded (sign-off or "ship DRAFT-labelled"); `docs/anchors.md` exists | TKT-09 → TKT-10 → TKT-11 · {TKT-12, TKT-13} in parallel with TKT-10/11 (disjoint files) → TKT-14 | EVAL-012 11/11 · 5/5 · 0 fabricated; EVAL-007 AskPanel + MobileMenu keyboard scripts at 390/1440; EVAL-005/004 on `/`; EVAL-006/008/010/011 full; EVAL-001 pack scored; full `pnpm eval` no regression | **5-second test:** Tushar confirms EVAL-001 6/6 at 390 and 1440 on `docs/screenshots/home/`; Ask answers read once (honesty microcopy visible); accent-per-section read |
| **4 · M-004** Work & case-study system | M-003 exit; atomic steps S15–S21 written at this boundary (§B living-plan note); FilterTabs bucket decision (default Experiments applied) | {TKT-15 → TKT-16 (TSK-13 → TSK-14 → TSK-15) → TKT-17} ∥ {TKT-18} ∥ {TKT-20 (TSK-19 → TSK-20 → TSK-21)} ∥ {TKT-21} → TKT-19 (TSK-16 → TSK-17 → TSK-18; needs TKT-15, TKT-18) | EVAL-002 hops 1–3; EVAL-014 four states; EVAL-015 VT fallback across 11 slugs; EVAL-004 on `/work`, `/work/teachspark`; EVAL-007/010 ShowTheThinking + OverviewToggle + FilterTabs; EVAL-011 crawler upgrades anchor WARNs to PASS; full `pnpm eval` | Editorial-grid read at 1440 and 768 ("never nine identical rectangles"); ExperienceStrip reads as employment, not product; one artifact board (`/dev/artifacts`) reviewed |
| **5 · M-005** Content & media | M-004 exit; Tushar decisions: TeachSpark metric date (default 2026-08-24), RailCite figure policy (default live-with-date), Cubicle deploy (by 2026-09-16), Cubicle role/team names (placeholder otherwise); atomic steps written at this boundary | Media lane (any time after TKT-01, must close 22/23/24 before TKT-50): TKT-22, TKT-23, TKT-24, TKT-25, TKT-26, TKT-27(cond.) · Content lane: TKT-28 → TKT-29 → TKT-30 → {TKT-31, TKT-32, TKT-33} → TKT-54 (TSK-25…29 sequential) → TKT-39 | EVAL-013 on all 11 records; EVAL-014 with real files; EVAL-003 8/8 (`eval-003-<sha>.md`); EVAL-004 on `/work/teachspark` with full content + media; EVAL-011 anchors; truth review of the three featured trace tables by the QA-tester; full `pnpm eval` | Tushar reads TeachSpark, RailCite, Velora pages once (truth + tone); confirms EVAL-003 mapping; RailCite recording blocker escalated here if auth blocks (PB4) |
| **6 · M-006** About · Thinking · Playground · Contact · 404 | M-002 exit + TKT-20 (MetricCard) + TKT-15 (playground data); Tushar defaults recorded: "via IntraEdge", gap framing (omit until answered), "7+ years"; may interleave with Phase 5 (disjoint files, separate implementers) | TKT-40 (TSK-22 → TSK-23 → TSK-24) → TKT-41 → TKT-42 · {TKT-43, TKT-44, TKT-45, TKT-46} in parallel after TKT-05/06 | EVAL-002 full recruiter path ≤6 clicks (resume hop per flag state); EVAL-007 ExperienceTimeline keyboard + deep link; EVAL-006/008/010 on all new routes; EVAL-013/016 PII grep (repo + bundle); EVAL-017 tags for about/thinking/playground/contact families; full `pnpm eval` | Tushar reads `/about` once (level, no PII, wording defaults); DRAFT essay labels acknowledged |
| **7 · M-007** Sweeps, deployment, production | All previous exits; TKT-22/23/24 closed (PB4); GitHub remote + Vercel scope confirmed by Tushar; domain name known (for TKT-53); atomic steps written at this boundary | {TKT-47, TKT-48, TKT-49} → TKT-50 → TKT-51 → TKT-52 → **[Stages 8–10 + `QA-report.md` gate]** → TKT-53 (needs TKT-08) | Release gates per evaluation-plan §6 on the **preview URL**: every Critical PASS, no High unaddressed; `eval-run-v1.0.0-rc.json`; headers, audit, PII grep; OG on both inspectors; then on production `eval-run-v1.0.0.json` + EVAL-002 live | **Deploy approvals:** (1) Vercel project creation scope, (2) preview shared, (3) LinkedIn inspector login if needed, (4) explicit "promote to production" after Stage 8–10 sign-off, (5) rollback path acknowledged; release provenance recorded |

**Cross-phase rules.** `pnpm eval` runs on every behaviour-changing ticket (A16 triggers) — not only at gates. Media tickets are filler work from Phase 2 onward but never run on the same files as a content ticket in flight. Any Tushar-blocked item keeps its default and an `EXE-n` placeholder note; production (TKT-53) is the only point where TKT-08 and the featured-3 videos become hard blocks. Worktree per phase branch; `main` receives squash-merges only at phase exits.

---

## D. Traceability table

Ticket → primary components/files → EVAL ids. TC ids are `TC-TBD` until `test-cases.md` (sibling Stage-6 artifact) lands; the orchestrator back-fills this column and `tickets.md` in the same commit. Native Backlog ids come from the 6.2 onboarding mapping (`decisions.md` TP0, `backlog/id-map.json`): TKT-01…33 → `TASK-1…33`, TKT-54 → `TASK-34`, TKT-39 → `TASK-35`, TKT-40…53 → `TASK-36…49`; sub-tasks are `TASK-<parent>.<k>`.

| Ticket | Backlog ID | Milestone | Components / files | EVAL | TC |
|---|---|---|---|---|---|
| TKT-01 | `TASK-1` | M-001 | `app/{layout,page,globals.css}`, `lib/{site,motion}.ts`, `scripts/{tokens-check,assert-static,avatar,eval}.ts`, `components/clay/*` (minimal), `components/navigation/*`, `components/hero/*`, `components/interactions/{Parallax,ViewTransitionLink}.tsx`, `components/projects/{ProjectCard,StatusBadge}.tsx`, `components/case-study/CaseStudyHeader.tsx` (shell), `app/work/**` (stubs), `public/avatar/*`, `playwright.config.ts`, `lighthouserc.*.json`, `tests/e2e/tracer.spec.ts`, `docs/screenshots/tracer/`, `evals/results/baseline-v1.json` | 001 (info), 004/005 (baseline), 006, 008, 010, 015 | TC-TBD |
| TKT-02 | `TASK-2` | M-001 | `evals/results/gate-tracer.md`, `decisions.md` EXE-1, `Design.md` deviations | 001, 009 | TC-TBD |
| TKT-03 | `TASK-3` | M-002 | `data/{schema,index,projects}.ts`, `lib/anchors.ts`, `scripts/{validate-content,forbidden-strings}.ts`, `tests/fixtures/invalid-project.fixture.ts`, `tests/unit/{schema,content-gate,forbidden-strings}.test.ts`, `evals/results/content-gate-proof.txt`, `docs/anchors.md` | 013, 016 | TC-TBD |
| TKT-04 | `TASK-4` | M-002 | `components/clay/{tiers,ClayCard,ClayButton,ClayPill,ClayTile,ClayFrame,ClayIcon}.tsx`, `components/common/*`, `app/dev/primitives/page.tsx`, `lib/dev-only.ts`, `tests/e2e/primitives.spec.ts`, `docs/screenshots/primitives/` | 006, 007, 008, 009, 010 | TC-TBD |
| TKT-05 | `TASK-5` | M-002 | `components/layout/{Container,Section,SectionHeading,Footer}.tsx`, `components/interactions/{Reveal,ProgressBar}.tsx`, `lib/motion.ts`, `tests/e2e/layout.spec.ts` | 008, 010, 011 | TC-TBD |
| TKT-06 | `TASK-6` | M-002 | `lib/{seo,og}.tsx`, `assets/fonts/*`, `app/opengraph-image.tsx`, `app/work/opengraph-image.tsx`, `app/work/[slug]/opengraph-image.tsx`, `app/{sitemap,robots}.ts`, `tests/unit/seo.test.ts`, `tests/e2e/eval-017.spec.ts` | 017 (tags), 004 (SEO) | TC-TBD |
| TKT-07 | `TASK-7` | M-002 | `vitest.config.ts`, `scripts/{eval,eval-cases,bundle-budget,security-headers}.ts`, `lib/format.ts`, `tests/e2e/{fixtures,crawler}.ts`, `tests/e2e/eval-0{02,06,07,08,10,11,14,15}*.spec.ts`, `tests/e2e/{routes,crawler-allowlist}.json`, `lighthouserc.*.json`, `.github/workflows/eval.yml`, `docs/eval.md` | 002, 004, 005, 006, 007, 008, 010, 011, 012 (slot), 013, 014, 015, 016, 017 | TC-TBD |
| TKT-08 | `TASK-8` | M-002 | `public/resume.pdf`, `lib/site.ts` (flag), `tests/unit/resume-pii.test.ts`, `tests/e2e/resume.spec.ts`, `.gitignore`, `decisions.md` EXE-n | 002, 013, 016 | TC-TBD |
| TKT-09 | `TASK-9` | M-003 | `data/knowledge.ts`, `lib/ask/{adapter,normalise,synonyms,local-provider,rag-provider,index}.ts`, `tests/unit/{ask-*,eval-012,rag-provider}.test.ts` | 012, 013 | TC-TBD |
| TKT-10 | `TASK-10` | M-003 | `components/ai/{AskProvider,AskPortfolio,AnswerView,SuggestedPrompts,EvidenceLinks}.tsx`, `app/dev/ask/page.tsx`, `tests/unit/use-ask.test.tsx`, `tests/e2e/ask-inline.spec.ts`, `docs/screenshots/ask/` | 007, 008, 010, 011, 012 | TC-TBD |
| TKT-11 | `TASK-11` | M-003 | `components/ai/{AskPanel,AskPanelLazy}.tsx`, `lib/focus.ts`, `components/navigation/{AskAIButton,MobileMenu}.tsx`, `tests/e2e/{ask-panel,eval-007}.spec.ts`, `tests/e2e/crawler-allowlist.json` | 005, 006, 007, 010, 011 | TC-TBD |
| TKT-12 | `TASK-12` | M-003 | `data/projects.ts` (+railcite, velora), `components/projects/{FeaturedWork,ProjectCard}.tsx`, `app/work/[slug]/page.tsx`, `tests/e2e/featured.spec.ts` | 001, 002 (hop 1), 011, 013, 015 | TC-TBD |
| TKT-13 | `TASK-13` | M-003 | `data/thinking-framework.ts`, `lib/stages.ts`, `components/home/HowIThink.tsx`, `tests/e2e/how-i-think.spec.ts` | 003 (supports), 007, 011, 013 | TC-TBD |
| TKT-14 | `TASK-14` | M-003 | `components/home/FinalCTA.tsx`, `components/common/CopyButton.tsx`, `app/page.tsx`, `app/opengraph-image.tsx`, `tests/e2e/home.spec.ts`, `docs/screenshots/home/`, `evals/results/eval-001-<sha>.md` | 001, 004, 005, 006, 008, 011 | TC-TBD |
| TKT-15 | `TASK-15` | M-004 | `data/projects.ts` (14 records) | 013 | TC-TBD |
| TKT-16 | `TASK-16` | M-004 | `app/work/page.tsx`, `components/projects/{WorkHero,FilterTabs,WorkGrid,EditorialGrid,EmptyState,ProjectCard}.tsx`, `lib/filters.ts`, `tests/e2e/work.spec.ts` | 002 (hop 2), 004, 007, 008, 010, 011 | TC-TBD |
| TKT-17 | `TASK-17` | M-004 | `components/projects/ExperienceStrip.tsx` | 007, 011, 013 | TC-TBD |
| TKT-18 | `TASK-18` | M-004 | `components/projects/DemoVideo.tsx`, `app/dev/video/page.tsx`, `tests/e2e/eval-014.spec.ts` | 004, 005, 011, 014, 015 | TC-TBD |
| TKT-19 | `TASK-19` | M-004 | `app/work/[slug]/page.tsx`, `components/case-study/{CaseStudyHeader,OverviewToggle,Chapter,ChapterNav,NextProject}.tsx`, `components/case-study/artifacts/MetricCard.tsx` (inline), `docs/anchors.md`, `tests/e2e/case-study.spec.ts` | 002 (hop 3), 004, 006, 007, 008, 010, 015, 017 | TC-TBD |
| TKT-20 | `TASK-20` | M-004 | `components/case-study/artifacts/*.tsx`, `app/dev/artifacts/page.tsx`, `tests/unit/artifacts.test.tsx` | 003, 006, 008, 009, 013 | TC-TBD |
| TKT-21 | `TASK-21` | M-004 | `components/interactions/{ShowTheThinking,ThinkingNode}.tsx`, `app/dev/thinking/page.tsx`, `tests/e2e/thinking.spec.ts` | 003, 006, 007, 008, 010 | TC-TBD |
| TKT-22 | `TASK-22` | M-005 | `public/video/teachspark*`, `content/media/teachspark/`, `public/media/teachspark/`, `scripts/encode-video.sh`, `data/projects.ts` (status) | 004, 014, 016 | TC-TBD |
| TKT-23 | `TASK-23` | M-005 | `public/video/railcite*`, `content/media/railcite/` | 014, 016 | TC-TBD |
| TKT-24 | `TASK-24` | M-005 | `public/video/{nuptis,velora}*`, `content/media/{nuptis,velora}/` | 014 | TC-TBD |
| TKT-25 | `TASK-25` | M-005 | `public/video/bhakti-vilas*`, `content/media/bhakti-vilas/` | 014, 016 | TC-TBD |
| TKT-26 | `TASK-26` | M-005 | `public/video/{pratyasa,tegaki,dino-arcade,cinematic-portfolio}*`, `content/media/*/` | 014, 016 | TC-TBD |
| TKT-27 | `TASK-27` | M-005 | `public/video/cubicle*` (cond.), `data/projects.ts` (featured swap), `decisions.md` | 013, 014 | TC-TBD |
| TKT-28 | `TASK-28` | M-005 | `data/projects.ts` (teachspark full), `docs/trace/teachspark.md` | 003, 004, 011, 013, 014 | TC-TBD |
| TKT-29 | `TASK-29` | M-005 | `data/projects.ts` (railcite full), `docs/trace/railcite.md` | 003, 011, 013, 014 | TC-TBD |
| TKT-30 | `TASK-30` | M-005 | `data/projects.ts` (velora full), `docs/trace/velora.md` | 003, 011, 013, 014 | TC-TBD |
| TKT-31 | `TASK-31` | M-005 | `data/projects.ts` (nuptis), `docs/trace/nuptis.md` | 011, 013, 014 | TC-TBD |
| TKT-32 | `TASK-32` | M-005 | `data/projects.ts` (cubicle), `docs/trace/cubicle.md` | 011, 013 | TC-TBD |
| TKT-33 | `TASK-33` | M-005 | `data/projects.ts` (bhakti-vilas), `docs/trace/bhakti-vilas.md` | 011, 013, 014 | TC-TBD |
| TKT-54 | `TASK-34` | M-005 | `data/projects.ts` (token-toli, pratyasa, tegaki, dino-arcade, cinematic-portfolio), `docs/trace/*.md`, `content/media/*/SOURCES.md` | 011, 013, 014 | TC-TBD |
| TKT-39 | `TASK-35` | M-005 | `test-cases.md` (EVAL-003 table), `evals/results/eval-003-<sha>.md` | 003 | TC-TBD |
| TKT-40 | `TASK-36` | M-006 | `data/{experience,skills,impact}.ts`, `components/about/{AboutHero,CapabilityClusters,Impact}.tsx`, `components/timeline/ProductJourney.tsx`, `app/about/page.tsx`, `app/about/opengraph-image.tsx` | 002 (hop 4), 004, 006, 008, 013 | TC-TBD |
| TKT-41 | `TASK-37` | M-006 | `components/timeline/{ExperienceTimeline,TimelineNode,StoryCard}.tsx`, `tests/e2e/timeline.spec.ts` | 006, 007, 008, 010, 011 | TC-TBD |
| TKT-42 | `TASK-38` | M-006 | `components/about/{Awards,Research,Education}.tsx`, `data/about.ts`, `app/about/page.tsx` | 002, 004, 013, 017 | TC-TBD |
| TKT-43 | `TASK-39` | M-006 | `data/writing.ts`, `components/thinking/{ThinkingList,EssayBody}.tsx`, `app/thinking/**` | 006, 011, 013, 017 | TC-TBD |
| TKT-44 | `TASK-40` | M-006 | `app/playground/**`, `components/playground/PlaygroundGrid.tsx` | 008, 009, 011 | TC-TBD |
| TKT-45 | `TASK-41` | M-006 | `app/contact/**`, `components/contact/ContactCard.tsx`, `tests/e2e/contact.spec.ts` | 002 (final hop), 007, 011, 013 | TC-TBD |
| TKT-46 | `TASK-42` | M-006 | `app/{not-found,error,global-error}.tsx` | 011, 015 | TC-TBD |
| TKT-47 | `TASK-43` | M-007 | `tests/e2e/sweep.spec.ts`, `docs/screenshots/<route>/` | 008 | TC-TBD |
| TKT-48 | `TASK-44` | M-007 | `tests/e2e/eval-006.spec.ts` (all routes), `scripts/contrast-audit.ts`, `docs/a11y-pass.md` | 006, 007, 010 | TC-TBD |
| TKT-49 | `TASK-45` | M-007 | perf fixes (fonts/images/chunks/shadows), `evals/results/perf-{before,after}-<sha>.json` | 004, 005 | TC-TBD |
| TKT-50 | `TASK-46` | M-007 | `next.config.ts` (headers), `app/layout.tsx` (analytics), `scripts/predeploy-check.ts`, `tests/unit/predeploy.test.ts`, `docs/deploy.md`, Vercel project | 002, 014, 016 | TC-TBD |
| TKT-51 | `TASK-47` | M-007 | `docs/og/*`, `docs/og/checklist.md` | 017 | TC-TBD |
| TKT-52 | `TASK-48` | M-007 | `evals/results/eval-run-v1.0.0-rc.json`, `HANDOFF.md`, `tickets.md`, `docs/handoff/stage-8-pack.md` | all 17 | TC-TBD |
| TKT-53 | `TASK-49` | M-007 | `evals/results/eval-run-v1.0.0.json`, `HANDOFF.md`, `decisions.md` EXE-n, `.github/workflows/eval-cron.yml` (opt.), `docs/deploy.md` | 002, 004, 016, 017 | TC-TBD |

**Reverse index (EVAL → tickets that make it pass):** 001 → 01, 02, 12, 14 · 002 → 07, 08, 12, 16, 19, 40, 42, 45, 50, 53 · 003 → 13, 20, 21, 28–30, 39 · 004 → 01, 06, 07, 14, 16, 18, 19, 22, 28, 40, 42, 49, 53 · 005 → 01, 07, 11, 14, 18, 49 · 006 → 01, 04, 07, 11, 14, 19–21, 40, 41, 43, 48 · 007 → 04, 07, 10, 11, 13, 16, 17, 19, 21, 41, 45, 48 · 008 → 01, 04, 05, 07, 10, 14, 16, 19–21, 40, 41, 44, 47 · 009 → 02, 04, 20, 44 (+ Stage 8) · 010 → 01, 04, 05, 07, 10, 11, 16, 19, 21, 41, 48 · 011 → 05, 07, 10–14, 16–19, 21, 28–33, 54, 41, 43–46 · 012 → 07 (slot), 09, 10 · 013 → 03, 08, 09, 12, 13, 15, 17, 20, 27–33, 54, 40, 42, 43, 45 · 014 → 07, 18, 22–27, 28–31, 33, 54, 50 · 015 → 01, 07, 12, 18, 19, 46 · 016 → 03, 08, 22, 23, 25, 26, 50, 53 · 017 → 06, 07, 19, 42, 43, 51, 53.

---

## E. Conflicts register (resolved here — Execution reads this before TKT-01)

| # | Conflict | Sources | Resolution | Where applied |
|---|---|---|---|---|
| E-1 | Footer credit line: "Built with curiosity." vs "Built with Claude Code" | `Design.md` §3 Footer (brief §39) vs `tickets.md` TKT-05 AC 3 + `CONTENT_INVENTORY.md` §1.6 | **"Built with curiosity." wins** (brief §39 is explicit; orchestrator ruling 2026-09-15, TP10). Authorship transparency moves to a one-line colophon on `/about` ("Designed and built with Claude Code"). tickets.md / test-cases.md / CONTENT_INVENTORY.md updated to match. |
| E-2 | Ask evidence link `/work?tab=enterprise` vs FilterTabs URL param `?filter=` | `CONTENT_INVENTORY.md` §9 row 7 vs `COMPONENT_ARCHITECTURE.md` §4, TKT-16 AC 2 | `?filter=enterprise` everywhere; `lib/filters.ts` is the single parser; `validate-content` rejects internal hrefs with unknown query keys | S09.03, TKT-16 |
| E-3 | Chapter ids (`bet`, `built`, `learned`) vs anchor slugs (`#04-product-bet`, `#05-what-i-built`, `#08-what-i-learned`) | TKT-03 AC 1 vs TKT-19 AC 5 / CONTENT_INVENTORY §1.5 hrefs | Both kept: ids are schema values, anchors are presentation; the mapping lives once in `lib/anchors.ts` + `docs/anchors.md` (TP8) and is validated at build | S03.03, TKT-19 |
| E-4 | TKT-16 AC 2 "server-read search param … filtered on first paint" vs static generation (S2, COMPONENT_ARCHITECTURE "static output") | `tickets.md` TKT-16 vs `decisions.md` S2 | Reading `searchParams` in a server component makes `/work` dynamic. Resolved by TP7: client-side `useSearchParams` in Suspense; a one-frame flash on deep links is accepted. `tickets.md` TKT-16 AC 2 wording to be amended at the M-004 boundary with an `EXE-n` note. | A1, TKT-16 |
| E-5 | "Static output" read as `output: 'export'` would break `next/image` optimisation, `headers()` and the future `/api/ask` | `COMPONENT_ARCHITECTURE.md` §6 vs DESIGN_DIRECTION §9, TKT-50, S7 | TP1: SSG on the default Next build with `assert-static.ts` enforcing "every route prerendered" | A1, S01.08 |
| E-6 | `--card-padding: var(--space-7) to var(--space-9)` is not valid CSS | `Design.md` §2 spacing block | Split into `--card-padding` (40 px) and `--card-padding-hero` (56 px); consumed via `ClayCard padding` prop | S01.05 |
| E-7 | `ClayButton` primary text colour ambiguous ("ink text… actually white/bg text") and `ink` on `accent` is 3.4:1 | `Design.md` §3 primitives table vs its own §2 contrast rule | `bg` (`#FAF9FF`) text on `accent` (4.9:1, AA); `accent-deep` on hover (higher contrast) | S03.03 |
| E-8 | `AskPanel` width at 768–1023 unspecified (Design.md lists 1024–1439 / ≥1440 / <768) | `Design.md` §3 AskPanel, D3 | 400 px right drawer at 768–1023 (D3 says drawer behaviour is unchanged there) | S11.01 |
| E-9 | Nav copy: CONTENT_INVENTORY §1.1 lists `Work · About · Thinking · Playground · Contact` | vs `SITEMAP.md` header nav `Home · Work · Thinking · About` (approved) | SITEMAP wins; Playground and Contact are reached via footer/CTAs | S04.03 |
| E-10 | Suggested prompts count: §1.3 says 8 chips | vs Design.md (5 home) + PB3 (11 total: 5 home + 6 panel) | PB3 wins: `surface` field splits 5/6, validated at build | S09.03, S09.04 |
| E-11 | Featured-card images listed in §1.4 | vs Design.md card anatomy (ClayIcon, no product imagery) | Design.md wins (already stated in TKT-12 notes); images stay in packs for case-study hero media | S06.03 (tracer), TKT-12 |
| E-12 | React 19.2 `<ViewTransition>` export name unverified (`ViewTransition` vs `unstable_ViewTransition`) | environment fact vs React release notes | Verified at S06.01 before any VT code; single re-export in `lib/motion.ts`; documented fallback if absent | S06.01, A14 |
| E-13 | TKT-01 AC 10 asks Playwright to cover both resume states, but the flag is a build-time constant | `tickets.md` TKT-01 vs PB5 | Placeholder state covered in e2e on the tracer; the `true` state covered by a unit test of `resumeAction()` on the tracer and by e2e once TKT-08 flips the flag (S08r.03) | S01.07, S07.03, S08r.03 |
| E-14 | `evaluation-plan.md` §7 asks for a copy in `evals/`; global rule says one fact in one place | `evaluation-plan.md` vs CLAUDE.md data defaults | `evals/evaluation-plan.md` is a one-line pointer to the root file; `evals/eval-cases.json` is the machine-readable authority | S08.02 |

**Open items carried (not conflicts — Tushar's calls, defaults applied):** TeachSpark metric date (08-24) · RailCite corpus figure policy (live-with-date) · Cubicle deployment (N/A default) · Cubicle role/team names (placeholder) · "via IntraEdge" (as resume) · 2019–2022 gap framing (omit) · years wording ("7+") · FilterTabs bucket for Token Toli/Pratyasa/Bhakti-Vilas (Experiments) · Soft Matter DOI (pending) · 5 essay titles + 11 Ask answers (DRAFT-labelled) · domain name · sanitised resume · GitHub repo creation · Vercel team scope.

---

*End of technical-plan.md · Stage 6.1 · living document — update via the change-management rule in §0.3.*
